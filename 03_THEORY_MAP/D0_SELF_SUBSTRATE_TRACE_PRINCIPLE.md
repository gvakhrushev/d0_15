# D0 — Self-Substrate Trace Principle

## 1. Scope

This document states the ontological entry layer for v15. D0 assumes no external vacuum, no external observer, and no background arena in which physical objects move. The finite support is the substrate. Motion is the production of an internal trace by the same substrate that continues to evolve.

## 2. Retained/traced split

Let

\[
\mathcal H_N=\mathcal H_N^{ret}\oplus\mathcal H_N^{tr},
\qquad
P_N+Q_N=I.
\]

Let \(U_N\) be the finite tick. For \(\psi\in P_N\mathcal H_N\),

\[
U_NP_N\psi=P_NU_NP_N\psi+Q_NU_NP_N\psi.
\]

The first term is retained continuation. The second term is archive trace. Since the trace is in \(Q_N=I-P_N\), it is internal to the same finite support.

## 3. Trace intensity

The feedback-return operator is

\[
F_N=P_NU_N^\dagger Q_NU_NP_N=(Q_NU_NP_N)^\dagger(Q_NU_NP_N).
\]

Therefore

\[
\langle\psi,F_N\psi\rangle=\|Q_NU_NP_N\psi\|^2.
\]

A finite event is registered when this trace intensity is nonzero.

## 4. Detector and observer discipline

A detector is an internal terminal readout node that makes the retained/traced split addressable. An observer is not a primitive of D0. Classical observation is a macroscopic shadow of finite terminal registration.

## 5. Sector consequence

The archive sector is organized memory of prior trace production. Its boundary, capacity and pressure are the finite origins of the geometric, gravitational and cosmological sectors used downstream by CVFT.
