# F3 ANTIDIAGONAL CANDIDATE v2 — the antidiagonal EMBEDDING of the direct/return dyad into its
# own square (door-(a) armament for row 547; POST-KILL REWRITE; DRAFT)

**Date:** 2026-07-18 · **Status:** DRAFT candidate memo v2 (compute + armament only).
**Kill record (v1, accepted in full — skeptic #17, §05.8.R):** v1's subset sentence
("`dyad = {C,D} ⊆ ABCD`, zones nest literally") was KILLED with named owned objects:
B01:1523/:1526/:1815 + `FiniteTypes.lean:9-10` own `ABCD = D₂ × D₂` (the dyad is BOTH FACTORS of
the square — a set cannot be a subset of its own Cartesian square), B01:1564 + row 560 own the
alphabet-level port-power language (ATT-4's invited grep SUCCEEDED), and B01:1571-1574 +
`CarrierForcing.lean:69-75` own the partition carrier `V = V9 ⊔ V11 ⊔ V13`, `|V| = 33` — literal
nesting would give 13. The ¬U_ι reductio was also killed (the owned ZONE INDEX already serves as
the which-copy discriminator for cross-shell ABCD copies — Ω₈ = ABCD×{±} in V9 vs bare ABCD in
V13, B01:1535/:1553). Errors of record: EoR-1 (subset form contradicts owned product), EoR-2
("nest literally" contradicts owned partition carrier), EoR-3 (§4 denied U_ι was a coordinate
embedding while §5's own Lean draft WAS one). **No defense.** What SURVIVED: the typing anchor
(U3 — verbatim-verified by the skeptic at :712-713, :749-751, :760, :794, plus :519-526
direct↦p₊/return↦p₋ and B00:293-294), and the embedding form below.
**Companion can-fail script:** `f3_antidiagonal_check.py` v2 — U3 = the sole SELECTIVE check;
U1/U2/U4/U5 demoted to illustrations (row-547 13th-pass tautology precedent); U6 = carrier guard.

---

## §0. The candidate sentence U_ι′ (v2 — the EMBEDDING form; NOT owned; door (a) for row 547)

> **U_ι′ (candidate book text for §01.20):** *"The direct/return dyad embeds in its own terminal
> square ANTIDIAGONALLY: `ι(d) = (d, σd)` (σ the dyad's own flip), image the exchange pair
> `{C = (3,4), D = (4,3)}`. The embedding consumes NO base point and NO port order — the image
> is port-swap invariant (swapping the factors carries `ι` to `ι ∘ σ`, same image), and the
> orientation `direct ↦ C₊` / `return ↦ D₋` is fixed by the owned forward/return names
> (B01:712-713, :794, :519-526). Hence the presentation-grade inclusion `V₁₁ ↪ V₁₃ over V₉`
> (each dyad letter reads as its exchange role), under which the nested narrative of B03
> §03.23.7 is the CAPACITY-DESCENT order (defect core → exchange pair opened → square closed) on
> TAGGED zone copies — the partition carrier `V = V₉ ⊔ V₁₁ ⊔ V₁₃`, `|V| = 33`, `|E| = 359` is
> UNCHANGED."*

Contrast with v1 (the kill applied): U_ι′ REFINES the owned factor/port-power reading instead of
contradicting it — the dyad stays both factors (`ABCD = D₂²` untouched); the port-power ladder
`{D₂¹, D₂²}` (row 560) is untouched at the alphabet level; the scene carrier and all its
invariants are untouched (U6). What it adds is exactly ONE owned-shaped identification: the
canonical antidiagonal section of the square, name-oriented.

**Why the two §4 killers do not apply to `d ↦ (d, σd)`** (the v1 dossier killed only CONSTANT
and DIAGONAL coordinate embeddings): no base point is consumed (`σd` is computed from `d`, not
from a constant `b₀`); no port order is consumed (the image is symmetric under the factor swap);
the diagonal rival `d ↦ (d, d)` remains dead on the owned typing (image = coalescent `{A,B}`,
contradicting forward/return = exchange). Among the four fixed-shape sections
`{(d,d), (d,σd), (σd,d), (σd,σd)}`: the two DIAGONAL shapes `(d,d)` and `(σd,σd)` die on typing
(both have image the coalescent `{A,B}`); the two ANTIDIAGONAL shapes `(d,σd)` and `(σd,d)` have the SAME
image `{C,D}` and differ only by the direct↔return relabel — which the owned names fix. So at
IMAGE level the exchange pair is the unique typing-compatible section shape; at MAP level the
orientation is name-fixed. This is the selectivity claim, and it is now stated at the level
where it is true (sections of the square), not over subsets.

## §1. What U_ι′ buys (graded honestly)

1. **Fork adjudication upgrade (presentation-grade, NOT dissolution).** Reading C's nesting
   becomes the capacity-descent order under the owned outside-in direction (B03:1076-1081), with
   `V₁₁ ↪ V₁₃ over V₉` as an authored PRESENTATION (tagged copies; carrier untouched). The row
   547 `radius → vertex-set` hook is NOT discharged (the row names a functor; U_ι′ supplies an
   order-match plus an embedding, conditional on minting); the consumer-safety clause of row 547
   ("blocks any zone-11 ⊂ zone-13 type/vertex-set fact") would need an explicit amendment ON
   MINT: the blocked fact stays blocked at CARRIER level; the authored inclusion lives at
   PRESENTATION level (Sum-type refinement), and the mint must say so.
2. **The typing anchor (U3 — survived the kill pass verbatim).** Chain: dyad = direct/return
   (B01:475) = `p₊/p₋` (:519-526) = forward/return roles `C₊/D₋` (:712-713, :794; B00:293-294;
   §01.20's own trace/reference naming at :1530) = the exchange pair (:760). Kills the diagonal;
   fixes orientation.
3. **Aut-hygiene (U5, illustration).** Unordered image pair; no vertex of any zone selected; row
   549 and the basepoint no-go untouched.

## §2. What U_ι′ does NOT do

- Does NOT dissolve the fork or unify the readings; does NOT touch the carrier partition, |V|=33,
  |E|=359, CAP, spectrum (U6 machine-checks the tagged-copy carrier is unchanged).
- Does NOT displace the owned two-port reading of B01:1560 — it lives INSIDE it (the embedding is
  built from the two-port product structure itself); the `Z(Q₈)` branch of that fork is untouched.
- Does NOT claim the ¬U_ι′ reductio forces anything: the cross-shell ABCD precedent
  (Ω₈ = ABCD×{±} in V9 vs bare ABCD in V13, zone index = the owned discriminator) is a CONFIRMED
  kill of the duplication-forcing route (v1 kill, ATT-3 resolved AGAINST forcing). U_ι′ is
  armament for owner authorship, nothing more.
- Does NOT resurrect the constant/diagonal coordinate embeddings (§0 grading; v1 §4 dossier
  stands for those).

## §3. Attack surface v2 (pre-registered)

- **ATT-1′ (armament grade).** No register argument is claimed at all in v2 (v1's was killed);
  the entire case for U_ι′ is: unique typing-compatible section shape + name-fixed orientation +
  zero consumed catalogs. A skeptic may rule this "canonical but optional" — authoring it is an
  owner preference, not an obligation. CONCEDED in advance; that is what door (a) means.
- **ATT-2′ (presentation vs carrier discipline).** The mint must thread the row-547
  consumer-safety clause (§1.1). If any consumer is found that dereferences a `V11 ↪ V13`
  type-fact from the presentation layer into the carrier layer, the armament is mis-scoped —
  grep invited.
- **ATT-3′ (naming chain robustness; cross-ref: the §0 residual value-formula freedom between `(d,σd)` and `(σd,d)` is fixed jointly with the unowned direct↔{3,4} assignment — same territory).** The orientation rests on the forward/return NAMES
  spanning two owned tables (the operational four-role cycle :712-715 and the terminal four-role
  square :749-776, bridged by "same four-role cycle" :749-751). If a skeptic finds an owned sentence
  breaking that bridge for C/D specifically, the orientation (not the image) falls back to a
  2-torsor — the image claim survives.

## §4. Ready-to-mint package (owner decision; NOT applied)

- Row 547 note: "DOOR-(a) ARMED v2 [2026-07-18, post-kill]: antidiagonal EMBEDDING candidate
  U_ι′ (`F3_ANTIDIAGONAL_CANDIDATE_MEMO.md` v2, cert `f3_antidiagonal_check.py` v2) —
  `ι(d) = (d, σd)`, image the exchange pair {C,D}, no base point / no port order consumed,
  orientation name-fixed; presentation-grade `V11 ↪ V13 over V9` on tagged copies; carrier
  partition and all scene invariants UNCHANGED (U6); v1 subset/literal-nesting form KILLED
  (EoR-1..3 recorded); fork stays OPEN pending owner authorship + consumer-safety amendment."
- If authored: Lean lift `def dyadAntidiagonal : Dyad ↪ Role := ⟨fun d => (d, σ d), injective⟩` +
  `V11 ↪ V13 over V9` at presentation layer + U6 carrier guard as theorems. NOT built until
  authored.

## §5. Final state

- v1 candidate: **KILLED — accepted in full** (EoR-1..3; named owned objects B01:1523/:1526/
  :1564/:1571-1574, `FiniteTypes.lean:9-10`, `CarrierForcing.lean:69-75`, row 560).
- v2 candidate U_ι′: **DOOR-(a) ARMED at presentation grade** (unique typing-compatible section
  shape; orientation name-fixed; carrier untouched; no forcing claimed).
- The fork (row 547): **OPEN** (unchanged).
- The duplication-forcing route: **DEAD** (cross-shell ABCD + zone-index discriminator — named
  instance, v1 kill).
