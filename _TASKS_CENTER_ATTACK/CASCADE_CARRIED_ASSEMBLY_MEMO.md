# CASCADE-CARRIED-ASSEMBLY — the carried cascade in one theorem (POST-SKEPTIC v2)

**Status:** POST-SKEPTIC v2 (2026-08-09). Skeptic #1: MINT-AFTER-REPAIRS, 0 kills; NEW ROW +
note-only umbrella update adjudicated; R1-R4 applied, errors of record below. Pre-flight: no assembly-of-carried-floors
content anywhere (`carriedFloorsExtended` nowhere; the scaffold's `carriedFloors` has FOUR
steps and its own row); the umbrella's registered FORMALIZATION SHAPE asks for the unifying
statement; six modules now carry cascade content with six scattered citable points. Target:
umbrella `D0-CASCADE-INSUFFICIENCY-CHAIN-001` (value 58.5, #2 of the queue, carries 6).

## Claim X (DEF-0.2.2 form)

X (Lean `D0.Foundation.CascadeCarriedAssembly`, clean axioms, standalone 3041 GREEN + D0.All
4476 GREEN): all carried content OF THE ONE-LINER CHAIN (six CascadeStep floors + four links
+ count leg) PLUS the address-ladder floor as a separate seventh conjunct is assembled into
ONE citable theorem (R3 scope):

1. `carriedFloorsExtended` — the scaffold's four `CascadeStep` floors + the two 2026-08-09
   floors (defect-closure, shell-closure), SIX steps, every one genuine
   (`extended_floors_are_genuine` via the scaffold's own `step_discriminates`);
2. `cascade_carried_assembly` — one conjunction: the six-floor genuineness + the four links
   (4→5→5→6 scaffold; 5→6→6→7 scale; order→defect-closure; closure→shell prose-anchored) +
   the terminal count leg (∀ admissible parameter) + the address-ladder floor
   (`stepOrientationParity` genuine — R1: it IS a CascadeStep, the registry's fifth
   registered floor, anchored at the ADDRESS-LADDER prose so it has no position in the
   ordered chain list; assembled as a separate conjunct), every conjunct CITED from its
   minted module, each at its own registered grade. Cross-ref D0-CASCADE-CHAIN-SCAFFOLD-001
   (whose cascade_chain_scaffold already contains conjunct 2 and the four-floor
   genuineness; the assembly cites, does not compete).

Grade claimed: ASSEMBLY — no new mathematics; the content is citability (one theorem the
umbrella can point to as "the carried cascade", replacing six scattered pointers) and the
machine-checked fact that the six modules' statements compose with clean axioms.

## Owned pre-facts

- Umbrella note (FORMALIZATION SHAPE): "the unifying 'each floor forced by the insufficiency
  of the previous' statement is the open obligation" — this assembly is the carried
  approximation, NOT the closure.
- The six source modules and their rows (all minted): CascadeChain (scaffold + link 1),
  CascadeInterlockScale (link 2), CascadeFloorDefectClosure (floor + link 3),
  CascadeFloorShellClosure (floor + link 4, prose-anchored grade), CascadeTerminalCount
  (count leg), CascadeFloorOrientationParity (imported, NOT re-assembled — predates the
  record shape, own row).

## Named risks & PRE-REGISTERED attack surface

- **ATT-A (strongest: assembly inflation).** A conjunction of minted theorems could read as
  progress on the umbrella's truth. Pre-registered: the row must say ASSEMBLY, list every
  conjunct's registered grade verbatim (prose-anchored fourth link stays prose-anchored;
  proxy grades stay proxy; umbrella stays OPEN; the seam unconstructed). A kill must show a
  conjunct's grade was upgraded by assembly.
- **ATT-B (floor-list semantics).** `carriedFloorsExtended` appends the new floors at their
  prose positions — but the scaffold's own `carriedFloors` (four steps) is minted with its
  own order and is NOT edited. The extended list is THIS module's object; no renumbering of
  the scaffold; the prose-order caveats of the interlock rows apply and are cited.
- **ATT-C (the six-vs-seven count).** The orientation-parity floor is NOT a `CascadeStep`
  (predates the shape); it is imported but not in the list; the row must count SIX steps and
  cite the seventh floor's row separately (repaired pre-skeptic — the draft docstring said
  seven).
- **ATT-D (does step_discriminates over the list add anything?).** It is one application per
  member — same grade as the scaffold's `carried_floors_are_genuine` (four members). The new
  content is only the two added members; stated as such.

## What this does NOT show

The umbrella theorem (each-floor-FORCED-by-previous over the full prose chain); any grade
upgrade of any conjunct; the topological closure reading; the typed count seam; the
insufficiencies↔layers identifications (prose grade). What it adds: one citable Lean spine
for the umbrella row, with clean-axiom composition machine-checked.

## Repairs (errors of record, accepted in full)

- R1/R2 (strongest): the draft docstring claimed an orientation-parity conjunct that did not
  exist AND falsely said the floor 'predates the record shape' -- stepOrientationParity IS a
  CascadeStep (CascadeFloorOrientationParity.lean:96, row 620 'fifth registered floor').
  Repaired the strong way: a seventh conjunct added (step_discriminates
  stepOrientationParity), the true exclusion-from-the-ordered-list reason stated
  (address-ladder anchoring, not chain position).
- R3: 'ALL carried cascade content' rescoped to 'all carried content of the one-liner chain
  + the address-ladder floor as a separate conjunct'; scaffold cross-ref added.
- R4: umbrella row 596 gets a NOTE-ONLY update (lean_module/lean_theorem/statuses untouched
  -- the graph/value-model edges stay adjudicated); registry prose says 'carried-content
  citation theorem', never 'the umbrella's Lean spine'; the stale early clause in the
  umbrella note ('floors defect=>closure=>shell ... unformalized') is repaired to the
  current state in the same edit.
- Advisories adopted: 'appended' -> 'inserted' + link-2 adjacency ownership sentence;
  classifier guardrails (no 'interpretation spine' phrase, no continuum/smooth/gauge/cosmo
  tokens).
