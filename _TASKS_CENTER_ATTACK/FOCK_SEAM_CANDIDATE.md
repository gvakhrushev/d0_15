# FOCK SEAM CANDIDATE — μ₂ realized as an eigenvalue of a constructed operator (VERIFIED DRAFT)

**Status:** candidate realization; `fock_seam_candidate.py` 9/9 PASS (exact, five reachable negative
controls); anti-circularity: no μ enters as input. One soft joint named, with two candidate owners.
No registry row edited. This is the constructive successor to `LINEAR_CARRIER_EXCLUSION.md`.

## The construction (owned ingredients only)

On the exclusion-forced carrier Λ*(V₁₁) (dim 2¹¹):

```
D̃  =  (π₀φ⁻²) · 2^N̂        (archive operator)
```

- the per-mode factor **2 is the owned primitive dyad D₂** (§01.20/§01.22: ABCD = D₂²,
  V₁₁ = V₉ ⊔ D₂) — binary occupancy of each of the 11 zone modes; the multiplicative lift is what
  the exclusion chain demands (the additive lift is bound-blocked — control (ii));
- the global **angle density π₀φ⁻² = 6/5 is owned** (π₀ derived from δ₀ balance, amplitude cert
  PASS line).

Readout: m₁ = 1/rank = 1/3 (owned, active side); **m₂ = the top-blade eigenvalue of D̃** — the
depth-2 circulation read at full saturation of the moving shell.

## What comes out (all exact)

- **m₂ = (6/5)·2¹¹ = 12288/5 = μ₂** — as an eigenvalue of a constructed operator, not an
  assembled form and not an input;
- **m₂u² + m₁u = α_alg⁻¹ = 159739/5 − (294902/15)φ exactly** (u = φ⁻³ = 2φ−3);
- the ratio 36864/5 is reached — possible only in the multiplicative class, exactly as the
  exclusion chain predicted;
- five-directional rigidity: trace readout, additive lift, partial blade, angle-free, wrong zones
  (V₉/V₁₃) all fail to μ₂.

The corpus's own named gap ("the 2¹¹ is not derived", amplitude cert line 75) is hereby answered at
the candidate level: **2¹¹ = the full-saturation eigenvalue of the per-mode dyad capacity** — the
multiplicative shadow of eleven owned D₂'s, not a free amplitude.

## The one soft joint (named, with two candidate owners — not yet written as forcings)

**Why the top blade (full saturation)?** Two independent candidate owners:

1. **Exchangeability + no-tail:** S₁₁-exchangeability (owned, §01.20 C1) restricts blade-degree
   couplings to the 11+1 invariant vectors v_k = Σ_{|I|=k}e_I; the owned μ₀ = 0
   (`feshbach_residue_no_zeta_tail`) kills the vacuum k = 0. What remains to force: k = 11 among
   k = 1..11 — the saturation clause. Candidate blade: §01.22's own q_T posture ("must saturate
   all unsigned terminal roles over the first moving dyadic shell") — the same saturation, one
   level up.
2. **Dominant-pole extraction:** the Feshbach residue functional extracts at the dominant archive
   pole; the top blade is the unique eigenspace of λ_max(D̃). "Residue at the leading singularity"
   is standard functional-analytic posture (this is where the external Dixmier/Wodzicki citation
   would now sit — reduced to one textbook clause).

Either owner closing ⇒ the realization equality is derived end-to-end; both failing ⇒ the honest
minimal external content of the entire C1-α triple is exactly: *"the depth-2 readout sits at full
saturation"* — one clause, with the operator, the carrier, the moments, and the α-assembly all
internal and exact.

## Skeptic guidance (pre-registered attack surface)

(a) is the top-blade selection a which-blade catalog? — adjudicate against owners 1/2 above;
(b) does anything owned couple depth-2 to a NON-top blade (a named second object would kill);
(c) the multiplicative lift: forced by the exclusion bound, but check no owned row demands the
additive lift; (d) registry: `D0-ALPHA-MU2-FULL-LEDGER-001` owns the counting identity — this
candidate must be minted as its OPERATOR REALIZATION (cross-reference, not duplicate; the ledger
row's uniform counting = the equal weight of the 2¹¹ states inside the top-blade capacity product).
