# TRANSPORT_FORK_ENDGAME — obligation (i): door 5 closure-negative candidacy, the two-12 no-merge, and the tick-mechanism location (POST-SKEPTIC 2026-08-02: 1 KILL accepted+repaired, confirm pass CLEAN)

Campaign 2 of `NEXT_FRONT_PLAN_2026_08.md`. Lean: `D0.Synthesis.TransportForkEndgame`
(sorry-free, wired into `D0/All.lean`, 2026-08-02). Axiom split: the assembled
`transport_fork_endgame` and all load-bearing theorems depend on
`propext/Classical.choice/Quot.sound` only; the two wire theorems `toral_matrix_relation` and
`trace_T17` additionally inherit `Lean.ofReduceBool` from the owned toral module's
`native_decide` and are kept OUT of the assembly. **No door is bound; no registry status
changes; everything here is candidate/DRAFT.**

## Claims (DEF-0.2.2 form)

**C1 (door-5 closure-negative CANDIDATE).** Prior record: the fork memo's POST-COLLAPSE
ADDENDUM (lines 159–165, 2026-08-01) already records both instabilities and the owner-gated
candidacy; C1's increment is the Lean assembly `door_five_unstable` (both conjuncts of
`no_equivariant_seam` restated). In the current fork of obligation (i) of
`D0-ALPHA-SEAM-FORM-FORCED-001` (live doors after the 5→4 collapse: `dim g_light`, `|V₁₁|+1`,
`commutant 12` — memo line 157), the door `commutant 12` is doubly unstable and is proposed for
closure-negative status: (a) the 12 is not dynamics-stable — adjoining the scene adjacency cuts
`Comm(Aut) = 12 = 3²+1+1+1` to `Comm(Aut, A) = 6 = 3+1+1+1` (`D0.Spectral.JointCommutant`,
session module); (b) the algebra's operator class carries zero Feshbach seam
(`no_equivariant_seam`), while the φ⁻¹² factor is a *seam* depth.

**C2 (two-12 no-merge, trap-(d) vaccination).** The 12 of door 1 (`dim g_light = 8+3+1`) and the
12 of door 5 (`Comm(Aut) = 9+1+1+1`, owned as the flavor-frame algebra `M₃⊕ℂ³` by
`D0-REPRESENTATION-RECONSTRUCTION-MAXIMALITY-NOGO-001`: "commutant = flavor-frame algebra
End(generation space) ⊕ ℂ³") have distinct invariant profiles — block partitions `[8,3,1]` vs
`[9,1,1,1]`, centre dims 1 vs 4, derived dims 11 vs 8 — and cannot be identified. Unlike doors
3/4, no collapse is available here.

**C3 (mechanism located; owned on the toral/tick side, impossible on the scenic side).** The
"per-crossing φ⁻¹" object that door 2's binding needs
(cert-comment grade: "φ⁻¹² (the EW transport, |V₁₁|+1 sectors) — product of sectors crossed",
fork memo line 48) is pinned:
  - *positive*: one tick contracts by exactly φ⁻¹ — already OWNED twice over: (i) the φ-ladder
    per-tick ratio `A_{n+1} = A_n·(1/φ)` (`D0-IM-003`, `D0.IM.ContinuumFromFractalTick`) with
    "per-tick entropy rate is log φ" (`D0-CONTINUOUS-TIME-SEMIGROUP-ENVELOPE-001`,
    `rate_factor_eq_tick_weight`); (ii) Lean: φ⁻¹ is the stable root of the toral
    characteristic relation `x²+x−1=0` (`stable_contraction_is_phi_inv`, exclusivity
    `toral_roots_factor`, matrix wire `toral_matrix_relation`) — the same operator that owns
    `ξ₅ = φ⁻⁵` and the rate import `h_KS = ln φ`. NOVELTY SCOPE: the golden quadratic for φ⁻¹
    is prior Lean (`D0.Core.Phi.phi_inv_satisfies_primitive`, via `D0-IM-002`); new is the
    wiring/exclusivity/non-root/`trace_T17` content. The ladder polynomial `x²−x−1` is the
    RECIPROCAL of the toral `x²+x−1`; the fold runs through row 390 + the 583(ii) adoption,
    not through "same quadratic".
  - *counting*: `12 = L₅ + 1 = |V₁₁| + 1` with `lucas 5 = 11` and `card V₁₁ = 11` both Lean-owned
    (`twelve_from_the_same_return`) — the door-2 count is generated by the same fifth return that
    owns ξ₅; the `+1` sector is anonymous. CONDITIONAL on the primitive, the composition
    `φ⁻⁵·φ⁻¹² = φ⁻¹⁷` reads as seventeen ticks (`seventeen_ticks` — unconditionally, `pow_add`
    on the number φ⁻¹, nothing about the seam).
  - *negative (scoped)*: neither φ⁻¹ nor −φ is a root of the transport cubic
    (`phi_inv_not_transport_root`, `neg_phi_not_transport_root` — self-contained instances of the
    `TransportNotGolden` ℚ(√5)-exclusion **on that cubic and nothing more**; its own retraction
    note forbids any wider "cannot reach" reading). The five named-open scenic routes of
    `SceneNativeMultiscaleTower` and the row-583(iii) hyperbolic-seam-generator slot are NOT
    excluded.

**Net effect claimed (RESCOPED post-skeptic, 2026-08-02):** the **door-2 route** reduces to one
named residual primitive: **PRIM-SEAM-CROSSING-TICK-IDENTIFICATION** — *one sector crossing of
the door-2 carrier `V₁₁ ⊔ {∗}` = one toral tick* (the `+1` sector anonymous; any identification
with the `V₁₃` witness belongs inside the primitive, not outside it). What remains live overall:
{the primitive} + {door 1 (`dim g_light`), mechanism unowned either way} + {door 5, owner-gated
closure candidacy}. The multiplicative "ticks" composition is CONDITIONAL on the primitive
(owned seam transport is parabolic-additive, `N² = 0`). If the primitive is bound → door 2
closes positive with depth = 17 ticks. If refuted → the depth axis RE-OPENS (no no-go is
delivered by this module). No probability claim on which.

## Owned pre-facts (verbatim, checked 2026-08-02)

1. Fork memo line 157: "Live doors: `dim g_light`, `|V₁₁|+1`, `commutant 12`." ✓
2. Fork memo line 48: ' "φ⁻¹² (the EW transport, |V₁₁|+1 sectors) — product of sectors crossed"
   — cert-comment' ✓ (door 2's mechanism is cert-comment grade, NOT owned — that is exactly the
   gap C3 addresses).
3. `theory_status_map.csv:163` (D0-IM-003): "constant per-tick ratio A_{n+1}=A_n*(1/phi)" ✓
4. `theory_status_map.csv:390`: "rate_factor_eq_tick_weight (exp(-log phi)=phi^-1, the per-tick
   entropy rate is log phi, forced by matching the discrete tick weight)" ✓
5. `theory_status_map.csv:479` (R1): "commutant = flavor-frame algebra End(generation space) ⊕
   ℂ³"; "dim Comm = 12 = 3^2+1+1+1 … commutantDim by decide" ✓
6. `D0/Core/FiniteTypes.lean:30`: `card_v11 : Fintype.card V11 = 11` ✓;
   `D0/Dynamics/ToralAutomorphism.lean:64`: `def lucas` with `lucas 5 = 11` by decide ✓.

## Pre-registered attack surface (strongest attacks we can build against ourselves)

- **ATT-1 (the relocation objection, strongest).** C3 does not bind anything: the crossing↔tick
  identification is exactly as unowned as the old "product of sectors crossed" comment. Answer we
  will stand on: the *reduction* is the content — mechanism, count, composition and scenic
  impossibility are now theorems; only the identification is left, and it is now NAMED. If the
  skeptic shows the residual is not smaller than before (e.g. shows "crossing = tick" is
  equivalent to the whole original obligation), C3's "net effect" line dies and the module
  becomes a mechanism-location record only.
- **ATT-2 (trap (d) on 17 = 5+12).** Any 17 splits many ways (8+9, 13+4…). Our defense: 5 and 12
  are not free — 5 is the ξ₅ exponent (owned), 12 = L₅+1 with L₅ the Lucas number of the SAME
  return. A skeptic kill would be a second owned split of 17 with equal standing.
- **ATT-3 (door-5 kill is instability, not impossibility).** The door reads a *number* off the
  commutant; a number can label a depth without the algebra carrying the seam. We do NOT claim
  logical impossibility — C1 is closure-negative *candidacy* on two instabilities. If the owner
  judges dimension-labeling legitimate despite both, the door stays open.
- **ATT-4 (the graded-commutant 8).** `nc(2,1) = 8` (row 487) and our derived-dim 8 (`sl₃`) are
  different 8s. Any merge attempt is trap (d); pre-registered so it cannot be smuggled in later.
- **ATT-5 (Lie-algebra profile grades).** Lean has only the numeric inequalities of C2; the
  structural identifications are: commutant `M₃⊕ℂ³` — OWNED (R1 row); `g_light = su(3)⊕su(2)⊕u(1)`
  — the α-line's own reading; centre/derived dims — standard Lie algebra facts stated in the
  docstring, not formalized. C2's grade is: numbers Lean, structure owned+standard.
- **ATT-6 (per-tick φ⁻¹ ownership).** The ladder rows own per-tick φ⁻¹ for the *scale envelope*
  A_n, not for a seam crossing. We claim only "the number and its per-tick reading are
  corpus-native", not "the seam inherits the envelope's dynamics".

## Negative controls

- The root checks can fail on wrong candidates: φ⁻² is NOT a root of `x²+x−1` (it is of
  `x²−3x+1`); −φ⁻¹ misses the transport cubic. The evaluation certificates (`−357φ−2218`,
  `357φ−2575`) are produced by `linear_combination` — not constructed from the conclusion.
- `toral_roots_factor` proves the factorization for ALL x — the roots are exhaustive, not chosen.

## Status language

Everything candidate/DRAFT. C1 mint text prepared but owner-gated. Row 583's obligation-(i) note
update (fork → primitive) goes only with an owner decision after the skeptic pass.


## SKEPTIC VERDICT (2026-08-02) — accepted in full, repairs applied

Three lenses (ownership / math / trap-d): WOUNDED-FIXABLE, WOUNDED-FIXABLE, KILL. All kills
accepted without defense; errors of record:

1. **KILL (trap-d lens): the net-effect line was false.** "Reduces to ONE named residual" — door
   1 untouched, door 5 only owner-gated candidacy; and "if refuted → depth-axis no-go" claimed a
   totality the theorems don't have (two root-checks against one cubic; `SceneNativeMultiscaleTower`
   names five scenic classes explicitly NOT excluded; row 583(iii) keeps an owned-open
   hyperbolic-seam-generator slot). REPAIRED: rescoped to the door-2 route; refutation branch
   corrected to "re-opens".
2. **KILL (same lens): "no scenic operator can supply the factor" / "every φ-power in the seam
   is a toral import" — universal quantifier false**, and the inference shape is the very one
   `TransportNotGolden`'s own retraction withdraws. REPAIRED: scoped to the cubic instances.
3. Witness identification `+1 = V₁₃-witness` was trap-(d)-shaped (owned witness lives in V₁₃,
   disjoint from V₁₁). REPAIRED: anonymous `{∗}`; identification folded into the primitive.
4. Unconditional "seventeen ticks" contradicted the owned parabolic-additive seam transport.
   REPAIRED: conditionalized on the primitive everywhere.
5. "Laplacian spectrum is integral" was uncited (nearest owner covers the 3×3 zone matrix).
   REPAIRED: dropped.
6. Novelty miscitation: the golden quadratic for φ⁻¹ was prior Lean
   (`D0.Core.Phi.phi_inv_satisfies_primitive`). REPAIRED: cross-referenced; "NEW" restricted.
7. "Same golden quadratic" folded reciprocal polynomials `x²−x−1` / `x²+x−1`. REPAIRED: named
   the legitimate bridge (row 390 + 583(ii)).
8. Axiom claim imprecision (two wire theorems inherit `ofReduceBool`). REPAIRED: split stated.
9. C1 prior record (fork-memo addendum 2026-08-01) now cited; increment = Lean assembly with
   BOTH `no_equivariant_seam` conjuncts (`door_five_unstable` extended).

Leads extracted from the kill: (a) a cheap upgrade exists for the negative leg — deg 3 is prime,
so the transport cubic's root field has no quadratic subfield, hence NO polynomial/rational
function of a transport eigenvalue lands in ℚ(φ)∖ℚ — formalizable extension of
`TransportNotGolden` (candidate follow-up, not claimed); (b) the skeptic confirmed no ownership
collision for `PRIM-SEAM-CROSSING-TICK-IDENTIFICATION` and no prior single-primitive reduction —
the door-2-scoped reduction stands as the record.


## C4 ADDENDUM (2026-08-03; POST-SKEPTIC: 1 KILL accepted + repaired, see verdict below): the per-crossing VALUE under the REGISTERED form

Lean: `D0.Synthesis.SeamCrossingWeightForced` (sorry-free, axioms
`propext/Classical.choice/Quot.sound`, wired, 4467 jobs).

**C4 (reduction of the residual primitive).** Inside door 2's product reading (cert-comment:
"φ⁻¹² … product of sectors crossed" over `V₁₁ ⊔ {∗}`), with per-sector weights positive reals:
  1. `equivariant_weights_equal` — Aut-equivariant weights are zone-constant (real-valued
     restatement of the owned swap argument): the ELEVEN `V₁₁`-sector weights are equal.
  2. `crossing_weight_forced` — IF the anonymous 12th weight is uniform (`u = w`) AND the
     REGISTERED total holds — row 583 verbatim: "depth phi^-17 = xi5*phi^-12 SPLIT: seam factor
     xi5 THE / **transport factor OPEN**"; the composition is "owned only as prose — a named
     open sub-leg, not THE" (BOOK_02 02.13.h:95) — then `w = φ⁻¹` EXACTLY (unique positive
     twelfth root). The total is hypothesis (γ), PROOF-TARGET grade, NOT an owned input.
  3. `nonuniform_freedom` — without uniformity, a one-parameter family (explicit witness): the
     hypothesis is load-bearing; the control can fail.
  4. `forced_value_is_toral_stable_root` — the conditionally determined value COINCIDES with
     the toral stable root `x²+x−1 = 0` (coincidence-of-values record; no explanation claim
     while (γ) is open).

**Net for obligation (i), door-2 route (RESCOPED post-KILL):** the primitive's semantic
content reduces to THREE hypotheses — (α) uniformity `u = w`; (β) the count/product reading;
(γ) the registered total `φ⁻¹⁷ = ξ₅·φ⁻¹²` (the OPEN sub-leg of the obligation itself) — under
the equivariance norm (applied to weights-as-moduli, NOT to the pairing operator, whose
non-equivariance `no_equivariant_seam` separately owns) and the positivity reading. Given all
three, the VALUE φ⁻¹ per crossing is a theorem (`seam_crossing_composed` — one composed Lean
statement). C4 advances the obligation MODULO (γ): each hypothesis is strictly weaker than the
original "crossing = tick", so the reduction shrinks the primitive — but any row-583 note
update must say "conditional on the open composition", never "derived from owned". Door 1
remains the live rival; nothing minted.

### Pre-registered attack surface (C4)

- **ATT-A (triviality/circularity, strongest) — [CRITERION FIRED: KILL-1 accepted, see
  verdict below; the block is preserved verbatim-in-spirit as the record of the failed
  defense].** The defense asserted "the depth total φ⁻¹⁷ and the split are owned (row 583 THE
  side)" and conceded "a kill must show the row does NOT own the total". The skeptic showed
  exactly that: the row, its cert, and BOOK_02 02.13.h:95 grade the composition as an OPEN
  sub-leg, "owned only as prose, not THE". The surviving reading: the triviality point stands
  (the value carries no content beyond the hypotheses), but the total is hypothesis (γ), not an
  owned input.
- **ATT-B (equivariance of the weight assignment is itself a hypothesis).** Why must the weight
  function be Aut-equivariant? Normative, not derived: a non-equivariant canonical assignment
  is the corpus's forbidden arbitrary step (same principle as every no-go in the campaign). The
  11-equal conclusion is conditional on that norm; pre-registered as such.
- **ATT-C (positivity/reality of weights).** `w > 0` is the modulus reading of a contraction
  factor; signed weights would break twelfth-root uniqueness (±). If a skeptic exhibits an
  owned signed/complex crossing-weight reading, the forcing rescopes to |weights|.
- **ATT-D (kill-shapes).** Universals are conditional on named hypotheses; carriers: zone-1 of
  the 33-scene + the α-line's own ℝ-dressing. Checked.


### C4 SKEPTIC VERDICT (2026-08-03) — KILL accepted in full, repairs applied

Lenses: ownership KILL, math WOUNDED-FIXABLE. Errors of record:

1. **KILL-1 (by ATT-A's own pre-registered criterion): the depth total φ⁻¹⁷ is NOT owned.**
   Row 583 is OPEN/PROOF-TARGET; its THE side is {ξ₅, sin channel, angle, linear form} only;
   the composition is "owned only as prose — a named open sub-leg, not THE" (BOOK_02
   02.13.h:95; cert S3). Every "derived from the owned form" phrase was a false grade.
   REPAIRED: (γ) named everywhere; "REGISTERED form", "conditional" framing throughout.
2. **KILL-2: suppressed hypothesis.** "(α)∧(β) ⇒ theorems" was false — the forcing consumes
   (γ), which is the open content of the very obligation served. REPAIRED: three-member
   residual; "advances the obligation MODULO (γ)" stated.
3. **W-1: truncated quote.** The memo cited the 583 split as "seam factor THE" dropping
   "transport factor OPEN" — the truncation manufactured the killed premise. REPAIRED:
   verbatim split restored at every citation.
4. **W-2 (flag for owner):** row 305 (`D0-ALPHA-HOLONOMY-002`) still contains the stale
   sentence "the STRUCTURE (… phi^-17 depth) is THE via …" — superseded by the 583 four-level
   reframe; rows 306/307 own angle and channel only, neither the depth. Recorded in
   REGISTRY_ROW_PROPOSALS mechanical-repair section; owner-gated.
5. **W3 (math lens): equivariance norm undercounted + uncomposed pipeline + shared-letter
   carriers.** REPAIRED: norm counted explicitly; `no_equivariant_seam` tension adjudicated
   (moduli vs pairing operator); composed corollary `seam_crossing_composed` added in Lean.
6. **W-3: "the toral tick becomes the EXPLANATION"** overreached while (γ) is open. REPAIRED:
   coincidence-of-values record.

Surviving content (skeptic-confirmed genuinely new, duplication sweep clean): the conditional
per-crossing determination, the uniformity/freedom dichotomy, the composed pipeline, and the
ℝ-restatement of the equivariance forcing.
