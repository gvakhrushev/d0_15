# GAP-W SYNTHESIS — deriving the three joints (W-BRIDGE-1′, W-T1, W-BIT) instead of registering them (DRAFT v2, post-skeptic-#1: Derivation I KILLED — accepted in full; Derivation II SURVIVES; Derivation III WOUNDED → repaired)

**Status:** DRAFT candidate; no registry row edited; no .lean file added to the built tree (Lean material lives as code blocks in this memo only). This memo is the BREAKTHROUGH-SYNTHESIS attack mandated by the owner on the three named joints of `GAP_W_WITNESS_MEMO.md` v2: do NOT register them as assumptions — attack each with the aim that the limitation dissolves. Multi-angle attack performed on each before any negative. **Skeptic pass #1 completed; verdict accepted in full, no defense; all repairs applied in this revision (log at end).**

**Pre-flight run:** `WITNESS`, `re-detection`, `ADDRESSAB`, `CENTER`, `Z2`, `BIT` — owners found and cross-referenced (never duplicated): `D0-GAP-W-WITNESS-PLUS-ONE-001` (frontier PROOF-TARGET, OPEN — the target of this memo), `D0-OMEGA8-CENTER-001` (`omega8_center_collapse`, CORE-FORMALIZED), `D0-Z2-SPINOR-COVER-001` (`z2_spinor_cover`, CORE-FORMALIZED), `D0-TOWER-STOP-NOEXT-001` (CASE-2 schema module), `D0-IM-004` (`witness_halting_cert`). No row owns any of the derivations below.

**Companion check:** `_TASKS_CENTER_ATTACK/gap_w_synthesis_check.py` — **19/19** check() calls PASS, rc=0 post-repair (~12 independent; check 0 is a quote-integrity guard, check 8 textual, checks 9/13 near-analytic, checks 15-17 integrative re-reads — and for Derivation I DEFENSELESS, see E-GWS-4). New check 18 records the kill's named second object. Extends, does not replace, `gap_w_witness_check.py` (10/10, unchanged).

**Verdict in one line (post-skeptic-#1):** **W-T1 DERIVED — joint RETIRED** (license = the DEF-0.2.2 schema itself, owned at full generality BOOK_00:162; type-free Lean statement verified; skeptic: SURVIVES). **W-BIT DERIVED after the SC-1 repair** (one-ℤ₂ uniqueness + computed four-horn fork, with the model enlarged by the archive memory layer; **H3 now explicitly leans on the same channel-exhaustiveness reading as W-REC**). **W-BRIDGE-1′: the derivation was KILLED** — named second object: the invariant NON-scalar (circulant) content of the owned emission channel itself (adverse owned text `BOOK_01:2002`, six lines below this memo's own `:1996` citation — the E-GW-1 failure mode repeated); the joint REVERTS to named status with a SHARPENED core, **W-ELEM** (element-realization: the class is realized as a base-element address, not as invariant operator content), while its outside-Ω₈, ≥1 and stationarity halves ARE derived (§II.6). Net: GAP-W = **FORCED GIVEN TWO residues {W-ELEM, W-REC}** — an honest 3 → 2, NOT the killed headline 3 → 1. NOT claimed: unconditional forcing.

## Claim GW-S (DEF-0.2.2 form, candidate — post-repair)

**GW-S (repaired).** Of the three GAP-W joints:

- **GW-S1 (was W-BRIDGE-1′): KILLED as a derivation, SHARPENED as a joint.** The attempted forcing consumed an unstated conclusion-shaped premise at S9 (element-realization). What genuinely derives (COMPUTED + CONTRACT, given W-ELEM and W-REC): the realizer lies OUTSIDE Ω₈, exists (≥ 1), and is stationary. The joint shrinks from "realized as ≥ 1 stationary marked base element" to **W-ELEM** alone (§II.6).
- **GW-S2 (was W-T1): DERIVED — retired.** The copy-catalogue step on marks is an APPLICATION of an owned universal schema (BOOK_00:162), with type-free Lean arithmetic and three owned cross-class precedents. Skeptic: SURVIVES.
- **GW-S3 (was W-BIT): DERIVED after SC-1 repair.** No second distinguishing bit on adjoined marks: owned one-ℤ₂ uniqueness + computed fork, model enlarged with the archive memory layer; the H3 horn leans on the mark-blindness of the only owned archive channel — the same channel-exhaustiveness reading as W-REC (counted ONCE in the residue inventory).

*Honest scope declared upfront:* GW-S does not make GAP-W unconditional and does not deliver the killed 3 → 1. It reduces the joint inventory from three to **two** ({W-ELEM, W-REC}), with W-ELEM strictly smaller than the W-BRIDGE-1′ it replaces. All candidate language; nothing minted.

## I. Owned pre-facts (verbatim, file:line — every one re-verified from disk by check 0)

### A. The contract layer (CONTRACT-grade)

**P1.** `01_BOOKS/BOOK_01_CONDENSED_FOUNDATIONS_AND_GRAPH_BIRTH.md:296`:

> "A morphism is D0-admissible only if it is visible through finite stages, has a halt quotient, produces a positive quadratic response, respects direct/return asymmetry, and does not introduce a second hidden calibration section."

**P2.** `BOOK_01…md:366` (descent obligation 5): "**halt quotient:** the comparison has a terminal finite quotient, rather than an infinite request for metadata;"

**P3.** `BOOK_01…md:1166` (finite obligations list, item 5): "halt quotient and stable re-detection class;"

**P4.** `BOOK_01…md:363-364` (descent obligations 2-3 — the contract's OWN record format is operator-valued):

> "2. **operator lift:** the finite factor carries a channel `\mathcal D_N` on `\mathcal H_N=\mathbb C^{S_N}`;
> 3. **positive response:** `R_N=\mathcal D_N^\dagger\mathcal D_N`;"

**P5.** `01_BOOKS/BOOK_00_ENTRY_CONTRACT_AND_ADMISSIBILITY.md:177` (Finite Holographic Self-Reading Principle): "The detector must read itself autonomously — hidden states and external memory backgrounds are strictly forbidden."

**P6.** `BOOK_01…md:996` (the re-detection concept is corpus-DEFINED over records):

> "Particles are stable re-detection classes over finite detector records. They are not primitive objects of Book 01."

**P7.** `BOOK_01…md:465`: "The halt quotient is the statement that a registration is completed when further refinement cannot change the finite event class without violating the D0 admissibility constraints."

### B. The schema layer (the W-T1 license)

**P8.** `BOOK_00…md:162` (the DEF-0.2.2 schema is owned at FULL generality, not per-class):

> "**Forcing (DEF 0.2.2 schema):** assume `¬X`; show `¬X` requires extra structure `θ` not fixed by prior DEF/THE; `θ` affects distinguishable outcomes, hence is an exogenous parameter; that contradicts M1; therefore `X`. This strikes not at candidates but at **any** alternative at once, through the single property "requires a catalog.""

**P9.** `BOOK_00…md:385-391` (DEF-0.2.2, the named 5-step recipe (i)-(v); status line beneath: "CORE-FORCING-SCHEMA … an explicit, reusable recipe rather than as ad hoc per-result contradiction arguments").

**P10.** Three owned cross-class instantiations of the copy-catalogue schema (evidence the schema is already applied type-generically):

- subgroup-conjugates: `BOOK_00…md:167` "A non-normal subgroup yields conjugate copies, hence a "which copy" catalog, hence ⊥M1"; `BOOK_01…md:792` "Telling those copies apart requires an external catalog of conjugates — exactly the catalog M1 (DEF-0.2.2) forbids."
- roles: `BOOK_01…md:1530` "Fewer than four leaves the act without a distinguishable record, or requires an external memory to hold the missing role. That is the catalog M1 forbids"
- zones: `09_LEAN_FORMALIZATION/D0/Tower/NoExtension.lean:15-17` (CASE-2), whose docstring itself carries the cross-class citation "(Same forcing as `Ω₈≅Q₈` via Dedekind, BOOK_01 §01.7.1A.)".

**P11.** `NoExtension.lean:47` — the formal statement is `1 < Fintype.card (Equiv.Perm (Fin 2))`; `NoExtension.lean:52` — `Fintype.card (Equiv.Perm (Fin 1)) = 1`. **COMPUTED (check 8): the statement text contains no zone token whatsoever** — pure cardinal arithmetic over `Perm (Fin n)`.

### C. The dynamics/emission layer (Derivation I inputs — INCLUDING the kill text)

**P12.** `BOOK_01…md:1993`:

> "Continuous circulation inside \(\Omega_8\) accumulates the logarithmic trace gradient. The finite event occurs when the circulation closes against \(\omega_0\); this is the terminal halt quotient. The archive sector is not external emptiness. It is the traced complement inside the same finite support, whose self-organization supplies the topological memory layer used downstream …"

Two owned facts used separately: (i) the circulation is confined **inside Ω₈**; (ii) the archive is the **memory layer inside the same finite support**.

**P13.** `BOOK_01…md:1996` (§01.24 Witness Halt Proof):

> "Because the trace gradient has traversed the full signed role cycle before halt, the emitted archive trace is an orbit-averaged shell emission over the group \(G_8\) of symmetries of \(\Omega_8\)"

**P13-ADV (the kill text — found by skeptic #1 SIX lines below P13; error of record E-GWS-1).** `BOOK_01…md:2002`:

> "This average is invariant, but it is **not** a scalar multiple of the identity unless irreducibility of the representation on the trace space is separately proved."

The corpus ITSELF states that the emission's output can be invariant WITHOUT being scalar — i.e., the owned channel can carry stable invariant (circulant) content that is not a base-element address. This is the named second object that kills S9's inference (§II.6). Computed instance: check 18 (avg(E₀₁) is shift-invariant, nonzero, non-scalar; the invariant image is the full 8-dim circulant algebra — exactly what check 2's projection image already exhibited).

**P14.** Lean model of P13: `D0/Topology/WitnessHalting.lean:37-42, 55-70` — `shiftMat`, `orbitAverage = (1/8) Σ_k P_k F P_kᵀ`, proved `= (9/2)·I` and shift-invariant (`witness_halting_cert`, D0-IM-004). Scope clause (:22-24) honoured. NB post-kill: the Lean `= (9/2)·I` scalar result holds for the SPECIFIC `F = diag(1..8)`; P13-ADV governs the general case — the two are consistent, and the general case is the adverse one.

**P15 (adverse, inherited from skeptic #1 of the witness memo — kept on the ledger).** `BOOK_01…md:846` (halt-quotient output = four-role readout algebra, +0) and `:858` ("The construction of the finite incidence graph marker `V_9` comes only after this quotient", +0 sequencing).

### D. The one-ℤ₂ layer (Derivation III inputs)

**P16.** `BOOK_01…md:806` (§01.7.1B, status THEOREM): "Inside `Q_8` the three canonical structural subgroups collapse to a single copy of `\mathbb Z_2`: \([Q_8,Q_8] = Z(Q_8) = \Phi(Q_8) = \{\pm1\}\)." Machine-checked owner: `D0-OMEGA8-CENTER-001` (`D0.Foundation.Omega8Center`, `omega8_center_collapse`, CORE-FORMALIZED). Identification synthesis: `D0-Z2-SPINOR-COVER-001` (`z2_spinor_cover` — "one ℤ₂, seven incarnations", ≥4 exhibited as the SAME order-2 element in Lean).

**P17.** `BOOK_01…md:1539` (home scope = Ω₈'s construction — used ONLY there in this memo): "The sign bit `{+,−}` is the irreversibility of write/erase; it is the one bit that distinguishes the act from its undo, and no further bit can be added without an exogenous orientation catalog."

**P18.** `BOOK_01…md:1530` (owned bit inventory): the four roles = D₂×D₂ forced; with the sign, 3 owned bits, `|Ω₈| = 2³ = 8` (P17). **COMPUTED (check 12): the 8 owned-bit addresses are bijective with the Ω₈ cells and all occupied.**

**P19.** `BOOK_00…md:143` (M1): "no exogenous parameters: no background dependence, no external catalog."

### E. Narrated texts — status CORRECTED post-kill

A1 `:1541` ("A reusable shell also requires a stationary marked witness section `ω0`"), A2 `:1987` ("first addressable graph-birth shell"), A5 `:1556` ("`V10` has an extra hidden marker"). **v1 of this memo demoted all three to corroboration; that demotion is PARTIALLY REVERSED (E-GWS-2):** with Derivation I killed, A1/A2 return to load-bearing status as the narrated owners of the surviving joint **W-ELEM** (they are exactly the texts that say the class realizer is a marked base ELEMENT — "witness section", "addressable shell"). A5 stays corroboration-only (the 10-kill is carried by Derivations II+III).

**Skeptic-found lead (recorded, NOT consumed — owner decision):** `BOOK_01…md:253`: "The symbol `\mathfrak q` is the irreducible addressable record quantum." — this text TYPES records as addressable quanta and is the strongest candidate owner for discharging W-ELEM by a future forge; it needs its own scope argument (is `\mathfrak q` the event-class record or the primitive dyad's registration quantum?). **Candidate future forge; OWNER DECISION; not used anywhere in this memo's chains.**

## II. Derivation I — W-BRIDGE-1′: KILLED (skeptic #1; accepted in full, no defense)

> **KILL BANNER.** The attempted derivation of W-BRIDGE-1′ is dead. Named second object: **the invariant non-scalar (circulant) content of the owned emission channel itself** — exhibited by this memo's own check 2 (the projection image is the 8-dim circulant algebra), recorded by the new check 18, and owned ADVERSELY by `BOOK_01:2002` (P13-ADV), six lines below the `:1996` sentence this memo cited. The stable re-detection class could be realized as invariant OPERATOR CONTENT of the archive record — no base-element address required — unless the element-realization premise is separately owned. S9's first clause ("an Ω₈-only base cannot host the realizer") silently consumed that premise, which is precisely the conclusion-shaped core of old W-BRIDGE-1′. By the letter of the pre-registered attack ATT-S2, the circularity attack LANDED. The chain below is retained for the record with its dead step marked; §II.6 states what honestly survives.

### II.1 The claim that was attempted

Old joint (witness memo v2, E3): *"the stable re-detection class is realized as ≥ 1 stationary marked base element."* v1 of this memo claimed it derived modulo W-REC. **That claim is withdrawn.**

### II.2 The chain (retained for the record; dead step marked)

| step | statement | tag | owner | post-kill status |
|---|---|---|---|---|
| S1 | any admissible registration has a halt quotient AND a stable re-detection class | OWNED-CONTRACT | P1 `:296`, P2 `:366`, P3 `:1166` | stands |
| S2 | re-detection classes are classes **over finite detector records** | OWNED-DEFINITION | P6 `:996` | stands |
| S3 | all records live in-support: external memory banned; the in-support memory layer is the archive | OWNED-CONTRACT + OWNED-TEXT | P5 `:177`; P12(ii) `:1993` | stands |
| S4 | the contract's own record format is finite-operator-valued | OWNED-CONTRACT | P4 `:363-364` | stands — and CUTS AGAINST S9: operator-valued records can carry the class as operator content (P13-ADV) |
| S5 | the event record emitted into the archive is the orbit-averaged shell emission over `G₈` | OWNED-TEXT + OWNED-LEAN model | P13 `:1996`; P14 | stands |
| S6 | **W-REC:** that emission is the ONLY cross-event record channel from the circulated sector | READING (residue) | P6 + P13 definite article; sweep §II.5 | stands as a named residue |
| S7 | orbit-averaging transmits ZERO interior ABSOLUTE-ADDRESS information (circulant image, constant diagonal; any transitive `G₈`) | COMPUTED | checks 1, 2, 7 | stands — scope corrected: absolute addresses only; the image is NOT contentless (check 18) |
| S8 | Ω₈ has no stationary element and no invariant nonempty proper subset | COMPUTED | checks 3, 4 | stands |
| S9 | ~~hence an Ω₈-only base cannot host the realizer~~ ⇒ ≥ 1 element beyond Ω₈ | **DEAD** | — | **KILLED: needs the unstated premise W-ELEM** — "the class must be realized as a base-element ADDRESS, not as invariant operator content". The invariant circulant content (P13-ADV, check 18) is a stable, in-support, channel-transmissible realizer candidate that S7/S8 do NOT exclude |
| S10 | any Ω₈-preserving bijection of the 9-point base fixes the 9th point; block averaging preserves the mark's marker | COMPUTED | check 5; P12(i) | stands — conditional on the +1 existing |

### II.3 DEF-0.2.2 instantiation — WITHDRAWN as written (E-GWS-3)

v1's step (ii) claimed ¬X forces extra structure θ. It does not, without W-ELEM: the re-detection class can be realized as the invariant operator content of the archive record — owned structure, no θ needed (P13-ADV). The mis-attribution of the forcing load to the P8/P9 schema, when the load actually sat on the unstated element-realization premise, is error of record E-GWS-3. The schema is not at fault; the instantiation was.

### II.4 Consistency with the adverse texts (P15) — unchanged

`:846`/`:858` (halt-proper +0, V₉ "only after") remain consistent with everything retained; nothing here reopens E-GW-1.

### II.5 The residue W-REC (still a named residue — of the SURVIVING halves and of Derivation III's H3)

> **W-REC:** "Every record available for cross-event re-detection of the circulated sector's event class is sourced in the archive memory layer (`:1993`), and every archive write from the circulated sector factors through the orbit-averaged shell emission of `:1996`."

Corrected properties (E-GWS-2):

1. **Conclusion-free:** stands (no ω₀, no +1, no stationarity in the sentence).
2. ~~**Strictly weaker:** W-REC + computation ⇒ old joint~~ — **FALSE, error of record E-GWS-2.** W-REC + computation does NOT imply the old joint; the implication additionally needs W-ELEM. Corrected statement: {W-ELEM, W-REC} + computation ⇒ old joint's full content.
3. **Sweep performed:** the only owned archive-write texts found for the circulated sector are P13 (`:1996`) and the archive map Δ (`:465` — continuum metadata, archive-side). No owned text names a second cross-event record channel. (Falsifiable; kill condition ATT-S1 stands.)
4. **Snapshot escape:** remark unchanged (phase-anchor regress), remark-level only.

### II.6 What honestly SURVIVES inside the dead derivation (the joint is SHARPENED, not restored whole)

Given **W-ELEM** (below) and **W-REC**, the following are now DERIVED — none of them was derived in the witness memo v2, where the whole package was one narrated joint:

- **Outside-Ω₈ (location):** no Ω₈-interior element or subset can be the element-realizer (COMPUTED: freeness, subset-triviality — checks 3, 4), and no interior absolute address survives the owned channel (COMPUTED: checks 1, 2, 7).
- **Existence (≥ 1):** given W-ELEM (the realizer is a base-element address) + S3 (in-support, no external memory) + finiteness: the element must exist and lie beyond Ω₈.
- **Stationarity:** given m = 1, forced by cardinality (COMPUTED check 5: all 40320 Ω₈-preserving bijections fix the 9th point; the mark's marker survives block averaging exactly) + P12(i) (circulation confined inside Ω₈). Not read off the word "stable".

**The sharpened joint, stated exactly (what sentence is still narrated):**

> **W-ELEM (element-realization):** "The stable re-detection class is realized as a **base-element address** — not as invariant operator content of the emission channel (the circulant algebra of `:2002`)."

Narrated owners: A1 `:1541` ("stationary marked witness **section**"), A2 `:1987` ("first **addressable** graph-birth shell"), A3/P12 `:1993` ("closes against ω₀"). Strictly SMALLER than old W-BRIDGE-1′: the old joint asserted existence + location + stationarity + markedness in one sentence; W-ELEM asserts only the realization FORMAT (element-address vs operator-content). Pre-registered discharge lead: `:253` ("irreducible addressable record quantum") — owner decision, §I.E.

**Per-joint verdict (Derivation I): W-BRIDGE-1′ = STILL-NARRATED, SHARPENED to W-ELEM.** The derivation died; the joint shrank. Missing sentence = W-ELEM above (plus the standing W-REC residue).

## III. Derivation II — W-T1: the instantiation license evaporates (skeptic #1: SURVIVES)

### III.1 The objection being dissolved

Witness memo v2 (ATT-3): "CASE-2's ⊥M1 reading is declared for zones; marks are a new object class" — a skeptic may demand a per-class schema owner.

### III.2 The dissolution (three prongs, each owned or computed)

1. **The license is the schema itself, and the schema is owned UNIVERSALLY.** P8 (`BOOK_00:162`) states DEF-0.2.2 as quantifying over ANY alternative: "This strikes not at candidates but at **any** alternative at once, through the single property 'requires a catalog.'" P9 (`:385-391`) makes it a named CORE-FORCING-SCHEMA — "an explicit, reusable recipe rather than … ad hoc per-result contradiction arguments". A per-class license demand contradicts the schema's own charter sentence. The demand dissolves at CONTRACT grade, not by analogy.
2. **The Lean content is type-free — verified, not asserted.** COMPUTED (check 8): the formal statement of `repeat_has_nontrivial_copy_symmetry` is `1 < Fintype.card (Equiv.Perm (Fin 2))` — no zone token appears in the statement; it is arithmetic about ANY repeated pair. Check 9: the count consumes cardinality only (four element types, same count). The zone-specificity lives exclusively in comments.
3. **The corpus already applies the schema cross-class, three times** (P10): subgroup-conjugates (BOOK_00:167; BOOK_01:792), roles (:1530), zones (NoExtension CASE-2 — whose docstring itself cites the Dedekind instance cross-class). Marks are instance #4 of an owned universal, not a new license.

What remains is the definitional step — *two indistinguishable copies admit no owned selector* — true by the meaning of "indistinguishable". A definitional step is not a joint; it cannot fail separately from its inputs.

### III.3 DEF-0.2.2 instantiation on marks

(i) assume two indistinguishable stationary marks with one of them acting as the class realizer/terminus; (ii) definiteness of the record requires selecting one; no owned data selects (definitional) — the selection is extra structure θ, and the copy symmetry is nontrivial (`1 < |S₂|`, OWNED-LEAN P11); (iii) θ affects which base element hosts the class address — a distinguishable outcome of the registered structure; (iv) θ = a "which copy" catalog, the exact object P10's owned instances name; ⊥M1; (v) no two indistinguishable marks. ∎ No-overfire: `|S₁| = 1` (P11, `first_instance_canonical`) — nothing fires at one mark (check 16).

**Per-joint verdict (Derivation II): W-T1 = DERIVED — joint RETIRED** (application of an owned universal schema; skeptic #1: SURVIVES).

## IV. Derivation III — W-BIT from the owned one-ℤ₂ uniqueness (skeptic #1: WOUNDED → SC-1 repair applied)

### IV.1 The claim being derived

Old joint: `:1539`'s "no further bit" transferred from Ω₈'s construction to labels on adjoined marks (scope transfer, load-bearing by toggle). New route: derive no-further-bit for marks from the OWNED uniqueness of the central ℤ₂ — never using `:1539` outside its home.

### IV.2 The owned uniqueness, sharpened computationally

P16 owns `[Q₈,Q₈] = Z(Q₈) = Φ(Q₈) = {±1}` (THEOREM + CORE-FORMALIZED). COMPUTED (check 11, extending the Lean owner): full subgroup enumeration gives 6 subgroups, 3 maximal, Frattini = {±1} DIRECTLY; and — the sharpest form — **Q₈ has exactly ONE involution (−1)**. So every ℤ₂ inside the owned role group IS the central one: there is no second owned bit anywhere in the role algebra. The role algebra's one canonical bit is spent: it is the ± of Ω₈.

### IV.3 The exhaustive fork (suppose marks ω₀ ≠ ω₁, both stationary, distinguished by θ)

Reduction first: **any distinguishing label is a bit** — COMPUTED (check 13, all 90 label maps). Fork on the bit's source (excluded middle at each node):

| horn | analysis | closure | tag |
|---|---|---|---|
| H1: θ = the owned central ℤ₂ **as action** | the central involution's owned action moves every element (translation by −1 is free — COMPUTED check 4); a pair carrying the owned ℤ₂-action is toggled: ω₀ ↔ ω₁, neither fixed | contradicts the STATIONARY hypothesis | COMPUTED + P16 |
| H2: θ = owned bits **as coordinates** | the owned bit space is exhausted: 2³ = 8 addresses, all occupied (COMPUTED check 12); a mark with an owned-bit address is either ON the cycle (moved — check 4 ⇒ not stationary) or a second occupant (indistinguishable copy ⇒ Derivation II fires) | contradiction either way | COMPUTED + P18 |
| H3: θ **decorative** (static assignment decoupled from all owned structure) | the mark-swap remains an automorphism of ALL owned structure **including the archive layer** (SC-1 repair below; COMPUTED check 10 extended) ⇒ two compatible assignments ⇒ θ not fixed by prior DEF/THE — Derivation II fires on the labeled pair; equivalently a static second ℤ₂ has NO owned source (§IV.2; D0-Z2-SPINOR-COVER-001) ⇒ θ exogenous directly | ⊥M1 twice over | COMPUTED + P16 |
| H4: θ **dynamical** (couples marks to owned dynamics) | coupling a mark into the circulation makes the cycle arity 9 ≠ 8 (owned arity 8: P18, P14 `shiftMat` over `Fin 8`), the mark is moved (not stationary), and the archive SEES the coupling (period change — COMPUTED check 10) | hypothesis fails | COMPUTED + P14 |

### IV.3a The SC-1 repair (skeptic #1 wound, accepted; applied here and in check 10)

The v1 model omitted an owned pre-graph-birth structure: the **archive memory layer** (`:1993`, P12(ii)). Repair: **SC-1 (extended inventory)** — at the graph-birth stage the owned structure on the base is {role algebra, sign, circulation, **archive memory layer**}. The H3 closure survives the extension because **the archive's only owned write channel from the circulated sector is the `:1996` emission, whose domain is the 8 cells only — mark-blind by construction** — so the mark-swap extends to an automorphism of the ENLARGED model (COMPUTED: check 10, `archive_write` mark-blindness + swap-compatibility; the dynamical horn is, correctly, VISIBLE to the archive — period change — confirming decorative/dynamical is the archive-relevant split).

**Stated explicitly (skeptic's requirement):** the mark-blindness of the archive channel is the same **channel-exhaustiveness reading as W-REC**. H3 — and therefore Derivation III — now LEANS on W-REC. Since W-REC is already in the residue inventory, this adds no new joint; it adds a second consumer of the existing one. If W-REC falls (ATT-S1 kill condition), both Derivation I's surviving halves AND Derivation III's H3 horn reopen.

DEF-0.2.2 read: in every horn, ¬X either contradicts its own hypotheses (H1, H2-on-cycle, H4) or requires θ unfixed by prior DEF/THE that affects a distinguishable outcome (H2-copy, H3) ⇒ ⊥M1 ⇒ X. ∎

Negative control (conclusion-failing, check 14): in a Q₈×C₂ world the center has 3 central involutions — a second owned bit would EXIST and labeled-V₁₀ would SURVIVE. The kill fires precisely because the owned group is Q₈ (Dedekind §01.7.1A + D0-OMEGA8-CENTER-001).

`:1539` (P17) appears only as home-scope corroboration. The scope transfer is retired.

**Per-joint verdict (Derivation III): W-BIT = DERIVED-AFTER-SC1-REPAIR** — conditional on W-REC (shared residue, counted once), resting otherwise on Lean-owned uniqueness + five computations, with the counterfactual failure world exhibited.

## V. Net verdict and the honest GAP-W statement (post-skeptic)

| joint (witness memo v2) | verdict | what replaced it |
|---|---|---|
| W-BRIDGE-1′ (conclusion-shaped, narrated ×3) | **STILL-NARRATED — SHARPENED to W-ELEM** (derivation KILLED by P13-ADV `:2002` / check 18) | outside-Ω₈ + ≥1 + stationarity halves DERIVED given {W-ELEM, W-REC} (checks 1-5, 7); only the realization-FORMAT sentence W-ELEM remains narrated (owners A1 `:1541`, A2 `:1987`, P12 `:1993`; discharge lead `:253`, owner decision) |
| W-T1 (instantiation license) | **DERIVED — RETIRED** (skeptic: SURVIVES) | DEF-0.2.2's own universal charter (P8-P9) + type-free Lean statement (check 8) + 3 owned precedents (P10) |
| W-BIT (`:1539` scope transfer) | **DERIVED-AFTER-SC1-REPAIR** (skeptic: WOUNDED → repaired; leans on W-REC) | one-ℤ₂ uniqueness (P16, Lean-owned ×2) + computed four-horn fork with archive-extended model (checks 4, 10-14) |

**Honest GAP-W statement (candidate):** `|V_base| = |Ω₈| + 1 = 9` is **FORCED GIVEN TWO named residues: {W-ELEM, W-REC}** — the 8-kill from {W-ELEM, W-REC} + computations (§II.6), the 10-kill from Derivations II + III (III leaning on W-REC), no-overfire by `first_instance_canonical`. Joint inventory: **3 → 2**, where W-ELEM ⊊ old W-BRIDGE-1′ (format-only, its other three halves derived) and W-REC is conclusion-free. The v1 headline "3 → 1" is DEAD (E-GWS-2). This 2-joint version supersedes the witness memo's 3-joint accounting **as a candidate only** — the SC-1 repair passed the re-run (19/19 rc=0), but registry supersession is the owner's call.

**What the two remaining joints exactly say:**

1. **W-ELEM:** "The stable re-detection class is realized as a base-element address — not as invariant operator content of the emission channel (the circulant algebra of `:2002`)."
2. **W-REC:** "Every record available for cross-event re-detection of the circulated sector's event class is sourced in the archive memory layer (`:1993`), and every archive write from the circulated sector factors through the orbit-averaged shell emission of `:1996`."

## VI. Lean path (post-repair skeleton — memo-only, NOT added to the built tree)

The decidable lemmas OB-S1 (marker-averaging identity), OB-S2 (invariant subsets trivial), OB-S3 (Ω₈-preserving permutation fixes the witness) and OB-S4 (unique involution in Q₈) remain valid, buildable obligations exactly as sketched in v1 (schematic block preserved in the git history of this file; the statements are mirrored by checks 1-5, 11). The conditional capstone is UNCHANGED in form:

```lean
-- theorem card_base_forced_conditional (m : ℕ) (h_halt : 1 ≤ m) (h_nocopy : m < 2) :
--     Fintype.card Omega8 + m = 9
```

with corrected support attribution: `h_halt` is backed by {W-ELEM, W-REC} + OB-S1/S2/S3 (NOT by contract alone — E-GWS-3); `h_nocopy` by Derivation II (owned schema) + Derivation III (leaning on W-REC). The hypotheses stay explicit in Lean; no over-claim.

**Registry note (PROPOSED text only — no row edited; v1's proposal was STALE, E-GWS-5):** row `D0-GAP-W-WITNESS-PLUS-ONE-001` EXACT-MISSING could truthfully become: "(1) built Lean module with OB-S1..OB-S4 + capstone; (2) TWO residues registered — W-ELEM (element-realization; narrated owners :1541/:1987/:1993; discharge lead :253 unforged) and W-REC (channel-exhaustiveness; consumed by BOTH the 8-kill and the 10-kill's H3); (3) W-T1 retired (derived), W-BIT derived-after-SC-1 (leans on W-REC); (4) GAP-E untouched." Per `MINTING_PACKAGES.md` vocabulary this remains PROOF-TARGET.

## VII. Computation — `gap_w_synthesis_check.py`, 19/19 PASS (rc=0, post-repair)

Count from the script's own counter: 19 check() calls; ~12 independent (0 quote-integrity — now including the kill text `:2002` and the lead `:253`; 8 textual; 9/13 near-analytic; 15-17 integrative re-reads). **Check 15's K-ADDRESS criterion encodes the killed S9 inference — the sweep has ZERO defensive power for Derivation I** (E-GWS-4); it is retained as bookkeeping under {W-ELEM, W-REC}. New check 18 = the kill's named second object, computed (adverse to Derivation I by design). Check 10 carries the SC-1 archive extension. Conclusion-failing negative controls: check 6 (non-transitive model saves an interior address), check 14 (second owned ℤ₂ saves labeled-V₁₀), check 18 (adverse: invariant non-scalar content exists — this one FIRES, which is exactly the kill).

## VIII. Named risks & attack surface (post-skeptic status per line)

- **ATT-S1 (W-REC is a reading) — STANDS, now with TWO consumers.** Kill condition unchanged: an owned text naming a cross-event record channel from the circulated sector that does not factor through the `:1996` emission kills the §II.6 surviving halves AND reopens Derivation III's H3. The §II.5 sweep found none.
- **ATT-S2 (representability circularity) — LANDED (the kill).** v1's answer ("never uses an addressability⇒representability axiom") was WRONG: S9 consumed exactly that premise, unstated. The pre-registered attack surface worked as designed; error of record E-GWS-3. The premise is now named W-ELEM and carried openly.
- **ATT-S3 (fork completeness / SC-1) — WOUND REPAIRED.** SC-1's inventory was incomplete (archive layer omitted); extended and recomputed (check 10). Standing reopening hook: owned pre-graph-birth structure that touches adjoined marks DIFFERENTIALLY and does not factor through the mark-blind emission would reopen H3/H4.
- **ATT-S4 (labels-vs-bits) — STANDS** (check 13; H3 closed by swap-automorphism computation on the extended model).
- **ATT-S5 (indistinguishability of marks) — STANDS** (any distinguisher re-enters the H1-H4 fork; adjunction-order is H3-decorative unless owned dynamics records it, and the owned order-memory is the central ℤ₂ — spent, H1).
- **ATT-S6 (model-dependence of transitivity) — STANDS, inherited** (check 6; conjugation max orbit 2 < 8 vs `:1996` full traversal; `:1996` remains narration — no new dependence added).
- **ATT-S7 (stationary-marks scoping) — STANDS** (non-witness 10th elements are GAP-E's 11/13 ladder, out of scope).
- **NEW — recurring failure mode, named (E-GWS-1 = E-GW-1 pattern #2): "citation stops short of its adverse continuation."** Twice now, a load-bearing citation was quoted accurately but its adverse continuation a few lines below was missed (`:858` in the witness memo v1; `:2002` here). Discipline adopted for future forges: every load-bearing quote must be swept ±10 lines for adverse continuations before the memo leaves DRAFT; candidate addition to the d0-adversarial-forcing-loop traps checklist (owner decision).

## IX. What this does NOT show

- It does NOT derive W-BRIDGE-1′: that derivation is KILLED; the joint survives sharpened (W-ELEM).
- It does NOT make GAP-W unconditional: TWO residues remain ({W-ELEM, W-REC}; exact sentences in §V).
- It does NOT deliver v1's "3 → 1" headline — that is an error of record (E-GWS-2); the honest count is 3 → 2.
- It does NOT edit the registry, the built Lean tree, or the witness memo; `gap_w_witness_check.py` (10/10) and `GAP_W_WITNESS_MEMO.md` v2 stand as the prior state of record; supersession by this 2-joint accounting is a candidate pending owner decision.
- It does NOT reopen E-GW-1 (halt-proper stays +0), and does NOT consume the `:253` lead (recorded for the owner, unforged).
- It does NOT identify the `8+1` and `4+5` decompositions of 9, and does NOT touch GAP-E.
- It does NOT upgrade D0-IM-004 — on the contrary, P13-ADV bounds it: its scalar result is F-specific; the general invariant is non-scalar (`:2002`).

## Skeptic pass #1 — verdict and repair log (accepted in full, no defense)

**Overall verdict (relayed 2026-07-06): Derivation I KILLED; Derivation II SURVIVES; Derivation III WOUNDED (repairable); headline "3 → 1" KILLED.** All repairs applied in this revision (v2); companion script re-run after repairs: **19/19 PASS, rc=0**.

Errors of record (enumerated, no defense):

- **E-GWS-1 (the kill).** Named second object: the shift-invariant circulant content of the owned emission channel (check 2's own image; Lean instance `witness_halting_cert`; adverse owned text `BOOK_01:2002` — SIX lines below the memo's `:1996` citation). The citation-stops-short pattern is E-GW-1 repeated — named as a recurring failure mode (§VIII) with a discipline adopted.
- **E-GWS-2 (false weakness claim).** §II.5 property 2 ("W-REC + computation ⇒ old joint") was FALSE; the implication needs W-ELEM. Headline "3 → 1" died with it; honest inventory 3 → 2. §II.5 corrected; A1/A2 re-promoted to load-bearing owners of W-ELEM (§I.E).
- **E-GWS-3 (load mis-attribution).** §II.3's step (ii) attributed its forcing power to the P8/P9 schema while the actual load sat on the unstated element-realization premise. Instantiation withdrawn; premise named (W-ELEM) and carried openly.
- **E-GWS-4 (defenseless sweep).** Check 15's K-ADDRESS criterion encodes the dead S9 inference — zero defensive power for Derivation I; sentence added to §VII and the script docstring.
- **E-GWS-5 (stale registry proposal).** v1's proposed EXACT-MISSING text ("the single residue W-REC registered, replacing the three-joint listing") was premature; replaced by the two-residue proposal (§VI).

Skeptic findings that STRENGTHEN the record (logged): the SC-1 archive-layer wound and its exact repair (applied, computed — check 10); the explicit statement that H3 leans on W-REC (applied, §IV.3a); the `:253` "irreducible addressable record quantum" discharge lead for W-ELEM (recorded, unforged, owner decision); the confirmation that Derivation II's three prongs hold with no named gap.
