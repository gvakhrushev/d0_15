# CLOSE_GAP_E_MINIMAL_FIRST — the owner-route 11th pass: MINIMAL-FIRST / NO-SKIP — the owner's second half: 15 and 17 are SELECTIONS, 13 is not — v2, POST-SKEPTIC

> ## ⚖️ VERDICT (independent skeptic §05.8.R, 2026-07-06 — WOUNDED-FIXABLE, NOT killed; three
> repairs ACCEPTED IN FULL and applied):
> **The argument SURVIVES at its self-declared OPERATIVE grade.** The kill of the even rivals
> 15/17 holds via the no-skip clause with the E-b begging-point discharged; the grade is
> **CLOSED-MODULO-THREE-ASSEMBLIES**, not clean closure. Repairs: **R1** (strongest finding) — the
> positive-voice equivalence "selecting a non-minimal admissible address demands a which-one
> catalog" was a THIRD assembly transfer **T3** smuggled into "one clause, two voices"; now named
> in the T-ledger (§7 SELF-ATT-1, §8), grade demoted MODULO-TWO → MODULO-THREE. **R2** — script
> VERDICT banner "z₃=13 FORCED" softened to "forced MODULO T1/T2/T3". **R3** — the OWN-4 EoR worry
> DISCHARGED affirmatively (§7 SELF-ATT-3): T2's order-omission reading is corpus-absent (grep=0
> outside this memo), and OWN-4's "no owned laid-out space supports skip" was about the empty-slot
> TRANSFER route — a different object from row 522's native clause (grep=0 in the EB-forge memo).
> No overclaim beyond modulo-three-assemblies. Script re-run: 52/52, `--deny-discharge` rc=2
> (modulo-assembly, not clean). No closure asserted; nothing minted.

**Status:** DRAFT candidate (post-skeptic v2); no registry row edited; no `.lean` added; no cert
minted; `053040` untouched; no commit. **Stop-rule note:** the 9th memo's stop-rule bars re-FORGING; this pass is
OWNER-AUTHORIZED (the owner supplied the argument — the second half of his closing argument after
pass 10's Leg 1) and enters through the stop-rule's own door (a): it forges the maximal brief for
an owner-route, and — where the assembled rule turns out to be owned-clauses-only — says so with
the transfers NAMED and GRADED (§8). It does not deny any standing kill; it overturns the GROUND
of one standing gate (P3′'s E-b gating) with on-disk owned text, the same move-class as pass 10's
Leg 1 (a standing clause applied to an instance the record had missed).

**Pre-flight (2026-07-06):** `preflight.sh no-skip minimal-first z3` — owners cross-referenced,
never duplicated: row 522 `D0-SCENE-STEP-PARITY-001` (LEAN_PROVED, CORE-FORMALIZED — BOTH legs
live there), row 257 `D0-TOWER-STOP-NOEXT-001` (COUNT-3), rows 546/547/549, row 556
`D0-P-M1-SATURATION-001`. Full GAP-E family re-read: R3/COMPLETENESS (E-b verbatim), E-b FORGE
(OWN-2/3/4 verbatim — the standing half-dissolution), DYAD-POWER, 5th, 6th, RAISE-7th, META-9th
(+ stop-rule), OWNER-10th (P3′ + verdict banner), `window_forcing_check.py`,
`gap_e_completeness_check.py`. Git timing verified: `SceneStepParity.lean` entered the tree
2026-06-24 (`a836144`) — the no-skip sentence PREDATES every GAP-E adjudication cited here; no
new sentence has appeared; this pass re-reads standing text, it does not discover new text.

**Companion script:** `close_gap_e_minimal_first_check.py` — **52/52 PASS**, can-fail,
mutation-tested (MUT-A/B/C/D/E + guard-separation NCs + QV layer of 26 verbatim quotes), integer
arithmetic only, no filter consumes the assembly partition bound `|X| ≤ 8` (checked functionally:
kills hold to z₃ = 61). **rc=0** = memo verified AT ITS STATED GRADE (operative, modulo T1/T2/T3 —
grade printed in the script's own verdict banner, R2); **`--deny-discharge` → rc=2** = the
pre-discharge world (passes 5–10 residue) reproduced, demonstrating exactly what the discharge
buys and that the script CAN reach the other verdict.

---

## §0. Claim (DEF-0.2.2 form)

**Claim MF (minimal-first / no-skip; the owner's argument forged to its maximal strength).**
Assume `¬MF`: a 3-zone tower with base 9 and third address `z₃ ∈ {15, 17}` (or any `z₃ > 13`;
the partition bound is NOT consumed) is M1-admissible. The owned corpus contains: (i) the
realized extension `V₁₃ = V₉ ⊔ ABCD` — 13's address-hood as an OWNED EXISTENCE fact, produced
with no step at all (B01:1548-54, VERIFIED count cert; EB-FORGE OWN-2, standing post-skeptic:
"13 IS an owned cardinality independent of the ladder"); (ii) the no-skip clause of row 522
(`SceneStepParity.lean:18-20`): "omitting an admissible address without an internal reason is a
skip-catalog, `⊥M1`", whose OWN canonical instance fires on an address (11) that is NOT a member
of the tower being stepped (9→13) and whose credential is nothing but an owned realization
(`11 = L₅ = |V₁₁|`); (iii) the certified nine-pass absence of any producing sentence — hence of
any internal reason — for the rivals. Then the rival tower omits the owned-admissible address 13
(both F3 horns, computed §4) without an internal reason: the skip-catalog, `⊥M1`. Contradiction.
Symmetrically every middle-slot rival (`z₂ > 11`) omits 11. The unique tower firing NO clause is
`(9,11,13)`: its omitted values 10, 12 carry owned internal reasons (parity + `:1556`), and
selecting 13 is not a selection — it is the ℕ-minimum of the admissible candidates, and a
which-one catalog is needed only to pick a NON-minimal element (§5). Hence `z₃ = 13` and the
window `[9,13]` is sealed above — **at the corpus's operative grade (narrated CORE-FORCING +
Lean-backed numerics), MODULO three NAMED assembly transfers T1/T2/T3 (§8; R1 post-skeptic), NOT
at Lean-owned grade.**

## §1. Owned pre-facts (verbatim, file:line — all 26 machine-verified by the script's QV layer)

- **P1 (the no-skip clause — row 522's second leg, in-tree since 2026-06-24).**
  `SceneStepParity.lean:18-20`: "**No-skip from `11 = L₅`.** A `+4` step `9 → 13` skips `11 = L₅`
  (the 5th Lucas number = `|V₁₁|`, an admissible torus address); omitting an admissible address
  without an internal reason is a skip-catalog, `⊥M1`. So the step is the minimal
  parity-preserving one, `+2`." Lean owns the arithmetic (`step_skips_eleven`, `lucas_five`,
  `det_T` — all `by decide`); the ⊥M1 closure is docstring-narrated. Registry row 522
  (CORE-FORMALIZED, "Lean-owned finite/formal D0 core statement") carries the clause's conclusion
  in its own notes field: "11=L5 is the skipped admissible address ruling out +4. Step=+2."
- **P2 (the book-level conclusion sentences, Status: CORE-FORCING).** B01:1893 "the address
  ladder is forced to advance by `+2`, never `+1`"; B01:1903 "Hence the minimal admissible
  junction step is `+2`"; B01:1909 "**Any other step requires an external splice-control
  parameter, hence `⊥M1`**" and "capacity sets the cardinalities, orientation-parity sets the
  increment, and **neither leans on the other**"; B03 §03.23.6 "Any step other than `+2`
  re-imports an external gluing parameter". Adjudicated scope (three independent skeptics, P4 of
  pass 10): the PRINTED parity mechanism reaches only ODD steps; "Any other step" is a
  conclusion-sentence whose even-step mechanism the books never print. **The even-step mechanism
  is exactly P1's skip-catalog sentence** — the two are the conclusion and the mechanism of one
  clause, held apart until now only by the E-b begging-point (P5).
- **P3 (13's slot-hood — owned existence, ladder-free; the discharge premise).** B01:1548 "The
  next two shell extensions are the only primitive unresolved capacities over the pointed shell:
  the direct/return dyad and the full terminal role square." B01:1551-53 `V₁₁=V₉⊔D₂`,
  `V₁₃=V₉⊔ four terminal roles A,B,C,D`. B01:1556 "This gives the first complete scene sizes
  `(9,11,13)`. The construction rules out alternatives: `V8` has no basepoint, `V10` has an extra
  hidden marker …" Cert `vp_v1141_abcd_omega8_v9_phi_capacity.py` VERIFIED (count). **EB-FORGE
  OWN-2 (standing, post-skeptic):** "`V₁₃ = V₉ ⊔ ABCD` is a **set-extension of the base shell**,
  produced with **no step at all** … **13 IS an owned cardinality independent of the ladder** —
  the parent memo's begging-point … is **refuted for the over-base reading**." Used here as
  MEMBERSHIP only (existence of the realization), never as exhaustiveness (DYAD-POWER upheld).
- **P4 (the E-b begging-point — the gate, exact wording and exact direction).**
  `GAP_E_COMPLETENESS_MEMO.md:94`: "**Named begging-point (pre-registered):** the transfer
  presumes the skipped address (13, when stepping 11→15) already *is* a slot that can be 'empty'
  — a pre-laid-out address space, which is close to assuming the ladder." Note the direction: the
  begging-point is stated FOR the kill-15 direction, and its content is a demand for a
  **pre-laid-out address space**.
- **P5 (the standing gate at pass 10 — what this pass overturns the ground of).**
  `CLOSE_GAP_E_OWNER_MEMO.md` P3′ (§1:91-104, skeptic-accepted 2026-07-06): the no-skip leg
  "stays E-b-missing … adjudicated here so Leg 1 cannot be read as upgrading it"; "the NO-SKIP
  leg has conclusion-words plus the E-b begging-point". The pass-10 skeptic's own strongest
  finding (relayed by the owner; on-disk trace = P3′ + verdict-banner R1): applied uniformly,
  no-skip would kill the even rivals too — `(9,11,15)` skips the admissible address 13 — held
  back ONLY by the begging-point. **So the entire question of this pass is: is the begging-point
  discharged?** Nothing else stands between the standing clause and the even rivals.
- **P6 (the EB-forge halves — what is already adjudicated).** OWN-2: half 1 DISSOLVED (P3 above).
  OWN-3: "Existence of the owned extensions does not entail non-existence of a rival extension"
  — half 2 (completeness/maximality) STANDS; this pass does NOT contest it (§9: the quantifier is
  not minted, it is made moot). OWN-4: "the pattern is a *surplus* detector, E-b needs a *skip*
  detector, and no owned laid-out space supports 'skip'" — adjudicated FOR the empty-slot-pattern
  TRANSFER route (B03:931/B01:1911 surplus instances). **Row 522's native skip clause is not that
  route and was not the object of OWN-4's sentence** — it IS a skip detector, in the tree, with
  its own firing instance (P1); timing verified (pre-flight): it predates the EB-forge memo, but
  OWN-4's scan was over the BOOK empty-slot instances; the docstring clause appears nowhere in
  the EB-forge memo's text. This is a records-coverage point of exactly pass-10 Leg-1's class
  (standing owned sentence, never applied to the recorded rival).
- **P7 (no internal reason for the rivals — certified absence).** Nine passes + pass 10
  (R3 → … → META-9th → OWNER-10th): no producing sentence, no preference sentence, no owned
  clause of any kind FOR `X₆`/`X₈`/15/17 exists (the 9th's taxonomy; the 10th's §4 residue
  "the two named even rivals"). Hence "without an internal reason" holds for the omission of 13
  by the rivals at certified-absence grade.
- **P8 (minimal-first as owned idiom — the positive reading's pedigree).** B01:190 "There is
  exactly one answer that imports no external catalog: … the length of its **minimal admissible
  code**" (the corpus's foundational referring device IS minimality-as-catalog-freedom);
  B01:1393 "the **first** nontrivial stable return"; B01:1990 "the **first** addressable
  graph-birth shell"; B01:1576 (C3) "or the addresses 9/11/13 stop being **minimal**"; P2's
  "minimal admissible junction step". Idiom, not a rule-sentence — graded as SUPPORT (§8).
- **P9 (the saturation shape — SHAPE-precedent only).** `RAISE_M1CORE_SATURATION_MEMO.md`
  P-M1-SATURATION ("`X_core` is the unique F-extremum in 𝒞", 4 instances RAISED, skeptic
  NO-KILL; row 556 OPEN/PROOF-TARGET). The candidate 5th instance: F = tower size-order,
  𝒞 = parity-admissible towers, extremum = minimum = the realized tower. **Cited as shape, not
  as transfer** — the row is itself OPEN; nothing here rides on it.
- **P10 (guards inherited).** Row 549 (same-size labeling = torsor, selector M1-forbidden): the
  rule must not and does not fire on same-size forks (no size fork ⇒ no omission). Row 257
  (COUNT-3): owns the STOP at three zones — the internal reason why no-skip cannot demand a
  4th zone at 15. Pass-10 Leg 1 (parity kill of odd letter-counts): untouched and consumed as
  the internal-reason ledger for 10/12 (plus `:1556`'s by-name exclusions).

## §2. The owner's argument, forged (the two halves in one line each)

**Half A (pass 10, closed):** every odd-letter-count rival dies by owned parity; the rival space
is `z₃ ∈ {15,17}` under the assembly bound, `z₃ odd > 13` without it.
**Half B (this pass):** *15 and 17 are not minimal; selecting them over 13 requires exactly the
catalog M1 forbids; 13 requires no catalog — it is first-in-order.* Formally: the rival's
admissibility hypothesis + owned 13-membership + the no-skip clause ⇒ the rival's construction
omits an owned-admissible address without an internal reason ⇒ skip-catalog ⊥M1. The owned
tower's omissions (10, 12) all carry owned internal reasons; its third address is the
ℕ-minimum, and ℕ's order is not a catalog (the comparator is the corpus's own cardinality
arithmetic — the same `<` that makes `4 = 2+2` readable in every horn). The which-one register
of pass-10's Leg 2 materializes ONLY for non-minimal choices: to pick 15 over 13 one must SAY
WHICH admissible address was passed over and WHY — that saying is the catalog; to pick the
minimum one says nothing, because nothing admissible was passed over. **Minimal-first is the
no-skip clause read positively — one clause, two grammatical voices** (R1, post-skeptic: reading
the two voices AS one clause is the NAMED transfer T3, §8; the underlying identity — argmin over
the parity-admissible pool == the unique no-skip survivor — is exact, script §VI, and is not the
transfer).

## §3. The E-b discharge (the load-bearing new step, adjudicated head-on)

The begging-point (P4) demands a **pre-laid-out address space** in which 13 pre-exists as a slot.
Three on-disk facts discharge it FOR THIS DIRECTION:

1. **13's address-hood is owned EXISTENCE, not presumption** (P3; EB-forge OWN-2 standing:
   "no step at all"). No pre-laid space is invoked: the credential is the realized extension.
2. **The clause's own canonical instance never used a pre-laid space either.** In "9→13 skips
   11", the credential for 11 is `L₅ = |V₁₁|` — an owned realization, NOT membership in the
   stepped tower (11 is no address of the walk 9→13) and NOT a slot of a pre-laid grid. **The
   firing condition, read off the clause's only owned instance, is: the omitted value carries an
   owned realization-credential and no internal reason for its omission exists.** 13 under the
   rivals satisfies it exactly as 11 under the +4 step does. This is the key structural
   precedent: the clause fires on addresses not in the rival tower — it always did.
3. **The pre-laid-space worry is real — for a DIFFERENT rule** (script MUT-C): a rule firing on
   every skipped INTEGER kills the owned tower itself (10, 12). Row 522's rule does not — the
   admissibility credential + internal-reason clause are exactly what separate it from the
   strawman the begging-point correctly fears. The begging-point thus lands on the pre-laid rule
   and misses the owned one.

**What the discharge does NOT touch:** EB-forge OWN-3 (existence ⇏ non-existence of rivals)
stands — this pass never argues 15 is impossible-as-extension; it argues SELECTING it is ⊥M1
given that 13 is owned-admissible and passed over. The completeness quantifier (is `{D₂, ABCD}`
the complete capacity list?) is not minted (§9); the window seals around it.

## §4. Computed results (script §V; every quantity from independent objects)

**Firing table (both F3 horns computed separately; every kill fires on BOTH — fork-robust, no
adjudication smuggled; `NO-SKIP[a,…]` lists the omitted owned-admissible addresses):**

| tower | verdict |
|---|---|
| **(9,11,13)** | **SURVIVES** (omits 10, 12 — both carry owned internal reasons, neither owned-admissible) |
| (9,11,15) | NO-SKIP[13] — the pass-10 skeptic's uniform application, now discharged |
| (9,11,17) | NO-SKIP[13] (15 is NOT owned-admissible — membership only is consumed) |
| (9,13,15) / (9,13,17) | NO-SKIP[11] — middle slot; dyad-before-square ORDER forced as a corollary |
| (9,15,17) | NO-SKIP[11,13] |
| (9,11,12) | PARITY only (no-skip silent: 12 has no credential — guard separation, Leg 1 intact) |
| (9,11,13,15) | COUNT3 only (no-skip silent: stop is row 257's, internally reasoned) |
| (9,11,19/21/…61 odd) | NO-SKIP[13] — **the partition bound `|X| ≤ 8` is NOT consumed anywhere** |

**Full scan:** base-9 strictly-increasing triples, `z₂ < z₃ < 62`: unique survivor `(9,11,13)`
(UNIQUE-SURVIVOR-BOUND-FREE). Corollaries: (i) the upper seal no longer leans on the
assembly-grade partition bound — pass 10's R7 finiteness caveat DROPS OUT of the window question
(the rival space needn't be finite to be dead); (ii) H7's residual burden (kill {6,8}) is
absorbed; (iii) the slot ORDER (dyad before square) is forced by the same clause, matching owned
construction order — a consistency bonus, not a new claim.

**Minimal-first equivalence (script §VI):** the unique no-skip survivor == the lexicographic
minimum of the parity-admissible pool, computed with ℕ's `<` alone. A which-one catalog is
demanded only by NON-minimal choices (MUT-E: maximal-first picks a no-skip-killed tower).
**Transfer flag (R1, post-skeptic):** reading this equivalence AS the positive voice of the
no-skip clause — "selecting a non-minimal admissible address demands a which-one catalog" — is a
NAMED assembly transfer **T3** (§8), not owned-rule-grade. The computed identity (argmin ==
no-skip-survivor) is exact; the CLAIM that the two clauses are one is the transfer.

## §5. Proves-too-much sweep (script §VII — every owned practice SAFE)

| owned practice | rule verdict |
|---|---|
| Lucas-index step 5→7 skipping 6 (the books' good example) | SAFE — 6 flips the det class: owned internal reason (B01:1903) |
| 9→11 omitting 10; 11→13 omitting 12 | SAFE — no credential + owned reasons (parity, `:1556`) |
| stop at 13, no 4th zone | SAFE — omission is defined below `max(t)` only; row 257 owns the stop |
| zone-2 = 11, not smaller | SAFE — 10 parity-dead; 11 is the minimal even step (docstring's own words) |
| first stable return 44, first addressable shell V₉ | consistent idiom (support-grade, not rule-consumed) |
| same-size relabelings (row 549 torsor) | rule cannot fire — no size fork ⇒ no omission (guard, P10) |

MUT-C is the sweep's teeth: the PRE-LAID mutant (fire on every skipped integer) kills the owned
tower — demonstrating the sweep would catch a proves-too-much rule, and that the credential
clause is what keeps the owned rule safe.

## §6. Mutations (script §VIII — each can fail the CONCLUSION)

- **MUT-A** (fake internal reason for omitting 13): uniqueness LOST — "without an internal
  reason" is load-bearing and conclusion-failable.
- **MUT-B** (revoke 13's membership credential — the pre-discharge world): the passes-5-10
  residue `z₃ ∈ {13,15,17,…}` is exactly reproduced — the discharge is THE load-bearing step.
- **MUT-C** (pre-laid-ℕ rule): owned tower dies — see §3.3/§5.
- **MUT-D** (parity-only scope, the old reading): even rivals survive —
  `window_forcing_check.py`'s `NC_PARITY_ADMITS_PLUS4` stays TRUE; this pass ADDS a filter and
  contradicts no prior cert (the NC was a statement about parity's scope, not no-skip's truth).
- **MUT-E** (maximal-first): picks a killed tower — the catalog-free extremum is the minimum.

## §7. Named risks & PRE-REGISTERED attack surface (strongest first)

- **SELF-ATT-1 (ownership vs authorship of the assembled rule — the strongest).** Is the rule
  "minimal-first = catalog-free" OWNED or AUTHORED? Honest answer, clause by clause: the no-skip
  SENTENCE is owned (P1, in-tree, row 522's notes field); the book CONCLUSION "any other step ⊥M1"
  is owned (P2, CORE-FORCING); 13's credential is owned (P3); the absence of internal reasons is
  certified (P7); the ℕ-comparator is arithmetic. What is NOT a quoted sentence: **(T1)** the
  instantiation "13 is an *admissible address* in P1's sense" — the corpus never defines
  'admissible address'; the clause's only instance credentials 11 by owned realization, and T1
  extends the predicate like-for-like to the other owned realization. Per trap (o), an owned
  universal clause instantiated is an ownership GAIN, not a new sentence — but the predicate's
  extension IS an assembly move and is graded as such. **(T2)** the over-base horn's "omission"
  is order-omission (admissible address unrealized below a realized larger one), not walk-skip;
  the ladder horn is verbatim, the over-base reading is an assembly inch. **(T3 — R1,
  post-skeptic, the skeptic's strongest finding.)** The positive-voice equivalence
  "selecting a non-minimal admissible address demands a which-one catalog" (§2, §4 minimal-first
  equivalence; script MINFIRST-EQUALS-NOSKIP-SURVIVOR) is a THIRD transfer: the identity
  argmin==no-skip-survivor is exact, but READING the two clauses as one — so that "13 is the
  minimum" carries the ⊥M1 force of "the rival skips 13" — is an assembly move, not an owned
  rule. It was smuggled into "one clause, two grammatical voices" (§2); it is now named. Grade
  T3 the same as T1: trap-(o) like-for-like from owned text (an ownership GAIN — the positive
  reading is DERIVED from the owned no-skip clause + ℕ's order, both owned), but a NAMED transfer,
  not owned-rule-grade. If a skeptic strikes T1, T2, or T3, the honest fallback is pre-registered:
  the result decays to DOOR-(a)-ARMED-PLUS — U∥H7 joined by a THIRD candidate sentence MF ("an
  owned-admissible address may not be passed over without an owned reason"), which unlike U and H7
  is one INSTANTIATION away from owned text, not one ban away. **The claim ladder is:
  CLOSED-MODULO-THREE-ASSEMBLIES (T1+T2+T3 granted) ⤳ DOOR-ARMED (any denied). This memo asserts
  the FIRST rung and prices the second.**
- **SELF-ATT-2 (overturning a one-day-old accepted adjudication).** P3′ (pass 10) kept no-skip
  gated and said Leg 1 must not be read as upgrading it. This pass does not re-read Leg 1 — it
  brings NEW on-disk material to the gate's GROUND: the begging-point's own text (P4) demands a
  pre-laid space; EB-forge OWN-2 (which P3′ did not cite) removed that demand's object for 13;
  the clause's own instance (P1) never used it. P3′'s gating cited COMPLETENESS E-b §R3 and the
  two-guard record — both of which predate none of these texts but never CONFRONTED OWN-2 with
  the firing-instance precedent. If the skeptic finds P3′ already considered and rejected this
  exact combination, this pass is KILLED — pre-registered falsifier, checkable against P3′'s
  four sentences (it did not: P3′'s ground list is "conclusion-words + begging-point + two-guard
  record", no mention of OWN-2 or of the 11∉tower precedent).
- **SELF-ATT-3 (does OWN-4 already block this? — DISCHARGED, R3).** EB-forge OWN-4's "no owned
  laid-out space supports 'skip'" reads, out of context, like a standing kill of any skip route.
  **Affirmative discharge (not a dilemma-dodge):** OWN-4's OBJECT is stated in its own text — the
  TRANSFER of the book's empty-slot (surplus) pattern (its cited instances are B03:931 and
  B01:1911, both surplus detectors; its final sentence "Half 1's dissolution does not rescue
  THIS" binds to that transfer). Row 522's native no-skip clause is a DIFFERENT object: it is an
  in-tree skip detector with its own firing instance (P1), and it is **absent from the EB-forge
  memo's entire text — grep-verified: zero occurrences of `SceneStepParity` / `skip-catalog` /
  `row 522` in that file.** OWN-4 therefore did not, and could not, adjudicate row 522's clause;
  its scope is the surplus-pattern transfer, and this pass rides the native clause, not that
  transfer. No EoR note against OWN-4 is needed — OWN-4 is CORRECT about the object it names.
  Symmetrically, the T2 "over-base order-omission" reading is itself **corpus-absent** (grep:
  the phrase occurs nowhere outside this pass's own memo + script) — it is disclosed as a NAMED
  transfer precisely because it is not owned text, and it is not in tension with OWN-4 (which
  concerns surplus, not order-omission).
- **SELF-ATT-4 (grade honesty).** The ⊥M1 of the no-skip clause is docstring/book narration —
  the SAME grade as the parity leg's ban (its `⊥M1` also rests on narrated no-aliasing, P3 of
  pass 10). Pass 10 established this as the corpus's operative grade, and three skeptics accepted
  Leg 1 at it. A referee stripping narrated CORE-FORCING strips both legs and the whole window
  discussion — the relative claim (the record's own standard, uniformly applied, forces z₃=13)
  survives at every grade. Conceded and priced.
- **SELF-ATT-5 (circularity).** The admissible-credential set {9,11,13} is built from owned
  realization quotes, and the conclusion is computed from filters over a 62-wide grid; the
  can-fail character is demonstrated by MUT-A/B (both flip the conclusion). `scene_triple_unique`
  (row 530) is not consumed (script H1). The saturation row (556, OPEN) bears no load (P9).
- **SELF-ATT-6 (role-assignment joint).** The argument is size/address-level throughout; the
  shell↦role-square assignment joint (COMPLETENESS E2.4) stays open and named, exactly as in
  pass 10.

## §8. THE GRADE (the pass's honest deliverable)

**Verdict claimed (post-skeptic, R1): CLOSED-MODULO-THREE-ASSEMBLIES, at the corpus's operative
grade.** The assembled theorem (full statement §10) uses owned clauses only, PLUS exactly three
named assembly transfers, each trap-(o) like-for-like from owned text (an ownership GAIN, but a
NAMED transfer, not owned-rule-grade):

- **T1 — admissible-address instantiation** (instantiation-assembly): extend P1's 'admissible
  address' predicate from its one owned instance (11, credential `L₅=|V₁₁|`) to the other owned
  realization (13, credential `V₁₃=V₉⊔ABCD` + VERIFIED cert + OWN-2). Like-for-like; no new
  quantifier; the predicate is applied to nothing else.
- **T2 — over-base order-omission reading** (reading-assembly): on the Sum-types horn "omits" =
  "leaves an owned-admissible address unrealized below a realized larger one". The ladder horn
  needs no T2 (walk-skip verbatim, the docstring's own +4 form with 11→15 in place of 9→13). The
  phrase is corpus-absent (grep, §7 SELF-ATT-3) — disclosed precisely because it is not owned.
- **T3 — positive-voice equivalence** (reading-assembly; the skeptic's strongest finding, R1):
  read the exact identity argmin==no-skip-survivor (script §VI) AS a single clause, so that "13
  is the ℕ-minimum" carries the ⊥M1 force of "the rival skips 13". The identity is exact; the
  ONE-CLAUSE reading is the transfer. Was previously smuggled into "one clause, two voices" (§2);
  now named. Without T3 the minimal-first framing is descriptive only and the kill rests on T1+T2.

NOT claimed: Lean-owned closure; the completeness quantifier (OWN-3 stands — moot, not minted);
finiteness of the abstract rival space at owned grade (irrelevant now — the kill is bound-free);
any change to row 549 (same-size torsor untouched); any re-reading of the 9th's grammar
adjudication (A2/A6 untouched: no-skip is a BAN with its own ⊥, not the M1 one-liner across
outcome classes, and not firewall non-promotion).

## §9. What this memo does NOT do

- Does NOT mint anything, edit any row/cert/book/built-`.lean`/`053040`; no commit.
- Does NOT mint the completeness quantifier: whether `{D₂, ABCD}` exhausts the abstract capacity
  inventory stays formally OPEN (EB-forge OWN-3 stands verbatim). The window seals because
  SELECTING any rival is ⊥M1 — rival admissibility-as-capacity becomes moot for the window.
- Does NOT deny pass-10 P3′ was correct WHEN MADE (its ground list simply did not contain OWN-2
  + the firing-instance precedent; this is a ground-supersession, not a contradiction).
- Does NOT resurrect: the 9th's presence-ban (this fires only on an OMISSION with a named second
  object — the passed-over owned address), the empty-slot transfer (OWN-4 stands for that
  route), the dyad-power generator, U-as-derivation (pass 10's R6 downgrade stands; U is now
  superfluous for the window but remains door-(a) inventory).
- Does NOT touch GAP-W (lower seal unchanged, registered-bridge grade).

## §10. FINAL STATE (pre-skeptic; the assembled theorem)

> **THEOREM-CANDIDATE (operative grade, modulo T1/T2/T3).** Over the owned corpus: (base-9, GAP-W) ∧
> (COUNT-3, row 257) ∧ (parity, B01:1893-1909/B03/row 522 — kills every even address, hence
> z₃=12 and all odd letter-counts, pass 10) ∧ (no-skip, row 522:18-20 with the E-b begging-point
> discharged by EB-OWN-2 + the clause's own firing instance — kills every tower omitting an
> owned-admissible address without an internal reason: all z₂>11, all z₃>13, bound-free) ⇒ the
> unique admissible 3-zone tower is `(9,11,13)`; `z₃ = 13`; the window `[9,13]` is sealed above.
> Equivalently (positive voice): the realized tower is the ℕ-minimal admissible tower, and
> minimality is the unique catalog-free resolution of the slot fork — selecting any non-minimal
> filling demands the which-one catalog M1 forbids (P-M1-SATURATION shape, 5th instance,
> shape-cite). Falsifier/reopening hook: exhibit an owned internal reason for omitting 13 (MUT-A
> flips the verdict), or an owned sentence confining 'admissible address' to Lucas-ladder values
> (T1 dies), or a P3′-style adjudication that already confronted OWN-2 with the firing instance
> (SELF-ATT-2 falsifier).

**Sustained by the skeptic at CLOSED-MODULO-THREE-ASSEMBLIES (WOUNDED-FIXABLE → repaired):**
mint package = one registry row (D0-GAP-E-MINIMAL-FIRST-NOSKIP-001, PROOF-TARGET → owner
decision; cert this script), EoR-style ground-supersession notes for P3′/row 547 two-guard/
B01:1562, records update for `window_forcing_check.py` (NC stays true; annotation only), H7/U
demoted from "required" to "superfluous-for-window, door-(a) inventory". **If wounded at
T1/T2/T3:** fallback = DOOR-(a)-ARMED-PLUS with candidate sentence MF (§7 SELF-ATT-1), one
instantiation from owned text. Stop-rule: intact — this pass entered by owner authorization and
leaves the quantifier un-minted either way. The three transfers are the EXACT difference between
this operative-grade seal and a clean owned closure — no more is claimed.
