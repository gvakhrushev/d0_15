# R1 result — RETRODICTION FAILED (recorded negative, frozen criterion)

**Freeze:** `R1_FROZEN_CRITERION.md`, commit `f6e315e`, BEFORE data contact.
**Data:** `d0_gap_labels.json` (25 plateaux of `D0-GAP-LABEL-GENERICITY-NOGO-001`).
**Runner:** `r1_run.py` (deterministic, null seed 20260814, 10 000 trials).

## Outcome under the frozen adjudication

- **R1a (primary, three-gap closure): FAIL, control FIRED.** The 25 sorted plateaux have
  **5** distinct exact circular gaps (as exact `(dn, dm)` module pairs), not 3:
  `(−21,34) ≈ 0.01316 ×1 · (13,−21) ≈ 0.02129 ×7 · (−8,13) ≈ 0.03444 ×11 ·
  (5,−8) ≈ 0.05573 ×5 · (−6,10) ≈ 0.18034 ×1`.
  Null control: 0.18% of random same-box label sets pass R1a (< 1% → the criterion was
  informative; the FAIL is a real FAIL, not a vacuous one).
- **R1b (secondary, reflection triples): FAIL.** T = 1 of 23; null 99th percentile = 1;
  PASS required T > 1.

**Frozen consequence (from the pre-registration):** the move-4 closure shape (two interior
elements generate the forced third) is NOT present in the move-5 gap-label dataset under
this criterion. The matrix cell (quasicrystal × rewind 5→4) takes the FAIL grade. N6's cap
stands unchanged.

## Post-hoc observation (recorded as post-hoc, NOT promoted, no credit claimed)

Seen only AFTER data contact: four of the five gap values are consecutive golden powers —
`0.01316 = φ⁻⁹, 0.02129 = φ⁻⁸, 0.03444 = φ⁻⁷, 0.05573 = φ⁻⁶` (Fibonacci pairs
`(F, −F′)`), the fifth is `2φ⁻⁵`; adjacent powers satisfy `φ⁻⁹ + φ⁻⁸ = φ⁻⁷` by the
recursion. This ladder is very likely MODULE-GENERIC, not D0-specific: any small difference
of two elements of ℤ + ℤ/φ is a small module element, and small module elements are
near-best-approximant Fibonacci pairs — i.e. exactly the shape N6 warns about. If anyone
wants to test the ladder, it needs a NEW frozen criterion in a NEW file with a null that
samples sparse MODULE subsets (not random labels); expected outcome under N6: the null
passes too, i.e. the ladder discriminates nothing.

## Program consequence

First rewind check of the coverage matrix: executed and FAILED honestly. The 5→4 rewind on
the quasicrystal column is dead under this criterion; the next-best rewind is **R3**
(IceCube kernel vs one-register model — quantum column, move 1→2), which unlike R1 tests a
PASSPORT cell against an OWNED floor rather than module combinatorics.
