# SELECTOR-OBSTRUCTION — the within-zone canonical selector as a torsor section over Aut(K)=S9×S11×S13: the Aut-EQUIVARIANT class is DECIDABLY impossible (obstruction non-trivial, 0 sections), but this arm rules out ONLY the equivariant class; the SSB/dynamical/basepoint arms are NOT closed by it and are treated by a separate relocation-of-catalog test (DRAFT v1, pre-skeptic)

**Status:** DRAFT candidate v1; no registry row edited, no cert minted, no `.lean` touched.
Companion script `selector_obstruction_check.py` — **25/25 PASS** (exact integer/permutation
arithmetic; enumeration for n≤4, simply-transitive theorem applied to the actual zones 9,11,13).
Pre-flight run (keywords: obstruction, torsor, equivariant, canonical section, selector): the
relevant owned content is **M2_PHASE_LABELING_MEMO.md** (the zone-9 torsor), **D0-CARRIER-NOT-
ICOSAHEDRAL-001** (induced zone symmetry trivial), **D0-DSIGMA-ROLE-CYCLE-CARRIER-CANONICAL-
NOGO-001** (BOOK_04:1399, "any rank-5 carrier is an arbitrary symmetry-breaking choice"), and
**D0-M1-PREDICATE-001** (the M1 forced/catalogue predicate). This memo **extends, does not
duplicate**: M2 proved zone-9 labeling is a torsor *up to G_res* under three text-readings; the
DΣ no-go proved the triangle-carrier analogue; this memo states the **general section-existence
obstruction over the full Aut group for the per-vertex within-zone selector**, decides the
equivariant class decidably, and — the load-bearing new discipline — **precisely delimits which
selector class it rules out** and runs the DEEP-TRAP relocation test against the SSB/basepoint
evaders.

---

## Claim X (DEF-0.2.2 form, candidate language)

Frozen scene K(9,11,13), zones V₉ ⊔ V₁₁ ⊔ V₁₃, automorphism group **Aut(K) = S₉ × S₁₁ × S₁₃**
(BOOK_01:1576 orbital theorem; BOOK_04:1399). Define a **within-zone labeling** of zone n as a
bijection ℓₙ : Vₙ → Lₙ onto a fixed label alphabet |Lₙ| = n (a "canonical vertex selector" is a
choice of such an ℓₙ, i.e. a section of the labeling torsor). Aut(K) restricts on zone n to the
full symmetric group Sₙ acting by precomposition on Vₙ.

**(X1) Torsor structure (verified, exact).** The set Bij(Vₙ, Lₙ) of within-zone labelings is a
**single Sₙ-orbit with trivial stabilizer** — a torsor. Computed: #orbits = 1, stabilizer = 1 for
n = 2,3,4 by full enumeration, and by the simply-transitive theorem for the actual n = 9,11,13.
Residual freedom of the torsor = the full group |Sₙ| (362880 / 39916800 / 6227020800). This
re-derives, at the per-vertex graph-slot level and over the *whole* Aut group, exactly the
"forced up to G_res, one orbit" torsor that M2 established on the detector shell.

**(X2) The equivariant section-existence obstruction is NON-TRIVIAL (decidable no-go).** An
Aut-**equivariant** canonical labeling is a section fixed by the whole Sₙ action — a bijection ℓₙ
with g·ℓₙ = ℓₙ for all g ∈ Sₙ. The count of such fixed bijections is **0 for every n ≥ 2**
(computed n=2,3,4; theorem for 9,11,13). Reason (the invariance wall, independently recomputed):
an Sₙ-invariant *function* Vₙ → Lₙ is constant on the single Sₙ-orbit of vertices, hence constant,
hence **not injective** for n ≥ 2 — so no invariant bijection exists. The joint labeling over
Aut(K) = S₉×S₁₁×S₁₃ is a product; a joint equivariant section exists iff one exists on each factor;
each factor (n ≥ 2) contributes 0, so **the joint count is 0**. The obstruction to an internal
Aut-equivariant canonical selector is therefore **non-trivial**: no such selector can exist.

**(X3) Scope delimitation — WHICH selector class this rules out (the crux).** X2 rules out
**exactly the Aut-equivariant class**: internal rules ℓₙ that commute with the full Sₙ relabeling.
It does **NOT**, by itself, rule out an owned rule generated by owned symmetry-**breaking**
(SSB / dynamics / a basepoint / an ω₀-anchor) — such a rule is by construction *not* Sₙ-equivariant
(it fixes the vertex the symmetry moves). The honest question is therefore split:

  * **X3a (closed here):** no internal *equivariant* canonical selector. DECIDABLE no-go, 0 sections.
  * **X3b (the real meta-question):** is there a canonical selector by ANY owned internal rule,
    including symmetry-breaking ones? This memo does **not** close X3b by the equivariant count.
    It closes it only for those SSB/basepoint arms that **fail the relocation-of-catalog test**
    (§ Deep-trap test below), and it identifies the single surviving evasion route precisely.

**(X4) Deep-trap verdict (the M1 relocation test).** Every owned symmetry-breaking mechanism
inspected either (a) produces a **degenerate manifold** of equally-owned outcomes and then needs an
external catalogue to pick one point on it — which does **not** escape M1, it relocates the
catalogue (BOOK_01:1570 C1 names this exact move: "A part-internal exception would name a
privileged vertex — an exogenous parameter — violating M1"); or (b) selects a **unique** object but
that object lives on the **pre-graph detector shell** V₉ = Ω₈ ⊔ {ω₀}, and its transport onto the
9 graph vertices/slots is again the un-owned torsor (M2 X4; "the graph begins only after this cycle
is lifted", BOOK_01:778). No inspected mechanism selects a unique graph-side vertex by an internal
owned rule with no residual freedom. **The honest closure is therefore the external bridge — but as
a FORCED consequence of the M1 relocation test, not a chosen convenience**, subject to the named
reopening hooks below.

---

## Owned pre-facts (verbatim, file:line — each verified to exist on disk this session)

1. **Aut(K) and C1 (the M1-owned no-privileged-vertex law)** — BOOK_01:1570:
   "**(C1) Exchangeability.** No permutation inside a part may change the physics, i.e. the
   adjacency matrix `A` is invariant under `S9 × S11 × S13`. A part-internal exception would name a
   privileged vertex — an exogenous parameter — violating M1." (Verified 2026-07-05.)
2. **The scene is forced, Aut = S9×S11×S13, |E|=359** — BOOK_01:1576: "From **(C1)**, `A` is
   constant on the orbits of `S9 × S11 × S13`." and :1580 "unique loopless-and-complete block
   pattern, the complete tripartite graph with `|E| = 359` (Lean-proved)." (Verified.)
3. **Basepoint is a pre-graph detector-shell object** — BOOK_01:778: "The detector closure is
   therefore not `8` as a graph vertex count. It is a closed signed terminal role cycle. **The
   graph begins only after this cycle is lifted.**" and BOOK_01:1541: "A reusable shell also
   requires a stationary marked witness section `ω0`." (Verified — ω₀ marks the shell, not a
   graph vertex.)
4. **DΣ role-cycle carrier no-go — the symmetry-breaking-is-a-choice precedent** — BOOK_04:1399:
   "the oriented automorphism group `Aut(K(9,11,13)) = S₉×S₁₁×S₁₃` is transitive on the 1287
   triangles (a single orbit), and the operational roles carry no intrinsic geometric attachment,
   so the five roles cannot be canonically injected into distinct cycle orbit-classes — **any
   rank-5 carrier is an arbitrary symmetry-breaking choice**" (D0-DSIGMA-ROLE-CYCLE-CARRIER-
   CANONICAL-NOGO-001, NO-GO). (Verified — this is the triangle-level analogue of X2/X4.)
5. **Induced zone symmetry is trivial (the size-asymmetry obstruction)** — CarrierNotIcosahedral.
   lean:11-14: "`Aut(K(9,11,13)) = S₉×S₁₁×S₁₃`; because the zone sizes are distinct (`9≠11≠13`),
   the induced symmetry on the three zone-classes (the rank-3 image) is **trivial** — only the
   identity preserves the size assignment." + theorem `induced_zone_symmetry_trivial` (card = 1,
   `09_LEAN_FORMALIZATION/D0/Claims/CarrierNotIcosahedral.lean:39-41`). (Verified — note: this is
   about the 3 zone-CLASSES; the per-VERTEX obstruction of this memo is the complementary within-
   zone statement.)
6. **M1 as a formal predicate (forced ⇒ unique; alternative ⇒ needs catalogue)** — M1Predicate.
   lean:54-62: `m1_forced_unique` ("canonical finite data forces at most one admissible answer")
   and `m1_alternative_needs_catalogue` (`RequiresExternalCatalogue Forced b := ¬ Forced b`,
   `09_LEAN_FORMALIZATION/D0/Foundation/M1Predicate.lean:50`). (Verified — this is the exact engine
   the relocation test invokes: a non-forced selection = a catalogue = ⊥M1.)
7. **Zone-9 detector labeling is a torsor up to G_res ∈ {1, V₄, (ℤ/2)³}, and graph-slot transport
   is the un-owned datum** — M2_PHASE_LABELING_MEMO.md X1/X4 (lines 34-83), pre-fact set 1-12
   there. In particular M2:80-83 "Per-slot consumers ... are NOT torsor-invariant ... they need
   the bridge, which C1 + transitivity forbid resolving internally." (Verified — the direct
   parent result this memo generalizes.)
8. **The +2 ladder step: an orientation choice would be an external sign catalogue** —
   BOOK_01:1889: "a `+1` step would demand an external orientation bit — a sign catalog — which is
   `⊥M1`." (Verified — precedent that an orientation/basepoint choice on the scene is itself
   catalogue-typed, relevant to the SSB evasion test.)

---

## The computation (companion script `selector_obstruction_check.py`, 25/25 PASS)

**Object.** Bijections b : Vₙ → Lₙ as tuples; Sₙ acts by (g·b)[v] = b[g⁻¹(v)] (relabel which
vertex carries which label). This is the labeling torsor.

**Exact enumeration, n = 1,2,3,4 (surrogate verification of the general theorem):**

| n | \|Sₙ\| | equivariant sections (fixed bij.) | #orbits | stabilizer | Sₙ-invariant functions | all constant? |
|---|---|---|---|---|---|---|
| 1 | 1 | **1** (control: section exists) | 1 | 1 | 1 | — |
| 2 | 2 | **0** | 1 | 1 | 2 | yes |
| 3 | 6 | **0** | 1 | 1 | 3 | yes |
| 4 | 24 | **0** | 1 | 1 | 4 | yes |

**Applied to the actual scene (simply-transitive theorem — enumeration of 9!,11!,13! bijections is
infeasible, so the arithmetic consequences are asserted from the theorem verified above):**

| zone | \|Aut restriction\| | equivariant sections | #orbits | residual \|G_res\| |
|---|---|---|---|---|
| V₉  | S₉ = 362880       | **0** | 1 | 362880 |
| V₁₁ | S₁₁ = 39916800    | **0** | 1 | 39916800 |
| V₁₃ | S₁₃ = 6227020800  | **0** | 1 | 6227020800 |

Joint over Aut(K) = S₉×S₁₁×S₁₃ (product action): equivariant section count = 0 · 0 · 0 = **0**.
Obstruction **NON-TRIVIAL**.

**The general theorem (why enumeration on n≤4 suffices).** Sₙ acts simply transitively on
Bij(Vₙ,Lₙ): if g·b = b with b a bijection then b(g⁻¹v)=b(v) ∀v ⇒ g⁻¹v=v ∀v ⇒ g=id. Hence for all
n ≥ 1: #orbits = 1, stabilizer = 1; and #fixed bijections = 0 for n ≥ 2 (an invariant function is
constant on the single vertex-orbit, so not injective). The n=1 row is the control that the code
*can* see a section when one exists (it reports 1), so the 0's are a real measurement of absence,
not a blind spot.

**Can-fail controls (each fails the CONCLUSION, not the technique):**
`CTRL_N1_HAS_SECTION` (size-1 zone → 1 section: guards against always-reporting-0);
`CTRL_ORBIT_IS_ONE` (torsor ⟺ single orbit); `CTRL_STAB_TRIVIAL` (free action ⟺ residual = full
group); `CTRL_BURNSIDE` (fixed-total/|G| = #orbits — catches a fixed-point-counter bug);
`CTRL_INVARIANT_FUNCTION_CONSTANT` (the invariance wall recomputed from functions, not bijections).

**Strengthening (self-audit finding, verified separately):** the equivariant-section count is 0
already under any *transitive* subgroup of Sₙ with trivial bijection-stabilizer — computed 0 under
the cyclic Cₙ (n=3,4,5) too. So the wall does not even require the *full* Sₙ; it only needs the
acting owned symmetry to be transitive on the zone's vertices. C1 gives the full Sₙ (a fortiori
transitive), so the scene result stands; the note pre-empts the attack "what if only a smaller
G_res acts" — a smaller-but-transitive residual still blocks the equivariant section.

---

## THE DEEP-TRAP TEST — does a symmetry-breaking / basepoint / dynamical arm evade X2?

X2 kills only the **equivariant** class. The task's deep trap: *a mechanism that breaks the
symmetry into a degenerate manifold still needs a catalogue to pick a point on that manifold — it
does NOT escape M1, it just relocates the catalogue.* A legal escape must select a **unique**
graph-side object by an **internal owned rule** with **no residual freedom** (or a residual that is
itself owned-quotiented). I run every SSB/dynamical/basepoint arm I can construct through the M1
relocation test (pre-fact 6):

| candidate evader | what it selects | relocation-test verdict |
|---|---|---|
| **Basepoint ω₀ (the strongest evader)** | a marked witness on the DETECTOR SHELL V₉ = Ω₈ ⊔ {ω₀} | ω₀ is **pre-graph** (BOOK_01:778); it marks the shell, not one of the 9 graph vertices. Transporting ω₀ to a graph slot is the un-owned torsor (M2 X4). **Relocates the catalogue** to the shell→graph map. NO ESCAPE. |
| **SSB / dynamical vacuum selection** | a vacuum in a degenerate S₉-orbit of vacua | the vacuum manifold IS the S₉-orbit (a torsor by X1); picking one point needs a catalogue. **Relocates the catalogue.** This is exactly what BOOK_04:1399 already calls "an arbitrary symmetry-breaking choice" at triangle level. NO ESCAPE. |
| **Orientation / +2-step anchor** | a sign/orientation bit on the ladder | BOOK_01:1889: an orientation bit "would demand an external orientation bit — a sign catalog — `⊥M1`." **Explicitly catalogue-typed.** NO ESCAPE. |
| **Spectral/dynamical extremum (e.g. a unique-minimizer functional)** | the vertex minimizing an owned functional | any owned functional is a class function (S₉-invariant, pre-fact 1 C1), hence **constant on the vertex-orbit** — no strict minimizer exists; ties = the full orbit. **Degenerate manifold → catalogue.** NO ESCAPE unless an owned tie-break exists, and none is greppable. |

**Surviving evasion route (named, NOT closed).** The ONE structure that is genuinely *not* killed
by the equivariant count is a selector that (i) is generated by an owned symmetry-breaking datum
that is itself **unique on its owned domain** AND (ii) transports to a **unique graph vertex**
with the residual freedom **owned-quotiented away**. On the current corpus no such object exists:
every owned unique datum (ω₀, the Q₈ table, the sign bit) lives on the pre-graph shell, and the
shell→graph transport is the un-owned torsor C1 forbids resolving internally. **This is a "no owned
mechanism" outcome for X3b, not a proof of impossibility for all conceivable owners** — see reopening
hooks. The equivariant sub-question X3a IS a proof of impossibility (X2, decidable).

---

## Meta-conclusion (coordinated across the arms)

1. **Equivariant selector: DECIDABLY IMPOSSIBLE.** No internal Aut-equivariant canonical within-
   zone labeling exists (X2, 0 sections, obstruction non-trivial). This is a clean theorem-grade
   no-go on the actual scene, machine-checkable, and it upgrades the M2 "torsor up to G_res" from
   the detector shell to the full per-vertex graph-slot level over the entire Aut group.
2. **General (any-owned-rule) selector: NO OWNED MECHANISM, honest closure = external bridge,
   FORCED by the relocation test — not merely chosen.** Every SSB/basepoint/dynamical evader
   inspected relocates the catalogue rather than escaping M1 (deep-trap table). The external
   bridge is therefore the honest closure **as a consequence**, subject to reopening hooks.
3. **What is NOT proved (no leap either direction).** X3b is **not** a universal impossibility
   theorem — it is "no owned mechanism found + every inspected evader relocates the catalogue."
   A future owned symmetry-breaking datum that is unique on its domain AND owned-quotients its
   graph-transport residual would evade this arm; the equivariant no-go X2 would still stand, but
   the meta-conclusion would move from "external bridge forced" to "internal selector found."

---

## Named risks & PRE-REGISTERED attack surface (strongest self-attacks first)

- **A1 (strongest): "equivariant" is a straw target.** The real claim is X3b (any owned rule), and
  X2 only settles the equivariant class. PRE-REGISTERED defense: the memo does NOT claim X2 settles
  X3b — it explicitly splits them (X3) and runs the relocation test (X4) as the X3b instrument. The
  load-bearing X3b result is the *relocation-of-catalogue* finding, not the equivariant count. If a
  skeptic exhibits an owned symmetry-breaking selector that selects a unique graph vertex with
  owned-quotiented residual, X3b's "no owned mechanism" is KILLED (and that is the correct outcome).
- **A2: the ω₀ basepoint DOES sit in V₉ — is the pre-graph/graph split real?** Attack: if the 9
  graph vertices of zone V₉ ARE the shell V₉ = Ω₈ ⊔ {ω₀} (9 = 8 + 1), then ω₀ names a specific
  graph vertex and the basepoint arm escapes. Counter-text: BOOK_01:778 "the graph begins only
  after this cycle is lifted" and M2's X4 non-invariance demo (per-slot membership is not
  torsor-invariant). This is a genuine corpus-layer tension (identical to M2 wound W2a / risk A4);
  adjudication belongs to the closure contract. If the identification V₉^graph = Ω₈⊔{ω₀} is owned,
  the basepoint arm reopens X3b — flagged as the single sharpest reopening hook.
- **A3: the relocation test is only as complete as the evader sweep.** I inspected basepoint, SSB,
  orientation, spectral-extremum. A missed owned symmetry-breaking primitive reopens X3b. Hook:
  `CTRL_INVARIANT_FUNCTION_CONSTANT` handles the extremum family (all class functions constant),
  but a genuinely non-class-function owned datum is outside the sweep.
- **A4: product-action subtlety.** Aut(K)=S₉×S₁₁×S₁₃ acts factor-wise; I assumed a joint section
  factorizes. If a cross-zone owned rule tied the three labelings together (e.g. via the between-
  zone edges), the joint count could differ from the product. Counter: K is complete tripartite,
  all between-zone blocks all-ones (BOOK_01:1576), so the between-zone structure is itself S₉×S₁₁×
  S₁₃-invariant and adds no cross-zone label constraint. Stated, not deeply swept — flagged.
- **A5: "non-trivial obstruction" phrasing vs a formal H¹ class.** I framed the obstruction as
  section non-existence (fixed-point count 0), NOT as a computed nonzero H¹(Aut, labelings) class.
  For a free transitive action the two coincide (the torsor is classified by a single nonzero
  class), but I did not build the cocycle. Candidate language kept: "the section-existence
  obstruction is non-trivial," not "H¹ ≠ 0 computed." The adjacent owned H¹(G,ℤ) charge-quantization
  (BOOK_04:92-97) is DIFFERENT content (charge, not vertex labeling) — cross-referenced, not reused.

---

## What this does NOT show

- Does NOT prove X3b (no canonical selector by ANY owned rule) as a universal impossibility — it is
  "no owned mechanism found, every inspected evader relocates the catalogue." HONEST outcome.
- Does NOT adjudicate the pre-graph/graph identification of V₉ (A2) — the single sharpest reopening
  hook; it defers to the closure contract, as M2 did.
- Does NOT compute a cocycle / H¹ class explicitly (A5) — states section-non-existence, candidate
  language on the cohomological phrasing.
- Does NOT edit, mint, or demote any registry row / cert / `.lean` / 053040 row — proposals here only.
- Does NOT duplicate M2 (detector-shell torsor up to G_res), the DΣ triangle no-go, or Carrier-
  NotIcosahedral (zone-CLASS symmetry) — it is the per-VERTEX, full-Aut, equivariant-section
  statement plus the X3a/X3b split and the relocation test.

---

## Proposed registry action (memo-only; do NOT apply without minting protocol)

Candidate row **D0-SELECTOR-EQUIVARIANT-OBSTRUCTION-NOGO-001** (NO-GO, CORE-candidate):
"No Aut-equivariant canonical within-zone vertex-labeling of K(9,11,13) exists — the labeling is a
single S₉×S₁₁×S₁₃-torsor with trivial stabilizer, so the equivariant section-existence obstruction
is non-trivial (0 sections). Scope: rules out the EQUIVARIANT selector class only; the SSB/basepoint/
dynamical class is treated by the M1 relocation test (every inspected evader relocates the
catalogue). cert `selector_obstruction_check.py` (can-fail: n=1 section-exists control + Burnside +
invariance-wall recompute)." Cross-refs: extends M2_PHASE_LABELING_MEMO, D0-DSIGMA-ROLE-CYCLE-
CARRIER-CANONICAL-NOGO-001, D0-CARRIER-NOT-ICOSAHEDRAL-001; instantiates D0-M1-PREDICATE-001.
Reopening hook: an owned proof that V₉^graph = Ω₈ ⊔ {ω₀} (A2) would reopen the basepoint arm of X3b.

---

## Independent skeptic verdict + repair log (ACCEPTED IN FULL)

**Verdict (independent pass, ACCEPT-IN-FULL): the mechanism SURVIVES.** The obstruction-cohomology
arm does NOT falsely claim to escape M1: it honestly concludes the catalogue is **relocated** for
every inspected evader, and that reading is CORRECT. This is the honest, correct outcome — not a
manufactured selector and not a leap. Two defects were found, **both minor**; neither touches the
surviving conclusion. Independently re-verified on disk this session (2026-07-05):

- **(a) Spectral/extremum row holds.** Any owned functional invariant under the acting transitive
  symmetry (full Sₙ, or a cyclic Cₙ sub-action) is a class function, hence constant on the single
  vertex-orbit — no strict unique minimizer exists. Re-derived by `CTRL_INVARIANT_FUNCTION_IS_CONSTANT`
  in `selector_obstruction_check.py` (25/25 PASS, exit 0, re-run 2026-07-05).
- **(b) Vacuum manifold IS the Sₙ-torsor (X1).** Picking one point needs a catalogue — confirmed by
  the free-transitive theorem (orbits=1, stabilizer=1; residual = |Sₙ|).
- **(c) Even the strongest evader (basepoint) at best shrinks S₉ to a residual torsor, never to a
  unique labeling.** Fixing ω₀ breaks S₉ only to Stab(ω₀)=S₈ (residual 8! = 40320), or, using the
  owned Ω₈≅Q₈ structure, to Aut(Q₈)=24 — residual >1 in every case. A full vertex-labeling still
  faces a nontrivial torsor and still needs a catalogue. **NO ESCAPE either way.**

**The memo does NOT over-claim escape and does NOT falsely relocate — it correctly diagnoses
relocation.** X3b is honestly scoped as "no owned mechanism found + every inspected evader relocates
the catalogue," NOT a universal impossibility. X3a (equivariant) IS a decidable theorem-grade no-go.
The catalogue is genuinely relocated; there is no surviving owned selector that picks a UNIQUE
canonical graph-side object by an internal owned rule. **DEAD as an M1-escape; the M1 relocation
diagnosis is the owned, correct result.**

### Errors of record (accepted, no defense)

- **ERR-1 (UNDER-CLAIM / mis-argued defense on the load-bearing arm).** The memo defends "basepoint
  relocates the catalogue" (A2, its self-declared "strongest evader" and "single sharpest reopening
  hook") via the CONTESTED pre-graph premise (ω₀ is pre-graph, cited from BOOK_01:778 and :1541),
  while **missing a stronger corpus-robust argument**: even IF ω₀ is graph-side and distinguished,
  fixing it only breaks S₉ → Stab(ω₀) = S₈ (residual 8! = 40320), or Aut(Q₈) = 24 (since Ω₈ ≅ Q₈ is
  owned at BOOK_01:780-802, Dedekind-forced theorem). The residual is >1 in every case, so a full
  labeling still faces a nontrivial torsor and still needs a catalogue. The A2 conclusion survives on
  the residual-torsor foot **regardless of** how the pre-graph/graph identification adjudicates. The
  memo bet A2 on the weaker foot. (Neither the memo prose NOR the check script currently carries the
  residual computation Stab(ω₀)=8! / Aut(Q₈)=24 — verified: no `40320` / `factorial(8)` / `=24`
  appears in `selector_obstruction_check.py`.)
- **ERR-2 (SELECTIVE-CITATION GAP on the load-bearing arm).** The memo rests A2 on ω₀ being pre-graph
  and cites BOOK_01:778 / :1541, but **NEVER cites the one line most directly against it** —
  BOOK_01:1987: "The witness/basepoint ω₀ gives **the first addressable graph-birth shell** V₉ = Ω₈ ⊔
  {ω₀}." That line identifies the size-9 graph zone WITH the pointed shell — precisely the A2 tension —
  and it is absent from the memo (verified: greppable BOOK_01 cites in the memo are 778, 1541, 1570,
  1576, 1889 only; 1987 and 1990 appear nowhere). The equation V₉ = Ω₈ ⊔ {ω₀} occurs verbatim at
  BOOK_01:1544, :1990, and the graph-birth-shell sentence at :1987. Selective-citation gap on the
  load-bearing arm. (NB: this gap does NOT flip the conclusion — see ERR-1: the residual-torsor foot
  survives either adjudication — but the load-bearing arm should have engaged its strongest
  counter-text.)
- **ERR-3 (PRESENTATIONAL over-claim).** The headline word "DECIDABLY impossible / clean no-go"
  (title line 1; X2/X3a) dresses up what is, at content, the near-vacuous fact "a transitive action
  on a ≥2-set fixes no point." It is TRUE and correctly SCOPED (equivariant-only, X3a; explicitly NOT
  extended to X3b), so it is not a substantive leap — but the drama over-states one line of content.

### Repairs applied / proposed (memo-only; no registry/cert/.lean/053040 edits)

1. **Cite BOOK_01:1987 and re-argue A2 via the residual-torsor foot.** The basepoint arm's
   corpus-robust argument is: fixing ω₀ leaves residual Stab(ω₀)=S₈ (8!) or Aut(Q₈)=24 (Ω₈≅Q₈,
   BOOK_01:780-802) — a nontrivial torsor either way, so a catalogue is still needed **independently
   of** the pre-graph/graph adjudication. BOOK_01:1987/1990/1544 (V₉ = Ω₈ ⊔ {ω₀}, "first addressable
   graph-birth shell") is the strongest counter-text and is now cited here. **A2 survives on this
   foot whether or not V₉^graph = Ω₈ ⊔ {ω₀} is owned.** (Proposed for the pre-facts / A2 body on the
   next memo revision; recorded here now.)
2. **Demote A2 to non-reopening for full-labeling selection.** Under Repair 1, resolving the
   pre-graph/graph identification (A2) does NOT by itself yield a unique full labeling — it at best
   fixes one vertex and leaves the S₈ / Aut(Q₈)=24 residual torsor. So A2 is a reopening hook for the
   "does ω₀ name a graph vertex" sub-question, but **NOT** a reopening hook for "a unique canonical
   full labeling exists": that still needs the residual torsor owned-quotiented, which no owned datum
   does. The proposed registry-row "Reopening hook" (line 266) should be narrowed accordingly.
3. **Relabel scene asserts as theorem-application, not measurement.** The n=9,11,13 residual/section
   figures (X1: 362880 / 39916800 / 6227020800; X2: 0) come from the simply-transitive **theorem**
   applied to the actual zones (enumeration only n≤4), not from a per-scene computation. Wording is
   already largely correct (memo §X1/script docstring say "theorem applied to n=9,11,13"); confirm no
   phrasing reads these as measured/enumerated at n=9+.
4. **(Optional) Calibrate the headline drama.** Soften "DECIDABLY impossible / clean no-go" to reflect
   that X2/X3a is the one-line transitive-action fact; keep "decidable" (accurate) but drop the
   theatrical framing. Cosmetic; does not affect content.

### Residual after repairs

- **A2 pre-graph/graph identification (BOOK_01:778/1541 vs :1987/1990) remains a genuine corpus-layer
  tension**, unadjudicated — but it is now **downgraded from load-bearing to a sub-question**: the A2
  conclusion (basepoint relocates the catalogue) survives on the residual-torsor foot regardless. The
  residual-torsor computation (Stab(ω₀)=8!, Aut(Q₈)=24) is NOT yet in the checked artifact — a
  can-fail check for it is a candidate hardening (not blocking).
- **A3 (evader-sweep completeness)** stands: a genuinely non-class-function owned symmetry-breaking
  primitive outside {basepoint, SSB, orientation, spectral-extremum} would reopen X3b. Unchanged.
- **A5 (H¹ phrasing)** stands: section-non-existence stated, no cocycle built. Candidate language kept.
- **Net:** the surviving result is UNCHANGED and CORRECT — no owned internal selector picks a unique
  canonical graph-side object; every inspected evader relocates the catalogue; the external bridge is
  the forced honest closure for X3b, and X3a is a decidable no-go. The mechanism is **not a leap in
  either direction**; the M1-relocation diagnosis is the owned, load-bearing content. Verdict accepted
  in full; the two defects are argumentation/presentation, not conclusion. (Verified 2026-07-05.)
