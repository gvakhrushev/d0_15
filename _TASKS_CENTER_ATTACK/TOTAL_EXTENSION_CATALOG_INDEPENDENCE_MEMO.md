# TOTAL-EXTENSION CATALOG INDEPENDENCE — N-parametric re-adjudication of D0-TOTAL-EXTENSION-PRIMITIVES-MINIMALITY-001 (DRAFT, pre-skeptic)

**Status:** DRAFT candidate; no registry row edited yet. Pre-flight run: `TOTAL-EXTENSION-PRIMITIVES`,
`SELF-READING-PRIMITIVE-MINIMALITY` (both already own rows — this is an UPDATE of row 477's cert +
artifacts, not a new mint; cross-refs listed below). Session 2026-08-09.

## Problem being fixed

`vp_total_extension_primitive_minimality.py` (pre-rework) asserted `len(P)==11` and an 11×11 matrix,
but motion **A(d)** of the CEMENT campaign (`APPLIED_MOTIONS_LOG.md:76`, commit `8a2e9d9`, 2026-07-06)
grew the catalog 11→19 as a **catalog-completion** motion ("content lifted from the consuming notes",
F-14 criterion: registry-named + ≥1 DAG-gate consumer) and did NOT touch the matrix or the cert.
The cert has been failing since. Root cause: **O2 policy made the file the SOLE canonical primitive
catalog** — a different object than "the pairwise-independent minimal primitives" the claim quantifies
over. The two layers must be split explicitly, and the failure is NOT repaired by bumping 11→19:
three of the July rows are not independent primitives at all (below).

## Claim X (DEF-0.2.2 form)

Let `C` = the row-list of `TOTAL_EXTENSION_PRIMITIVES.csv` (N=20 after this campaign: 11 original +
8 motion-A(d) rows + `PRIM-SEAM-CROSSING-TICK-IDENTIFICATION` added HERE by the same F-14 criterion,
DAG-gate consumers=3). Then:

1. every row of `C` is fully typed (9 required fields non-empty) and the signature triples
   `(id, mathematical_type, minimal_input)` are pairwise distinct;
2. the derive-relation on `C` is EXACTLY the 9-edge ledger
   `TOTAL_EXTENSION_PRIMITIVE_DERIVE_EDGES.csv`, every edge owned by an already-registered claim:
   - `CANONICAL-SELF-READING-FUNCTOR` ⊃ {E1,E2,E3-role,E3-cocycle,E4} + transitive `GNC`
     (owner `D0-SELF-READING-PRIMITIVE-MINIMALITY-001`, LEAN_PROVED conjunction-of-independent);
   - `ISOMETRIC-DIRAC-J` ⊃ {`DIRAC-SCALE-SELECTION`, `COMPARISON-MAP-XI-N`}
     (owner `D0-VNEXT-ISOMETRIC-DIRAC-TOWER-NOGO-001`: "the single primitive splits into +2");
   - `FINITE-SPECTRAL-TRIPLE-REP` ⊃ `GRADING-NEUTRAL-CURRENT-OPERATOR`
     (owners E1 no-go — interface `(ρ,Γ,J,Q_role)` contains Γ, two-completion IS nc 8≠12 — + GRADAXIS);
3. `C` minus the 3 registered bundle/component rows is a **17-row pairwise-independent core**
   (no derive-edge inside; construction edges — Lean-owned `P1→P2`, and Ξ-on-tower — are NOT
   derive-edges, per the original 11×11 adjudication's own precedent);
4. all 190 unordered pairs carry individual justifications
   (`TOTAL_EXTENSION_PRIMITIVE_PAIR_JUSTIFICATIONS.csv`, generated by
   `build_total_extension_matrix.py`; trap-sensitive pairs hand-justified).

Lean scope is split honestly (hybrid owner, as in `D0-EDGE-COVER-FAMILY-001`):
`total_extension_primitive_independence` continues to own the ORIGINAL 11 rows; the full-catalog
layer is cert-borne. No Lean edit in this campaign.

## Owned pre-facts (verbatim, file-cited)

1. `CLAIM_TO_LEAN_MAP.csv` row 474 (`D0-SELF-READING-PRIMITIVE-MINIMALITY-001` area): "PRIM-CANONICAL-SELF-READING-FUNCTOR is NOT a genuine single derived object subsuming the four
   -- it is S0 (owned) + 4 semantically-independent primitives (… PRIM-FINITE-SPECTRAL-TRIPLE-REP,
   PRIM-SCENE-HISTORY-REFINEMENT-RULE, PRIM-PHASON-PRESSURE-ENERGY-ROLE-ASSIGNMENT +
   PRIM-PHASON-COORDINATE-FUNCTOR, PRIM-LEPTON-BRANCH-FIXING-OPERATOR)". ✓
2. `theory_status_map.csv` `D0-VNEXT-ISOMETRIC-DIRAC-TOWER-NOGO-001`: "PRIM-ISOMETRIC-DIRAC-J_N does
   NOT collapse to one extension datum … The single primitive splits into +2 independent primitives." ✓
   (The July row dropped the `_N` suffix; same object — same Perron-GNS-tower input, same
   `gns_refinement_isometry` citation.)
3. `theory_status_map.csv` `D0-POSTCORE-REPRESENTATION-EXTENSION-NOGO-001`: "Even with the full
   (rho,Gamma,J,Q_role) interface … TWO admissible completions … EXACT-MISSING:
   PRIM-FINITE-SPECTRAL-TRIPLE-REP." ✓ — the E1 two-completion IS the grading signature nc 8≠12.
4. `theory_status_map.csv` `D0-GRADING-NEUTRAL-CURRENT-MAXIMALITY-NOGO-001` GRADAXIS[2026-07-18]:
   "signature-subclass exhaustion + argmin are Lean-owned … PRIM-GRADING-NEUTRAL-CURRENT-OPERATOR
   stays the missing outside-class object". ✓
5. `04_VERIFICATION/FIVE_PRIMITIVE_NONDERIVATION_WITNESSES.md`: "exactly one construction edge
   P1 -> P2 (the coordinate functor requires the pressure-energy sector) … No merge: P1 and P2 do
   not fuse (asymmetric dependence, not equivalence)." ✓
6. `theory_status_map.csv` `D0-X5-PHASON-COORDINATE-CONTRACT-001`: "model = phi-tick cocycle
   z(s)=phi^s-1 (z(1)=phi-1) + deletion-minimality (drop internal-normalization -> phi-tick vs
   integer-tick)" ✓ — the July row's "role-fixing + cocycle-fixing" over-bundled its own X5 scope
   (rescoped this campaign, row-text repair).
7. `APPLIED_MOTIONS_LOG.md:76`: "A(d) Add 8 registry-consumed orphan primitive rows …
   (11 → 19 rows) … content lifted from the consuming notes". ✓ — no independence adjudication in
   the motion.
8. `05_CERTS/vp_register_exhaustiveness.py` (run 2026-08-09): `PRIM-SEAM-CROSSING-TICK-IDENTIFICATION:
   ORPHAN — … DAG-gate consumers=3` ✓ — highest consumer count among real orphans; F-14 criterion met.
9. `theory_status_map.csv` `D0-SEAM-CROSSING-WEIGHT-001`: "Residual of
   PRIM-SEAM-CROSSING-TICK-IDENTIFICATION = (alpha) AND (beta) AND (gamma) … FRAMING CONSTRAINT
   (mandatory): conditional on the open composition, never 'derived from owned'." ✓ — row 20's
   fields transcribe this note (framing constraint carried into negative_controls).

## The forcing / construction (all computed, deterministic)

- Catalog: +1 row (seam, fields lifted verbatim-traceably from rows 583/625/630 notes), 4 row-text
  repairs (functor/GNC/IDJ honesty cross-refs + PCF rescope to the X5 cocycle scope).
- `build_total_extension_matrix.py` (single source of truth) emits: 20×20 matrix
  (362 `independent` / 9 `subsumes` / 9 `component-of` / 20 `self`), the 9-edge ledger with owner
  claims, and 190 per-pair justifications. Transitive-closure of the edge set asserted at build time.
- Cert `vp_total_extension_primitive_minimality.py` re-verifies EVERYTHING independently of the
  generator: field audit, Nodup, matrix↔catalog alignment, matrix↔ledger EXACT match (converse cells
  included), owners exist in `CLAIM_TO_LEAN_MAP.csv`, transitive closure, core independence,
  exclusion justification (every excluded row edge-incident).

## Verification

`vp_total_extension_primitive_minimality.py` — ALL PASS (20 rows; 9 edges; 17-row core) + 4
**executable** negative controls, each attacking the CONCLUSION, not the technique:
blanked required field / derive-edge hidden as `independent` (the silent-weakening move) /
ledger edge dropped / component smuggled into the core — all 4 caught.
`vp_register_exhaustiveness.py` C2: seam ORPHAN line cleared; seam consciously baselined in
`register_exhaustiveness_manifest.json` (its `_note` procedure followed). Pre-existing C1/C4/C5
drift untouched (intended-RED state of that cert's other legs).

## Named risks & PRE-REGISTERED attack surface

- **(A) [strongest self-attack] The (FST ⊃ GNC) edge could be UNDER-read or OVER-read.** Under-read:
  GRADAXIS deliberately kept BOTH rows ("E1 classification UNCHANGED classScoped … operator-level
  class untouched") — a skeptic can argue the corpus intends them as peer primitives and my edge
  breaks that intent. Over-read: the self-reading note says "the Aut part-size order RESOLVES the E1
  Weyl-role leg" — a skeptic can argue E1's residual content = EXACTLY the grading, i.e. the honest
  verdict is a full MERGE (same-gap), not a component edge, and FST's row-text
  ("graph alone does not choose Weyl roles") is STALE. My position: component edge + both rows kept,
  because the E1 interface also carries the S3 Weyl-role bijection freedom on the generation block
  (E1 note: "also free S3 Weyl-role bijection") which the part-size order (a statement about zone
  sizes 9<11<13) does not obviously kill; the "two Weyl-role freedoms" ambiguity is REGISTERED here,
  not resolved.
- **(B) Universal-over-unexhausted-class**: the 163 default 'independent' justifications quantify
  over completion classes nobody exhausted. Scoped defense: each default verdict claims only
  "neither datum DETERMINES the other" on distinct registered carriers/no-gos — a pairwise
  non-derivation reading, same grade as the original 11×11, not a new universal.
- **(C) Carrier-mismatch-by-shared-word**: pre-checked pairs 'branch' (LEP/BIR), 'triple' (NCT/FST),
  'J' (FST/IDJ), 'edge/seam/alpha' (EHS/SCT, EHS/PER, PER/SCT), shared numbers {20,22,24} (SMW vs
  PPE/PCF), 'Galois sign+positivity' (MAG/PPE). Each carries a hand justification in the pairs CSV.
- **(D) The seam row mint**: door-2-scoped residual registered as a catalog row. Attack: a
  route-conditional residual is not an unconditional extension primitive. Defense: the catalog's
  membership criterion is F-14 (registry-consumed, DAG-gated), not unconditionality; the row carries
  its own scope + reopening hook verbatim from its owner rows; consumers=3 is the highest orphan
  fan-out. Rival reading stays live (door 1), stated in the row.
- **(E) PCF rescope**: editing a registered row's necessary_conditions. Defense: the row now matches
  its OWN cert-closed X5 contract; the old text contradicted it (over-bundle vs the E3/five-primitive
  split). This is a desync repair with named owners, not a semantic change.
- **(F) Left-out orphans**: `PRIM-HIGGS-SSB-SIGN`, `PRIM-ROLE-VERTEX-SECTOR-ATTACHMENT`,
  `PRIM-TORUS-PARAMETER`, `PRIM-WINDING-METRIC` (+ token `PRIM-EFT-IR`) still fail C2 — NOT added
  here (each needs its own content-lift + adjudication). Named follow-up, not silent.

## What this does NOT show

- No new physics; no promotion of any completion class; the 3 bundle/component adjudications create
  NO new no-go (each cites its existing owner).
- The seam row does not close door 2 and does not disfavor door 1; per-crossing φ⁻¹ stays CONDITIONAL
  (never "derived from owned").
- The Lean theorem is NOT extended to 20 rows (follow-up candidate: extend the Nodup/derive-relation
  statement; until then the catalog layer is cert-borne only).
- PROOF-TARGET tripwires: if `D0-VNEXT-ISOMETRIC-DIRAC-TOWER-OWNER-001` or
  `D0-PHASON-COORDINATE-FUNCTOR-OWNER-001` lands, the touched rows stop being primitives and this
  adjudication re-runs.
