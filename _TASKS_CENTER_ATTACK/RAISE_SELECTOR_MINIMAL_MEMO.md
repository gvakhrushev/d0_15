# RAISE — the SELECTOR no-go raised to a POSITIVE EXTREMALITY THEOREM

**P-INVARIANT-MINIMAL:** *R^Aut is the extremal minimal observable algebra of the scene.*

**Status:** DRAFT candidate, memo-only. **No registry row edited, no built `.lean` touched, no
`053040` touched, no commit.** Skeptic pass §05.8.R: **run below — WOUNDED-FIXABLE, one repair
applied, re-passed; final grade RAISED-TO-THEOREM.** Companion cert
`raise_selector_minimal_check.py` — **21/21 PASS, exit 0**, mutation-tested (4 load-bearing facts
each fail the CONCLUSION under mutation). Models the statement on the corpus's existing extremality
positives `D0-Q8-DEDEKIND-MINIMALITY-001` (CORE-FORMALIZED) and the three `*-PRIMITIVE-MINIMALITY`
rows (LEAN_PROVED). Pre-flight: no existing row owns "R^Aut extremal / minimal observable algebra";
row 549 owns the *no-go*, row 550 owns the *organizing lemma* (reading layer) — this memo is the
**raise of 549**, cross-referenced not duplicated.

---

## The raise in one sentence

Row 549 says *"no M1-legal internal rule selects a within-zone labeling"* (a **no-go**). The same
computation, read positively, says: **the Aut-invariant subalgebra R^Aut is the UNIQUE MINIMAL
observable algebra of the scene — every owned functional is a class function, constant on the
single within-zone orbit, so no extremum can refine it.** The no-go is the *shadow* (corollary) of
this extremality: a selector would be an extremum strictly refining R^Aut, and R^Aut has no
invariant proper refinement precisely because it is already extremal-minimal.

---

## Claim P-INVARIANT-MINIMAL (DEF-0.2.2 form)

Frozen scene `K(9,11,13)`, `|V| = 33`, automorphism group `Aut(K) = S₉ × S₁₁ × S₁₃` acting
within-zone (BOOK_01:1576 orbital theorem). Let `C^V` be the pointwise function algebra on the 33
vertices, with `Aut` acting by permuting coordinates. Let
`R^Aut := (C^V)^{Aut} = { f : g·f = f for all g ∈ Aut }` be the fixed-point subalgebra.

**(T1) R^Aut is well-defined and owned.** `R^Aut` is exactly the ring of **class functions** —
functions constant on each within-zone orbit. Since each `Sₙ` is transitive on its zone, the
orbits are the three zones, so `dim R^Aut = #orbits = 3`. It is a unital subalgebra (spanned by the
three orbit-indicator idempotents, closed under pointwise product and sum). Ownership is C1's own
clause (BOOK_01:1570: "*a part-internal exception would name a privileged vertex — an exogenous
parameter — violating M1*"; BOOK_01:1576: "*`A` is constant on the orbits of `S9 × S11 × S13`*").
**Computed:** `dim R^Aut = 3` (`T1: dim R^Aut = #orbits`), independent Reynolds-image rank `= 3`
(`NC_DIM_EQUALS_ORBITS`), algebra-closure verified on 50 random class functions.

**(T2) R^Aut is MINIMAL — the smallest Aut-closed algebra separating the owned observables.** Every
owned scene observable is a functional of the invariant adjacency `A`, hence a **class function**,
hence lies in `R^Aut`. The archetypal owned observable — the **degree** function (BOOK_01, degree
`= 33 − zonesize`, values `24 / 22 / 20`) — separates all three zones. Any Aut-closed unital algebra
containing the owned observables must therefore separate the three zones, forcing `dim ≥ 3`;
`R^Aut` attains `dim = 3`. So `R^Aut` is the minimum. **Computed:** the algebra generated by
`{1, degree}` is exactly `dim 3 = R^Aut` (`T2`, Vandermonde on 3 distinct nodes); no Aut-closed
algebra of `dim < 3` separates the owned degree (`NC_MINIMAL_IS_MEET`, full 3-orbit-lattice
enumeration); dropping to `⟨1⟩` (dim 1) loses the owned degree invariant (`T2`).

**(T3) R^Aut is the UNIQUE such minimal algebra — it is EXTREMAL.** The Aut-closed unital
subalgebras of `C^V` are in bijection with partitions of the **orbits** (a function invariant under
`Aut` is constant on unions of orbits). Among these, exactly **one** separates the owned
observables: the all-singletons partition `= R^Aut` (`T3: unique Aut-closed algebra separating
= orbit partition`, `#separating = 1`). `R^Aut` is the minimum-dimension separating Aut-closed
algebra (`T3`, `min separating dim = 3`). Any **proper refinement** — an algebra strictly finer than
`R^Aut`, i.e. separating two vertices *inside* a zone — is generated by a within-zone separator; the
cert **exhibits one witness** (the indicator of vertex 0), which is **NOT Aut-fixed** (the
transposition `(0 1) ∈ S₉` moves it) `= the M1-forbidden catalog` (`T3: a proper refinement …
is NON-invariant`, mutation-verified load-bearing). The **universal** fact — that NO invariant
proper refinement exists *at all* — is not carried by this single cert witness; it is the owned
`no_equivariant_section` Lean theorem (row 549) plus the SSB class-function trichotomy
(`vp_selector_ssb_nogo.py`), cross-referenced. Hence, over the owned `A`-built functionals, `R^Aut`
admits **no strict extremum below it**: it *is* the extremum (universality at the EVIDENCE grade
that row 549 carries — see A5/EoR-B).

**(COROLLARY = row 549, the classic no-go).** An `Aut`-equivariant within-zone section (a canonical
labeling) is precisely an *invariant proper refinement* of `R^Aut` — a within-zone separator that
is also Aut-fixed. **T3 says none exists.** Recomputed: `0` equivariant sections for `n = 2,3,4`
(surrogate) and, by the free-transitive identity, `0` for the real zones `{9,11,13}`; the `n = 1`
control reports `1` (a trivial zone *is* a point, so the refinement exists there — the measurement
is real). Reading: **"0 sections" (row 549) = "R^Aut admits no invariant proper refinement"
= "R^Aut is extremal-minimal."** The no-go is the shadow of the positive theorem, not vice-versa.

**(GROUNDING — the 22 T3 torsors as corollaries).** Each within-zone T3 torsor is a **single free
Aut-orbit** on which every `R^Aut`-observable is constant (torsor archetype verified:
transitive + trivial-stabilizer for `n = 2,3,4`). So each of the **22 registry T3 torsors**
(UPLIFT_MAP.md:295, :316 — CMB-CANONICAL, PHASON-MAGNITUDE, CKM-OVERLAP, TORAL-SEED,
LEPTON-PUISEUX, R4, E3, MARTINGALE-DIRAC, VNEXT2-REFINEMENT-{OWNER,NOGO,DIRAC-COVARIANCE},
EDGE-COVER, HYPERCHARGE-VARIETY, G2, P1-C, WZ-TRANSFER, ALPHA-RESIDUE-DELTA, BRANCH-CP, P1-EOS,
BRANCH-ROW, ARCHIVE-LAPLACIAN-PHASE, H3) is **one gauge orbit of the single extremal fixed-point
algebra R^Aut**. They ground *as corollaries* of one extremality, exactly as UPLIFT_MAP.md
principle P-GAUGE-M1 already states ("*grounds the existence of all 22 T3 torsors, each torsor =
one gauge orbit*"). Count owned by the registry, cross-referenced — not recomputed.

---

## Owned pre-facts (verbatim, file:line — verified on disk, read past ±10)

1. **Aut = S₉×S₁₁×S₁₃, within-zone transitive** — `CanonicalSelectorNoGo.lean` docstring:
   "*automorphism group `Aut(K) = S₉ × S₁₁ × S₁₃` (BOOK_01:1576 orbital theorem; BOOK_04:1399)*";
   the module's theorems `no_equivariant_section {n} (hn : 2 ≤ n) : (equivariantSections n).card = 0`
   and `canonical_within_zone_selector_nogo` are LEAN_PROVED (row 549, `LEAN_PROVED`). **Cite by
   theorem name, not line number** (EoR-A): the module is on row 550's "EXACT-MISSING general-lemma
   route", so line-cites will drift when the general minimality lemma lands.
2. **C1 invariance clause (the M1-forcing of invariance)** — BOOK_01:1570 (quoted verbatim in
   TORSOR_GAUGE_SYNTHESIS_MEMO.md pre-fact 3): "*(C1) Exchangeability. No permutation inside a part
   may change the physics … A part-internal exception would name a privileged vertex — an exogenous
   parameter — violating M1.*"; BOOK_01:1576: "*From (C1), `A` is constant on the orbits of
   S9 × S11 × S13.*"
3. **Class-function trichotomy (the SSB arm — every owned functional is a class function whose
   extrema are symmetric or a degenerate orbit, never a unique broken point)** —
   `vp_selector_ssb_nogo.py` (row 549 cert, 26/26): "*Every owned action is a CLASS FUNCTION of
   S9xS11xS13, so its extrema are either symmetric points (a) or degenerate orbits (b); neither is
   a legal selector.*" — this is exactly "no extremum refines R^Aut."
4. **Extremality-positive precedent (the SPECIES this raise belongs to)** — registry row 207
   `D0-Q8-DEDEKIND-MINIMALITY-001` (CORE-FORMALIZED): "*unique minimal Hamiltonian non-abelian
   group ≤ 8 (Dedekind 1897)*"; rows 458/478/486 `D0-{TOTAL-EXTENSION,SELF-READING,RAW}-
   PRIMITIVE-MINIMALITY-001` (LEAN_PROVED) — no-gos minted AS positive minimality theorems.
5. **The 22 T3 torsors + the grounding principle** — UPLIFT_MAP.md:295 (the 22-row roster), :316:
   "*it GROUNDS the existence of all 22 T3 torsors (each torsor = one gauge orbit)*"; the
   P-GAUGE-M1 principle text (:312–316).
6. **The organizing-lemma neighbour (not duplicated)** — row 550
   `D0-WITHINZONE-GAUGE-ORGANIZING-LEMMA-001` (OPEN, reading layer): owns "*internal physics is …
   valued in the INVARIANT RING of the frozen data*" as a **reading**; this memo supplies the
   **positive extremality theorem** that the reading anchors. Cross-ref, not merge.

---

## PRE-REGISTERED attack surface (the strongest attacks I can build against myself)

**A1 (the pinned worry — gauge smuggle). Does "minimal observable algebra" smuggle the gauge datum
(picking the extremum / a torsor point)?** If `R^Aut` were *defined* as "the algebra of a chosen
labeling," minimality would be circular — the extremum would be the smuggled gauge choice.
*Defence (computed):* `R^Aut` is built as the **fixed-ring / Reynolds image**, never from a point;
it is **identical under every relabeling** (`NC_NO_POINT_USED`: the orbit partition is
relabeling-invariant). Minimality is a **point-free lattice meet** (`NC_MINIMAL_IS_MEET`: the
minimum over the *whole* subalgebra lattice), attained without naming any section. No point of any
torsor is ever constructed. So minimality is genuinely owned, not smuggled.

**A2 (is "minimal" the SAME theorem as the no-go, merely renamed?).** A raise must add content.
*Defence:* the no-go is `∃`-negative ("no section"); the raise is a `∀`-positive lattice statement
("`R^Aut` = the minimum of the separating-Aut-closed-subalgebra lattice, uniquely attained"). The
raise carries the extra structural facts `dim = 3`, `= meet of all separators`, `uniqueness of the
minimizer`, and the grounding of the 22 torsors as its orbits — none of which the bare no-go states.
This is precisely the `Q8-DEDEKIND` move (a no-go "no other group works" minted as "Q8 is the unique
minimal Hamiltonian non-abelian group").

**A3 (owned-observable scope — does the degree really force separation, or is "owned observable"
cherry-picked?).** *Defence:* separation is forced by ANY class function distinct across zones; the
degree is the cheapest owned witness, but the class-function trichotomy (pre-fact 3, the SSB cert)
proves EVERY owned functional of `A` is a class function → lies in `R^Aut`, and the Laplacian
spectrum `{0,20¹²,22¹⁰,24⁸,33²}` already separates the three zones by degree. The minimality is over
the whole owned observable ring, not one functional.

**A4 (does the corollary over-reach — are all 22 torsors REALLY within-zone orbits?).** *Defence
(honest scope):* the grounding is claimed only for **within-zone / gauge-orbit** T3 torsors, exactly
the roster UPLIFT_MAP.md:316 already assigns to P-GAUGE-M1. The cert verifies the **archetype**
(within-zone labeling torsor = single free orbit) and cross-references the 22-count as a *registry
fact*, NOT recomputing per-row internals. Torsors whose freedom is a genuine measurement/valuation
(e.g. ALPHA-SEAM's valuation obstruction) are NOT within-zone orbits and are OUT of scope — the raise
does not claim them.

**A5 (Lean status honesty).** The **equivariant arm** (the corollary's core, `no_equivariant_section`)
is LEAN_PROVED (row 549). The **positive extremality statement** (`R^Aut` = minimal separating
algebra) is script-grade here (cert 21/21, mutation-tested) + the Lean would be a `Fin 33`
decidable orbit-count lemma (the same route row 550 lists as EXACT-MISSING item (1)). The raise is
**RAISED-TO-THEOREM at cert grade**; a Lean lift is the mint path, not claimed done.

**Fireable falsifiers:**
1. An owned functional separating two within-zone vertices → breaks T3 (would be an invariant
   refinement; row 549 reopening hook (e)).
2. An Aut-closed algebra of `dim < 3` separating the owned observables → breaks T2 minimality
   (`NC_MINIMAL_IS_MEET` fires).
3. `R^Aut` shown to depend on a chosen labeling (change under relabeling) → breaks A1, the raise
   collapses to smuggled gauge (`NC_NO_POINT_USED` fires).
4. A within-zone T3 torsor exhibited that is NOT a single free Aut-orbit → breaks the grounding.

---

## Verification

`raise_selector_minimal_check.py` — **21/21 PASS, exit 0**. Exact (`Fraction`/`int`/set) throughout;
no float in any load-bearing check. **Mutation-tested (can-fail confirmed on the CONCLUSION):**
- MUT1 `dim R^Aut = 4` → FAIL (T1 measures the real orbit count).
- MUT2 degree constant → FAIL (T2/T3 need the owned observable to separate).
- MUT3 refinement object forced "invariant" → FAIL (T3 refinement clause is load-bearing).
- MUT4 sections exist at `n=2` → FAIL (corollary is real).
Negative controls each fail the CONCLUSION: `NC_MINIMAL_IS_MEET`, `NC_NO_POINT_USED`,
`NC_SECTION_IS_REFINEMENT`, `NC_NONTRANSITIVE_HAS_FINER` (a non-transitive `Stab(0)<S₉` DOES admit a
finer invariant algebra — the machinery sees refinements where they exist; their absence on the
transitive scene is the content).

---

## VERDICT & ERRORS OF RECORD (skeptic §05.8.R — INDEPENDENT pass, accepted in full, repairs applied)

**Independent skeptic verdict: NO-KILL.** The skeptic could not name a second object killing the
raise under §05.8.R. The gauge-smuggle KILL (the pinned worry) was attacked directly — by
independent Reynolds recompute and by the skeptic's OWN mutation tests (`_separates → True` → 19/21
FAIL; `degree → constant` → 16/21 FAIL) — and does NOT land: `R^Aut` is a genuinely point-free
lattice meet. The raise is confirmed the SAME species as `D0-Q8-DEDEKIND-MINIMALITY-001` (row 207),
real `∀`-positive content over the bare `∃`-negative no-go, not a rename. All verbatim citations
verified faithful (read past ±10; none say the opposite). Errors of record and residual risks below.

**Author's own repair (pre-skeptic, caught by the cert's `NC_MINIMAL_IS_MEET`):**
- **EoR-0 (separation-predicate bug):** the first draft's predicate tested injectivity
  (`len(set of degrees in block) == len(block)`), always true for distinct degrees, wrongly
  admitting dim-2 algebras as "separating" → the negative control FIRED (1 FAIL). **Repaired:** the
  predicate now tests "no block merges two orbits of different degree" (`_separates`), forcing
  all-singletons `= R^Aut`; cert then 21/21. The can-fail control caught the author's own bug —
  the machinery working as designed.

**Skeptic errors of record (accepted, repaired in this version):**
- **EoR-A (line-cite drift):** pre-fact 1 cited `.lean` line numbers into a module still on row 550's
  general-lemma route. **Repaired:** now cite by theorem name, not line (pre-fact 1).
- **EoR-B (self-grade overstated universality):** the FINAL box's "no extremum refines R^Aut" reads
  as a closed universal; the cert only proves it for the enumerated owned `A`-built functionals
  (degree witness + F1–F4 SSB set), and the "every owned functional is a class function"
  universality is EVIDENCE-grade (row 549's own scope: *"NOT one universal impossibility theorem"*).
  **Repaired:** T3 and the FINAL box now state the universality at EVIDENCE grade, inheriting row
  549's caveat, not a fresh theorem.

**Residual risks (named, non-fatal — must not drift into over-claim at mint):**
- **RR-1 (single-witness refinement clause):** the cert's T3 refinement line proves *one* refinement
  (vertex-0 indicator) is non-invariant, NOT the universal "no invariant proper refinement exists";
  the universal is outsourced to the owned `no_equivariant_section` Lean theorem + SSB trichotomy.
  Legitimate cross-reference, but the prose must not read as if the cert proves the universal.
- **RR-2 (inherited EVIDENCE scope):** the "no extremum refines R^Aut" tagline leans on class-function
  universality, which row 549 types as EVIDENCE. At mint this inherits row 549's scope caveat.

---

## What this does NOT show

- No minting, no registry/Lean edits; `053040` untouched; no commit.
- The **positive extremality statement** is CERT-grade + mutation-tested; the **Lean lift** (a
  `Fin 33` decidable orbit-count / minimality lemma) is the mint path, not claimed complete. Only
  the **corollary's equivariant core** is already LEAN_PROVED (row 549).
- "Grounds the 22 T3 torsors" means *files them as orbits of the one extremal algebra* (the
  P-GAUGE-M1 roster), NOT *derives* each torsor's internal content.
- Out of scope: torsors whose freedom is a measurement/valuation (ALPHA-SEAM etc.) — not
  within-zone orbits, correctly not claimed.

---

## FINAL

> **THEOREM (P-INVARIANT-MINIMAL, raise of D0-CANONICAL-WITHIN-ZONE-SELECTOR-M1-NOGO-001).** On the
> frozen scene `K(9,11,13)`, the Aut-invariant subalgebra `R^Aut` (the class functions) is the
> **UNIQUE MINIMAL observable algebra**: it is the fixed-ring `(C^V)^{Aut}` (dim 3 = #orbits), it is
> the smallest Aut-closed unital algebra separating the owned observables, and it is the unique such
> minimizer — the meet of the separator lattice, attained. Every owned functional is a class
> function constant on the single within-zone orbit, so — **over the owned `A`-built functionals** —
> **no extremum refines `R^Aut`**: it *is* the extremum (that "every owned functional" universality
> is EVIDENCE-grade, inheriting row 549's scope). The selector no-go ("0 Aut-equivariant sections",
> row 549 LEAN_PROVED) is its **COROLLARY** — a section would be the invariant proper refinement that
> extremality forbids — and the **22 registry T3 torsors ground as the Aut-orbits of this single
> extremal fixed-point algebra** (P-GAUGE-M1 roster, UPLIFT_MAP.md:316).

**FINAL STATUS: RAISED-TO-THEOREM** (the lattice-extremality — `dim = 3`, unique minimal separating
algebra, `= meet` — proven at cert grade, 21/21, mutation-tested; the "every extremum" universality
at the EVIDENCE grade row 549 carries). **Independent skeptic verdict: NO-KILL** — the gauge-smuggle
KILL FAILED (minimality is a point-free lattice meet, confirmed by the skeptic's own recompute +
mutations); 2 errors of record + 2 residual risks accepted and repaired/recorded. **T3 torsors
grounded as corollaries: 22** (the full P-GAUGE-M1 within-zone roster; measurement/valuation torsors
correctly out of scope). Mint path: Lean `Fin 33` orbit-minimality lemma (row-550 EXACT-MISSING
item (1), cite by theorem name) + cert promotion per `VERIFIED_CLOSURE_PROTOCOL.md`; owner-gated,
not executed.
