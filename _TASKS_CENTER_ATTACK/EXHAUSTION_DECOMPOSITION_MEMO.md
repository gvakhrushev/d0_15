# EXHAUSTION_DECOMPOSITION — obligation (iii) of `D0-ALPHA-SEAM-FORM-FORCED-001` decomposed per axis: RATE axis at inventory grade v2; (iii) reduces to (i) + standing tripwires (POST-SKEPTIC v2; the v1 rate-axis KILL accepted and repaired)

> ## ⚖️ VERDICT (skeptic #23, independent agent, 2026-07-18): **RATE-AXIS INVENTORY v1 KILLED · FORM-AXIS SURVIVES · K1-BINDING SURVIVES (one nick) · NET-REDUCTION WOUNDED → all repaired; overall: WOUNDED-WITH-ONE-KILL, kill accepted in full, no defense.**
> **The kill (named second object, four-layer-owned):** `phasonFlipTransferMatrix`
> (`Cosmology/PhasonFlipEntropy.lean:22` — fun-literal, ℚ entries `[[3/2,1/16],[1/10,3/2]]`,
> spectral radius EXACTLY `3/2 + √10/40 ∈ ℚ(√10)`, expanding, non-φ; owned by row 114, wired in
> `All.lean`, cert `vp_phason_flip_entropy_sde.py`, printed as the S_DE active window at
> BOOK_08:43). v1's literal-only sweep was structurally blind to it, and v1's tripwire claim
> ("R1 fires on any new matrix def") was FALSE for fun-literals, `![![` literals, rational
> entries, and derived defs (skeptic's live evasion list: `V = Hd−Hu` radius 2,
> `TimeEnergyOperator := T^2`, `edgeLeakP/H`, `VmixA/B`, `fin2SkewD/fin2A`). **Errors of
> record:** EoR-1 ("the ONLY non-φ expanders are four WITNESS objects" — false as printed);
> EoR-2 (the tripwire scope overclaim). **Repairs applied (cert v2 + this memo):** (a) the
> sweep now works by TYPE ANNOTATION (`def/abbrev … : Matrix (Fin 2) (Fin 2) R`/`ZMat2`/`M2`),
> catching every body form; all six evasion objects swept and classified; (b) the phason
> transfer is the FIFTH named non-φ expander, adjudicated as a WINDOW object: per row 252's own
> identification its ITERATED carrier is the row-stochastic `zoneTransport` with radius exactly
> 1 (rate 0) — the expanding `3/2+√10/40` is the S_DE spectral WINDOW, not an iterated growth
> rate; **the fork is printed: an owner classing the phason transfer as ITERATED dynamics
> REOPENS the rate axis** (cert mutant N6 fires exactly there); (c) derived defs
> (`V`, `TimeEnergyOperator`, `ImM/ImN`, `N`) declared with live body tokens; (d) §0/§1/§5
> rewritten to the corrected count and tripwire scope. **The skeptic's nick on R5** (count
> phrase checked, magnitude not) repaired: R5 now requires BOTH multiplicands. The skeptic
> verified the k=1 binding pins both in print, the SL(2,ℝ) trichotomy is mathematically correct
> as used (no fourth class in traceless 2×2), the four v1 witness classifications hold (none
> iterated anywhere), the zone matrix is row-stochastic radius 1 (`native_decide`), and the
> depth-leg numbers are drift-free across all three sibling memos and certs.

**Date:** 2026-07-18 · **Status:** POST-SKEPTIC v2; mint applied after repairs (§5).
**Lineage:** third α-line pass of the external-review session (after obligation (ii) ADOPTED,
skeptics #20/#22, and the (i) five-candidate fork, skeptic #21). Pre-flight: rows
243/253/285/288/529/564 re-read; `ALPHA_SEAM_NOGO_V2` hooks untouched (object B only); S2 of
`vp_alpha_seam_form_forced.py` re-read (its seven rivals are POINT separations; this memo is
the AXIS-space complement).
**Companion cert:** `_TASKS_CENTER_ATTACK/rate_axis_inventory_check.py` — 5/5 PASS rc=0;
`--selftest` 5/5 mutants caught. Live sweep of ALL literal integer 2×2 operator defs across
`D0/**.lean` (29 found, none hardcoded), exact spectral radii (disc-case analysis, ℚ(√5) for
the φ-family), quoted role anchors live-verified per entry.

---

## §0. Claim (honest grade: per-axis decomposition; ONE axis newly exhausted at inventory
grade; NO joint-exhaustion theorem claimed)

Obligation (iii) — "joint exhaustion (no owned rival on any axis)" — is not a single theorem;
it is five axis-claims of different types. This memo (a) makes the decomposition explicit
with per-axis states and owners, (b) EXHAUSTS the RATE axis at inventory grade — the corpus's
registered operator inventory contains NO owned expanding rate outside the φ-family
`{k·ln φ}`, with `k=1` selected in-print by the adopted per-turn binding — and (c) reduces
the residual of (iii) to: the (i) fork (depth axis) + two standing machine tripwires. The
inventory-indexed cap is stated, not hidden: like the NOGO's inventory-indexed legs, this is
exhaustion over what is OWNED TODAY, made future-firing by construction (R1/R4 fail on any
new matrix def or reclassification), not future-proof by theorem.

## §1. The five axes (per-axis state, each with its owner or its named residual)

| axis | tuple value | state | carrier |
|---|---|---|---|
| channel | `sin` (off-diagonal) | **EXHAUSTED-OWNED**: a 2×2 rotation has exactly two channels (diagonal `cos` / off-diagonal `sin`); role-changing transport forces off-diagonal (`G²=−I`, row 288 `D0-Q8-SIN-CHANNEL-001`, Lean); `cos` is the separated control | owned THE + S2 point-separation |
| form | linear `1+h·sinθ` | **EXHAUSTED-CONDITIONAL**: the classical trichotomy of one-parameter `SL(2,ℝ)` classes (elliptic/parabolic/hyperbolic — EXTERNAL classical owner, cited not re-proved, same citation grade as Adler–Weiss) exhausts the generator classes; elliptic (`G`) ⇒ bounded readout (owned, row 529 module); parabolic (`N`, the named single-crossing input) ⇒ exactly linear (owned THE, row 529); hyperbolic seam generator ⇒ would be an owned expanding seam operator — NONE exists (cert R1/R4 sweep: seam class = {G, U(θ), N, seamB, rotJ}, all radius ≤ 1) and minting one fires the tripwire | row 529 + external trichotomy + cert R4 |
| angle | `θ_seam = 12/5` | **ANCHORED**: exact `2π₀(2−φ) = 12/5` (`D0-PI0-DISCRETE-ANGLE-001`, THE/Lean); a rival angle requires a rival `π₀` derivation — that is `ALPHA_SEAM_NOGO_V2` hook (iv) territory: REDIRECTED to the realization no-go's owner, never absorbed here (the F5 firewall of the FORM row) | owned THE + hook-(iv) firewall |
| **rate** | `h_KS = ln φ` | **INVENTORY-EXHAUSTED v2 (post-kill repair)**: the type-annotation sweep sees EVERY named 2×2 def (any body form — the v1 blindness is the recorded kill); every ITERATED-class operator has radius ∈ {≤1} ∪ {φᵏ}; `k=1` pinned in-print (count AND magnitude, R5); the corpus's non-φ expanders are FIVE-plus NAMED non-iterated objects: silver-ratio SNF factor `P_M` (ℚ(√2)), Schur block (2), Davis–Kahan examples `Hu`/`Hd` (4/5), mismatch `V` (2), and the **phason S_DE transfer** (`3/2+√10/40` ∈ ℚ(√10)) — adjudicated WINDOW: its iterated carrier is the row-stochastic `zoneTransport`, radius exactly 1 (row 252); an owner classing the phason transfer as ITERATED dynamics REOPENS the rate axis (the printed fork; cert mutant N6) | cert `rate_axis_inventory_check.py` v2 |
| depth | `φ⁻¹⁷ = ξ₅·φ⁻¹²` | **OPEN = obligation (i)**: seam factor ξ₅ THE; transport factor = the five-candidate fork (`TRANSPORT_TWELVE_FORK_MEMO.md`, skeptic #21); composition exponent-rigid (5+12=17; rivals 9/13/16/18, the 16/18 pair on the separated F3 surface) | the (i) fork |

**Net state of (iii):** channel/form/angle/rate carry owned-or-inventory exhaustion with named
caps; the depth axis is the single axis still holding (iii), and it is exactly obligation (i).
(iii) therefore REDUCES to (i) plus the standing tripwires — no separate (iii) program
remains. Release status: unchanged (PROOF-TARGET).

## §2. The rate-axis computation (cert, all exact)

Sweep: every `def … := !![a,b;c,d]` over ℤ/ℚ/ℝ in `D0/**.lean` (ZMod rings excluded —
nonspectral; non-2×2 shapes excluded by type annotation). 29 literals found; each classified
(role, exact radius): DYNAMICS = {T, M, M2, Mφ, Mphi, fibFusionMatrix, sturmianIncidence} —
all radius φ; SEAM = {seamG, seamN, seamB, rotJ} — radii ≤ 1 (isometries/nilpotents);
CONTROL = {hypCtrl} — φ²; WITNESS = 17 objects (conjugators, SNF factors, projectors, frozen
Pauli generators, gap-bound examples, Galerkin instance, Schur block). Derived/book-registered
(exact trace/det, anchors): −T, T², N_τ, A=N_τ², B=SR, −S — all φ or φ². Radius decision:
disc < 0 ⇒ radius² = det; disc = 0 ⇒ |tr|/2; disc > 0 ⇒ ℚ(√squarefree) with φ-membership
decided by exact ℚ(√5) equality against φᵏ, k ≤ 7. Rival criterion: EXPANDING (radius > 1)
and non-φ — contractions and isometries cannot rival a positive rate.

## §3. Pre-registered attack surface

- **ATT-1 (strongest self-attack: inventory ≠ exhaustion).** The claim is inventory-indexed
  and says so five times. What makes it more than bookkeeping: the sweep is LIVE and
  complete-by-construction over the Lean corpus (R1 fails on any new literal), the roles are
  quoted not authored (R2), and the four non-φ expanders are NAMED with their in-file roles —
  the reopening path (F4) is now a machine event, not a vigilance hope. A skeptic grading
  this "still just an inventory" concedes exactly the cap the memo states.
- **ATT-2 (the derived-operator hole).** Products/powers of φ-family matrices need not stay
  φ-family; the sweep covers literals + six NAMED derived operators, not the free semigroup.
  Priced: a rate rival must be an OWNED operator with iteration semantics — ownership means a
  def/row/book anchor, which the sweep or R1 then sees. An unnamed product is not owned.
- **ATT-3 (role-classification torsor, trap (k)).** DYNAMICS-vs-WITNESS is my classification;
  the quoted anchors ground it, but a skeptic may contest an entry (the Schur block and the
  Davis–Kahan pair are the deliberate boundary cases — N4 shows a reclassification fires R4).
  Invited: name an entry whose quoted anchor supports iteration semantics I denied it.
- **ATT-4 (3×3-and-larger hole).** The sweep is 2×2. Larger owned operators with iteration
  semantics: the zone matrix M (row 252, 3×3 STOCHASTIC — spectral radius exactly 1, rate 0,
  hand-noted); Laplacians/adjacency (static, no iteration semantics). Priced as a named scope
  line; a future owned expanding 3×3 flow would need its own adjudication — R1 does not see
  it (stated, not hidden).
- **ATT-5 (form-axis borrow).** The SL(2,ℝ) trichotomy is an external classical fact used at
  citation grade — exactly the grade the corpus already uses for Adler–Weiss/Tomita–Takesaki.
  If a skeptic rejects citation-grade exhaustion for the form axis, the axis falls back to
  "owned two of three classes + tripwire on the third" — the reduction of (iii) to (i)
  survives unchanged.

## §4. What this does NOT show

No joint-exhaustion THEOREM; no future-proofing (stated cap); no change to the (i) fork or
any release status; no touch of hook (iv) (angle rivals redirect to the realization no-go);
no claim about operators without owned iteration semantics; the S2 numeric separations remain
the FORM row's falsifier surface, untouched.

## §5. Ready-to-mint package (owner decision; apply after skeptic)

Row 564 note append (additive; v2 post-kill wording): "(iii) DECOMPOSED [2026-07-18,
EXHAUSTION_DECOMPOSITION_MEMO.md POST-SKEPTIC #23 (v1 rate-axis KILL accepted -> repaired),
cert rate_axis_inventory_check.py v2]: channel EXHAUSTED-OWNED (two channels, off-diag
forced, row 288); form EXHAUSTED-CONDITIONAL (SL(2,R) trichotomy external-cited; elliptic
bounded + parabolic linear owned at 529; hyperbolic seam generator none owned, tripwired);
angle ANCHORED (12/5 THE; rivals redirect to hook (iv)); rate INVENTORY-EXHAUSTED-v2
(type-annotation sweep over every named 2x2 def, any body form: ITERATED rates = phi-family
only, k=1 pinned in-print count+magnitude; non-phi expanders = five-plus NAMED non-iterated
objects incl. the phason S_DE transfer radius 3/2+sqrt10/40, adjudicated WINDOW against row
252's stochastic carrier; FORK PRINTED: classing it ITERATED reopens the axis); depth OPEN =
obligation (i). NET: (iii) reduces to (i) + tripwires R1/R4 (corrected scope); inventory-
indexed, not future-proof."
