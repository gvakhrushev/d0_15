# PROOFREADING FLOOR — the first structural import of the cross-substrate program (MINTED)

**Status:** MINTED as `D0-CASCADE-FLOOR-PROOFREADING-001` (2026-08-14) after independent
skeptic verdict **MINT-AFTER-REPAIRS** (0 kills; skeptic reproduced builds, axioms, and
all six identifiers character-for-character). **Repairs applied (errors of record):**
R1 the "same grade as Baer 1933" claim was FALSE — Baer's classification is load-bearing
in a registered leg; Hopfield/Ninio appear in NO proof here — regraded to
docstring/bridging-definition citation (defect-closure precedent); R2 the preflight
"'kinetic' — ZERO hits" was a FALSE absence claim (kill shape 3, caught again): BOOK_00:75
+ BOOK_03:180 + D0.Algebra.GaugeKineticPositivity carry physics-sense 'kinetic' — all
unrelated to fidelity, the no-duplicate conclusion stands, the claim as written did not;
R3 (strongest) the ladder's "each stage a genuine floor for every n" was a FALSE GRADE —
no n-stage carrier is defined; proofreading_ladder orders error TARGETS only; docstrings
repaired; R4 the "reason DNA replication needs an exonuclease is THIS floor" causal claim
is NOT licensed by parametric arithmetic — removed to structural-anchor wording; R5
adjacency/membership: NOT in carriedFloors/carriedFloorsExtended, no chain position,
adjacency OPEN, umbrella note-only update; R6 the note carries the multiplicativity-
encodes-reset honesty + load-bearing definitions list. Pre-flight (corrected):
'proofread', 'Hopfield', 'Ninio' — zero hits in registry, books and Lean; 'kinetic' —
physics-sense hits only (above), none touching fidelity/discrimination; the two 'error
correction' registry matches are unrelated (row 271's is the NAME of a negative control,
row 288 an error-of-record note). Floor namespace D0-CASCADE-FLOOR-* has seven owners
(comparison, one-loop, order-memory, defect-closure, orientation-parity, scale-ratio,
shell-closure) — none touches fidelity/discrimination. ID:
`D0-CASCADE-FLOOR-PROOFREADING-001`. Adjacent theme, cited not rival:
GOLDEN_COVERAGE_LEDGER.csv:960 (v17 holographic-QECC) — the coverage matrix's second
anchor, unclaimed here.

## Claim X (DEF-0.2.2 form)

X (Lean `D0.Foundation.CascadeFloorProofreading`; standalone 3030 GREEN; all SIX proposed
theorems at axioms propext/Classical.choice/Quot.sound): for every discrimination limit
`0 < δ < 1` (δ = the wrong/right acceptance-ratio floor of a single equilibrium test —
one Boltzmann factor, the carrier's DEFINING hypothesis, stated inside the statement):

1. **Insufficiency** (`one_test_insufficient`): the one-test carrier cannot reach the
   proofread error level `errOf (δ²)` — `errOf` is strictly monotone
   (`errOf_lt_errOf`) and `δ² < δ`;
2. **Control** (`two_stage_control`): the two-stage carrier (two tests separated by an
   irreversible reset; ratios compose multiplicatively) reaches `errOf (δ²)` exactly;
3. **Registered shape** (`stepProofreading` + `stepProofreading_discriminates`): the pair
   is a genuine `CascadeStep` — same obligation, false below, true above, discriminating
   by the scaffold's own theorem;
4. **Ladder** (`proofreading_ladder`): the error TARGETS `errOf (δⁿ)` descend strictly at
   every depth (levels only — no n-stage carrier constructed; post-skeptic R3 wording);
5. assembly `proofreading_floor`.

The imported structure is kinetic proofreading (Hopfield 1974 PNAS 71:4135; Ninio 1975
Biochimie 57:587) — **external-cited as the physical reading of the carrier definitions,
docstring/bridging-definition grade (post-skeptic R1: NOT the Baer grade — Baer is
load-bearing in a registered leg; Hopfield appears in no proof here)**: the
detailed-balance bound ("without an irreversible intermediate the two tests collapse into
one") is the cited external structure; D0 owns the finite arithmetic floor in its own
registered `CascadeStep` shape.

## Why this is the program's first real import (not decoration)

The cross-substrate program (license `D0-INFORMATION-CONNECTIVITY-001`) named a floor of
this FAMILY from the biology column BEFORE this module existed (coverage matrix, target
A2 — post-skeptic A2: the named shape was "a record channel below fidelity f cannot carry
a comparison", the delivered obligation is "reach errOf(δ²)" — same family, not identical
shape). The import direction is structure INTO the cascade, as SHAPE only (post-skeptic
R4): whether any actual biochemical step instantiates the one-test carrier is exactly the
A1/A2 passport question; no causal claim about biochemistry is registered. Per the frozen
R-A2 record (committed 02413ce, BEFORE this mint): a real stage that beats its
equilibrium bound (Gromadski–Rodnina 650× vs 350×) is graded internally multi-test — the
floor's register count is a lower-bound reading, never a per-biochemical-stage count.
No biological number is claimed anywhere.

## Named risks & PRE-REGISTERED attack surface (strongest first)

- **ATT-A (strongest: trivial arithmetic).** "δ² < δ dressed in a structure" — defense,
  pre-registered: the registered floors are EXACTLY of this grade (one-loop: two runs of
  one ℤ register collapse — elementary; order-memory: ℤ×ℤ is abelian — elementary); the
  cascade's content has always been the CARRIER pair + the same-obligation discipline,
  which this module satisfies with real insufficiency and real control (the scaffold's
  `step_discriminates` applies non-vacuously). A kill must name a floor-grade defect, not
  the elementarity of the arithmetic.
- **ATT-B (the δ-bound is a stipulation).** The one-Boltzmann-factor bound on a single
  equilibrium test is the carrier's defining hypothesis INSIDE the statement (grade
  identical to "one ℤ register" in 4→5) and is the cited content of the external anchor
  (Hopfield's detailed-balance argument). A kill must show the corpus owns a DIFFERENT
  record-channel semantics this contradicts.
- **ATT-C (multiplicativity smuggles the conclusion).** The two-stage carrier's
  multiplicative composition encodes the irreversible-reset assumption — TRUE and stated
  (honest scope #1 in-module): without the reset, detailed balance collapses the pair;
  that collapse is Hopfield's own point and is carried as the DEFINITION of the two
  carriers, not proved. The interlock "the reset is the arrow floor's object" is a NAMED
  TARGET, not claimed.
- **ATT-D (biology decoration).** No biological numbers; A1/A2 passports pending; the
  import is the SHAPE. The row must not cite any error-rate value.
- **ATT-E (duplication).** Pre-flight grep clean (above); nearest neighbours are the
  comparison floor (monopoly acceptor) and one-loop floor (register memory) — different
  obligations (verifiability / history vs fidelity).
- **ATT-F (umbrella contract).** The umbrella `D0-CASCADE-INSUFFICIENCY-CHAIN-001` and
  assembly `D0-CASCADE-CARRIED-ASSEMBLY-001` enumerate carried floors — this row must NOT
  claim membership in the carried assembly (it is a NEW floor at the record end, not yet
  wired into the chain adjacency); the note must say "adjacency into the chain (which
  floor's repair is this floor's failing carrier) is open".

## What this does NOT show

The chain adjacency of the new floor (open); the arrow-floor interlock (named target);
any biological quantity (A1/A2 pending); any claim about δ's value; the n-stage cost
accounting (energy per stage — future, pairs with the arrow floor).
