# OWNER-EDGE DRAFT — Fibonacci gap labeling: saturated and measured (the cleanest bridge in the corpus)

**Status:** DRAFT owner-edge for `D0-KTHEORY-GAP-MODULE-001` ("Bellissard shadow", CORE-FORMALIZED)
and the EXTERNAL-GAP-marked K-theory rows; minting per protocol required; no registry row edited.
Source: full Damanik-import sweep (this campaign; all citations arXiv-verified by the scout;
one attribution flag carried below).

## The three-layer owned statement (golden hull, α = φ⁻¹)

1. **Allowed labels (K-theory owner):** for Schrödinger operators over the golden subshift, the IDS
   on spectral gaps takes values in (ℤ + ℤα) ∩ [0,1] — the trace image of K₀(C(Ω)⋊ℤ).
   *Owner: Bellissard–Bovier–Ghez, Rev. Math. Phys. 4 (1992) 1–37 (+ BHZ 2000 review).*
   **The label group ℤ + ℤφ⁻¹ is literally D0's owned rank-2 module row** — the "Bellissard shadow"
   has a literal Bellissard owner.
2. **Saturation (all gaps open, every coupling):** {IDS values on gaps} = {mα mod 1} ∪ {1} exactly;
   every allowed label is realized. *Owner: Damanik–Gorodetski–Yessen, Invent. Math. 206 (2016)
   629–692 (arXiv:1403.7823), Thm 1.3 — valid for EVERY λ > 0; small-λ display + linear gap-opening
   rates: Damanik–Gorodetski, CMP 305 (2011), arXiv:1001.2552.*
3. **Measured (A-grade passports):** Tanese et al., PRL 112, 146404 (2014) — cavity polaritons,
   "mini-gaps obeying the gap labeling theorem and log-periodic oscillations of the IDS" (verbatim);
   Baboux et al., PRB 95, 161114(R) (2017) — integer gap labels measured as winding numbers.
   **Integer-valued predictions, zero coefficient freedom — genuinely discriminating** (the exact
   property whose absence demoted the PMNS δ₀ row).

## Hypothesis chain (fed by D0's own owned rows)

Strictly ergodic golden subshift with unique invariant measure of cylinder weights (φ⁻¹, φ⁻²) —
which IS D0's owned unique-trace row (`D0-PHI-CYLINDER-TRACE-UNIQUE-001`) and the golden weight
layer of `DetectorSupportGoldenWeight.lean` (this campaign). The hypothesis side of the bridge is
already internal.

## Bonus exact fact (verified here): the toral generator is the substitution's inverse

A·T = I exactly for A = [[1,1],[1,0]] (Fibonacci abelianization) and T = [[0,1],[1,−1]] (D0 toral
generator): **T = A⁻¹**, nothing stronger (not conjugate; charpolys x²−x−1 vs x²+x−1). The owned
external statement (DG arXiv:1001.2552, verbatim factor map): at zero coupling the trace map is a
2:1 factor of L_A on 𝕋²; for λ > 0 only hyperbolicity and the **log φ entropy numerator** persist
(DGY Thm 1.6 exact identities) — matching D0's owned h_KS = ln φ. Claim ceiling fixed accordingly.

## Mandatory declarations (anti-conflation, per the sweep's cautions)

- **Two spectra live on one hull:** dynamical/Koopman spectrum = pure point with eigenvalue group
  ℤ+ℤα (Hollander–Solomyak — D0's owned clean zone) vs Schrödinger spectrum = zero-measure purely
  singular continuous Cantor set (Sütő 1989 + Damanik–Lenz 1999, every λ ≠ 0, every hull point).
  Every D0 time-layer "spectral" claim must declare which; the rank-2 module is the correct COMMON
  object (dynamical eigenvalue group ↔ gap-label group), theorem-backed on both sides.
- **No single spectral dimension:** DGY's strict chain γ < dim ν < dim Σ < α̃_u forbids quoting one
  number; small-coupling exact constants do NOT exist in print (linear bounds only).
- **Built-in anti-numerology control:** the large-coupling dimension constant is log(1+√2) — the
  SILVER ratio. Not everything golden stays golden; any D0 large-coupling claim inherits this.
- **Attribution flag:** all-λ hyperbolicity credited to Cantat (Duke 149, 2009) via DG's GAFA quote;
  primary hypothesis phrasing must be checked before ledger entry.

## Why this row first (sweep's recommendation, adopted)

Exact module identity with an existing owned row (no reinterpretation), hypothesis side already
internal, integer predictions with two independent lab anchors, and the whole package converts the
corpus's honest "Lean = EXTERNAL-GAP" markers into named owner edges with passports.
