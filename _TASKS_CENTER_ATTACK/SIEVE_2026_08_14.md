# The cross-substrate sieve — 2.85·10⁹ comparisons, zero unregistered survivors

**What ran (2026-08-14):** six parallel sieve agents, one per dataset family, each scanning
EVERY external physical quantity in its family against the full D0 motif library (φ^±k
k≤17, Lucas/Fibonacci, scene invariants 9/11/13/33/359/1287/960/2574, seam objects ξ₅,
12/5, 359/160, owned integers 20/44/206, group orders), with mandatory trials accounting,
10 000-draw null controls (seed 20260814), a known-match exclusion list, and deterministic
scripts (preserved in `sieve_2026_08_14/`). Reporting threshold: look-elsewhere-corrected
expected false hits < 0.5.

## Result

| family | files | quantities | comparisons | survivors |
|---|---|---|---|---|
| particles (PDG 2025 mass/width table, 425 q) | 13 | 425 | 40 754 015 | **0** (min E = 535.7) |
| cosmology (DESI dm/rd + z_eff) | 18 | 12 | 467 130 | **0** (E ≈ 277 each) |
| astro (GWOSC 276 events, SPARC 3375 rows) | 23 | 35 451 | 2 762 519 175 | **0** (min E = 2.25·10⁵) |
| neutrino/nuclear (IceCube HESE 164 events, SRC) | 11 | 13 869 | 39 839 395 | **0** (best E ≈ 3.4) |
| internal spectra (gap band edges, vacuum feedback) | 17 | 52 | 8 285 730 | **0** |
| QFT/RG (templates only) | 4 | 0 | 466 251 | **0** (no external data at all) |
| **total** | 86 | ~49 809 | **2 852 331 696** | **0** |

Sanity checks that the sieve worked: the registered match m_μ/m_e = 206 was re-detected
and correctly excluded; the dense p/r ≤ 60 rational grid was correctly punished by its own
null rate (~5.9·10⁻⁴/comparison); the tightest raw hits everywhere were quotation-rounding
artifacts (2–3 significant-figure catalog values landing on low rationals), exactly what
the null predicts.

## What this MEANS (two theorem-shaped consequences)

1. **A no-cherry-picking certificate.** The registered empirical record (sin²θ_W, m_s/m_d,
   Cabibbo, 206, α leading term, Coldea φ, the capped PMNS family, S_DE window) is the
   COMPLETE set of motif coincidences in the repository's external data at this tolerance —
   it does not sit on top of an unreported sea of comparable coincidences. A 2.85·10⁹-
   comparison adversarial sweep is the strongest anti-look-elsewhere statement the corpus
   has ever had. (Candidate registry row if wanted: a recorded-negative in the style of the
   route audit; scripts are deterministic and reproducible.)
2. **The numeric-coincidence channel is EXHAUSTED on current data.** New gold cannot come
   from re-mining these files. It can only come from (a) NEW data — the genetics/biology
   acquisition targets A1–A4 of the coverage matrix, and refreshing the external caches
   listed below — or (b) STRUCTURAL transfer (R3-style retrodictions, the error-correction
   floor import), which tests shapes, not decimal coincidences.

## Data-integrity findings (surfaced by the sieve, actionable)

- Manifests with status=MISSING and header-only results: CKM (empty V_ij), both meson
  dirs, DESI_BAO, PlanckCMB — these passports are SHELLS awaiting data.
- Three manifests record a REVERTED faked-READY history (DESI, GWOSC, IceCube) — already
  recorded in the manifests themselves; the frozen local copies (IceCube 164-event table,
  GWOSC mass-defect CSVs) are scanned here but are NOT hash-verified against the pinned
  releases. Refreshing these caches is an acquisition task of the same rank as A1–A4.
- The one fully pinned external dataset is PDG `mass_width_2025.mcd` (SHA-verified).

## Program consequence

The sieve closes the "просеять всё сразу" step: done, honestly, at scale. The frontier is
now exactly two roads: **new data in** (A1–A4 + cache refresh) and **structure across**
(R3; error-correction floor). Numerology inside the existing files is a dead channel — by
measurement, not by opinion.
