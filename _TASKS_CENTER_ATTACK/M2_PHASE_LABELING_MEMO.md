# M2-PHASE-LABELING — the owned labeling of the DETECTOR-LAYER pointed shell V₉ = Ω₈ ⊔ {ω₀}: forced up to G_res, Q₈-typed not μ₉-cyclic; canonical cyclic/phase labeling IMPOSSIBLE from owned data; zones 11/13 own nothing per-vertex; transport onto K(9,11,13) graph slots stays un-owned (DRAFT v2, post-skeptic-#1)

**Status:** DRAFT candidate v2; no registry row edited, no cert minted. Skeptic #1 verdict:
**SURVIVES** (torsor forcing + cyclic impossibility + module upheld; his independent transitivity
reimplementation closed the count-only gap; fingerprint verified in exact Fractions). TWO WOUNDS +
script repairs, all accepted in full and applied (repair log at end). Companion script:
`m2_phase_labeling_check.py` — 50/50 PASS. **Framing correction (skeptic wound W2, applied to the
title and throughout): the owned object is a labeling of the detector-layer pointed shell
V₉ = Ω₈ ⊔ {ω₀}, NOT of K(9,11,13) graph slots — the memo's own non-invariance result (X4) shows
graph-slot transport is exactly the un-owned datum.** Pre-flight run twice (keywords: mu9,
labeling, omega0, role cycle, Q8, phase / MU9, PHASE-LATTICE, OMEGA8, ROLE-CYCLE): no existing row
owns "canonical within-zone labeling"; the K-theory gap-labeling rows (D0-KTHEORY-001,
D0-KTHEORY-GAP-MODULE-001) are DIFFERENT content (spectral gap labels, not vertex labels) —
cross-referenced, not duplicated. Adjacent owners cross-referenced throughout:
D0-Q8-TERMINAL-FOURIER-001 (pre-owner of the E-system, see W1), D0-Q8-DEDEKIND-MINIMALITY-001,
D0-OMEGA8-CENTER-001, D0-DSIGMA-ROLE-CYCLE-CARRIER-CANONICAL-NOGO-001,
D0-REPRESENTATION-RECONSTRUCTION-MAXIMALITY-NOGO-001, TICK_COUPLING_SCHUR_MEMO.md (S2),
T2_PRIME_FINAL_STATE.md, SHELL_RULE_R_LEDGER.md (M2).

**Correction of record for the campaign brief itself (found in pre-flight, stated first;
sentence corrected per skeptic wound W2b):** the brief's memory line "the owned phase lattice is
μ₉" has NO repo text behind it. `CAMPAIGN_FINAL.md` contains zero occurrences of `μ`, `mu9`,
`mu_9`, or `phase lattice`; `ALPHA_SEAM_NOGO_V2.md` contains μ-symbols ONLY as the seam moments
μ₁/μ₂/μ₃ (lines 9, 25-26, 37, 48, 55, 82 — e.g. "μ₂ = 2¹²·ρ exactly"), none of which is a μ₉ or
phase-lattice minting. The only μ₉-operator text in the corpus is the U₃ CANDIDATE in
`TICK_COUPLING_SCHUR_MEMO.md:38-46`, whose own pre-registered risk S2 says the opposite:
"**mu_n-as-diagonal-operator is not a greppable owned object**" (line 93-94). This memo is the
attempt to force that ownership; its result is that μ₉ is NOT forceable but a different labeling is.

## Claim X (DEF-0.2.2 form, candidate language)

Frozen scene K(9,11,13), zones V₉ ⊔ V₁₁ ⊔ V₁₃.

**(X1) Zone-9 positive branch (detector layer).** The owned structure on the DETECTOR-LAYER
pointed shell V₉ = Ω₈ ⊔ {ω₀} (the pre-graph object of §01.7/§01.8 — "The graph begins only after
this cycle is lifted") forces a labeling ℓ: V₉ → Q₈ ⊔ {∗} (basepoint ω₀ ↦ ∗, A± ↦ ±1), canonical
**up to a residual group G_res**, where G_res depends on which of three owned-text readings binds
(adjudication NOT made here). Transport of ℓ onto graph slots of K(9,11,13) is NOT part of the
claim — it is exactly the un-owned torsor datum (X4):

| reading | additionally owned | G_res | admissible labelings |
|---|---|---|---|
| R-strong | act/undo sign section per pair (§01.20 write/erase line read per-element) | **1** | 1 |
| R-mid | the recorded Q₈ table incl. Hamilton `ijk=−1` as a role fact | **Inn(Q₈) ≅ V₄** | 4 |
| R-weak | group structure up to iso only + typed pairs + basepoint | **(ℤ/2)³** | 8 |

In every reading the admissible labelings form exactly ONE G_res-orbit (a torsor): "forced up to
G_res" is exact, not approximate. The Out(Q₈) ≅ S₃ axis-naming is EXCLUDED from G_res in all
readings as pure naming (the roles B,C,D are typed distinct; per T2_PRIME_FINAL_STATE.md:63 the
axis assignment is "Out(Q₈) gauge" = passport-naming, not physics; trap (j): naming freedom ≠
freedom of physics).

**(X2) Impossibility branch (the M2-relevant negative).** No canonical cyclic (ℤ/9, μ₉, or ℤ/8, μ₈)
labeling of V₉ exists from owned data. Three independent walls, each computed:
(i) **type wall** — Ω₈ ≅ Q₈ is THEOREM-grade and Q₈ has element orders {1,2,4,4,4,4,4,4}: no
order-8 element, exponent 4; the owned group structure contains no cyclic order;
(ii) **relation wall** — the owned role structure is a typed PARTITION ("reading (A), read (B),
trace (C), reference (D)"), not a successor order; the only owned dynamics on Ω₈ is "continuous
circulation" whose emitted trace is explicitly **orbit-averaged over G₈** — the corpus itself
symmetrizes away any traversal order at emission;
(iii) **invariance wall** — under R-mid/R-weak, G_res has 2-element orbits, so NO bijective
per-vertex phase labeling is even statable canonically (a G_res-invariant function is constant on
orbits; a bijection cannot be). Quantified freedom: with ω₀ ↦ 1 pinned, μ₉-labelings fall into
40320 / 10080 / 5040 owned-equivalence classes (R-strong/mid/weak) with **no owned selector among
them** — versus exactly **1** class for the Q₈ ⊔ {∗} labeling in every reading. The residual is
named: **the cyclic-order selector** (equivalently: which of the 8! vertex orders is "the" 9-cycle)
is un-owned; the Feshbach real-form input ρ = cos(π/9) is built exactly on that un-owned datum
(the 9-cycle C₉; reproduced exactly in the script).

**(X3) Zones 11/13.** NOTHING owned labels vertices within V₁₁ or V₁₃. The capacity ladder
V₁₁ = V₉ ⊔ D₂, V₁₃ = V₉ ⊔ ABCD is **count-grade only** (its cert computes integer sums; §01.20:
"The scene sizes above fix only the *partition*"); the vacuum contract C1 forbids graph-side
privileged vertices; within-zone Aut-transitivity makes every graph-definable vertex functional
zone-constant (computed exactly); E₄-extension-invisibility (T2_PRIME_FINAL_STATE.md) blocks
extension-based keys, and the T2′-v2 kill is the precedent that per-vertex readings of the dyad
derive contradictions. **The forcing is zone-9-only.** This suffices for the tick (which fires on
zone-9-only carriers, verified below).

**(X4) Consumer split (computed).** All spectral tick consumers are invariant under BOTH G_res
(largest reading) and the full within-zone relabeling torsor S₉×S₁₁×S₁₃ — they are well-defined
on the labeling CLASS and never needed the graph-bridge resolved. Per-slot consumers
(M2 shell-membership) are NOT torsor-invariant (computed negative demo) — they need the bridge,
which C1 + transitivity forbid resolving internally.

## Owned pre-facts (verbatim, file:line)

1. **Typed roles, not a list and not an order** — BOOK_01:1530: "The four roles are not a list to
   be chosen — they are forced as the minimal self-sufficient act of record: reading (A), read
   (B), trace (C), reference (D). Fewer than four leaves the act without a distinguishable record,
   or requires an external memory to hold the missing role. That is the catalog M1 forbids."
2. **Sign bit semantics** — BOOK_01:1539: "The sign bit `{+,−}` is the irreversibility of
   write/erase; it is the one bit that distinguishes the act from its undo, and no further bit can
   be added without an exogenous orientation catalog."
3. **Basepoint** — BOOK_01:1541: "A reusable shell also requires a stationary marked witness
   section `ω0`." And BOOK_01:1993: "The finite event occurs when the circulation closes against
   \(\omega_0\); this is the terminal halt quotient."
4. **Q₈ identification (THEOREM)** — BOOK_01:782: "The signed terminal role space
   `\Omega_8=\{A,B,C,D\}\times\{+,-\}` is *strictly isomorphic* to the quaternion group
   `Q_8=\{\pm1,\pm i,\pm j,\pm k\}`" (§01.7.1A; forced via Hamiltonian + non-abelian + Dedekind;
   registry D0-Q8-DEDEKIND-MINIMALITY-001, CORE-FORMALIZED).
5. **Center = orientation bit (THEOREM)** — BOOK_01:820: "The `+/-` of `\Omega_8` *is* the memory
   of read/write order — orientation and order memory are not two facts, they are one."
   ([Q₈,Q₈] = Z(Q₈) = Φ(Q₈) = {±1}; D0-OMEGA8-CENTER-001, CORE-FORMALIZED.)
6. **Table facts read physically** — BOOK_01:824: "The quaternion relations `i^2=j^2=k^2=-1` are
   not inert. A squared half-turn lands on the central `-1`" (§01.7.1C; Hamilton relations
   `i²=j²=k²=ijk=−1` Lean-proved in D0.Foundation.FrobeniusDivision3D).
7. **No traversal order at emission** — BOOK_01:1996: "Because the trace gradient has traversed
   the full signed role cycle before halt, the emitted archive trace is an orbit-averaged shell
   emission over the group \(G_8\) of symmetries of \(\Omega_8\)." (Preceded at 1993 by
   "Continuous circulation inside \(\Omega_8\)".)
8. **C1 vacuum contract** — BOOK_01:1570: "**(C1) Exchangeability.** No permutation inside a part
   may change the physics, i.e. the adjacency matrix `A` is invariant under `S9 × S11 × S13`. A
   part-internal exception would name a privileged vertex — an exogenous parameter — violating M1."
9. **Partition-only sizes** — BOOK_01:1568: "The scene sizes above fix only the *partition*; they
   do not yet fix which edges exist." Ladder cert `vp_v1141_abcd_omega8_v9_phi_capacity.py` is
   integer arithmetic (`V11=V9+D2` as numbers); §01.20 marks it "VERIFIED (count)".
10. **Axis gauge correction** — T2_PRIME_FINAL_STATE.md:63: "'Forced role dictionary' retracted
    (axis = Out(Q₈) gauge)". And the structural theorem at :21-25: "**E₄ is extension-invisible.**
    ... no extension-based key can ever decide which zone carries E₄".
11. **Adjacent no-go (cross-ref, extended not duplicated)** — BOOK_04:1399:
    "`Aut(K(9,11,13)) = S₉×S₁₁×S₁₃` is transitive on the 1287 triangles (a single orbit), and the
    operational roles carry no intrinsic geometric attachment ... any rank-5 carrier is an
    arbitrary symmetry-breaking choice" (D0-DSIGMA-ROLE-CYCLE-CARRIER-CANONICAL-NOGO-001 — that row
    is about the DΣ 5-cycle on triangles; this memo's graph-side wall is the per-VERTEX analogue,
    computed independently).
12. **Tick-side inputs** — TICK_COUPLING_SCHUR_MEMO.md:38-46 (U₃ candidate, nilpotency index 12),
    :93-94 (S2: "mu_n-as-diagonal-operator is not a greppable owned object").

## The construction (every constant computed, exact)

**Owned-structure model.** V₉ = {ω₀} ⊔ ({A,B,C,D} × {+,−}) with pre-facts 1-6 as the relation set;
zones 11/13 carry the empty within-zone vocabulary (pre-facts 8-10). Q₈ realized as exact integer
pairs; all group computations table-exact (512 associativity triples checked).

**G_res enumeration (script S1-S2).** Aut(Q₈) enumerated = 24 (≅S₄); Inn(Q₈) = 4, all involutions
(≅V₄) — Hamiltonicity (every subgroup normal, the §01.7.1A M1 blade) is exactly why inner
automorphisms fix every axis setwise. Typed roles kill Out: axis-preserving automorphisms = Inn
exactly. Act-section-preserving automorphisms = {id}. Admissible labelings: 8 (R-weak) ⊃ 4 (R-mid,
= even sign sections, detected by the gauge-invariant triple product χ = act_B·act_C·act_D ∈ {±1}:
χ = −1 iff even iff Hamilton `ijk=−1` holds in role terms) ⊃ 1 (R-strong). Each set is ONE
G_res-orbit: torsor verified.

**The canonical function system (script S4).** PRE-OWNERSHIP (skeptic wound W1, accepted): the
E₀/E₃/E₄ system on ℂ[Q₈] is ALREADY CORE-FORMALIZED — **D0-Q8-TERMINAL-FOURIER-001**
(`09_LEAN_FORMALIZATION/D0/UnifiedFiniteCore/Q8Terminal.lean:24-79`: E₀, E₄, E₃ as explicit ℚ
idempotents, orthogonality, sum = I, ranks (1,4,3); registry row 514, "Forced finite fact from
Q8"). My pre-flight keyed "Q8" and MISSED the row literally named Q8-TERMINAL-FOURIER — error of
record; the Fourier system is CITED here, not minted. The genuinely NEW remainder of this section
is narrowed to: (i) the extension to the POINTED shell — ℂ^{V₉} = ℂω₀ ⊕ (E₀ ⊕ E₃ ⊕ E₄), dims
1+1+3+4; (ii) the std₉-refinement statement: this refines the graph-irreducible std₉ (8-dim under
S₉) into 1 ⊕ 3 ⊕ 4 — structure the bare graph cannot define (context:
D0-REPRESENTATION-RECONSTRUCTION-MAXIMALITY-NOGO-001's commutant is blind below zone resolution);
(iii) the reading-independence packaging: all four block projectors verified invariant under the
FULL (ℤ/2)³ flip group (a fortiori V₄), E₃ characters pointwise-invariant — the decomposition is
canonical under EVERY G_res reading; (iv) the carrier application below.

**Tick consumer audit (script S5-S6; frozen split P = range A (rank 3), Q = ker A (dim 30)).**

- Owned carriers U = diag(zone-9 values from the canonical system; identity on 11/13):
  χ_B, χ_C, χ_D (all readings) and the act/undo sign s (R-strong only). Every one is unitary,
  real, and FIRES: **‖PUQ‖²_F = 80/81 exactly** (from the ±1 pattern with |Σd| = 1:
  ‖PUQ‖²_F = (81−|Σd|²)/81). The Neumann ladder lives on an EXACT 1-dim invariant subspace with
  contraction ratio **−1/9** (terms decay by exactly 1/9): convergent, closed-form in ℚ; total
  z=1 delay weight (80/81)·(9/10) = **8/9**. Global ρ(QUQ) = 1, but the eigenvalue-1 directions
  are exactly orthogonal to the ladder support (untouched zones + within-class differences) — the
  ladder never sees them. All four owned carriers share the ONE fingerprint
  **{80/81, |ratio| = 1/9}** — the spectral tick data requires NO selection among them.
- The ω₀-extension bit is NAMED, not owned: χ(ω₀) := ±1 both give 80/81 and |ratio| = 1/9; the
  ratio SIGN flips (delay weight 8/9 vs 10/9). Residual freedom of the carrier construction: one
  bit, consequences quantified.
- The deepest-owned zone-9 unitary — the central flip L_z (the §01.7.1B ℤ₂ register acting by
  translation) — is a graph automorphism, hence **PUQ = 0: VACUOUS**. Owned-ness alone does not
  fire the tick; the firing grade is exactly the label-VALUE (diagonal) grade. This sharpens
  TICK memo (A): the equivariant-vacuity wall passes THROUGH the most-owned carrier.
- Comparators (un-owned): Φ₉ = diag(μ₉ on zone 9) fires (‖PUQ‖_F = 1) but its identity-padded
  ladder is NOT geometric (ratios fluctuate 0.85-2.49); zone-9-block complex compression
  ρ = 0.011988; the REAL form (symmetrized 9-cycle (C₉+C₉ᵀ)/2) gives ρ = cos(π/9) = 0.939692621
  EXACT — reproducing the Feshbach-track input and locating it precisely on the un-owned
  cyclic-order datum. U₃ = diag(μ₉;μ₁₁;μ₁₃): (QUQ)¹² = 0 and ‖PUQ‖_F = √3 reproduced.
- Torsor-invariance: fingerprint invariant under 20 random within-zone relabelings (S₉×S₁₁×S₁₃
  conjugation); per-slot diagonal values are NOT invariant (computed) — the M2-membership gap in
  one line.

**J1/frontier-lane consumer verdicts (TOTAL_EXTENSION_PRIMITIVE_MINIMALITY_PROOFS.md lanes).**
Served by a zone-9-only canonical labeling: PRIM-FINITE-SPECTRAL-TRIPLE-REP (lane G) — the
1+1+3+4 module with its canonical projectors is exactly a "finite representation module" the
graph alone cannot choose (it does NOT choose SM Weyl roles; D0-HYPERCHARGE-GRAPH-FLOW-OWNER-001's
four walls untouched); the tick/Feshbach carrier class (this memo). NOT served (each named):
PRIM-DIRAC-SCALE-SELECTION (needs a scale cocycle; ±1 labels carry no scale),
D0-CKM-FINITE-OVERLAP-INVARIANT-001 (needs cross-zone up/down basis carriers B_up/B_down;
zones 11/13 own nothing per-vertex, and D0-CKM-OVERLAP-UNDERDETERMINATION-NOGO-001's two
admissible completions are a value-freedom, not a label-freedom), PRIM-NONCOMMUTING-TRIPLE
(supplies candidate zone-9 unitaries but the Higgs triple needs owned Higgs content),
PRIM-BARYON-PHYSICAL-LABEL-TRANSFER (physical naming = passport), lanes A/B/C cosmology selectors
(untouched). Honest count: 2 of ~11 lanes materially fed, both rep/carrier-shaped.

**M2-membership application (the campaign's convergence question).** NOT fed, twice over:
(i) every owned label value is RATIONAL (characters are ±1-valued; Q₈ has exponent 4, so even the
2-dim irrep's traces are in {0, ±2}) — SHELL_RULE_R_LEDGER's M2 ("an owned mass-blind per-slot
ℚ(φ)\ℚ invariant") is not supplied; Galois-on-values stays empty; (ii) per-slot use ALSO needs the
graph-bridge (which vertex is which slot), which is exactly the non-torsor-invariant datum C1
forbids owning. Under R-mid/R-weak this memo CLOSES the "canonical per-vertex phase labeling"
route to M2 (no such labeling is statable); under R-strong the labeling exists but is
rational-valued. An M2 owner would need BOTH the R-strong adjudication AND an owned irrational
value map on Q₈ ⊔ {∗} — neither exists in the corpus.

## Verification

`m2_phase_labeling_check.py` — **50/50 PASS** (v2, post-skeptic). Exact where load-bearing:
integer Q₈ table; labeling counts by enumeration; the tick fingerprint now FRACTION-EXACT via the
closed form (‖PUQ‖²_F = (81−s²)/81 = 80/81 for all four owned carriers, ladder ratio (q−p)/9 =
−1/9 with signed Rayleigh confirmation, delay weight 8/9; ω₀-bit variant +1/9 and 10/9), floats
demoted to corroboration at 1e-9. Negative controls (each can fail the CONCLUSION):
CTRL_NO_ORDER8 (an order-8 element would refute the type wall); CTRL_ZONE11_NO_BASEPOINT (fails
if zones 11/13 secretly carry owned per-vertex structure); CTRL_REPEATED_PHASE_DESTROYS (U₃
boundary reproduced; finiteness-guarded); CTRL_GRES_ORDERS_FROM_SCRATCH (skeptic repair: the old
CTRL_GRES_ORDERS_EXACT tested a hardcoded dict against itself — replaced by an independent
8!-sweep over all 40320 Ω₈-permutations deriving the relabeling-group orders (8,4,1) from the
owned relations themselves; a claimed order 3/6/24 is refuted by enumeration, not assertion);
MU9_NO_INVARIANT_BIJECTION (skeptic repair: now EXERCISES the free-action argument — 7 nontrivial
relabelings x 500 random bijections, zero invariant bijections — instead of a dict-literal swap);
CTRL_FINGERPRINT_DISCRIMINATES (a 6/3 split gives 72/81 ≠ 80/81 — the 80/81 follows from the
owned 5/4 pattern, not the technique).

## Named risks & PRE-REGISTERED attack surface (strongest self-attacks first)

- **A1 (strongest): the Ω₈-cyclic-order ownership question.** The words "cycle" and "traversed the
  full signed role cycle" (BOOK_01:1996) could be read as an owned traversal ORDER. Counter-facts
  relied on: the circulation is "continuous" (:1993), the emission is G₈-orbit-averaged (:1996-99),
  and no text names a successor relation. PRE-REGISTERED fallback: even granting an owned 4-cycle
  order on role TYPES (A→B→C→D), the signed 8-lift is not determined — Q₈ has no 8-cycle, so a
  Möbius/double-cover traversal needs an un-owned lift rule; the impossibility would narrow, not
  close (recount the classes under the granted order before conceding anything).
- **A2: does basepoint + (hypothetical) order pin μ₉ or only ℤ/9-torsor structure?** Even with an
  order, μ₉ VALUES need an orientation and a "step = ·ζ₉" identification. The owned orientation
  datum runs AGAINST a static choice: det T = −1 = B, one tick REVERSES orientation
  (BOOK_06:387-399, THE 06.8.W). At best a ℤ/9-torsor with an orientation PAIR — not μ₉.
- **A3: the reading adjudication is OPEN.** This memo does not adjudicate R-strong/mid/weak; the
  headline must stay "G_res ∈ {1, V₄, (ℤ/2)³}". All delivered consumers were made invariant under
  the LARGEST freedom precisely so their conclusions are reading-independent. Attack surface: the
  claim that §01.20:1539's act/undo line pins a per-PAIR section (R-strong) vs only typing the bit
  globally; and whether Hamilton `ijk=−1` is owned as a ROLE fact (R-mid's χ = −1) or only as an
  abstract-ℍ fact (then R-weak). The χ triple product is the crisp adjudication hook.
- **A4: C1 strong reading.** "No permutation inside a part may change the physics" read STRONGLY
  would forbid detector-layer labels from having any physical consumer (the χ-carriers included).
  Counter-text: §01.7.1's "The graph begins only after this cycle is lifted" and C1's own argument
  is explicitly about which EDGES exist (:1568-1570). This is a genuine corpus-layer tension
  (graph-layer vs detector-layer); adjudication belongs to the closure contract. If the strong
  reading wins, X1 survives as pure combinatorics but the tick-consumer section demotes to
  candidate-carrier status (same status U₃ already has).
- **A5: the ω₀-extension bit** (named above): the carrier construction, not the labeling, carries
  one un-owned sign (delay weight 8/9 vs 10/9). Any downstream consumer of the SIGNED ratio must
  first own this bit.
- **A6: model-completeness of the owned-vocabulary sweep.** The zones-11/13-empty verdict is only
  as strong as the sweep (BOOK_01 §01.8/§01.20/§01.24, BOOK_06 torus/Sturmian sections, T2′ file
  set, capacity certs). A missed owned per-vertex datum for 11/13 reopens X3 (control
  CTRL_ZONE11_NO_BASEPOINT is the computational hook).

## What this does NOT show

- Does NOT adjudicate among the three G_res readings (A3) — no "G_res = V₄" headline is claimed.
- Does NOT supply M2's ℚ(φ)\ℚ per-slot invariant (values are rational in every reading) and does
  NOT make any particle-side membership claim — per-slot use needs the lane-G/R1 rep bridge AND
  the graph-bridge torsor, both un-owned (and the latter C1-forbidden).
- Does NOT select THE canonical tick carrier among {χ_B, χ_C, χ_D, s} (selection ≠ forcing) — it
  shows the spectral tick data does not DEPEND on that selection (fingerprint is class-canonical).
- Does NOT edit or demote any CERT-CLOSED row; extends (does not duplicate) the DSIGMA role-cycle
  no-go from triangle-carriers to per-vertex labels via an independent computation.
- Does NOT claim zone-11/13 labels are impossible for an EXTERNAL owner — only that no internal/
  owned datum exists (passport route stays open, named as such).

## Repair log (skeptic #1, verdict SURVIVES; all wounds accepted in full, no defense)

Skeptic upheld: the torsor forcing (X1), the cyclic impossibility (X2) with all three walls, the
1+1+3+4 module facts, zone-11/13 emptiness (X3), and the consumer split (X4); his independent
transitivity reimplementation CLOSED the count-only gap in my zone-11 control; the tick
fingerprint was re-verified by him in exact Fractions. His own attack on X3 through the V₁₁ dyad
DIED — strengthening X3 (the extension-key route re-fails exactly as T2′-v2 predicted).

Wounds and errors of record, all repaired above:
- **W1 (strongest finding) — pre-ownership.** The E₀/E₃/E₄ Fourier system on ℂ[Q₈] was already
  CORE-FORMALIZED as D0-Q8-TERMINAL-FOURIER-001 (Q8Terminal.lean:24-79, ranks (1,4,3)); my
  pre-flight keyed "Q8" and missed the row literally named Q8-TERMINAL-FOURIER. Error of record.
  Repair: cited as pre-owner; novelty NARROWED to {pointed-shell extension ℂω₀, std₉-refinement
  statement, reading-independence packaging, carrier application}.
- **W2a — over-scoped framing.** Title/headline said "labeling of K(9,11,13)"; the owned object is
  a labeling of the DETECTOR-LAYER pointed shell V₉ = Ω₈ ⊔ {ω₀}; graph-slot transport is exactly
  the un-owned torsor (my own X4). Repaired in title, status block, and X1.
- **W2b — false grep sentence.** "ALPHA_SEAM_NOGO_V2.md contains zero occurrences of μ" was FALSE
  as written (it contains seam moments μ₁/μ₂/μ₃ on lines 9, 25-26, 37, 48, 55, 82). The
  substantive point (no μ₉/phase-lattice minting) survives; sentence rewritten precisely. Error
  of record.
- **W3 — script self-tests.** CTRL_GRES_ORDERS_EXACT tested a hardcoded dict against itself
  (and the Verification section mis-described it); MU9_NO_INVARIANT_BIJECTION checked a
  dict-literal swap. Both replaced (independent 8!-sweep; exercised free action); dead `img` dict
  removed; fingerprint checks upgraded to Fraction-exact with signed Rayleigh confirmation.
  Script now 50/50 PASS.

A1 STRENGTHENED (skeptic's addition, adopted with credit): the anti-order reading of the
role/torus material is not just absence-of-text — BOOK_01:618 states the chain "the torus is
abelian, so order is not encoded ⇒ defect" (order-encoding expelled from abelian carriers), and
the T2′ v1 kill died on exactly that citation; both texts actively push AGAINST an owned
traversal order on Ω₈, raising the bar for any future A1 attack.
