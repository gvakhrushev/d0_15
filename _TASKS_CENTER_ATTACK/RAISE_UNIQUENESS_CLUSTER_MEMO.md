# MINIMALITY-RAISE FORGE — uniqueness / homogeneity cluster (candidates 2, 7, 8)

Date: 2026-07-06. Scope: **memo-only**. No registry / book / .lean edits; `053040` untouched; no commit.
Deliverables: this memo + `raise_uniqueness_check.py` (can-FAIL, mutation-tested; clean run 12/12 PASS,
mutation sweep 4/4 caught). Precedent template: `D0-FIBONACCI-ANYON-UNIQUENESS-001` (CERT-CLOSED),
Lean `D0.Geometry.FibonacciAnyonUniqueness`.

Goal: raise three standing no-gos from "no other / can't refine" to **positive extremality theorems**
(a homogeneity theorem, a two-branch-minimality theorem, a unique-partition theorem). Each is a finite
exact computation on already-owned structure — the same species as the minted precedent.

Inputs frozen at read time: `MINIMALITY_LENS_MAP.md` candidates 2, 7, 8; registry
`09_LEAN_FORMALIZATION/docs/CLAIM_TO_LEAN_MAP.csv` rows 349/350/395/460/516;
`BOOK_04:1399`; `BOOK_01:610,1130,1134`.

---

## Verbatim citations (verified on disk, read past ±10)

- **BOOK_04:1399** (RAISE 1) — *"the oriented automorphism group `Aut(K(9,11,13)) = S₉×S₁₁×S₁₃` is
  transitive on the 1287 triangles (a single orbit), and the operational roles carry no intrinsic
  geometric attachment, so the five roles cannot be canonically injected into distinct cycle
  orbit-classes — any rank-5 carrier is an arbitrary symmetry-breaking choice."*
- **Registry row 395** (RAISE 1) — *"acts TRANSITIVELY on the 9\*11\*13=1287 oriented triangles, so the
  primitive cycle class is a SINGLE orbit; … a canonical assignment would inject 5 roles into 1
  orbit-class — impossible (pigeonhole, Lean `Fintype.card_le_of_injective`)."* CLOSING[2026-07-06]:
  GENUINE-BOUNDARY-PROVEN, RHS cardinality 1, `Fin5 → {1}` theorem-level impossibility, skeptic NO-KILL.
- **BOOK_01:1134** (RAISE 2) — *"Over the degree-2 carrier `ℚ(φ)/ℚ` … the toral-time operator has
  exactly two eigen-branches `φ⁻¹,−φ`, so Fibonacci is the unique admissible nontrivial fusion
  bottleneck … The Ising-type carrier has three simple objects `{1,σ,ψ}`; `3>2` forces an extra
  independent branch label not generated by `p+p²=1` … so Ising is excluded (closed-negative NO-GO)."*
- **BOOK_01:1130** (RAISE 2, minimality of the degree) — *"The degree is exactly 2 because there are
  exactly two comparison kinds: a would-be third is a degree-3 term `p³=2p−1`, which reduces into
  `span{1,p}` … not an independent slot."* (BOOK_01:568–569 same: `p+p^k=1, k>2` is a hidden runtime,
  not a new branch.)
- **Registry row 516** (RAISE 3) — *"Return orders (4,3) source-forced (**unique order-12 cycle type of
  partitions of 7**)."* The row's own body already carries this uniqueness fact; the third-generation /
  generation-row half is explicitly held separate ("generation-row half stays Continent 3 with L4").

---

## Compute-first exact results (all reproduced in `raise_uniqueness_check.py`)

**RAISE 1.** `9 · 11 · 13 = 1287`. The product of transitive symmetric-group actions is transitive on
the product set (verified on the concrete finite model `S₂×S₃×S₄`: the orbit of the first tuple
exhausts all `2·3·4 = 24` tuples → single orbit). Hence the 1287 oriented triangles form **one**
Aut-orbit. A transitive action has exactly two invariant subsets ({} and the whole set) and its
orbit-partition has cardinality **1**; an Aut-equivariant `Fin 5 ↪ {orbit-classes}` is `Fin 5 ↪ Fin 1`,
impossible since `5 > 1`.

**RAISE 2.** Toral charpoly `x² + x − 1` has discriminant `5 ≠ 0` → exactly **2** distinct roots
(`φ⁻¹, −φ`). Ising has `|{1,σ,ψ}| = 3` simple objects. `3 > 2`; excess `= 1`.

**RAISE 3.** Enumerated all **15** integer-partition-types of 7 and their permutation orders (lcm of
parts):

| order | partition-types of 7 |
|------:|----------------------|
| **12** | **(4,3)  ← UNIQUE** |
| 10 | (5,2) |
| 7 | (7) |
| 6 | (6,1), (3,2,2), (3,2,1,1) |
| 5 | (5,1,1) |
| 4 | (4,2,1), (4,1,1,1) |
| 3 | (3,3,1), (3,1,1,1,1) |
| 2 | (2,2,2,1), (2,2,1,1,1), (2,1,1,1,1,1) |
| 1 | (1⁷) |

Exactly **one** partition-type of 7 has order 12, and it is **(4,3)**. (Control: order-12 partitions
of *8* give (4,3,1) — still unique but with a fixed point, so the (4,3) *no-fixed-point* uniqueness is
specific to n=7, consistent with the owned `shell_no_fixed_point`.)

---

## The three RAISES as positive theorems

### RAISE 1 — DSIGMA-ROLE-CYCLE → **HOMOGENEITY-MINIMALITY**
> **Theorem (homogeneity).** The 1287 oriented triangles of `K(9,11,13)` form a **single
> `Aut = S₉×S₁₁×S₁₃`-orbit** — an orbit-minimal (irreducible homogeneous) carrier: the only
> Aut-invariant subsets are `∅` and the whole set, so the carrier admits **no proper Aut-invariant
> refinement**. Consequently the standing `5↪1` canonical no-go is exactly the statement *the carrier
> is orbit-minimal (extremally symmetric), hence no canonical rank-5 representative can exist* —
> `Fin 5 ↪ Fin 1` is a theorem-level impossibility (`5 > 1`).

The no-go and the homogeneity theorem are the **same fact** read two ways: "you can't refine it" ⇔
"it is already extremally homogeneous." Mirror of `D0-Q8-DEDEKIND-MINIMALITY-001` (a "no other"
no-go minted as a positive minimality row).

### RAISE 2 — ISING-ANYON → **TWO-BRANCH-MINIMALITY**
> **Theorem (two-branch minimality).** The degree-2 relation `p + p² = 1` (equivalently the toral
> charpoly `x² + x − 1`, discriminant `5`, two distinct roots `φ⁻¹, −φ`) fixes **exactly two** branch
> labels — the **minimal-and-forced** branch count of the CORE degree-2 carrier. The Ising exclusion
> `3 > 2` is exactly *two is the minimal forced branch count, so Ising's third simple object cannot be
> generated internally.*

Direct mirror of the already-minted `D0-FIBONACCI-ANYON-UNIQUENESS-001` (same species, lowest risk;
same Lean owner file already CERT-CLOSED). "Forced-two" is fully owned (degree = 2, two distinct
roots). "Minimal-faithful" is the honest adjacent reading — a 1-branch carrier cannot carry a genuine
quadratic dichotomy — and is not over-stated beyond the minted 3>2 content.

### RAISE 3 — LEPTON 2<3 → **UNIQUE-PARTITION-EXTREMALITY** (adversarial, SPLIT)
> **Theorem (unique partition-type).** Among all 15 integer-partition-types of 7, **(4,3) is the
> unique** one of permutation-order 12. Hence the source-forced return order-12 cycle-type on the
> 7-point shell is **unique** (extremal), and the 2-orbit structure of the lepton carrier is forced.

**SPLIT / firewall (explicit).** This raises **only** the cycle-type *uniqueness*. It does **not**
touch the separate **third-generation existence** wall (the `2<3` generation-count no-go and the
electron-branch existence), which STAYS a no-go / external per verified memory
(`d0-15-repo-verification-state`: "3rd-generation NO-GO"). The two objects are disjoint: (4,3) has
**2 parts ⇒ 2 orbits**, so the uniqueness *reinforces* the 2-branch reading and provably **cannot**
manufacture a 3rd generation (generation count = orbit **count** = 2, not the part **sum** 7).

---

## INDEPENDENT SKEPTIC PASS (§05.8.R)

Probe A (RAISE 1 — does "orbit-minimal ⇒ no canonical representative" smuggle?). **No.** The premise is
the *computed* transitivity (orbit count = 1), not an assumed "roles have no attachment." Given a single
orbit, the orbit-partition has cardinality 1 and `Fin 5 ↪ Fin 1` is pure pigeonhole. The homogeneity is
the *cause*; the no-go is a corollary. Clean. **NO-KILL.**

Probe B (RAISE 2 — does "minimal-faithful=2" over-claim past the minted 3>2 row?). The **forced-two**
core (degree of `x²+x−1` is exactly 2; discriminant `5 ≠ 0` ⇒ 2 distinct roots) is fully owned and is a
faithful mirror of the minted row. The strict **"minimal-faithful"** adjective (no 1-branch carrier is
faithful) is *adjacent*, not load-bearing, and the memo does not mint it as new content beyond 3>2.
Scope honored. **NO-KILL** (with the framing kept at "forced-two = mirror," minimal-faithful noted as
reading).

Probe C (RAISE 3 — does the uniqueness bleed into the forbidden 3rd-generation derivation?). **No.**
(4,3) is a **cycle-type** of a permutation of `Fin 7`; the 3rd-generation wall is about **existence of a
third generation** (a separate object: 3-zone count + electron-branch existence). (4,3) has **2 parts ⇒
2 orbits ⇒ generation-count 2**, so it reinforces `2<3` and cannot yield a 3rd generation. The
adversarial misread "(4,3) → 4+3=7 states → hidden 3rd gen" is refuted: parts are orbit **sizes**, the
generation count is the orbit **count** (=2). The existence wall is **untouched** and stays external.
**NO-KILL, no bleed.**

Mutation testing (can-fail proof): `--mutate-all` flips one owned fact per raise
(`role_count→1`, `ising→2`, `order→10`, `n→8`); each makes the corresponding raise **FAIL**, and the
clean run passes 12/12. The script asserts real content.

---

## FINAL VERDICT

| # | raise | positive theorem | verdict | skeptic |
|---|-------|------------------|---------|---------|
| 1 | DSIGMA-ROLE-CYCLE → HOMOGENEITY-MINIMALITY | 1287 triangles = single Aut-orbit ⇒ orbit-minimal carrier; `5↪1` = homogeneity theorem | **RAISED-TO-THEOREM** | NO-KILL |
| 2 | ISING-ANYON → TWO-BRANCH-MINIMALITY | `p+p²=1` fixes exactly 2 branches = minimal forced count; Ising `3>2` excluded | **RAISED-TO-THEOREM** (mirror of minted precedent) | NO-KILL |
| 3 | LEPTON 2<3 → UNIQUE-PARTITION-EXTREMALITY | (4,3) is the unique order-12 partition-type of 7 (uniqueness half only; 3rd-gen wall untouched) | **RAISED-TO-THEOREM** (uniqueness half); 3rd-gen existence stays NO-GO/external | NO-KILL, no bleed |

**Count raised: 3 / 3** (all RAISED-TO-THEOREM). All three are memo-only proposals pending a registry
motion; each mirrors an already-minted minimality/uniqueness row and is backed by a verbatim on-disk
citation and a can-fail, mutation-tested cert. RAISE 3 is delivered under an explicit firewall: the
cycle-type uniqueness is raised, the third-generation existence no-go is preserved intact.
