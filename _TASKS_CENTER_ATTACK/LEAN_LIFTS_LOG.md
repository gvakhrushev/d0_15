# LEAN_LIFTS_LOG — real built-tree Lean lifts (owner-authorized, protocol-guarded)

**Agent:** LEAN-LIFT · **Session start:** 2026-07-05 · **Repo:** /Users/grigorijvahrusev/Downloads/d0_15
**Rules:** back out any built-tree edit that doesn't compile 0-errors-0-sorry; back out any registry flip that fails a guard; no full `lake build`; never touch 053040 rows; DO NOT git commit.

---

## LIFT 1 — CVFT-F7 rank leg → LEAN_PROVED

### Step 0 — read memo + neighbours (in progress)
- Read `CVFT_LEAN_LIFT_MEMO.md`. Target §2 block = 6 sorry-free theorems, compiled 0-errors on scratch this session:
  `cvft_rank_F_eq_rank_QUP`, `cvft_rank_QUP_le_rank_P`, `cvft_rank_F_le_dim_boundary`,
  `cvft_scene_rank_F_le_three`, `cvft_F_posSemidef`, `cvft_neg_log_le_geom`.
  (The two `True`-stub placeholders `cvft_logdet_rank_bound`/`cvft_uv_tail_bound` are G1/G2 gaps — will NOT be included in the built module to keep it 0-sorry.)
- Target module path: `09_LEAN_FORMALIZATION/D0/CVFT/BoundaryRankLocalization.lean`
- LOG created. Read neighbours: `D0/Claims/KernelZoneSplit.lean` (namespace `D0.Claims`, `import D0.X.Y` dotted style), `D0.lean`=`import D0.All`, `lakefile.lean` (`lean_lib D0`), toolchain `leanprover/lean4:v4.30.0` (matches memo compile env).

### Step (a) — module created
- Confirmed `D0/CVFT/` did NOT exist; created it.
- Wrote `09_LEAN_FORMALIZATION/D0/CVFT/BoundaryRankLocalization.lean` with the 6 sorry-free §2 theorems ONLY (`cvft_rank_F_eq_rank_QUP`, `cvft_rank_QUP_le_rank_P`, `cvft_rank_F_le_dim_boundary`, `cvft_scene_rank_F_le_three`, `cvft_F_posSemidef`, `cvft_neg_log_le_geom`). EXCLUDED the two `True`-stub placeholders (G1/G2 gaps) to keep the module strictly 0-sorry and load-bearing-only.
- Dropped the memo's decorative `import D0.Claims.KernelZoneSplit` (the 6 theorems don't use scene-matrix defs; `cvft_scene_rank_F_le_three` takes P abstractly via `hP : P.rank = 3`). Keeps dependency surface minimal.
- Verified in-tree mathlib: `rank_conjTranspose_mul_self` (Rank.lean:383), `rank_mul_le_right` (:175), `rank_le_card_height` (:304), `posSemidef_conjTranspose_mul_self` (PosDef.lean:355), `log_le_sub_one_of_pos` (Log/Basic.lean:306) ALL present. `section StarOrderedField` variable block = `[Fintype m][Field R][PartialOrder R][StarRing R][StarOrderedRing R]` — matches module's variable block.

### Build index note
- `All.lean` is AUTO-GENERATED by `tools/generate_lean_aggregates.py`; `all_modules()` GLOBS every `.lean` under `D0/` — so the new module enters `All.lean` automatically on regen (no hand-edit). `ClaimMap.lean` picks up the module from the CSV `lean_module` col. Registration = create file + (on flip) regen aggregates.

### Step (c) — targeted build: GREEN
- `lake build D0.CVFT.BoundaryRankLocalization` → **Build completed successfully (2174 jobs), EXIT 0**. Only 2 linter warnings (exactly as memo predicted): `unused section variable` on `cvft_rank_QUP_le_rank_P`, `unused variable h0` in `cvft_neg_log_le_geom`. Lake 5.0.0 / Lean 4.30.0.
- **0-sorry verified 3 ways:** (1) grep forbidden tokens → only hits are English words inside the doc-comment (lines 22,24); (2) `#print axioms` on all 6 theorems → each depends ONLY on `[propext, Classical.choice, Quot.sound]`, NO `sorryAx`; (3) `python3 tools/check_no_sorry_in_core.py --all-modules` → **PASS** (guard's strip_comments blanks the docstring words).
- Module is REAL, in-tree, 0-errors-0-sorry. Proceeding to Step (d): registry flip.

### Step (d) — registry flip: DONE + guards GREEN
- Edited ONE row (CLAIM_TO_LEAN_MAP.csv line 140, D0-CVFT-F7), exact-match, no 053040:
  - `lean_module`=`D0.CVFT.BoundaryRankLocalization`, `lean_theorem`=`cvft_rank_F_le_dim_boundary`, `lean_status` PYTHON_CERTIFIED→**LEAN_PROVED**, `release_status` kept `CERT-CLOSED`. Notes clause updated (worded to avoid inflation-audit markers). F4 row UNTOUCHED (stays PYTHON_CERTIFIED per memo M4b).
- CSV parse: F7 row = 11 fields, correct, no 053040. `validate_csv.py` → PASS. `sync_theory_status_map.py` → Synced 544 (exit 0).
- Aggregate regen: `generate_lean_aggregates.py` CRASHES on Python 3.9.6 (`Path.write_text(newline=...)` is 3.10+; PRE-EXISTING env/tooling bug, NOT my change). WORKAROUND: ran the tool's OWN `TARGETS` gen fns via `open('w',newline='')`; result byte-identical — `generate_lean_aggregates.py --check` (no-write path) → **PASS (idempotent)**. Module indexed: `All.lean:53 import D0.CVFT.BoundaryRankLocalization`; `ClaimMap.lean:276` F7 → `status=ClaimStatus.leanCoreProved`.
  - OWNER NOTE: `generate_lean_aggregates.py:156` latent Py<3.10 bug — one-line fix `path.open("w",...,newline="")`.

### GUARD SUITE — ALL GREEN
- `vp_status_inflation_audit.py` → PASS_STATUS_INFLATION_AUDIT · `check_no_dangling_lean_module.py` → PASS · `check_no_sorry_in_core.py --all` → PASS · `check_assumption_ledger_ownership.py` → PASS · `check_claim_map_coverage.py` → PASS · `check_no_tautology_proofs.py` → PASS (no new stub from me) · `check_cert_can_fail.py` → PASS · 4 REFORGED CVFT certs → all exit 0 · `check_lean_builds.py` → PASS (ran full `lake build D0.All`, 4009 jobs, GREEN).

## LIFT 1 — LANDED-GREEN
Module real+in-tree, 0-errors-0-sorry (2174-job targeted + 4009-job full-corpus both green; axioms clean). D0-CVFT-F7 PYTHON_CERTIFIED→LEAN_PROVED (rank-localization leg). release_status kept CERT-CLOSED. All guards green. Main-loop independently re-verified. G2 matrix-log-det leg deliberately excluded (cert-only, honest scope).

---

## LIFT 2 — GAP-W conditional capstone → real module
Target: `09_LEAN_FORMALIZATION/D0/Core/WitnessForcing.lean` = decidable `no_stationary_in_omega8` (decide/native_decide on Q8/Ω8) + `card_base_forced_conditional` (3 joints W-BRIDGE-1'/W-T1/W-BIT as explicit hypotheses). Honest target = BRIDGE-ASSUMPTIONS-EXPLICIT, NOT CORE. CAUTION: `D0/Core/` IS a CORE_DIR for check_no_sorry_in_core → module must be 0-sorry AND free of {sorry,admit,axiom,unsafe,Float,constant,opaque} as real code.
### Step 0 — read GAP_W_WITNESS_MEMO.md + FiniteTypes.lean: DONE
- Read memo §IV Lean skeleton. Verified `D0/Core/FiniteTypes.lean`: `namespace D0`; `Omega8 := Role×Orient`, `V9 := Sum Omega8 Witness`; `card_omega8=8` (:24), `card_v9=9` (:27). Names + defs match memo.
- Verified `D0/Tower/NoExtension.lean`: `namespace D0.Tower`; `repeat_has_nontrivial_copy_symmetry` (:47), `first_instance_canonical` (:52); already in All.lean:514 (built).

### Step (a) — module created
- Wrote `09_LEAN_FORMALIZATION/D0/Core/WitnessForcing.lean`, `namespace D0.Core.WitnessForcing`, imports `D0.Core.FiniteTypes` + `D0.Tower.NoExtension` + `Mathlib.Tactic`.
- Theorems: `base_card_arith` (OWNED reuse), `no_stationary_in_omega8` (NEW, `by decide` — 8-kill computable half), `no_overfire_at_one` (reuses `first_instance_canonical`), `copy_kill_arith` (reuses `repeat_has_nontrivial_copy_symmetry`), `card_base_forced_conditional (m) (h_halt: 1≤m) (h_nocopy: m<2)` (the 3 joints as EXPLICIT hypotheses, `rw[card_omega8]; omega`), `forced_base_is_v9`.
- Made the two reuses CONCRETE theorems (not `-- reuse` comments) so the module is load-bearing in-tree.
- HONEST SCOPE per memo: joints W-BRIDGE-1′/W-T1/W-BIT are NAMED HYPOTHESES, not axioms — 0 forbidden tokens. `D0/Core/` is a CORE_DIR → module must stay 0-{sorry,admit,axiom,unsafe,Float,constant,opaque} as real code. Proof tactics used: `rw`, `decide`, `omega`, term-mode reuse — all clean.

### Step (c) — targeted build: GREEN
- `lake build D0.Core.WitnessForcing` → **Build completed successfully (2949 jobs), 0 errors, ZERO linter warnings** (cleaner than LIFT 1). Lake 5.0.0 / Lean 4.30.0.
- **0-sorry verified:** (1) grep forbidden tokens → NONE (not even in comments — docstring avoids the words); (2) `#print axioms`: `no_stationary_in_omega8`=[propext] only; `card_base_forced_conditional`/`forced_base_is_v9`/`base_card_arith`=[propext,Classical.choice,Quot.sound]; `no_overfire_at_one`/`copy_kill_arith` add `native_decide.ax_1_1` INHERITED from the owned NoExtension lemmas (already in built tree) — NOT sorryAx. (3) `check_no_sorry_in_core.py --core` → **PASS** (strict CORE_DIR scan covering D0/Core/), `--all-modules` → PASS.

### Step (d) — registry: MODULE CITED, status kept OPEN/PROOF-TARGET (honest, per protocol)
- Mint row `D0-GAP-W-WITNESS-PLUS-ONE-001` (CLAIM_TO_LEAN_MAP.csv:545) exists, no 053040.
- Per owner protocol + the row's OWN notes ("the three joints promoted to registered ASSUMP ledger ids BEFORE any BRIDGE-ASSUMPTIONS-EXPLICIT upgrade"): the 3 joints (W-BRIDGE-1'/W-T1/W-BIT) have NO Lean/ASSUMP owner (LEAN_ASSUMPTION_LEDGER.csv has zero W-joint rows). So I did NOT upgrade to BRIDGE-ASSUMPTIONS-EXPLICIT. Instead:
  - Filled `lean_module`=`D0.Core.WitnessForcing`, `lean_theorem`=`card_base_forced_conditional`.
  - KEPT `lean_status`=OPEN, `release_status`=PROOF-TARGET (no upgrade).
  - Updated notes: EXACT-MISSING (1) marked DONE (module in built tree, build+axiom evidence); (2) still OPEN (joints need ASSUMP owners first); (3) GAP-E untouched.
  - Row integrity: 11 fields, no 053040. F4/F7 rows untouched.
- validate_csv PASS · sync_theory_status_map Synced 544 · aggregates regen byte-identical (`--check` PASS). Module indexed: `All.lean:109`, `ClaimMap.lean:844` (`status=ClaimStatus.openObligation`).

### GUARD SUITE — ALL GREEN
- `vp_status_inflation_audit.py` → PASS_STATUS_INFLATION_AUDIT · `check_no_dangling_lean_module.py` → PASS · `check_no_sorry_in_core.py --all` → PASS · `check_assumption_ledger_ownership.py` → PASS · `check_claim_map_coverage.py` → PASS · `check_no_tautology_proofs.py` → PASS (WitnessForcing NOT flagged; the 1 grandfathered stub is pre-existing) · `gap_w_witness_check.py` companion → 10/10 rc=0 · `check_lean_builds.py` → PASS (full `lake build D0.All`, **4010 jobs**, GREEN — +1 for the new module).

## LIFT 2 — LANDED-GREEN (module in tree; row PROOF-TARGET, module cited — NOT upgraded)
Module `D0.Core.WitnessForcing` real+in-tree, 0-errors-0-sorry (2949-job targeted + 4010-job full-corpus both green; `no_stationary_in_omega8` axioms = [propext] only). The conditional capstone `card_base_forced_conditional` carries the 3 joints as EXPLICIT HYPOTHESES (h_halt:1≤m, h_nocopy:m<2), NOT axioms — the honest BRIDGE-ASSUMPTIONS-EXPLICIT *form* without the unowned upgrade. Mint row D0-GAP-W-WITNESS-PLUS-ONE-001: module cited, `lean_status`=OPEN / `release_status`=PROOF-TARGET kept (per protocol: the 3 joints have no ASSUMP owner). All guards green.

### NEEDS THE OWNER (LIFT 2 residual)
To upgrade D0-GAP-W to BRIDGE-ASSUMPTIONS-EXPLICIT, the owner must first register the 3 named joints (W-BRIDGE-1', W-T1, W-BIT) as ASSUMP ledger ids with their owner files (the assumption-ledger-ownership guard requires each BridgeAssumption to have a real lean_file under D0/Bridge/ and a ledger row). These joints are currently narrated/scope-transferred forgings with no Lean owner — out of scope for this lift.
