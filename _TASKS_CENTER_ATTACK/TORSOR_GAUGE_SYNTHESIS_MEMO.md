# TORSOR-GAUGE SYNTHESIS — ORGANIZING LEMMA (demoted from "derived gauge principle" per skeptic #1; pre-registered demotion EXECUTED): within-zone labels are gauge-fixing data; internal physics lives in the invariant ring; the import ledger is FOUR-typed A / B(meas·adj·ext) / C / D (DRAFT v2.1, post-skeptic-#2)

**Status:** DRAFT candidate v2.1; **no registry row edited, no built `.lean` touched, no
`053040` row touched.** Skeptic #1 verdict: **WOUNDED-FIXABLE, accepted in full, no defense** —
including execution of the memo's own pre-registered demotion path (v1 risk R1): the claim
grade is **ORGANIZING LEMMA / reading layer over C1 + row 549 + M2**, not a "derived gauge
principle". Skeptic #2 (focused re-pass on the v2 load-bearing new text): **WOUNDED, NO KILL —
four-cell classification, script repairs, and CKM B-ext filing all SURVIVE**; both forced
repairs + wording fixes applied in v2.1 (§Verdict & errors of record, EoR-7..9). Companion
script: `torsor_gauge_check.py` v3 — **28/28 PASS**, exit 0 (three script errors of record
logged, all repaired). Pre-flight (v1, unchanged): no existing row owns "gauge principle"/"invariant ring";
adjacent owners cross-referenced, not duplicated: row 549
`D0-CANONICAL-WITHIN-ZONE-SELECTOR-M1-NOGO-001`, rows 543/544 (M2),
`D0-REPRESENTATION-RECONSTRUCTION-MAXIMALITY-NOGO-001` (R1),
`D0-LEPTON-BRANCH-SELECTOR-MAXIMALITY-NOGO-001` (R4) + E4 + L4,
`D0-CKM-OVERLAP-UNDERDETERMINATION-NOGO-001`, `D0-GEN-MASS-001`,
`D0-DSIGMA-ROLE-CYCLE-CARRIER-CANONICAL-NOGO-001`, `D0-Q8-TERMINAL-FOURIER-001`,
`D0-COSMO-TICK-GAUGE-EQUIV-001`, `D0-OPERATOR-GAUGE-CURVATURE-ORIGIN-001`,
`D0-GAUGE-MATTER-002`, `D0-HYPERCHARGE-GRAPH-FLOW-OWNER-001` — plus, new in v2, the
third-continent rows named in pre-fact 12.

**The lemma in one sentence.** The selector search discovered a *structure*, not just a wall:
the scene owns the **torsor** of within-zone labelings, not a point of it; C1's own
justification clause already prints the invariance direction, row 549 closes non-fixability;
what this memo adds is the **packaging** (torsor / reduction-of-structure-group /
no-canonical-section) and a **computable four-typed import ledger** — point-choices on owned
torsors (class C, gauge-fixing) are contentless; choices of un-owned ansatz structure (class D)
are NOT; invariant underdeterminations (class B: measurement / adjudication / extension) are
physical; the invariant ring (class A) is where internal theorems live.

---

## Claim X (DEF-0.2.2 candidate language, v2 — organizing-lemma grade)

Frozen scene K(9,11,13); frozen Feshbach split P = proj(range A) (rank 3), Q = proj(ker A)
(dim 30); detector-layer pointed shell V₉ = Ω₈ ⊔ {ω₀}, Ω₈ ≅ Q₈ (theorem).

**(X-L1) The gauge-shaped structure is exact (computed, script §T).**
- Fibers: within-zone labelings Bij(V₉,L₉) × Bij(V₁₁,L₁₁) × Bij(V₁₃,L₁₃).
- Structure group: G = Aut(K(9,11,13)) = S₉×S₁₁×S₁₃, acting **simply transitively** (T2) —
  a G-torsor; G is computed from the scene, not assumed (N2: K(2,2) grows a part swap,
  |Aut| = 8; K(2,3) does not, |Aut| = 12).
- Sections = catalogs; **row 549 = "no canonical global section"** (T3: 0 equivariant sections
  for n = 2,3,4 enumerated; 1 for the n = 1 control; free action at 9/11/13) — structurally a
  gauge-fixing impossibility statement.
- Reduction of structure group on zone 9 (T4a–c), reading-conditionality **explicit**
  (repair 5):

  | rung | licensed by | group | order | grade |
  |---|---|---|---|---|
  | full zone-9 torsor | C1 | S₉ | 362880 | **owned** |
  | marked witness ω₀ | BOOK_01:1541 | Stab(ω₀) ≅ S₈ | 40320 | **owned** |
  | typed axes + group structure | BOOK_01:782, :1530 | (ℤ/2)³ | 8 | **reading-conditional** (R-weak) |
  | Hamilton `ijk=−1` as role fact | BOOK_01:824 | V₄ | 4 | **reading-conditional** (R-mid) |
  | act/undo sign section | BOOK_01:1539 | 1 | 1 | **reading-conditional** (R-strong) |

  The rungs below S₈ are **candidate** rungs: which one is G_res depends on the un-adjudicated
  3-way reading (M2 memo A3, OPEN). The script computes all three candidates and their subgroup
  chain; T4c shows the R-mid candidate = the axis-preserving flip automorphisms (Inn action),
  i.e. the Hamilton role fact and the group law select the same V₄.
- The v1 flat-connection remark on the scalar tick (BOOK_06:489) is retained ONLY as flagged
  narration (risk R4); strike it and nothing else moves.

**(X-L2) The lemma statement, with corrected credit (repair 3).** The invariance direction
"M1 ⇒ within-zone permutation-invariance" is **not this memo's derivation — it is C1's own
justification clause**, printed at BOOK_01:1570: "A part-internal exception would name a
privileged vertex — an exogenous parameter — violating M1." Row 549 adds the **non-fixability
closure**: no M1-legal internal rule selects a point of the torsor. Grade split (repair 3):
theorem-grade **only on the equivariant class** (0 sections, decidable); elsewhere the closure
is relocation-**evidence** over the **inspected** evaders — SELECTOR_MECHANISM_REPORT.md **§3,
line 46** (pointer corrected from v1's "§4"): "every arm that could select must import a
non-G-invariant datum, which is definitionally the external catalog", carried with that
report's own §4 scoping ("FORCED-EXTERNAL-BY-RELOCATION-TEST, *not* ... a universal
impossibility theorem"). The organizing lemma binds these owned facts into one reading:
**within-zone labels are gauge-fixing data; internal physics is valued in the invariant ring.**
Nothing new is forced by this memo.

**(X-L3) The FOUR-typed import classification (repairs 1 + 2 — the load-bearing v2 content).**
Domain restriction (repair 1): the classification applies to (i) functionals of the frozen data
and (ii) declared imports, **each with its acting group declared** (full torsor S₉×S₁₁×S₁₃ /
candidate G_res / S₇-conjugation on the shell-torus / common-basis rotations). Cells:

- **class A — invariant under the declared group AND determined by frozen data** ⇒ internal
  theorems/predictions.
- **class B — invariant but underdetermined**, sub-typed (repair 2): **B-meas** (an
  experimental number: masses, coefficients), **B-adj** (an open *internal* adjudication,
  resolved by a decision not a datum: the G_res reading, the moment convention, the
  ω₀-extension bit, the staticity clause), **B-ext** (a corpus-typed **NEW primitive /
  extension**: the CKM completion — its own row says "a canonical selector
  (symmetry-breaking/modulus-fixing) is a NEW primitive (extension), not a present-core
  theorem").
- **class C — point-choice on an OWNED torsor** ⇒ gauge-fixing. The "cannot be wrong" license
  applies to THIS CELL ONLY (repair 6): a class-C choice adds **no new invariant** (computed:
  every class-A value is identical at every torsor point — A4d, C1).
- **class D — choice of UN-OWNED ansatz/reduction structure** (repair 1 — the skeptic's
  strongest finding). Witness: the cyclic order behind ρ = cos(π/9). Its value is
  **family-canonical** (identical across the whole ansatz class — script D1) yet **conditional
  on structure the scene does not own** (row 544 + M2 memo:66-68). A class-D import DOES
  inject a new determined-looking invariant and **can be wrong**; v1 filed it under class C —
  that was the v1 soundness gap. Computable D-vs-C test (script D1): does the choice generate a
  new family-canonical invariant (D) or none (C)?

D-test clarification (skeptic #2 repair 4): **"new" means new relative to class A** (a value
the frozen-data invariant ring does not already determine); **"family" means the ansatz class**
(the value is constant across points WITHIN the class and compared ACROSS classes — D1 does
both comparisons: 9-cycles agree at cos(π/9), the K₉ class sits at 3.5).

The B/C/D boundaries are computed, not asserted: C4 (invariant changes ⇒ not gauge ⇒ **B-ext**,
crediting the CKM row's Lean-proved 16/25 vs 144/169), D1 (family-canonical + ansatz-dependent
⇒ D), C1/C2/C3/C5 (no invariant change ⇒ C).

**(X-L4) The wall map, re-scoped (repair 4): three continents; only freedom-walls covered.**
The v1 word "retrodiction" is **retracted**. The lemma organizes **freedom-shaped walls only**
(blockers that are un-fixed choices). The skeptic's sweep exposed a **third continent** the v1
table omitted — **existence/insufficiency walls** (missing or non-existent objects,
insufficient mechanisms — not freedoms). Wired in as a **gain**: the wall map now has three
named continents (§Alignment v2), and the lemma is explicitly **silent** on the third.

**Fireable falsifiers (replacing v1's unfireable one; repair 4):**
1. an owned functional separating within-zone vertices (row 549's reopening hook (e)) — breaks
   the C-cell;
2. a freedom-shaped matter wall that the computable filing procedure (declare group → test
   invariance → test determinacy → test family-canonicity) cannot place in exactly one of
   B/C/D — breaks exhaustiveness (skeptic #2 note: the borderline-hunting escape hatch of this
   falsifier is closed by the explicit continental border criterion, §Alignment v2);
3. an internal derivation of any class-C datum — breaks 549/C1 and the lemma with them;
4. a class-C choice exhibited to change a class-A value — breaks the C-license. Scope (skeptic
   #2 repair 3): this falsifier is live on **reading-conditional declared groups** (candidate
   G_res rows, where the acting group itself could be mis-declared); on full-torsor rows it
   reduces to a C1-consistency check (A4d/C1 catch it mechanically).

---

## Owned pre-facts (verbatim, file:line; v2 corrections marked)

1. **M1** — BOOK_00:143: "**M1** — no exogenous parameters: no background dependence, no
   external catalog."
2. **Forcing schema** — BOOK_00:396: "'**forcing**' in this corpus is shorthand for '**reductio
   against M1**'".
3. **C1 — including its own invariance-direction clause (repair 3)** — BOOK_01:1570: "**(C1)
   Exchangeability.** No permutation inside a part may change the physics, i.e. the adjacency
   matrix `A` is invariant under `S9 × S11 × S13`. **A part-internal exception would name a
   privileged vertex — an exogenous parameter — violating M1.**" Strengthened BOOK_01:1576:
   "From **(C1)**, `A` is constant on the orbits of `S9 × S11 × S13`."
4. **Row 549** (non-fixability) + the relocation sentence at **SELECTOR_MECHANISM_REPORT.md §3,
   line 46** (pointer corrected; the §4 "not a universal impossibility theorem" scoping carried).
5. **The torsor owner** — M2_PHASE_LABELING_MEMO.md X1/X4 (row 543); the cyclic-order
   impossibility row 544 — in v2 also the **class-D witness owner**.
6. **Reduction data** — BOOK_01:782 (Ω₈ "*strictly isomorphic*" to Q₈); :1541 (ω₀ marked
   witness); :824 (Hamilton relations "not inert"); :1539 (act/undo sign bit) — binding grade
   per rung as in the X-L1 table.
7. **"Gauge" is owned corpus vocabulary** — T2_PRIME_FINAL_STATE.md:63: "'Forced role
   dictionary' retracted (**axis = Out(Q₈) gauge**)"; row `D0-COSMO-TICK-GAUGE-EQUIV-001`
   (Lean `D0.Geometry.ArchiveTickGaugeFixing`, "Tick gauge equivalence holds by parameter
   absorption").
8. **Scalar tick** — BOOK_06:489: "Each archive-delay tick multiplies the active amplitude by
   `φ⁻¹` ... hence `A_{k+1}=φ⁻¹A_k`" (flagged narration only).
9. **Rule-R names the torsor as its blocker** — RULE_R_MEMBERSHIP_MEMO.md:14 ("REQUIRES the
   C1-forbidden graph-slot attachment"), :47 ("the full within-zone relabeling torsor
   S₉×S₁₁×S₁₃ ... Per-slot consumers (M2 shell-membership)").
10. **The policing layer** — BOOK_05:472 ("A number that *fits* is not a number that is
    *forced*"); shortcut rows BOOK_05:452 ("CKM basis label => physical CKM angles and CP
    phase"), :463 ("PDG names used to choose spin/flavour projectors"); §05.8.U calibration
    BOOK_05:524 (`W` stays "a physical input, **not** core").
11. **NEW (v2): the B-ext type is corpus-typed** — `D0-CKM-OVERLAP-UNDERDETERMINATION-NOGO-001`
    notes: "a canonical selector (symmetry-breaking/modulus-fixing) is a **NEW primitive
    (extension)**, not a present-core theorem."
12. **NEW (v2): third-continent owners** (existence/insufficiency walls, verbatim anchors):
    `D0-COLOUR-GENERATION-TYPED-CARRIER-NOGO-001` ("dim Commutant(weak)=dim R(Q8)=**8<9**=dim
    M3(C), so a source-native colour M3 does not fit");
    `D0-HIGGS-CONDENSATION-PRESENT-CORE-MAXIMALITY-NOGO-001` ("every present-core frozen
    projector is a polynomial a*1+b*T in the return operator T ... a new independently-forced
    (U,Q0,Pi_H) is required (extension)") + `D0-HIGGS-PHASON-ORBIT-TRIVIAL-NOGO-001`;
    `D0-BRANCH-CP-READOUT-NOGO-V15` ("same marginals, different coherence. Missing
    PRIM-BRANCH-ISOTROPIC-READOUT") — **re-filed to Continent 2 / B-ext in v2.1** per the
    border criterion (its alternatives DIFFER on invariant content);
    `D0-CKM-CLASS5-PARITY-EXCLUSION-001` ("orientation PARITY alone cannot exclude CKM winding
    class 5 ... excluding 5 would also exclude the identity class");
    `D0-LEPTON-PUISEUX-UNIQUENESS-OBSTRUCTION-001` ("ramification index 1/n does NOT uniquely
    determine the operator").

---

## The computation (torsor_gauge_check.py v3, 28/28; exact unless FLOAT-CORROBORATION)

**Class A — invariant ring** (acting group: full torsor S₉×S₁₁×S₁₃ unless noted; each value
computed from the frozen data — plus, for A4, the DECLARED owned-carrier class import (the M2
χ-carriers; skeptic #2 repair 1) — recomputed after 20 random relabelings, compared exactly):

| # | quantity | exact value | check |
|---|---|---|---|
| A1 | rank/nullity; quotient charpoly | 3/30; **x³ − 359x − 2574** (coefficients = owned counts: 359 cross edges, 2574 = 2·1287 triangles) | A1 |
| A2 | counts | 33 / 359 / 1287, invariant | A2 |
| A3 | trivial-isotype multiplicity | 3 (= generations = rank A) | A3 |
| A4 | tick fingerprint (χ_B, χ_C, χ_D, s) | ‖PUQ‖²_F = **80/81** and ratio MAGNITUDE **1/9**: class A. Ratio **sign** (−1/9) and delay weight (8/9): **A conditional on B-adj(ω₀-bit)** — A4c itself computes the flip to {+1/9, 10/9} under the open bit (skeptic #2 repair 1). ρ(QUQ) = 1 with a **single computed witness** direction exactly orthogonal to the ladder columns (dead `*0` line replaced — EoR-S3); all invariant over 20 random g | A4a–d |
| A5 | power traces | Tr A² = 718, Tr A³ = 7722, Tr A⁴ = 257762, invariant | A5 |
| A6 | invariance lemma | 10 random words in {A, A², P}: trace + Frobenius invariant exactly | A6 |
| A7 | seam-moment arithmetic | μ₂ = 12288/5 = 2¹²·ρ, ρ = 3/5 (identity only; values owned by the α certs) | A7 |
| A8 | lepton skeleton (group: S₇-conjugation) | det(I−zU) = (1−z⁴)(1−z³); (4,3) the unique order-12 cycle type among the 15 partitions of 7 | A8 |
| X | U₃ comparator | (QU₃Q)¹² = 0, ‖PU₃Q‖_F = √3, invariance preserved [FLOAT-CORROBORATION] | X |

**Class C — owned-torsor point-choices (gauge-fixing; no invariant changes):**

| # | quantity | acting group | computed fact | check |
|---|---|---|---|---|
| C1 | per-slot carrier value / membership | full torsor | slot value flips +1 → −1; support set moves; no class-A value moves | C1 |
| C2 | E₀/E₃/E₄ per-slot realization | full torsor vs candidate G_res | ranks (1,3,4); invariant under all 8 candidate-G_res flips, moved by 10/10 generic S₈ — the (reading-conditional) reduction buys the refinement; **class flips with the open reading** (declared per repair 1) | C2 |
| C3 | branch→generation assignment | S₇-conjugation | σA ~ σB conjugate (explicit h), same order 12 / same resolvent, different maps | C3 |
| C5 | per-slot hypercharge attachment | slot permutations | ΣY = ΣY³ = 0 invariant; slot row moves (mirrors LEAN_PROVED sums `D0-GAUGE-MATTER-002` vs PROOF-TARGET attachment `D0-HYPERCHARGE-GRAPH-FLOW-OWNER-001`) | C5 |

**Class D — un-owned-ansatz import (v2 cell; NOT contentless):**

| # | quantity | computed facts | check |
|---|---|---|---|
| D1 | the cyclic order behind ρ = cos(π/9) | x³−3x−1 divides char(P₈) **exactly**; value family-canonical across all 9-cycles [FLOAT-CORROBORATION]; ansatz-dependent (K₉ pattern → 3.5). Family-canonical + un-owned ⇒ class D: the import injects a determined-looking invariant conditional on structure row 544 forbids owning | D1 |

**Class B — invariant underdeterminations:**

| # | quantity | sub-type | computed / row fact | check |
|---|---|---|---|---|
| C4 | CKM completion | **B-ext** (row-typed "NEW primitive (extension)") | 16/25 vs 144/169, both exactly orthogonal — **reproduces the CKM row's Lean-proved content** (credit corrected, repair 2); common-rotation half = R3-extension illustration only | C4 |
| — | masses; lepton coefficient | B-meas | rows 37 / 306 (owners) | — |
| — | G_res reading; moment convention; ω₀-bit; staticity clause | B-adj | M2 A3; α-seam convention note; A4c; TICK S1' (owners) | — |

**Negative controls (each can fail the CONCLUSION):** N1 — C1 broken ⇒ a selector EXISTS
(contingency on C1); N2 — gauge group computed, not assumed; N3 — 6/3 split ⇒ 72/81 ≠ 80/81;
N4 — a fixed external catalog breaks the invariance lemma (trace 31→19, ‖·‖² 464/81→488/81).

---

## Alignment v2 — the wall map in THREE continents (repair 4)

**Continental border criterion (explicit per skeptic #2 repair 2).** For a wall's blocking
slot, compute the admissible set under the declared group:
- **empty** admissible set ⇒ **existence wall** (Continent 3);
- **≥ 2 alternatives AGREEING on all invariant content** ⇒ **insufficiency wall** (Continent 3:
  the invariant data cannot separate them, and no choice among them carries content — e.g.
  LEPTON-PUISEUX: both companion matrices share charpoly x⁴−λ);
- **≥ 2 alternatives DIFFERING on invariant content** ⇒ **freedom wall** (Continents 1/2, files
  B/C/D — e.g. CKM-OVERLAP: 16/25 ≠ 144/169).

Applying the criterion re-filed one v2 row (skeptic #2's forced re-file):
**D0-BRANCH-CP-READOUT-NOGO-V15** — its own text reads "same marginals, **different
coherence**. Missing PRIM-BRANCH-ISOTROPIC-READOUT": ≥ 2 admissible readouts differing on
invariant content (coherence), with the resolution typed as a missing primitive ⇒ **freedom
wall, class B-ext** (Continent 2, parallel to CKM-OVERLAP) — moved out of Continent 3.

**Continent 1 — freedom walls filed class C (gauge-fixing; lemma covers):**

| row | freedom | acting group |
|---|---|---|
| 549 SELECTOR-M1-NOGO | the torsor point | full torsor — **is** the lemma |
| 543 M2 labeling (graph-slot transport leg) | slot attachment | full torsor |
| DSIGMA-ROLE-CYCLE | triangle-orbit point | Aut on 1287 triangles |
| R4/E4 lepton branch | block→generation assignment | S₇-conjugation (computed C3) |
| HYPERCHARGE-FLOW (attachment leg) | slot row | slot permutations (computed C5; sums already class A) |
| PDG/baryon naming; Out(Q₈) axis naming | physical names | passport naming (owned vocabulary) |
| R1 GL(3) basis | isotype basis | commutant — **R3-flagged extension, un-minted** |

**Continent 2 — freedom walls filed class B or D (physical/conditional; the lemma covers the
filing, not the resolution):**

| row / item | class |
|---|---|
| CKM-OVERLAP-UNDERDETERMINATION | **B-ext** (row-typed extension) |
| BRANCH-CP-READOUT-V15 | **B-ext** (re-filed from Continent 3 in v2.1 per the border criterion: "same marginals, different coherence" = invariant-content difference; "Missing PRIM-BRANCH-ISOTROPIC-READOUT" = extension typing) |
| GEN-MASS-001; row 306 lepton coefficient | **B-meas** |
| G_res reading; moment convention; ω₀-bit; staticity clause | **B-adj** |
| 544 cyclic order (consumer: ρ = cos(π/9), Feshbach real-form input) | **D** (re-filed in v2; v1's central mis-filing) |

**Continent 3 — existence/insufficiency walls (NEW in v2 — the skeptic's finding, wired in as
a gain; the lemma is SILENT here — these are not freedoms):**

| row | blocker |
|---|---|
| COLOUR-GENERATION-TYPED-CARRIER | commutant gap **8 < 9**: colour M₃(ℂ) does not fit — empty admissible set (missing carrier) |
| HIGGS-PHASON-ORBIT-TRIVIAL + CONDENSATION-MAXIMALITY | no non-commuting present-core projector exists — empty admissible set (PRIM-NONCOMMUTING-TRIPLE) |
| CKM-CLASS5-PARITY-EXCLUSION | mechanism insufficiency (parity selector degenerate: alternatives agree, sel(5)=sel(1)) |
| LEPTON-PUISEUX-UNIQUENESS | insufficiency: two companion matrices AGREE on the invariant (charpoly x⁴−λ) — the border criterion keeps this row on Continent 3, against the freedom-shaped first impression |
| L4 BRANCH-FIXING (counting half) | 2 branches < 3 generations — empty admissible set for the third branch (assignment half stays Continent 1) |

**Scoped claim (v2):** freedom-shaped matter walls file into exactly one of C / B / D by the
computable procedure; existence/insufficiency walls are a disjoint third continent the lemma
does not touch. Falsifiers: §X-L4 (1)–(4), each fireable.

---

## Ownership audit (v2)

**Owned:** the torsor + candidate reductions (543/544, M2); non-fixability (549); the
class-function wall (SSB memo Y1/Y2); **the invariance direction itself — C1's own
justification clause (credit reassigned in v2)**; gauge-as-naming vocabulary (T2':63,
tick-gauge rows); the CKM two-completion Lean content and its "NEW primitive (extension)"
typing; the third-continent rows; C1 / M1 / §05.8 policing.

**New coinage (all candidate, reading-layer):** the organizing-lemma packaging
(torsor / reduction / no-canonical-section); the **A / B(meas·adj·ext) / C / D import typing**
and its computable filing procedure; the three-continent wall map; the commutant-layer
extension (R3 — most speculative, un-minted).

---

## Consequence for the ledger (v2, license restricted per repair 6)

> Proposed (owner decision, not executed): type matter-sector imports four ways.
> **Gauge-fixing data** (class C — point-choices on OWNED torsors: within-zone labelings, slot
> attachments, branch→generation assignments, physical names) are contentless and cannot be
> wrong — **this license applies only where the torsor itself is owned**. **Ansatz imports**
> (class D — e.g. a cyclic order on V₉) are structural hypotheses: they inject family-canonical
> invariants conditionally and CAN be wrong. **Adjudications** (B-adj) cost a decision;
> **measurements and extensions** (B-meas, B-ext) cost physical content — D0's predictivity
> budget is counted there. Concretely: `ASSUMP-WITHINZONE-LABELING-EXTERNAL` (draft assumption,
> SELECTOR_MECHANISM_REPORT.md §5) is correctly typed **gauge-fixing (class C)** under the
> repaired definition — it imports a point of the owned torsor, not new structure.
> `ASSUMP-DIXMIER-TRACE` stays class B.

---

## Verification

`torsor_gauge_check.py` v3 — **28/28 PASS**, exit 0. Exact (Fraction/int) where load-bearing;
floats confined to the U₃ comparator and the D1 corroboration, labelled. Transported and
untransported values computed independently (trap (f)); the closed form (81−s²)/81 is checked
against the matrix computation, not used to produce it.

**Script errors of record (all repaired):**
- **EoR-S1 (first run, now-D1):** unshifted power iteration on the bipartite path (±λ pairs)
  oscillated; fixed by a +I shift.
- **EoR-S2 (first run, N4):** the control conjugated A by g ∈ G — an identity move (gAg⁻¹ = A
  *is* C1) — so it could never fire; replaced by carrier-transport with the external catalog
  held fixed.
- **EoR-S3 (skeptic #1, repair 5):** A4b contained a content-free `... * 0` "orthogonality"
  line (always true — quarantine-class fragment). Replaced by the realized computation
  (vᵀ·QUP = vᵀ·QUQ·QUP = 0 exactly); check renamed to single-witness wording.

---

## VERDICT & ERRORS OF RECORD (skeptic #1 — accepted in full, no defense)

**Verdict: WOUNDED-FIXABLE; the memo's own pre-registered demotion path executed** — "derived
gauge principle" → **organizing lemma / reading layer** over C1 + row 549 + M2.

1. **EoR-1 (strongest finding — the X-G3 soundness gap):** v1's class C conflated point-choice
   on an OWNED torsor with orbit-choice of UN-OWNED structure. Witness: ρ = cos(π/9) (v1's own
   C6) is family-canonical, so v1's class-C license ("cannot be wrong") would have promoted it
   to a determined internal invariant — exactly what row 544 + M2:66-68 forbid. **Repair:**
   fourth cell (class D) + domain restriction + per-row acting-group declaration; C6 re-filed
   as D1; alignment table re-tallied.
2. **EoR-2:** class B was undifferentiated; the CKM completion was mis-filed as "measurement"
   when its own row types it "NEW primitive (extension)"; the ω₀-bit is an adjudication gap,
   not an experimental number. **Repair:** B-meas / B-adj / B-ext split before any
   predictivity claim; C4 re-credited to the CKM row's Lean-proved content; C4's "gauge half"
   re-scoped to an R3-extension illustration.
3. **EoR-3:** credit misassignment — v1 presented "M1 ⇒ invariance" as the memo's derived
   direction; it is printed inside C1 itself (BOOK_01:1570, second sentence). **Repair:** X-L2
   restated as C1's-clause + 549's-closure; theorem/evidence grade split restored ("inspected"
   evaders); pointer §4 → §3:46 fixed.
4. **EoR-4:** "retrodiction" overclaimed; the falsifier was unfireable; the v1 table omitted
   the existence/insufficiency walls. **Repair:** re-scoped to freedom-shaped walls; third
   continent added explicitly (a gain); four fireable falsifiers installed.
5. **EoR-5:** the T4 reduction chain presented reading-conditional rungs as owned. **Repair:**
   rungs below S₈ marked candidate/reading-conditional everywhere (memo + script).
6. **EoR-6:** the dead `*0` script line and A4b's overstated wording. **Repair:** realized
   computation + single-witness wording (EoR-S3).

**Skeptic #2 (focused re-pass on the v2 load-bearing new text only): WOUNDED, NO KILL —
four-cell classification SURVIVES (all three counterexample constructions failed), script
repairs SURVIVE, CKM B-ext filing SURVIVES.** Two forced repairs + wording fixes, all applied
in v2.1, no defense:

7. **EoR-7 (skeptic #2 repair 1 — A4 conflation):** v2 filed the ladder ratio-sign (−1/9) and
   delay weight (8/9) as unconditional class A while its own A4c computes their flip to
   {+1/9, 10/9} under the open ω₀-bit. **Repair:** 80/81 and the ratio magnitude 1/9 stay
   class A; sign and weight re-marked "A conditional on B-adj(ω₀-bit)"; class-A table header
   now declares the χ-carrier import.
8. **EoR-8 (skeptic #2 repair 2 — continental border underspecified):** Continent 3 membership
   was asserted row-by-row without a criterion, and `D0-BRANCH-CP-READOUT-NOGO-V15` was
   mis-filed there: its own text ("same marginals, **different** coherence" + "Missing
   PRIM-BRANCH-ISOTROPIC-READOUT") shows ≥ 2 alternatives DIFFERING on invariant content with
   an extension-typed resolution. **Repair:** explicit border criterion installed (empty set ⇒
   existence; alternatives agreeing on invariant content ⇒ insufficiency; differing ⇒ freedom,
   files B/C/D); BRANCH-CP re-filed to Continent 2 as B-ext; LEPTON-PUISEUX and CKM-CLASS5
   re-checked and CONFIRMED on Continent 3 under the criterion (their alternatives agree).
9. **Wording (skeptic #2 repairs 3-4):** falsifier 4 scoped to reading-conditional declared
   groups (on full-torsor rows it is a C1-consistency check); falsifier 2's escape hatch noted
   closed by the border criterion; the one-line D-test clarification added ("new = new relative
   to class A; family = the ansatz class, compared across classes").

---

## Named risks & attack surface (v2)

- **R1' (new strongest): the D-cell boundary.** Filing D vs C requires an *ownership*
  adjudication of the imposed structure (grep-grade, fallible), and family-canonicity is
  tested per ansatz class — a wrong ownership call mis-files the import. Pre-registered
  discipline: every D-filing must cite the owner row declaring the structure un-owned (here:
  row 544).
- **R2' (carried):** the freedom-walls sweep may still miss rows; falsifier (2) remains open to
  fire. Cosmology unswept.
- **R3 (carried):** the commutant-layer "gauge" extension is coinage, un-minted.
- **R4 (carried):** the flat-connection remark is narration.
- **R5 (carried):** the lemma inherits row 549's reopening hooks (a)–(e) and dies with it.
- **R6 (carried):** the C1 strong-reading tension (M2 memo A4).

## What this does NOT show (v2)

- No minting, no registry/Lean edits; `053040` untouched.
- No individual no-go re-proved; "covers" means *files*, never *derives*.
- No connection/curvature sector; no class-B resolution; no zones-11/13 refinement; no
  cosmology sweep.
- **No principle claim: the grade is organizing lemma.** The A-cluster computations are
  script-grade facts independent of that grade.

## What would MINT it (v2 path)

1. **Lean:** green `D0.Foundation.CanonicalSelectorNoGo` (row 549's pending item, general-lemma
   route per SELECTOR_NOGO_MINT_LOG.md:111); the A6 invariance lemma over `Fin 33` (decidable).
2. **Cert:** promote `torsor_gauge_check.py` per `VERIFIED_CLOSURE_PROTOCOL.md` (can-fail
   machinery present; 28/28).
3. **Registry:** ONE reading-layer row (candidate id
   `D0-WITHINZONE-GAUGE-ORGANIZING-LEMMA-001` — demoted-grade name) + notes-only cross-refs on
   549/543/544 — only via the protocol. Skeptic pass #2 on the v2 load-bearing text is DONE
   (WOUNDED NO-KILL, repairs applied in v2.1); the minting decision is the owner's.
4. **Ledger four-typing:** owner decision (§Consequence).

---

## FINAL — the organizing-lemma statement (v2 grade)

> **ORGANIZING LEMMA (candidate, reading layer).** The within-zone labeling of K(9,11,13) is
> gauge-fixing data: the scene owns the **torsor** (and, on the detector layer, its owned
> reduction to S₈ with three reading-conditional candidate rungs below), and C1's own clause
> plus row 549 close the two directions — invariance is required, fixing is impossible.
> Internal physics is therefore valued in the **invariant ring** of the frozen data, and every
> external import types as exactly one of: **A** (no import — theorem), **B-meas / B-adj /
> B-ext** (measurement / adjudication / extension — physical cost), **C** (owned-torsor
> gauge-fixing — contentless), **D** (un-owned ansatz — conditional structure, falsifiable).
> Freedom-shaped matter walls file into B/C/D computably; existence/insufficiency walls are a
> disjoint third continent the lemma does not touch.
