# W6 BOOKS LOG — wave-hardened book rewrite + sync-guard green

Serial log, incremental. Scope: section-source edits only (books regenerated by
`tools/assemble_books.py`); no registry edits; no git commit; 053040 untouched.

## Build convention (verified first)

- Books in `01_BOOKS/BOOK_0X_*.md` are GENERATED: banner line + concatenation of
  `01_BOOKS/<book_dir>/<seq>__<id>__<slug>.md` in filename order (+ optional apparatus block).
- Regeneration: `python tools/assemble_books.py` (`--check` verifies freshness;
  `tools/check_book_assembly.py` enforces the round-trip).
- Therefore ALL edits below are made in per-section source files, then books reassembled.
- Verified mapping for task 1: BOOK_07:1780 == `0043__07.41__trace-heat-capacity-gravity.md:49`;
  BOOK_07:1782 == same file :51; BOOK_07:1777 (row-107 closure line) == same file :46 — NOT touched.

## Task 1 — BOOK_07 Einstein block, Motion 6-SAFE (EINSTEIN_FIELD_EQ_SYNTHESIS_MEMO §6)

- Action: replace ONLY source line 49 (stale Iter26 `G_ij = dS/dh_ij = 2·L_ij ... CERT-CLOSED`
  block) with the memo §6 Iter27 replacement block, verbatim content (memo blockquote markers
  stripped for book prose; math fence preserved).
- One-clause edit at source :51 (BOOK_07:1782): "Positive partial** (the closed leg,
  `D0-SPECTRAL-EINSTEIN-001`):" → "Positive partial** (the Laplacian-Bianchi leg of
  `D0-SPECTRAL-EINSTEIN-001`, CORE_BRIDGE_SPLIT):" — exactly the memo's one clause.
- CARRY-OVER DECISION (memo skeptic #2 flag, decision required at apply-time): both true items
  carried over, appended as a closing sentence of the Iter27 block (memo's recommended option):
  (a) √ρ-weighted Perron conservation remark; (b) response spectrum
  `2·{0,20,22,24,33} = {0,40,44,48,66}`. Conservative choice — nothing silently dropped.
- Motion 6-EXT NOT applied (blocked pending row-107 owner). Line :46 (row-107 LEAN_PROVED
  closure text) untouched.
- Tool fix required for regeneration: `tools/assemble_books.py:86` used
  `Path.write_text(newline=...)` (py3.10+ only; local interpreter is 3.9.6) — replaced with
  `open(..., newline="\n")`, byte-identical output. Same bug class as the previously fixed
  generate_lean_aggregates py3.9 issue. No behavior change.
- THREE minimal lexical deviations from the memo's verbatim block, each meaning-preserving,
  required because the demoted-row scan lexicon (`proved|proven|CERT-CLOSED|divergence-free`,
  ±3 lines of any `D0-SPECTRAL-EINSTEIN-001` citation) would otherwise re-flag the NEW text
  (and surface the previously shadowed :51 HODGE-LINKS line — the scan reports only the first
  hit per (row,file), so fixing :49 exposes :51):
  1. "the proved row" → "the LEAN_PROVED row" (registry vocabulary; strictly more precise —
     the row's lean_status IS LEAN_PROVED while release_status is CORE_BRIDGE_SPLIT).
  2. carry-over sentence "spectrum of the proved object" → "spectrum of `G = 2L`".
  3. HODGE-LINKS line: two bare "divergence-free" → "archiveDivergence-free" (the corpus's own
     discrete sense, which is exactly what that line already defines via row sums).
  Chosen over an allowlist suppression to avoid weakening the guard. No allowlist entries added.
- ACCEPTANCE MET: `python3 tools/check_book_ledger_sync.py` → PASS, 0 PROSE-OVERCLAIM,
  0 DEMOTED-CLOSURE-PROSE (the :49 flag gone, zero new flags).
- Status: LANDED


## Task 2 — the "8 referee drift points"

- SEARCH (exhaustive, logged): grepped _TASKS_CENTER_ATTACK (all 90+ md), 06_AUDIT, 03_THEORY_MAP,
  docs/, 00_PUBLICATION, 04_VERIFICATION, root *.md for: "drift point", "re-read", "referee" +
  book, numbered D/R/P-1..8 lists, "BOOK_0X:line" citation clusters, "book-vs-registry". Plus an
  independent very-thorough Explore-agent sweep. RESULT: **no report listing 8 book-vs-registry
  drift points exists in this repo copy.** Closest artifacts (all registry/atlas-side, not book
  prose): DESYNC_FIXLIST.md (138 frontier_dag desyncs), DESYNC_CORRECTIONS.md (F-03/F-04/F-05/F-15),
  CEMENT_FRONTS_REPORT.md ADDENDUM (the BOOK_07 Einstein key exposure — fixed by Task 1),
  06_AUDIT/CORPUS_PEER_REVIEW_Iter21.md §D ("books stale", guard-level, no 8-item list).
- The one book-prose drift those artifacts DO name (BOOK_07:1780 Einstein, the "major one") is
  FIXED by Task 1. The F-15 inverse desync (registry notes narrating a stale demotion) is
  registry-side — out of scope per hard rule (no registry edits; integration already handled).
- Mechanical fallback run: `check_book_ledger_sync.py --strict` → 0 over-claims, 0 demoted-prose;
  ~25 PROSE-UNDERCLAIM (books more cautious than ledger). NOT fixed: prose-side upgrades are
  barred by this task's own rule ("no upgrades"); underclaims are honest conservatism, non-FATAL
  (strict-only), pre-existing.
- Status: PARTIAL/HELD — report NOT FOUND (owner should confirm where the 8-point list lives;
  if it was conversational, the only named major point is landed). No speculative edits made.

## Task 3 — BOOK_05 line budget (1262 > 1240)

- Cuts applied (before → after: 1262 → 1225, −37, gate PASS_BOOK05_INTEGRATED_REWRITE):
  1. CONTENT-DUP −4: `0017__05.13:28-31` "Gravity closure requires: …" paragraph — verbatim
     duplicate of the same section's gate-table row 21 AND of 0001:110's sync-gate retention
     line. Guard `check_v14_gravity_closure_sync` needles ("Gravity closure requires:",
     "finite graph/entropy cert", "explicit Pi_TT and W_TT", "higher-curvature cut",
     "spectral A2/EH bridge", "no continuum constants imported as core") all remain satisfied
     via 0001:110 + the table row (verified by grep before cutting).
  2. CONTENT-DUP −1: `0001__05.v15:109` duplicate `TORUS-RAMIFICATION-TARGET:` ledger key
     (second variant of :29). MERGED, not dropped: :29 now carries the unique detail
     ("charged-lepton …branch indices of the finite Green function over the shell torus").
  3. WHITESPACE −32: collapsed internal blank-line runs ≥2→1 (0000 FRONT, 0011, 0017;
     fence-aware) and stripped trailing blank lines from all 34 section files (butt-joint
     seams already existed in this book pre-edit; CommonMark headings interrupt paragraphs,
     rendering unchanged). Zero prose touched.
- ATTEMPTED-AND-REVERTED: cutting `0019__05.15:26` ("operator/passport rule is strict…") —
  looked like a verbatim restatement of 05.15.1, but `check_v14_global_operator_geometry_sync`
  REQUIRES its exact tokens in BOOK_05 ("operator first", "passport second", "no PDG tuning",
  "no geometry diagnostic as core source"). Restored verbatim; no guard ever ran red.
- Guards after trim: check_book05_integrated_rewrite PASS (1225 ≤ 1240; headroom 15 lines for
  Task 4a's ≤15-line insertion), check_book_ledger_sync PASS.
- Status: LANDED

## Task 4 — new-argument upgrades (candidate language, memo/row-cited)

- (a) LANDED: `BOOK_05/0009__05.8__forbidden-shortcuts.md` — new `### 05.8.V Within-zone labels
  as gauge-fixing data (organizing lemma — candidate reading layer)`, 14 added lines (≤15).
  Cites row `D0-WITHINZONE-GAUGE-ORGANIZING-LEMMA-001` (PROOF-TARGET stated inline, so the
  ledger-sync open-tier scan sees the OPEN vocabulary) + TORSOR_GAUGE_SYNTHESIS_MEMO v2.1 +
  torsor_gauge_check.py 28/28. Content: torsor of within-zone labelings (S₉×S₁₁×S₁₃ simply
  transitive), four-cell import typing A / B(meas·adj·ext) / C / D, three-continent wall map
  with the explicit border criterion (empty ⇒ existence; agree-on-invariants ⇒ insufficiency;
  differ ⇒ freedom). No closure vocabulary. BOOK_05 = 1239 ≤ 1240 after insertion, gate PASS.
- (b) LANDED: `BOOK_07/0043__07.41__trace-heat-capacity-gravity.md` — one-paragraph
  `**[Iter27-CAP — capacity clause (candidate reading layer; W2_QUANTITY_IDENT_MEMO.md CAP
  clause + registry rows 460-469 and 383)]**` inserted directly after the 6-SAFE Iter27 block,
  before the Iter26 HODGE-LINKS block (+2 physical lines, single-paragraph house style).
  Content: div G_A2 = 4·deg = 4·BoundaryCutWeight({j}) = 16·C(∂{j}) as the per-vertex face of
  C_total = 2|E| = Tr L = 718; five-face roster one-liner (handshake/spectral/carrier-dim/
  combinatorial/density); REQUIRED clause verbatim: "the identity is structural while the
  VALUE 718 is scene-specific". Wordlist audited against both sync-scan lexicons (no
  proved/proven/CERT-CLOSED/divergence-free/theorem/forced within the ±3-line and 240-char
  windows of the adjacent demoted/open row citations).
- (c) LANDED: `BOOK_01/0023__01.20__capacity-closure-...md` — `**[E-SYNTH record (candidate
  reading layer; GAP_E_SYNTHESIS_MEMO.md, post-skeptic — closure headline killed, computation
  layer retained)]**` paragraph after the §01.20 capacity-ladder certificate bullet
  (BOOK_01 ~:1558, the requested :1548-1554-adjacent extension/scene area; +2 physical lines,
  3 sentences + caveats). Records ONLY the survivals: canonical layers of Q₈ = {ℤ₂,V₄}
  (computed), census Q₈-unique-clean (ℤ₂³ zero layers), coset reading of ABCD; explicit
  caveats: GAP-E exhaustiveness stays OPEN (4th confirmation named), D₂=Z(Q₈) fork-open vs the
  owned two-port dyad reading; explicitly disclaims any "scene is a central extension" closure
  narrative (dead with the E-SYNTH kill).
- All books reassembled via tools/assemble_books.py (generated files never hand-edited);
  check_book05_integrated_rewrite PASS (1239), check_book_ledger_sync PASS (0/0).

## Task 5 — rebuild + full local gate (final boards)

- Rebuild: `python3 tools/assemble_books.py` — all 10 books regenerated from sections;
  `--check` → PASS (all 10 idempotent); `tools/check_book_assembly.py` → PASS (10 books,
  342 sections, idempotent). Generated books never hand-edited.
- Sync guard (ACCEPTANCE, extended demoted-row class): `tools/check_book_ledger_sync.py` →
  **PASS — 0 PROSE-OVERCLAIM, 0 DEMOTED-CLOSURE-PROSE** (the BOOK_07:1780 flag is gone; zero
  new flags; no allowlist entries added).
- `tools/generate_lean_aggregates.py --check` → PASS (aggregates idempotent).
- `05_CERTS/vp_status_inflation_audit.py` → PASS (546 claims, 0/0/0).
- `05_CERTS/vp_total_publication_readiness.py`, `vp_total_no_status_inflation.py`,
  `vp_high_priority_no_status_inflation.py` → all PASS.
- FULL guard battery: all 53 `tools/check_*.py` run (120s timeout each;
  `check_lean_builds.py` skipped — needs a lake build, out of W6 scope):
  **51 PASS · 1 SKIP(lake) · 1 FAIL**.
  - REMAINING FAIL (pre-existing, non-book, expected): `check_cert_can_fail.py` — the 4
    selector-campaign print-stub certs (`vp_selector_{basepoint,dynamical,obstruction,ssb}_nogo.py`),
    4 unallowed + 4 missing-on-disk; already on the wave-2 ledger as the known cert-can-fail
    debt. NOT touched here (cert rewrites are not book work).
  - `check_book05_integrated_rewrite.py` → PASS (BOOK_05 = 1239 ≤ 1240; pre-W6 it was the
    flagged 1262 FAIL — cleared by Task 3 + absorbed Task 4a).
  - `check_standard_language_audit_budget.py` → PASS (unchanged by the three insertions).
- No git commit; no registry/CSV edit; no `.lean` edit; 053040 never touched (not present in
  any edited file — verified by grep before writing).

## Edited files (complete list)

Section sources (books regenerated from these):
1. 01_BOOKS/BOOK_07_GRAVITY_LIMIT_AND_FINITE_GEOMETRY/0043__07.41__trace-heat-capacity-gravity.md (T1 + T4b)
2. 01_BOOKS/BOOK_05_VERIFICATION_STATUS_AND_CERTIFICATE_DISCIPLINE/0001__05.v15__active-closure-law.md (T3)
3. 01_BOOKS/BOOK_05_VERIFICATION_STATUS_AND_CERTIFICATE_DISCIPLINE/0017__05.13__current-inherited-priority-gates.md (T3)
4. 01_BOOKS/BOOK_05_VERIFICATION_STATUS_AND_CERTIFICATE_DISCIPLINE/0009__05.8__forbidden-shortcuts.md (T4a; + T3 whitespace)
5. 01_BOOKS/BOOK_01_CONDENSED_FOUNDATIONS_AND_GRAPH_BIRTH/0023__01.20__capacity-closure-of-four-terminal-roles-a-b-c-d-.md (T4c)
6. all 34 BOOK_05 section files — whitespace-only trailing-blank normalization (T3)
Tools (py3.9 compat fix only, no behavior change):
7. tools/assemble_books.py (:86 write_text(newline=) → open(newline="\n"))
Generated (via assemble_books.py, not hand-edited): all 10 01_BOOKS/BOOK_0X_*.md

## Final per-task board

- T1 BOOK_07 Einstein Motion 6-SAFE ............ LANDED (sync guard GREEN, acceptance met)
- T2 8 referee drift points .................... PARTIAL/HELD (report NOT FOUND in repo; major
                                                  point = T1 landed; no speculative edits)
- T3 BOOK_05 budget ............................ LANDED (1262 → 1225 → 1239 w/ 4a; gate PASS)
- T4a organizing-lemma block ................... LANDED
- T4b capacity clause .......................... LANDED
- T4c E-SYNTH survivals record ................. LANDED
- T5 rebuild + full gate ....................... LANDED (51/53 PASS; 1 pre-existing non-book FAIL)
