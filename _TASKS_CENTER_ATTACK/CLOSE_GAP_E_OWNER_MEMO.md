# CLOSE_GAP_E_OWNER — the owner-route 10th pass: LEG 1 (owned parity kill of X₃/z₃=12 — a verified MISSED OWNED KILL + errors of record) and LEG 2 (the which-one-register uniqueness candidate) — v2, POST-SKEPTIC

> ## ⚖️ VERDICT (independent skeptic §05.8.R, 2026-07-06 — ACCEPTED IN FULL, all nine repairs applied):
> **LEG 1: CLOSED-as-EoR-correction** — the parity kill of `z₃ = 12` and of every odd-letter-count
> alphabet stands at the clause's OPERATIVE grade (narrated CORE-FORCING + Lean-backed numerics,
> row 522 — the grade the corpus itself used for "(9,11,15) is the escape"); EoR-A…E verified in
> context. **LEG 2: DOOR-(a)-ARMED** — the which-one-register candidate survives as the maximal
> armament for owner authorship, with the θ-typing DOWNGRADED from "same type as :792" to
> **"analogous form (:792-shaped)"** (the co-presence clause fails at object level: conjugate
> copies are forced into the realized group by the axioms; the rival's ADMISSIBILITY flag is
> text-level — the memo's own SELF-ATT-U1, landed). No closure is asserted; rc stays 2.
> Repairs applied: R1 (no-skip adjudication, §1 P3′), R2 (script: {Z,Q₈∖Z} guard rebuilt from
> partitions L2-3G, two tautologies deleted, MUT-C rebuilt, honesty-guard de-duped, DEMO labels),
> R3 (script honesty sentence — operative grade + no-skip not used), R4 (B-class layers QV-pinned
> to W1_W3:187 / TORSOR_GAUGE:246), R5 (clause (iii) disclosed as AUTHORED, §5.4), R6 (θ-typing
> downgrade + inner/outer clause, §5.3), R7 (assembly-grade tag on the finiteness claim, §4/§8/§9),
> R8 (GREP-guard wording + extension recommendation, §8.4), R9 (two-horns wording, §0/§2).

**Status:** DRAFT candidate; no registry row edited; no `.lean` added to the built tree; no cert
minted; `053040` untouched; no commit. **STOP-RULE note:** the 9th memo's stop-rule (§8) bars
re-FORGING; this pass is owner-authorized and enters through the stop-rule's own doors — Leg 1 is
not a forge at all (it is the discovery that an ALREADY-OWNED banning clause was never applied to
the recorded rival — an error-of-record correction; NOTE (R8): this class of error is one the
META GREP-guard does NOT currently watch — the guard fires on NEW producing/banning sentences,
not on missed APPLICATIONS of existing ones; §8.4 recommends extending it), and Leg 2 is the
door-(a) OWNER-authorship route: the owner supplied the argument and this memo forges its maximal
supporting brief + the exact candidate sentence, without minting it.

**Pre-flight (2026-07-06):** `preflight.sh GAP-E z3 X3 parity alphabet` — owners found and
cross-referenced, never duplicated: row 257 `D0-TOWER-STOP-NOEXT-001` (COUNT-3), row 522
`D0-SCENE-STEP-PARITY-001` (LEAN_PROVED, CORE-FORMALIZED — the parity owner), row 546/547/549,
row `D0-WITHINZONE-GAUGE-ORGANIZING-LEMMA-001` (four-cell lemma, OPEN/candidate). Prior GAP-E
family re-read in full: R3/COMPLETENESS, E-b, DYAD-POWER, E-SYNTH, 5th, 6th, RAISE-7th, META-9th
(+ its stop-rule), `window_forcing_check.py`, `gap_e_completeness_check.py`.

**Companion script:** `close_gap_e_owner_check.py` v2 (post-skeptic) — **58/58 PASS** (count
printed by the script's own counter), can-fail, mutation-tested (MUT-A/B/C/D/D′/9TH + NC1-3 +
in-check honesty guard + L2-3G derived-tower guard), exact integers/Fractions only, no check
builds its key quantity from the conclusion (two v1 tautologies deleted per skeptic — R2).
**rc=2** (honest final state: Leg-1 kill verified, door armed, window NARROWED, not closed);
`--grant-uniqueness` → **rc=0** (MUT-A sufficiency demo).

---

## §0. Claims (DEF-0.2.2 form)

**Claim L1 (Leg 1 — verification of a missed owned kill; NOT a new forcing).**
Assume `¬L1`: the tower `(9,11,12)` (the X₃ Aut-orbit alphabet, 3 letters over `V₉`) is
M1-admissible. BOTH horns of the F3 fork — the ladder horn, whose text stands at two sites
(consecutive B03:1049-56; nesting §03.23.7), and the over-base horn (:1551-53) — then contain
a layer splice of ODD step
(`11→12 = +1`, the book's own worked example; or `9→12 = +3`). An odd step flips the `ℤ₂`
orientation/holonomy class (`det(T^n) = (−1)^n`, computed L1-1), and the corpus's owned banning
sentence — B01:1903 / B03 §03.23.6(3), status CORE-FORCING, Lean-backed by row 522 — states that
splicing across the flip requires an exogenous orientation bit, the θ it NAMES ("a sign catalog",
"an exogenous `Z_2` orientation bit not stored in the address"), `⊥M1`. Contradiction. Hence
`z₃ = 12` is inadmissible **by owned text**, and likewise every odd-letter-count alphabet
(`|X| ∈ {1,3,5,7}`: X₃, the nested chain `{1,Z,Q₈}`, the conjugacy-class partition (5 → z=14),
singletons-below). The memos 5–9's clause "z₃ = 12 remains admissible under every clause the
corpus owns" is an **error of record** (enumerated §3).

**Claim U (Leg 2 — the uniqueness candidate; CANDIDATE, not owned, no closure asserted).**
If one extension slot carried TWO co-admissible capacities of different letter-count, the
completed construction would carry a which-one register (≥1 bit, computed) whose setting is
invariant-CHANGING (exact deltas §5: `|V|`, `|E|`, `CAP = tr L`, the Laplacian spectrum), hence
contentful (blade cell (A)) and not gauge (four-cell: not class C); the register is of
**:792-ANALOGOUS form** (post-skeptic R6 — analogous SHAPE to the owned Q₈-normality catalog,
NOT the same θ-type: the graded dis-analogies are §5.3's last two rows) — a multiplicity of like
structures whose discrimination demands an external catalog. An owned sentence **U** banning
that register (candidate text §6) yields per-slot size-uniqueness; with owned membership
(B01:1548) this gives `AdmExt` sizes `= {2,4}`, the unique tower `(9,11,13)`, `z₃ ≤ 13`, window
`[9,13]` sealed above (MUT-A). **U is not owned; this memo does not assert it; it arms door (a).**

## §1. Owned pre-facts (verbatim, file:line — all 15 machine-verified by the script's QV layer)

- **P1 (the parity ban, narrated CORE-FORCING).** B01:1893: "the address ladder is forced to
  advance by `+2`, never `+1`, because a `+1` step would demand an external orientation bit — a
  sign catalog — which is `⊥M1`." B01:1903: "The orientation/torsion class flips, and to splice
  the two layers across that flip one must supply an external orientation bit (an element of
  `Z₂`) that is **not** contained in the address itself." B01:1909: "Status: CORE-FORCING".
- **P2 (the same ban, second book, with the machine-check).** B03 §03.23.6(3) (:1037-1041): "A
  step `+1` (`5 -> 6`) flips the orientation/holonomy class; gluing the layers then needs an
  exogenous `Z_2` orientation bit not stored in the address. That is an external catalog. Banned
  by M1." and (:1043-1047): "proved as `det(T^{n+2}) = det(T^n)` (with the `+1` flip
  `det(T^{n+1}) = -det(T^n)` as the negative control)".
- **P3 (the Lean owner, row 522).** `SceneStepParity.lean:12-17` (LEAN_PROVED): `det T = −1`
  (`det_T`, by decide); docstring: "A parity-*flipping* ladder would need a *second* independent
  `ℤ₂` sign bit, an exogenous catalog (`⊥M1`)". Honesty: the ⊥M1 closure "rests on the
  pre-existing no-aliasing principle" (its own docstring) — Lean owns the arithmetic, the ban is
  narrated CORE-FORCING. This is the SAME grade at which the campaign's own L0 tier ran.
- **P3′ (R1, load-bearing — the OTHER leg of row 522, NO-SKIP, stays E-b-missing; adjudicated
  here so Leg 1 cannot be read as upgrading it).** `SceneStepParity.lean:18-20` (docstring) also
  carries the no-skip leg: "A `+4` step `9 → 13` skips `11 = L₅` … omitting an admissible address
  without an internal reason is a skip-catalog, `⊥M1`." **Leg 1 does NOT use it and nothing here
  upgrades it.** The asymmetry between the two legs of row 522 is a matter of record: the PARITY
  leg has a PRINTED mechanism (the defect-sign alternation B01:1895-1903, machine-checked as
  `det(T^{n+2}) = det(T^n)` with the `+1` flip as negative control) plus THREE independent
  skeptic adjudications of its scope (P4); the NO-SKIP leg has conclusion-words plus the E-b
  begging-point (the "skipped address" presupposes a pre-laid address space — COMPLETENESS E-b
  §R3 and F1/E-GE-2: G-STEP is one-sentence-killable), and the registry's two-guard record (row
  547: "GAP-E is two-guarded") keeps it as the E-b MISSING clause. Consequence: odd-size kills
  need ONLY the parity leg (operative); the even rivals `{6,8}` would need no-skip or U — and
  remain open precisely because no-skip is not operative. This is why Leg 1 closes and the
  window does not.
- **P4 (the scope, ALREADY adjudicated three times — the crucial pre-fact).** The exact question
  "what does the printed parity argument kill?" was answered identically by three independent
  prior passes, all skeptic-accepted:
  1. `GAP_E_DYAD_POWER_MEMO.md` OWN-4: "the ⊥-mechanism fires **iff the step flips the `ℤ₂`
     orientation class**, i.e. iff the step is **ODD**"; its independent skeptic EOR-2: "The even
     `+4`→V₁₅ escape is un-killed by parity" (odd IS killed — that is the contrast being drawn).
  2. `GAP_E_COMPLETENESS_MEMO.md` F1/E-GE-2 (skeptic repair): the printed ⊥-proof "kills only
     orientation-PARITY-flipping steps"; and its kill-matrix tier **L0 is labeled "Lean-owned
     today: base-9 + count-3 + step-evenness"** with computed survivors "(9,11,z₃), z₃ **odd**
     ≥ 13" (`gap_e_completeness_check.py:202`, quote-verified).
  3. `window_forcing_check.py:102`: `(9, 12, 15), # +3 ladder — odd step` — killed by the PARITY
     filter; `:145` NC: `(9,13,17)` passes ("evenness, not '+2', is what det_T_pow owns").
- **P5 (COUNT-3 owner — the task's ownership question ANSWERED).** Row 257
  `D0-TOWER-STOP-NOEXT-001`, `NoExtension.lean:61-65` (`no_extension_theorem`), docstring "The
  COUNT (3 slots, no 4th) is proved here". Owned: **exactly 2 extension slots**; silent on sizes
  (DYAD-POWER OWN-0.3/:61). Hence the question after COUNT-3 is pure **SLOT-RIVALRY** — replacing
  an alphabet, never adding a zone — exactly as the owner framed it. (`(9,11,13,15)` dies here.)
- **P6 (the θ-precedent).** B01:792: "**Normality is forbiddance of a conjugate catalog.** A
  non-normal subgroup carries distinct conjugate copies `gHg⁻¹`. Telling those copies apart
  requires an external catalog of conjugates — exactly the catalog M1 (DEF-0.2.2) forbids."
- **P7 (membership + the list).** B01:1548 "the only primitive unresolved capacities over the
  pointed shell: the direct/return dyad and the full terminal role square"; :1551-53
  `V₁₁ = V₉⊔D₂`, `V₁₃ = V₉⊔ABCD`. Owned as MEMBERSHIP, not exhaustiveness (DYAD-POWER, upheld).
- **P8 (the grammar adverse set — inherited whole from the 9th, A1-A6).** In particular A2: M1's
  one-line form (B00, quote-verified Q11) compares constructions **at the same outcome class** —
  it cannot adjudicate between towers with different invariants; A6: firewall force =
  non-promotion, never negation; blade cell (A) (Q12) states an OBLIGATION ("must be
  derived/justified"), not a ban. The 9th's kill and precedent taxonomy are ACCEPTED IN FULL and
  form the constraint surface Leg 2 is built against, not around.
- **P9 (the four-cell lemma — CANDIDATE layer, disclosed).** `TORSOR_GAUGE_SYNTHESIS_MEMO.md`
  v2.1 + row `D0-WITHINZONE-GAUGE-ORGANIZING-LEMMA-001` (OPEN): class A/B(-meas/-adj/-ext)/C/D;
  BOOK_05:520 "Candidate language; row stays open." Leg 2's class-typing rides on a candidate
  lemma and is graded accordingly.
- **P10 (row 549).** `D0-CANONICAL-WITHIN-ZONE-SELECTOR-M1-NOGO-001` (LEAN_PROVED): the
  within-zone selector is M1-FORBIDDEN — same-size labeling freedom is torsor/gauge, the scene
  never selects a point of it. Used as the GUARD that Leg 2 must not fire on same-size forks.

## §2. LEG 1 — the verification (computed; script L1-1 … L1-4)

**L1-1.** `det T = −1` and `det(T^n) = (−1)^n` for `n = 1..40`, computed from the integer matrix
power (never assumed). Orientation class of address `n` = `(−1)^n`; a splice `m→n` is class-safe
iff `n − m` is even — this is the full content of the owned machine-check (P2).

**L1-2 (the kill, fork-robust).** The X₃ tower `(9,11,12)` is killed under **both horns of the
F3 fork** (the fork is not picked — the kill survives each horn separately; R9: consecutive and
nesting are two textual SITES of the one ladder horn, not independent readings): ladder horn —
step `11→12 = +1`, verbatim the banned worked example of BOTH books, at both sites (B03:1049-56;
§03.23.7); over-base horn (Sum types :1551-53) — splice `9→12 = +3`, odd, same flip. The owned
tower and the even rivals `(9,11,15)`, `(9,11,17)`, `(9,13,17)` survive every reading (NC1-3):
the filter reproduces exactly the adjudicated scope — evenness, nothing more. **No fork
adjudication is needed and none is smuggled.**

**L1-3 (alphabet level).** `zone(X) = 9 + |X|`; odd `|X|` ⇔ even zone ⇔ flip against every owned
odd layer. Computed kills = exactly `{1,3,5,7}`: **X₃ (3)**, the nested chain `{1,Z,Q₈}` (3), the
conjugacy-class partition (5 → z=14). Honesty guard (passes): parity does NOT kill `|X| ∈ {6,8}`
— the V₁₅ escape and singletons-8 stay parity-alive; Leg 1 claims nothing about them.

**L1-4 (why this is a MISSED kill, not a new one).** The campaign's own L0 tier — which the
COMPLETENESS memo and its skeptic graded "Lean-owned" — already computed survivors "z₃ odd ≥ 13".
`z₃ = 12` was excluded at that layer BEFORE X₃ was ever exhibited. The 5th pass then found X₃ at
the alphabet level and the tower-level filters were never re-run on its tower image; the 6th
pass's P3 introduced the propagating misquote (§3 EoR-B); the 7th and 9th inherited it. Leg 1
adds **zero new text**: it applies P1-P4 to one more instance. Its grade is the grade of P1-P4:
narrated CORE-FORCING + Lean-backed numerics + three skeptic adjudications — the SAME grade at
which the corpus already records "(9,11,15) is the parity-surviving escape".

**Consistency dilemma (the pin).** Either the L0/evenness standard of the completeness pass was
right — then `z₃ = 12` was NEVER admissible and memos 5-9 carry an error of record — or that
standard was wrong — then the completeness memo's own tier table and residual family are the
error of record. There is no reading of the corpus on which the record is currently consistent.
The first horn is the one three independent skeptics already certified.

## §3. ERRORS OF RECORD (Leg 1 corollary — enumerated, mechanical exhibits in script L1-4)

- **EoR-A (5th).** `CLOSE_GAP_E_MEMO.md` :8/:229/:342/:374/:419 — X₃ presented as a surviving
  canonical rival ("z₃ = 12 … remains admissible") with the owned parity clause absent from the
  D1-D6 filter set. The 5th's partition rule itself is untouched and stands.
- **EoR-B (6th — the origin of the propagation).** `CLOSE_GAP_E_6TH_MEMO.md:96-97` (P3)
  attributes to `GAP_E_DYAD_POWER_MEMO §VERDICT` the clause "Size 6 (→ V₁₅) **and size 3
  (→ V₁₂)** are 'excluded by the absence of a generator … silence, not forcing.'" The DYAD-POWER
  memo contains **no "size 3" clause anywhere** (mechanically verified); its verdict names size 6
  only, and its OWN-4 adjudicates odd steps as owned-killed. The extension of the
  silence-exclusion to the odd size is the 6th memo's interpolation — a misquote — and every
  later "z₃ = 12 admissible under every clause the corpus owns" descends from it. Also :274.
- **EoR-C (7th).** `RAISE_GAP_E_MINIMALITY_MEMO.md:316` "Absent it, `z₃ = 12` …".
- **EoR-D (9th).** `CLOSE_GAP_E_META_MEMO.md` §5:143-144 + §7 registry note; and
  `close_gap_e_meta_check.py:142` assigns X₃ `ban_tag=None` — false: the banning sentence for its
  CLASS (parity-flipping steps) exists and is P1/P2; the taxonomy's "no owned banning sentence
  for any rival" is true only of the EVEN rivals. The 9th's kill of the meta-route and its
  precedent taxonomy are NOT affected (the taxonomy in fact predicted this: X₃ dies by mechanism
  (i), an owned banning sentence naming its θ — the sentence was there all along).
- **EoR-E (in-book candidate note).** B01:1562 (window final state note, candidate layer):
  "absent either, `z₃ = 12` remains admissible under every clause the corpus owns" — false under
  the corpus's own L0 standard; proposed replacement text in §8 (NOT applied — owner's book).
- **EoR-F (session memory).** The GAP-E stop-rule memory note carries the same clause —
  correction to be propagated by the owner of that note.
- **NOT an error:** the nine-pass conclusion "the completeness QUANTIFIER is unowned" stands
  untouched — Leg 1 kills one rival by owned text; it does not mint the quantifier. R3, E-b,
  DYAD-POWER, E-SYNTH and the 9th's grammar adjudication all survive verbatim.

## §4. What Leg 1 buys (computed; script L2-4 cascade)

Filters in owned/assembly order over alphabet pairs `(|X₂|,|X₃|) ∈ {1..8}²` (COUNT-3 fixes two
slots; the 5th's partition rule bounds `|X| ≤ 8` — ASSEMBLY grade, disclosed):
64 → F1 distinct-sizes+order (E4 assembly, `|S₂|>1`) 28 → **F2 parity (OWNED, Leg 1) 6**:
`(2,4)` owned + five rivals `(2,6),(2,8),(4,6),(4,8),(6,8)` — tower images `(9,11,15)`,
`(9,11,17)`, `(9,13,15)`, `(9,13,17)`, `(9,15,17)`.

- The rival space is now **FINITE — GIVEN the ASSEMBLY-grade partition bound (R7 tag)**:
  partition rule (5th, D1-D4, assembly) + parity (owned) ⇒ `z₃ ∈ {13, 15, 17}`. **On
  owned-parity-only filters the residue is `z₃ odd ≥ 13` — UNBOUNDED** (exactly the prior L0+
  family; the finiteness is bought by the assembly layer and is graded as such, not as owned).
  The prior record's "{13,15,17,19,21,…}" was additionally scan-bounded at 21; the H7 residue
  sentence's burden shrinks from a universal quantifier over all sizes to the **two named even
  rivals** `|X| = 6` and `|X| = 8` (plus middle-slot analogues) — under the assembly bound.
- The GAP-E residue reshapes: the ONE open sentence no longer needs to exclude X₃ (owned-dead);
  H7's "coset partition of a characteristic-chain term" candidate loses its hardest client and
  a strictly weaker sentence now suffices (any owned clause killing 6 and 8).

## §5. LEG 2 — the which-one register (CANDIDATE; computed support; guards)

**5.1 The fork is invariant-CHANGING (exact, rank-verified).** `K(9,11,z₃)` invariants, Laplacian
spectra computed by exact rank deficiency over ℚ (independent of the multipartite formula):

| z₃ | \|V\| | \|E\| | CAP = tr L | Laplacian spectrum (λ^mult) |
|----|----|-----|-----|------------------------------|
| 12 | 32 | 339 | 678 | 0¹ 20¹¹ 21¹⁰ 23⁸ 32² |
| **13** | **33** | **359** | **718** | **0¹ 20¹² 22¹⁰ 24⁸ 33²** |
| 15 | 35 | 399 | 798 | 0¹ 20¹⁴ 24¹⁰ 26⁸ 35² |
| 17 | 37 | 439 | 878 | 0¹ 20¹⁶ 26¹⁰ 28⁸ 37² |

Every size-fork moves `|V|` (=33), `|E|` (=359), `CAP` (=718) and the spectrum — the "инвариантами
разрушить" leg: the fork is contentful in the blade's cell-(A) sense, so it is NOT relabeling
freedom. GUARD (computed): the same-size rival `{Z, Q₈∖Z}` (2 letters, blocks 2,6 → z₂ = 11) has
**all-zero graph deltas** — an invariant-PRESERVING fork, class-C/torsor territory (row 549), on
which the register argument must NOT and does not fire.

**5.2 The register.** Two co-admissible capacities for one slot ⇒ the completed tower carries a
which-one datum of `⌈log₂ k⌉ ≥ 1` bit. DEF 0.3.1 three-prong test on that datum: (1) not derived
(by hypothesis of the fork — nine passes certify no producing sentence for the rivals); (2)
affects the result (the §5.1 deltas — distinguishable outcomes); (3) not part of the
distinguishability protocol (within either branch the protocol runs identically). All three
prongs met ⇒ the register is an exogenous parameter in the owned definition's exact sense.

**5.3 θ-typing side-by-side with B01:792 (the memo's spine; script L2-3 computes both sides).**
**Graded result (post-skeptic R6): ANALOGOUS FORM (:792-SHAPED), NOT same-type.** The skeptic's
clause-level comparison landed on the co-presence row exactly as pre-registered (SELF-ATT-U1):
the downgrade is executed, not merely priced. The table now carries TWO dis-analogy rows.

| clause | Q₈-normality (:792, OWNED) | slot-fork (Leg 2, CANDIDATE) |
|---|---|---|
| multiplicity source | non-normal `H` ⇒ distinct conjugates `gHg⁻¹` | `k ≥ 2` co-admissible capacities ⇒ distinct fillings |
| co-presence | copies coexist in the ONE realized group (group closure; D₄ contrast computed: 4 non-normal subgroups, catalog size 2) | the candidate partitions coexist as structures on the ONE realized carrier `Q₈` (computed: X₃ and the role-coset partition are both partitions of the same 8-element set) |
| what must discriminate | the role-addressing protocol (scene-internal) | the extension act / the completed tower's specification |
| catalog type | conjugate-index catalog | which-one / slot-fill register |
| ban | "exactly the catalog M1 (DEF-0.2.2) forbids" | the SAME sentence-form, candidate U |
| conclusion | every subgroup normal (situation banned) | per-slot capacity-size unique (fork banned) |
| **dis-analogy 1 (co-presence — the downgrading one)** | the copies' existence is THEOREM-grade (group axioms force `gHg⁻¹` into the realized object) | the rival's ADMISSIBILITY is not theorem-grade — it is exactly the not-excluded-by-owned-text status; the carriers are object-internal, the admissibility FLAG on them is text-level |
| **dis-analogy 2 (inner/outer — R6 extension)** | the catalog is demanded by a scene-INTERNAL discriminating agent (the role-addressing protocol must tell the copies apart to operate) | the register is demanded at the OUTER level — by the extension act / the tower's specification; no owned sentence makes the scene itself consume the which-one bit |

The two dis-analogy rows are why the typing verdict is **"analogous form"**: the candidate U has
the :792 sentence-SHAPE (multiplicity → catalog → M1-ban → uniqueness), and the owner may author
it in that shape; but it is not DERIVABLE by transfer from :792 — the transfer fails at exactly
these two clauses (the same typing-transfer class the 5th/6th skeptics killed on C1).

**5.4 Why this is NOT the killed 9th (the discriminator, encoded as the firing rule).** The 9th's
v1 banned the underived PRESENCE of one rival — no second object, no register; the skeptic named
the smuggle ("underived presence = choice rule" is authored, not verbatim). Leg 2's pattern
`register_fires(k, Δ≠0, layer)` structurally REFUSES that route: it needs (i) `k ≥ 2` (a real
fork — the register exists only in the two-candidate situation, the alternative-carried
structure the 9th's skeptic demanded and v1 never exhibited), (ii) `Δ ≠ 0` (invariant-changing —
else gauge/torsor, row 549), (iii) construction layer (bridge-layer B-class rows — n_s, CKM
B-ext — legitimately consume DECLARED external input via the ASSUMP ledger; without clause (iii)
the pattern would auto-close every B-filed row — MUT-D′ shows the mutant fires on them, the
guarded pattern does not). All three clauses are load-bearing, each demonstrated by mutation.
**DISCLOSURE (R5): clause (iii) is AUTHORED IN THIS MEMO.** No owned sentence restricts the
register-ban pattern to the construction layer; the construction/bridge distinction is read off
the corpus's PRACTICE (⊥M1 forcings vs the registered-ASSUMP ledger) but is not itself an owned
rule. It is therefore part of U's candidate CONTENT — if the owner authors U, clause (iii) must
be authored with it (the §6 draft carries it implicitly in "realizing the scene"); the B-class
QV pins (script L2-2, W1_W3:187 / TORSOR_GAUGE:246) fix only the LAYER FILINGS of the two guard
rows, not the distinction's ownership.

**5.5 What Leg 2 does NOT overcome (stated before the skeptic says it).** Under the 9th's
accepted adjudication (P8), the grammar's own closed-world force is NON-PROMOTION: absent U, the
existence of the `(9,11,15)` fork makes the owned tower non-THE at the formal layer — it does not
make the rival inadmissible. A2 blocks the M1 one-liner across different-outcome towers; blade
cell (A) yields an obligation, not a ban. The step from "the fork carries an exogenous-typed
register" to "the fork is banned" is exactly ONE sentence of ownership — U — and cannot be
assembled from owned text (the precedent taxonomy demands an owned banning sentence naming this
θ, and none names the which-one register). **Leg 2's honest ceiling is: the maximally-armed
door-(a) package, not a closure.** The GAP-W mirror is noted: the window's LOWER seal went
through a REGISTERED bridge assumption; the analogous `ASSUMP-SLOT-UNIQUENESS` route exists but
the owner rejected axiom registration (9th §6) — not proposed here.

## §6. The candidate owner sentence U (door (a) — drafted, NOT minted, NOT owned)

> **U (candidate book text, :1539/:792 type, names its θ):** *"A zone-extension slot whose
> admissible primitive capacity is not unique carries distinct co-admissible fillings of
> different letter-count; realizing the scene then requires an external which-one register — a
> slot-fill catalog — exactly the catalog M1 (DEF-0.2.2) forbids, since the fillings differ in
> distinguishable invariants (`|V|`, `|E|`, `CAP`, the Laplacian spectrum). Hence each of the two
> extension slots admits exactly one primitive capacity size; with the membership list (:1548)
> these are the direct/return dyad (2) and the terminal role square (4). Same-size fillings are
> within-zone labeling freedom (the owned torsor; the selector is M1-forbidden) and carry no
> register."*

**Sufficiency (MUT-A, computed):** granting U collapses the cascade to `(2,4)` alone — tower
`(9,11,13)`, `AdmExt` sizes `{2,4}`, `z₃ ≤ 13`, window sealed above. **Minimality:** without U
the owned+assembly filters leave the 5 even rivals (§4); no strictly weaker owned clause in the
corpus closes them (nine passes + this one). **Burden after Leg 1:** U's quantifier ranges over a
FINITE named set — it must kill exactly `|X| ∈ {6, 8}` per slot — the weakest form the campaign
has ever needed. Alternative door (b) unchanged: an owned algebraic exhaustion of the :1548
inventory (row-257-CASE-1 type); Shell-3's Krull-Schmidt theorems (7th) are its natural core and
already prove the {2,4}-factor mathematics — only the factor-grammar's ownership is missing.

## §7. Named risks & PRE-REGISTERED attack surface (strongest first)

- **SELF-ATT-U1 (the graded dis-analogy — the strongest; LANDED, downgrade EXECUTED).** In :792
  the conjugate copies are dragged into the realized object by the group axioms; in Leg 2 the
  rival filling is "present" only as a not-yet-excluded possibility. The skeptic ruled exactly
  this: the co-presence clause fails at the object level, and the inner/outer clause (dis-analogy
  2) fails with it. U's θ-typing is accordingly DOWNGRADED to **"analogous form (:792-shaped)"**
  throughout this memo (§0 Claim U, §5.3, banner). As pre-priced: the side-by-side supports the
  owner's DECISION (the sentence-shape to author) but not a derivation; §5.5 already conceded the
  closure cannot be assembled. The kill narrowed the claim; it did not invert it.
- **SELF-ATT-U2 (circularity check, task item ii).** The argument must not consume the tower's
  THEOREM status. Audit: §5.2 prong (1) uses only "no producing sentence for the rival exists"
  (nine-pass certified, text-level); the deltas (prong 2) are graph computations; nothing cites
  `SceneTripleUnique` (row 530 is conditional on GAP-E — using it would be EoR-4 of the 9th,
  the circular-Lean route, and it is not used). The pump texts (B00:484-486) appear only in the
  adverse set P8, on the OTHER side.
- **SELF-ATT-L1 (Leg 1 scope, task item iii).** Is the over-base splice a "junction step" in
  :1899's sense? Priced: the kill does not need that horn — it holds on EVERY owned reading
  separately (L1-2), including the two ladder-native ones where `11→12 = +1` is the books' own
  worked example. A skeptic rejecting tower-level parity application entirely must also strike
  the completeness memo's L0 tier and residual family — the consistency dilemma (§2) makes
  "no error of record anywhere" an unavailable verdict.
- **SELF-ATT-L2 (does Leg 1 over-claim the parity grade?).** Parity's ⊥M1 closure rests on the
  narrated no-aliasing principle (P3's own docstring). Conceded and priced: Leg 1's kill is
  narrated-CORE-FORCING + Lean-numerics grade — identical to the grade at which the corpus
  already asserts "(9,11,15) is the escape". If a referee strips narrated CORE-FORCING, the whole
  window discussion (not just this kill) moves to the L0-minus-evenness layer; the RELATIVE
  correction (12 was recorded as an admissibility the corpus's own standard denies) survives at
  every grade.
- **SELF-ATT-U3 (four-cell candidacy).** The class-typing lemma is candidate-layer (P9). Priced:
  Leg 2 uses it as ORGANIZING VOCABULARY; the load-bearing content (deltas ≠ 0 ⇒ contentful) is
  the owned blade cell (A) (Q12), not the lemma. If the lemma is struck, §5.1's table still
  stands as exact computation.
- **SELF-ATT-U4 (role-assignment soft joint).** The slot-rivalry framing ("the ABCD slot") must
  not smuggle the shell↦role-square assignment (COMPLETENESS E2.4's IF, the B01:1617 "role
  lists" joint). Audit: Leg 1 is size-only (no role semantics); Leg 2's U is stated over
  capacity SIZES per slot, not role assignments. The assignment joint stays open and named.

## §8. READY-TO-APPLY proposals (text only — NOT applied; owner's decision)

**8.1 In-book note correction (B01:1562, candidate layer).** Replace "absent either, `z₃ = 12`
… remains admissible under every clause the corpus owns" with: "absent either, the surviving
rivals are `z₃ ∈ {15, 17}` (`|X| ∈ {6,8}`, finite GIVEN the assembly-grade partition bound;
owned-parity-only residue: `z₃` odd ≥ 13); `z₃ = 12` and every odd-letter-count alphabet are
excluded by the owned orientation-parity ban (B01:1893-1909, B03 §03.23.6, row 522) — see
`CLOSE_GAP_E_OWNER_MEMO.md`."

**8.2 EoR-correction notes for the five affected records (one line each, ready to append):**
- `CLOSE_GAP_E_MEMO.md` (5th): "[EoR 2026-07-06, 10th pass] X₃'s tower image (9,11,12) dies on
  the owned parity clause (odd step; B01:1903/B03 §03.23.6(3)/row 522) — the D-filter set omitted
  the tower-level parity filter; 'z₃ = 12 admissible' is retracted. Partition rule unaffected."
- `CLOSE_GAP_E_6TH_MEMO.md`: "[EoR 2026-07-06, 10th pass] P3 (:96-97) misquotes the DYAD-POWER
  verdict — 'size 3 (→V₁₂)' is an interpolation absent from the source (which names size 6 only,
  and whose OWN-4 adjudicates odd steps owned-killed); the final line's 'z₃ = 12 remains
  admissible under every clause the corpus owns' is retracted. Product-factor discriminator and
  the ownership verdict on the quantifier unaffected."
- `RAISE_GAP_E_MINIMALITY_MEMO.md` (7th): "[EoR 2026-07-06, 10th pass] :316 'z₃ = 12 remains
  admissible' retracted (owned parity kill). L2/L3/block-count THEOREMS unaffected."
- `CLOSE_GAP_E_META_MEMO.md` (9th): "[EoR 2026-07-06, 10th pass] §5/§7 'z₃ = 12 remains
  admissible under every clause the corpus owns' retracted; the taxonomy's own mechanism (i) in
  fact covers X₃ (banning sentence B01:1903/B03 §03.23.6(3), class = parity-flipping steps).
  Residue re-narrowed to the even rivals {6,8}. The META kill, taxonomy, and stop-rule stand."
- `close_gap_e_meta_check.py:142`: set the X₃ row to
  `ban_tag="B01:1903/B03:03.23.6(3)"` (and re-run: the taxonomy separation then reads X₃ as
  precedent-side, the even rivals as the only ban-less objects).

**8.3 Registry note for the GAP-E ledger row (candidate text):** "[10th pass, owner-route
2026-07-06, post-skeptic] LEG 1 CLOSED-as-EoR-correction: X₃/z₃=12 + all odd-letter-count
alphabets KILLED at the owned parity clause's operative grade (missed owned kill; EoR in memos
5-9 + B01:1562 corrected). Rival space: z₃ ∈ {15,17} GIVEN the assembly-grade partition bound
(owned-parity-only: z₃ odd ≥ 13, unbounded). LEG 2 DOOR-(a)-ARMED: which-one-register candidate
U drafted (:792-ANALOGOUS form, clause (iii) authored+disclosed, mutation-tested sufficiency).
Door (a) now carries TWO candidate sentences — U (uniqueness/register) ∥ H7 (coset-chain, META
§5) — either closes the window via the standing shells. Cert: close_gap_e_owner_check.py (58/58,
rc=2 honest-open; --grant-uniqueness rc=0)."

**8.4 Guard extension (R8 recommendation).** The META GREP-guard watches for NEW producing or
banning sentences touching the rivals. Leg 1's error class — a standing owned banning sentence
whose CLASS covers a recorded-admissible rival, never applied — is invisible to it. Recommend:
extend the tripwire with a coverage sweep (for each recorded-admissible rival, recompute the
owned filter set against its tower image — the `parity_kills_*` functions of this memo's script
are the first instance) so a missed application fires the guard the way a new sentence does.

## §9. What this memo does NOT do

- Does NOT mint U, register any assumption, edit any registry row / cert / built `.lean` / book /
  `053040`; no commit.
- Does NOT claim the window is closed (it is NARROWED: `z₃ ∈ {13} ∪ {15,17}` under the assembly
  bound; `z₃ ∈ {13} ∪ {odd ≥ 15}` on owned-only filters — R7).
- Does NOT claim the {15,17} finiteness at owned grade (it is assembly-tagged, §4).
- Does NOT resurrect the dyad-power/product-factor generator, the 9th's presence-ban, or any
  killed route (the firing rule structurally refuses them — MUT-9TH).
- Does NOT strike the nine-pass conclusion that the completeness quantifier is unowned.
- Does NOT adjudicate the F3 fork (the Leg-1 kill is proven on each horn separately).
- Does NOT claim U is derivable from :792 (the typing is ANALOGOUS FORM only — R6).

## §10. FINAL STATE (post-skeptic)

- **X₃ / z₃ = 12 is DEAD by owned parity** — with the nested chain (3 letters) and the
  conjugacy-class partition (5 letters): every odd-letter-count alphabet. This corrects the
  record of passes 5-9 (EoR-A…E) and REMOVES the named hardest client of the residue sentence.
- **The rival space is FINITE** — `z₃ ∈ {15, 17}` (`|X| ∈ {6, 8}`) — given the assembly-grade
  partition bound; owned-parity-only residue `z₃ odd ≥ 13` (unbounded).
- **Door (a) now carries TWO candidate sentences, either of which closes the window** via the
  standing shells (5th partition rule, 6th product-factor discriminator, 7th Krull-Schmidt
  theorems, this pass's parity kill + cascade):
  - **U** — the owner's uniqueness/which-one-register sentence (§6; :792-ANALOGOUS form; needs
    clause (iii) authored with it; sufficiency mutation-tested, MUT-A);
  - **H7** — the coset-chain sentence (META §5; Lagrange-supported; its burden now reduced to
    killing `|X| ∈ {6,8}`).
  Door (b) — owned algebraic exhaustion of the :1548 inventory (row-257-CASE-1 type; Shell-3 is
  its mathematics) — also stands.
- **Verdicts:** LEG 1 **CLOSED-as-EoR-correction** (operative-grade owned kill; no new text
  minted). LEG 2 **DOOR-(a)-ARMED** (candidate; no closure asserted; rc=2).
- **The stop-rule stands** for forging; the window moves ONLY by owner authorship (U or H7) or
  owned mathematics (door (b)).
