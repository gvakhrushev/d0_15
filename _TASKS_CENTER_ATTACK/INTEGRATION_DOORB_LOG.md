# INTEGRATION_DOORB_LOG — guarded integration of the GAP-E door-(b) dimensional / port-exhaustion result

Date: 2026-07-07. Owner-authorized. Discipline mirrors `INTEGRATION_GAPE_1011_LOG.md` /
`GAPE_STATUS_FLIP_LOG.md`. Serial, incremental, small tool calls.

Source: `_TASKS_CENTER_ATTACK/CLOSE_GAP_E_DIMENSIONAL_MEMO.md` §7 (the mint package).

**Canonical** = `09_LEAN_FORMALIZATION/docs/CLAIM_TO_LEAN_MAP.csv` (hand-edit ONLY this by `claim_id`,
then regen `tools/sync_theory_status_map.py` + `tools/generate_lean_aggregates.py`; generated files
never hand-edited). **`053040` NEVER touched. No git commit.**

## Dialect ground-truth (measured on disk this session, NOT trusted from prior-log prose)
Prior logs' prose claims "559 CRLF" / "559 CR/LF/CRLF pairs". **This is inaccurate for the current
on-disk state.** Byte-level measurement of the pristine file:
- **489927 bytes, CRLF pairs = 0, bare-LF = 559, CR = 0.** The true dialect is **pure LF**.
- The task instruction "preserve CRLF" is honored as "preserve the actual on-disk dialect exactly" —
  which is pure LF. New rows append bare `\n`, never `\r`. Any `\r` appearing post-write = a real red.

Pristine backup (scratchpad `doorb_backups/`):
- `CLAIM_TO_LEAN_MAP.csv.pristine` md5 `67a62919c2642c7184d025e2ed9b3dee` (555 canonical rows)
- `theory_status_map.csv`, `theory_graph.json` (generated snapshots for diff)

Guard board (ALL green after every batch; BACK OUT on any real red):
`tools/validate_csv.py` · `05_CERTS/vp_status_inflation_audit.py` (PASS_NO_INFLATION + planted control) ·
`tools/check_book_ledger_sync.py` · `tools/check_physical_bridge_discipline.py` ·
`tools/generate_lean_aggregates.py --check` · `05_CERTS/vp_assumption_ledger_green.py` ·
`tools/check_assumption_ledger_ownership.py` · the 53-set `tools/check_*.py`.

## BASELINE (before any edit) — ALL GREEN
- validate_csv PASS, **555** canonical rows
- status-inflation PASS_NO_INFLATION (555, 0/0/0) + FAIL_PLANTED control fires
- book-ledger-sync PASS (0/0) · physical-bridge PASS (555) · aggregates --check PASS (idempotent)
- assumption-ledger-green PASS (+ negative controls) · ownership PASS
- 53-set: **53/53 PASS, 0 FAIL**
- `053040`: 0 hits in CSV.
- GAP-E row `D0-WINDOW-9-13-DISSOLVE-001` present (1 hit): pre-state **CERT-CLOSED / OPEN**,
  cert `window_forcing_check.py`, notes 9160 chars (carries GAPE-1011 + FABLE + STATUS-FLIP clauses).
- New claim_ids `D0-GAP-E-PORT-EXHAUSTION-001`, `D0-PORT-GOLDEN-SPLIT-BRIDGE-001`: **0 hits** (not present).
- Dep rows present: `D0-DETECTION-QUADRATIC-001` (1), `D0-INVARIANT-GENERATION-BRIDGE-001` (3 — template).

## CERT GATE (BATCH 1 pre-condition) — VERIFIED
`_TASKS_CENTER_ATTACK/close_gap_e_dimensional_check.py`: default run **rc=2** (CLOSED-MODULO-PORT-CAP,
honest can-fail state — 7/7 lemma rows PASS, cap not granted); `--grant-port-cap` **rc=0**
(OWNED-CLOSURE, rivals {6,8} killed). The rc=2-default is the honest can-fail footing; the row names
this cert exactly as other operative rows name a can-fail cert.

---

## BATCH 1 — mint `D0-GAP-E-PORT-EXHAUSTION-001` (append-only) — APPLIED

New row appended (fields from memo §7.1; grade-language from §6 verdict + §7):
- `claim_id` = `D0-GAP-E-PORT-EXHAUSTION-001`
- `book` = `BOOK_01`; `section` = `01.20/01.22 door-(b) port exhaustion`
- `lean_module`/`lean_theorem` = EMPTY (categorical leg is a PROOF-TARGET, not built in-tree — parallel
  to how the parent's categorical leg is not the built symbol)
- `lean_status` = **OPEN** (honest: categorical port=degree identification NOT machine-checked)
- `python_cert` = `close_gap_e_dimensional_check.py` (default rc=2 = honest can-fail; --grant-port-cap rc=0)
- `release_status` = **PROOF-TARGET** (operative; parent-grade, exactly D0-DETECTION-QUADRATIC-001's
  forcing-reading leg)
- `notes` = the door-(b) closure (memo §7 content, faithful): domain GENERATED by CORE
  D0-DETECTION-QUADRATIC-001 (deg-2 exhaustion, p^3 reduces at B01:556 = row-257 algebra), cap owned twice
  (:1130 two-comparison-kind + :860 falsification hook + :1816 |ABCD|=D2^2), size-set exactly {2,4}, rivals
  {6,8} killed, GRADE OWNED-inheritance. Cross-refs D0-DETECTION-QUADRATIC-001 / row 257 /
  D0-WINDOW-9-13-DISSOLVE-001.

Applied via `scratchpad/doorb_batch1.py --apply` (append-only, csv.reader/csv.writer roundtrip):
- **All 559 pre-existing physical lines byte-for-byte identical** (prefix-equality asserted line-by-line
  vs pristine) — zero collateral to any other row.
- Exactly **+1** physical line appended; byte-delta **+2021** (the new row only).
- Dialect preserved: **CRLF 0 / bare-LF 560 / CR 0** (pure LF, matching pristine +1 line).
- `053040`: 0 hits; new note contains zero 053040 references.

Regen: `sync_theory_status_map.py` → **Synced 556 claims**; `generate_lean_aggregates.py` rewrote
ClaimMap.lean + ActiveClosureIndex.lean.

**Guard board after BATCH 1 — ALL GREEN. KEPT.** validate_csv PASS · status-inflation PASS_NO_INFLATION
(556, 0/0/0, planted control fires) · book-ledger-sync PASS · physical-bridge PASS (556) · aggregates
--check PASS · ledger-green PASS · ownership PASS · **53-set 53/53 PASS 0 FAIL**. Row count **555→556**.
(status-inflation accepts the mint: PROOF-TARGET is not in the CLOSED set, lean_status=OPEN non-empty.)

---

## BATCH 2 — GAP-E row note upgrade `D0-WINDOW-9-13-DISSOLVE-001` (notes-only) — APPLIED

`release_status` = **CERT-CLOSED** (UNCHANGED), `lean_status` = **OPEN** (UNCHANGED) — asserted
byte-unchanged pre/post. Only the `notes` field grew, by a pure append prefixed `DOORB[2026-07-07]:`
recording the JUSTIFICATION upgrade: the upper bound is now OWNED-inheritance (door b via port-exhaustion
/ D0-GAP-E-PORT-EXHAUSTION-001), SUPERSEDING the operative-modulo-3-transfers footing of the GAPE-1011
note (T1/T2/T3 no longer load-bearing; the domain is GENERATED by the owned CORE degree-2 exhaustion, not
transferred). All prior notes (GAPE-1011 + FABLE + STATUS-FLIP) preserved verbatim.

Applied via `scratchpad/doorb_batch2.py --apply`:
- Field-level confinement asserted: **exactly one row differs** (index 542 = `D0-WINDOW-9-13-DISSOLVE-001`),
  and on it **only the `notes` field** changed; all 555 other rows byte-identical.
- Pure append verified (`b.notes == a.notes + APPEND`); byte-delta **+804**.
- release_status asserted == `CERT-CLOSED`, lean_status asserted == `OPEN` (no drift).
- Appended text contains NONE of the inflation markers (`open inside` / `not yet closed` /
  `proof-target still open`) — pre-asserted, so the CERT-CLOSED row does not trip the closed+open-inside fire.
- Dialect: **CRLF 0 / bare-LF 560 / CR 0**. `053040`: 0 hits.

Regen: `sync` Synced 556 claims; aggregates rewrote ActiveClosureIndex.lean.

**Guard board after BATCH 2 — ALL GREEN. KEPT.** validate PASS · status-inflation PASS_NO_INFLATION (556,
0/0/0, planted control fires) · book-ledger PASS · physical-bridge PASS (556) · aggregates --check PASS ·
ledger-green PASS · ownership PASS · **53-set 53/53**. Row count **556** (unchanged — notes-only).

---

## BATCH 3 — mint `D0-PORT-GOLDEN-SPLIT-BRIDGE-001` (THE 6.1.2 identity bridge, append-only) — APPLIED

Reading-bridge (identity-of-readings, both sides OWNED — template `D0-INVARIANT-GENERATION-BRIDGE-001`):
`x²=x+1 ÷x² ⇒ φ⁻¹+φ⁻²=1` = the owned external/internal weight split `W_ext+W_int=1`, `W_int=W_ext²`
(BOOK_06 THE 6.1.2). New row:
- `claim_id` = `D0-PORT-GOLDEN-SPLIT-BRIDGE-001`; `book` = `BOOK_06`; `section` = `06.1.2 port/golden identity bridge`
- `lean_module`/`lean_theorem` = EMPTY; `lean_status` = **OPEN**; `python_cert` = **EMPTY**
  (memo §7.4a provides NO Lean leg and NO dedicated cert — verified: grep of memo names none; the two
  on-disk `*golden*` cert files are unrelated GHP/Bartholdi certs)
- `uses_bridge_assumptions` = **False** (both sides independently owned — no external bridge assumption)
- `release_status` = **PROOF-TARGET** (reading-grade)
- `notes` = the identity + HONESTY clause: NOT a new forcing; content = definition-unfolding (`/x²`) +
  identification of two owned readings; strengthens door (b) by showing the two-port cap and the golden
  two-weight are the SAME owned 2. Cross-ref D0-GAP-E-PORT-EXHAUSTION-001 / BOOK_06 THE 6.1.2 /
  D0-INVARIANT-GENERATION-BRIDGE-001.

Applied via `scratchpad/doorb_batch3.py --apply` (append-only): all 560 pre-existing physical lines
byte-identical; +1 line; byte-delta **+1343**; dialect **CRLF 0 / bare-LF 561 / CR 0**; `053040` 0 hits.
Regen: `sync` Synced 557 claims; aggregates rewrote ActiveClosureIndex.lean.

**Guard board after BATCH 3 — ALL GREEN. KEPT.** validate PASS · status-inflation PASS_NO_INFLATION (557,
0/0/0, planted control fires) · book-ledger PASS · physical-bridge PASS (557 — the BRIDGE-named
PROOF-TARGET row with uses_bridge_assumptions=False is accepted) · aggregates --check PASS · ledger-green
PASS · ownership PASS · **53-set 53/53**. Row count **556→557**.

---

## HOLD (owner-gated — NOT done this pass; logged)
- Any `release_status` change on the GAP-E row `D0-WINDOW-9-13-DISSOLVE-001` — **HELD**; stays CERT-CLOSED /
  OPEN (BATCH 2 was a notes-only justification upgrade, no status flip).
- The completeness-quantifier Lean mint (the §7.2 domain sentence as a machine-checked categorical theorem)
  — **HELD**; the categorical leg of `D0-GAP-E-PORT-EXHAUSTION-001` stays PROOF-TARGET / lean_status OPEN.
- Book edits (B01:1562 EoR "door (b) taken", BOOK_06 THE 6.1.2 bridge note, base-4 anchor note beside
  D0-DETECTION-QUADRATIC-001) — **HELD** (not authored/applied).

## FINAL STATE

| batch | scope | verdict |
|---|---|---|
| 1 | mint `D0-GAP-E-PORT-EXHAUSTION-001` (PROOF-TARGET / OPEN, cert close_gap_e_dimensional_check.py) | **APPLIED** |
| 2 | GAP-E row `DOORB[2026-07-07]:` note upgrade (status byte-unchanged CERT-CLOSED / OPEN) | **APPLIED** |
| 3 | mint `D0-PORT-GOLDEN-SPLIT-BRIDGE-001` (PROOF-TARGET / OPEN, no cert/leg — reading-bridge) | **APPLIED** |
| — | GAP-E status flip · completeness-quantifier Lean mint · book edits | **HELD (owner-gated)** |

- **Row count: 555 → 557** (exactly 2 mints; BATCH 2 notes-only).
- **Byte-delta total +4168** = 2021 (b1) + 804 (b2) + 1343 (b3), **fully accounted, zero unexplained bytes**.
- **Confinement vs pristine (whole-file diff):** NEW ids = {`D0-GAP-E-PORT-EXHAUSTION-001`,
  `D0-PORT-GOLDEN-SPLIT-BRIDGE-001`}; REMOVED = none; CHANGED shared rows = exactly
  {`D0-WINDOW-9-13-DISSOLVE-001`}, field changed = `notes` only. No other row touched.
- **Dialect PRESERVED (pure LF, the true on-disk state):** CRLF 0 / bare-LF 561 / CR 0 throughout.
- **`053040`:** 0 hits in CSV; no 053040-named file touched.
- **Generated files:** theory_status_map.csv + theory_graph.json + ClaimMap.lean + ActiveClosureIndex.lean
  regenerated via tools only (never hand-edited).
- **Guard board GREEN after every batch** (validate · status-inflation PASS_NO_INFLATION + planted control ·
  book-ledger · physical-bridge · aggregates --check · ledger-green · ownership · 53-set 53/53).
- **No git commit** (HEAD unchanged: `8a2e9d9`).
