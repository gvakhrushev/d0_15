# T2′ CLOSING MEMO — the X5 lepton postulate reduces to one identification (DRAFT, no status claimed)

**Author:** chief researcher. Status: DRAFT reduction argument; conditional on the W8 sweep; edits no
registry row. Inputs: `TASK_W3_REPORT.md`, `TASK_W6_REPORT.md`, `TASK_W2_REPORT.md`.

## 1. State after W3/W6 (facts of record)

1. The third lepton branch **exists internally**: E₀, the rank-1 trivial isotype of
   `ℂ[Q₈] ≅ ℂ⁴ ⊕ M₂(ℂ)` with ranks (1,4,3), already carried as the unramified electron entry
   ("exact and THE": `Q8Terminal.lean`, `LeptonPuiseuxUniquenessObstruction.lean:73`).
2. The 7-point no-go carrier is the (1,4,3) system with E₀ removed; the restriction locus is a bare
   literal `orbitSizes := [4,3]` — **SILENT/CONVENTION**, no forcing recorded (W6 §b).
3. `D0-X5-LEPTON-CONTRACT-001` postulates an **assignment** (a `Fin 3 → Fin 3` bijection under 5
   laws), not an existence (W6 §a).
4. Residual gap (W6 §d, one sentence): a canonical bijection between the intrinsically-rank-ordered
   Q₈ triple (1,4,3) and the S₃-symmetric scene generation triple (Tr(T²)=3).

## 2. The reduction argument (DEF-0.2.2 shape, conditional)

**Premise Π (to be verified by TASK W8):** no owned invariant of the corpus distinguishes the three
scene generation labels other than through the branch data itself — i.e. every owned
generation-tagged claim is S₃-equivariant in the generation labels (the label-freedom recorded at
`FinitePathRepresentation.lean:49` and `D0-REPRESENTATION-RECONSTRUCTION-MAXIMALITY-NOGO-001` holds
corpus-wide, with mass decimals staying EFT/IR passport).

**Argument.** Assume Π. Any two bijections Q₈-triple → generation-triple differ by an S₃ relabeling
of the scene side; by Π this changes no distinguishable outcome. A mandatory choice that changes no
distinguishable outcome is not physical structure — it is exactly a **format convention** in the
sense of BOOK_00 §00.9 ("format conventions are allowed when they add no information"), and M1+
canonicalization fixes the representative mechanically (size-keyed: rank 1 ↦ first, 4 ↦ second,
3 ↦ third — the same order-by-invariant rule the corpus uses elsewhere). Therefore **the bijection
component of the X5 contract carries zero physical content**: postulating it as a physical primitive
would promote a naming convention to structure, which the corpus's own discipline forbids.

**What the contract cannot be reduced away from.** W6 §e names the honest remainder (inherited from
W3's mode-2 caveat): the identification

```
P_iso :  E₀-isotype (representation-theoretic, sourced from Ω₈)
         =  generation mode (phason-dynamical, sourced from the scene, PhasonStrainGenerations)
```

is a genuine cross-category identification and is currently **unpaid**. This — not the bijection —
is the physical content of the lepton postulate.

**Conclusion (conditional on Π).** `D0-X5-LEPTON-CONTRACT-001` over-postulates: its content
decomposes as (naming convention, fixed by M1+ for free) ⊕ (one identification P_iso). The minimal
honest contract is P_iso alone. The matter-sector "third generation problem" reduces from "construct
a missing branch" (already dissolved by E₀) plus "choose an assignment" (a convention) to **one
sharply-posed question: exhibit the owned object that carries the Q₈ isotype structure and the scene
generation structure simultaneously.**

## 3. The attack surface for P_iso (where an owner could come from)

`UnifiedTheorem.lean:31–36` states the branch orders (1,4,3) "appear **simultaneously** in the Q₈
Fourier ranks and the return orders." If the return-order structure and the phason-mode structure are
readings of ONE owned operator, P_iso acquires an internal owner and the X5 contract empties
entirely; if they are two objects with only the integer pattern in common, P_iso stays a postulate
and the corpus should say so. This dichotomy is checkable — it is W8's second charge.

## 4. Obligations created

1. **W8 sweep (gates §2):** verify Π corpus-wide; any owned generation-distinguishing invariant
   found ⇒ the reduction fails at that row — name it.
2. **W8 dig (feeds §3):** trace the "simultaneously" of `UnifiedTheorem.lean:31–36` to its proof
   term: same operator or same integers?
3. If both return favorable: draft the re-scoped contract statement (plan-only, per W6 §e inventory)
   and submit to the closure protocol; the prose owners describing the third datum as "genuinely
   external" (BOOK_04 §04.v15:170–171, `X5_CLAIM_OWNER_MATRIX.csv:5`) enter statement-tension and
   must be re-read, never silently edited.
