# T2′ FINAL STATE — after two accepted kills: the exact minimal contract (document of record)

**Author:** chief researcher. Status: consolidated endpoint of the T2′ campaign after skeptic passes
#1 (killed forcing v1) and #2 (killed forcing v2). Both kills accepted in full. No registry row
edited. This document supersedes the *targets* of the earlier memos, not their surviving facts.

## The two kills, accepted

- **v1** died on its own citation (torus memory is abelian; order-encoding expelled to the defect).
- **v2** died on **the mirror**: its θ/⊥M1 sentence, run at the 11-leg with the corpus's role-side
  reading of the V₁₁ dyad (§01.22: ABCD = D₂², the dyad is the base of the role square), derives
  11↔E₃ = m′ by the identical step. Under the uniform strong reading the registration premise is
  satisfiable by NO bijection — both zone extensions are ±-collapsed role content and E₄ contains
  no ±-invariant function. A rule no assignment satisfies cannot force one. Also accepted:
  Horn 2 of v2's dichotomy is false — the bit IS consumed downstream (the dressed kernel attaches
  the z⁴ Feshbach factor to Q₁₁ and z³ to Q₁₃, feeding the exponent row), so "pure naming ⇒ empty"
  does not hold; a consumed convention is exactly the X5-contract shape.

## The structural fact the mirror exposed (new, machine-grounded)

**E₄ is extension-invisible.** The zone tower's added capacities (V₁₁: D₂; V₁₃: ABCD) are entirely
abelianization-side (±-invariant; home = E₀⊕E₃ = ½(I+L₋₁), verified); the spinorial/faithful block
E₄ is reachable only through Ω₈ itself at V₉. Consequence: **no extension-based key can ever decide
which zone carries E₄** — the two dead forcings are not two accidents but one theorem-shaped
impossibility. Any future attempt through zone extensions should be refused on this ground.

## What stands (reviewer-confirmed across both passes)

1. Third-branch **existence** is internal: E₀, the trivial isotype (unramified electron).
2. The **holomorph module** (L_i, α) with conjugation-invariant block signatures; Umu = L_i|⟨i⟩-span
   verbatim; E₀ the common fixed block. (`w8_holomorph_check.py`, 18/18 PASS, negative control live.)
3. The **base leg 9↔E₀** — agreed by every party and every key.
4. **m′ has zero positive owned support** (skeptic #2, charged with saving it): the faithfulness key
   *inverts* when made owned (ℂ[V₄] has kernel exactly Z — anti-faithful), and the return-order key
   (THE-status material: muon owned-located at zone 11, exponent row exact THE with owned keying
   "muon through the 4-cycle, tau through the 3-cycle") lands on **m** at the 11-leg. Every coherent
   owned-material chain converges on m = (9↔E₀, 11↔E₄, 13↔E₃); none supports m′.

## The exact residual (the minimal honest X5 content)

Convergence is evidence, not forcing (their own E-accounting: family-coherence strengthens, never
promotes). The residual is precisely:

- **R1 — the licensing bit.** Which keying rule is owned? All owned-material keys select m, but each
  keying rule ("extension homes in the block" — now dead by the mirror; "resident return-order types
  the block") is itself unowned. The unique live owner-candidate: the muon chain
  (muon ↔ 1/4, owned THE) → (1/4 ↔ 4-cycle, owned orbit-keyed exponent map) → (z⁴ factor ↔ Q₁₁)
  — whose last weld lives in the **dressed kernel of the unified chain, status
  CONDITIONAL-EXTENSION** ("the holonomy λ … chosen, not forced", UnifiedTheorem.lean:12–16).
  So R1 reduces to a previously-known, well-defined front: **un-conditionalize the unified chain**
  (own the λ=1 slice or force λ). No new nail; the old one, precisely located.
- **R2 — the structural scene attachment.** The Lean gap of record: no map `TorusShell → Shell3` /
  `TorusShell → radius`; the abbrev transports cardinality only.

Minimal honest X5 lepton contract = {R1, R2}. This is the campaign's deliverable: not a closure,
but the exact shape and location of what remains — down from "postulate the third branch and the
full branch→row assignment" to one conditionality-promotion (R1) and one Lean attachment (R2), with
all owned evidence pointing at a single matching (m) and a proven impossibility guarding against
false forcings through zone extensions.

## Corrections of record carried forward

"Forced role dictionary" retracted (axis = Out(Q₈) gauge); §00.8 citation mis-addresses noted;
"class functions" in v2's P3 must read **Z-coset functions** (Q₈ conjugacy classes split {1},{−1}
and the 2-dim character lives in E₄ — the coset reading is the one all checks implement).
