# CLOSE_GAP_E_LEAN_LIFT — the 13th pass: Lean lifts of the door-(b) arithmetic (PortPowerExhaustion), the 7th-pass factor/block theorems (FactorBlockLaw), and a KILLED fork-adjudication attempt (ZoneReadingFork) — POST-SKEPTIC, WIRED, MINTED (owner-authorized)

> ## ⚖️ VERDICTS (three independent skeptics, 2026-07-17/18):
> **Skeptic #13 (PortPowerExhaustion): SURVIVES-WITH-REPAIR** — repair applied (zone_bound
> docstring re-conditioned from "only port⟺kind" to the FULL four-part conditioning: Channel A
> port⟺kind reading, Channel B `:860` falsifier, ladder `:1548/:1816`, base-V₉ reading; `1 ≤ k`
> grounded at B01:1556/OWN-5). No kill: cap genuinely inherited (not a numeral), no resurrection
> of the dyad-power generator, negative control real, no registry conflict.
> **Skeptic #14 + #14b (FactorBlockLaw): WOUNDED → all six wounds discharged, SURVIVES.**
> Wounds: coordinate-only quantification presented as general (named second object: the diagonal
> summand `⟨(1,1,1)⟩ ≤ ℤ₂³`); "Krull–Schmidt" title with no KS statement; "exactly {2,4}"
> without realization; native_decide; undisclosed Python-derived `(1,1,6)`; "composes" across
> distinct ambients. Repairs: group-grade Lagrange theorem `no_subgroup_size_three_five_six`
> (ALL subgroups, via `AddSubgroup.card_addSubgroup_dvd_card`), requalified docstrings, KS
> disclaimer, `ladder_coordinates_realized`, kernel `decide`, `(1,1,6)` disclosure,
> corroborates-not-composes. #14b re-verified each item: all discharged, no new overclaim.
> **Skeptic #15 (ZoneReadingFork v1): KILLED — accepted in full, no defense.** Executable second
> object: the identical method proves `¬(zone9 ⊆ zone11)`, refuting the base inclusion Reading A
> itself owns — the headline theorem was a disjoint-union tautology on an AUTHORED carrier
> (`Sum V9 (Sum V11 V13)`), begging the question against Reading C; the carrier's ownership
> claim was false (M2's consumer carrier is a zone-9 diagonal with identity padding; CKM NOT-fed).
> Errors of record: EoR-1 (authored carrier presented as owned), EoR-2 (tautology presented as
> the §2b adjudication). v2 reduced to the surviving cardinal legs only.

**Status:** WIRED + MINTED under explicit owner authorization (2026-07-18 session goal: full
completion, refactoring and status-raising allowed). This pass performs NO new forcing — it
lifts already-adjudicated arithmetic to machine-checked grade and records one kill. The
completeness quantifier's CATEGORICAL leg is untouched (PROOF-TARGET, forcing-reading, exactly
parallel to `D0-DETECTION-QUADRATIC-001`'s own categorical leg).

**Pre-flight (2026-07-17):** `preflight.sh GAP-E window 15 17 SCENE-TRIPLE z3`, then
`KrullSchmidt factor X3 block-count`, then `TOWER-STOP fourth zone NOEXT tower-stop` — owners
found and cross-referenced: rows 546/547/549/560, row 522 parity owner, row 257 COUNT-3,
row 264 DETECTION-QUADRATIC. Prior family re-read: DYAD-POWER, RAISE-7th, OWNER-10th (LEG 1/2),
DIMENSIONAL-12th (§6 attacks, §7 mint package), F3_FORK_ADJUDICATION_MEMO.

---

## §1. What was built (all compile; full `lake build D0.All` green, 4018 jobs)

1. **`D0/Tower/PortPowerExhaustion.lean`** — the module `CLOSE_GAP_E_DIMENSIONAL_MEMO.md` §7.1
   proposed. Machine-checked ARITHMETIC leg of door (b): `portCap := Fintype.card ComparisonKind`
   with `portCap_eq_two := two_comparison_kinds` (inheritance, not a numeral); `port_power_sizes`
   (`{2,4}` under cap); `ladder_sizes_realized`; `six_not_port_power` (cap-independent, monotone
   dyad powers); `eight_needs_third_port` (cap-gated); `eight_readmitted_at_cap_three` (the
   can-fail negative control); `rivals_killed`; `zone_bound` (`z₃ ≤ 13`, FULL conditioning named);
   capstone `port_power_exhaustion`.
2. **`D0/Tower/FactorBlockLaw.lean`** — Lean lift of the 7th pass's Sub-lemmas 2/3 with a
   STRENGTHENING beyond the Python: `no_subgroup_size_three_five_six` at GROUP grade (Lagrange,
   all subgroups of `ℤ₂³` — the Python's `factor_sizes` only enumerated coordinate subproducts);
   `factor_sizes` / `proper_factor_sizes` / `ladder_coordinates_realized` (coordinate sizes
   exactly `{2,4}`, both realized); `block_count` + `coord_fibers_uniform` (kernel `decide`);
   `X3_not_factor_shape` (`{1,1,6}` vs every `replicate (2^k) (2^(3−k))`); contrast lemma;
   bundle `factor_block_law`. Ownership hinge NOT claimed; KS uniqueness NOT formalized;
   `(1,1,6)` disclosed as Python-derived input.
3. **`D0/Core/ZoneReadingFork.lean` v2** — post-kill reduction: kill record in the header
   (EoR-1/EoR-2 enumerated), surviving cardinal legs only: `role_card_split`
   (`|Role| = |Dyad ⊕ Dyad| = 4 = 2+2`, cardinal-only; product≠coproduct non-canonicity stays
   narrated), bundle `zone_tower_cardinal_legs`. FORK-OPEN untouched.

## §2. What was minted (registries + book; owner-authorized; backups in session scratchpad)

- `CLAIM_TO_LEAN_MAP.csv` + `03_THEORY_MAP/theory_status_map.csv`, row
  `D0-GAP-E-PORT-EXHAUSTION-001`: lean fields → `D0.Tower.PortPowerExhaustion` /
  `port_power_exhaustion` / `LEAN_PROVED` (ARITHMETIC leg only — note spells this out);
  `release_status` UNCHANGED (`PROOF-TARGET`, held by the categorical leg alone).
- Row `D0-WINDOW-9-13-DISSOLVE-001`: LEANLIFT note (FactorBlockLaw corroboration, distinct
  ambient).
- Row `D0-ZONE-TOWER-READING-FORK-001`: KILL RECORD note (skeptic #15; fork stays OPEN).
- Row `D0-DETECTION-QUADRATIC-001`: BASE4 anchor note (§7.4(b) of the 12th memo; not a mint).
- `D0/All.lean`: three imports added; `lake build D0.All` green (4018 jobs) — this also
  discharges the referee's "kernel build pending" caveat for this working copy.
- BOOK_01 §01.20 section source
  (`0023__01.20__capacity-closure-of-four-terminal-roles-a-b-c-d-.md`): "Window final state —
  UPDATE" paragraph appended (door (b) taken; upper-SEALED at owned-inheritance/operative grade;
  categorical leg PROOF-TARGET; reopening hooks per the 12th memo). `assemble_books.py` re-run,
  `check_book_assembly.py` PASS (idempotent).
- Guards re-run after every mint: `check_no_dangling_lean_module`, `check_book_ledger_sync`,
  `check_book_cert_references`, `check_lean_builds` — all PASS.

## §3. What this pass does NOT do

- Does NOT machine-check the categorical leg ("two ports/kinds exhaust the ladder") — it stays
  a forcing reading, PROOF-TARGET, and `release_status` of row 560 is unchanged for exactly
  that reason.
- Does NOT close or adjudicate the F3 reading fork (row 547 OPEN; the §2b contradiction is an
  OWNERSHIP fact — absence of any owned `V11 ↪ V13` — with no Lean-internal formalization; the
  v1 attempt to fake one is the recorded kill).
- Does NOT resurrect the dyad-power generator, the factor-lattice ambient as owned, or any
  killed route; does NOT claim Krull–Schmidt uniqueness in Lean.
- Does NOT edit row 549, the H7 candidate, or the stop-rule (which is now moot for the window:
  door (b) taken).

## §4. Reopening hooks (inherited, unchanged)

- Row 560: sever BOTH channels — deny port ⟺ comparison-kind AND read `:860` as a
  consistency-check — or exhibit a third comparison kind (which re-admits `8 = D₂³`;
  machine-exhibited by `eight_readmitted_at_cap_three`).
- Row 547: the owned `radius → vertex-set` functor, or an owner identification
  `Role ≅ Dyad ⊕ Dyad` (proved non-canonical as product vs coproduct; only an OWNED
  identification would count).

## §5. Session propagation note

The 12th memo's §7.3 asks to update the `d0-gap-e-final-stop-rule` session memory; no memory
directory exists for the d0_15 project in this environment — this memo is the durable record
instead.
