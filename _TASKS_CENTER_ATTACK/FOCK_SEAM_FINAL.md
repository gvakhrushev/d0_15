# FOCK SEAM FINAL — seventh kill accepted; corrections applied; the honest α-residual (record)

**Author:** chief researcher, adjudicating skeptic pass #6. The candidate is REFUSED as a
realization; all kill lines accepted. One genuine lead extracted from the strongest finding and
verified. No registry row edited.

## Accepted kills (enumerated)

1. **No Feshbach architecture existed:** no B, C, no W_eff(z), no coefficient extraction — the
   cert computed `angle · 2¹¹` (the ledger arithmetic in an eigenvalue costume; the binomial
   identity Σ C(11,k) = 2¹¹ makes the "dimension" and "top eigenvalue" readings indistinguishable
   by any check).
2. **Depth-2 exactness kills every positive invertible archive:** μ₃ = Tr(BD̃²Bᵀ) > 0 unless
   B = 0; the top-blade seam is a divergent geometric series (λ_max·u ≈ 580). Any future
   realization must supply an archive that annihilates depth-≥3 seam excursions — nilpotent-type,
   not Γ-lift-type.
3. **The archive is Lean-pinned at dim 30** (`archive_dim_thirty`, fingerprint {24×8, 22×10, 20×12});
   a 2048-dim archive with no refinement map contradicts the owner. Named second object: the owned
   archive itself.
4. **Owner 2 (dominant pole) is dead on owned text:** §02.13.4's functional is a finite sum over
   ALL archive poles; "dominant pole / top blade" occur nowhere in the corpus.
5. **Owner 1 is sign-catalog-loaded:** under the signed S₁₁-action Λ¹¹ is the **sign
   representation** — coupling into the top blade requires an orientation of the 11 modes, exactly
   the sign catalog §01.22's +2-argument bans. And the §01.22 saturation posture is lcm-*covering*,
   which lifts to *all* blades, against top-blade concentration.
6. **My exclusion-chain conclusion was a non sequitur:** the bound constrains spectral radius, not
   dimension (a 1-dim archive [required-ratio] meets the moments trivially). "The Fock carrier is
   forced" is RETRACTED; the honest derived statement is the radius requirement below.

## The extracted lead (verified; observation-grade, anti-numerology flag attached)

The skeptic's deficit-of-exactly-3 is the fingerprint of the **owned asymmetric moment
normalization**: the amplitude cert derives μ₁ = Tr(floor)/rank (the /rank sits INSIDE μ₁), while
μ₂ is unnormalized. The uniform functional M_k = Tr(BD^{k−1}C) that our edge cert froze was OUR
convention, not the owned one. Under the owned asymmetric convention the required operator ratio is

```
Tr(BD̃C)/Tr(BC) = μ₂/(rank·μ₁) = 12288/5 = 2457.6   (not 36864/5 = 7372.8)
```

— verified exact. Both exclusion legs SURVIVE the correction (edge witness 15.86 and the linear
radius table ≤ 44 are far below 2457.6 too), so no prior kill is disturbed; but every future
realization attempt must target 2457.6, and the coincidence λ_max((6/5)·2^N̂) = 2457.6 is recorded
as an OBSERVATION ONLY (the candidate carrying it is dead on kills 2–5 regardless).

## The honest C1-α residual (per the skeptic, adopted verbatim in substance)

- **P1 stands exactly as registered:** Res_D0(W_eff) = Δα via the 2¹¹ active–archive pairing —
  not reduced, not relocated, not one clause.
- **Three true new facts** from this round's work: vertex seam ≡ 0 (theorem); the exact edge
  mismatch witness (84971/5356); the radius requirement — any symmetric-seam realization needs an
  archive block of spectral radius ≥ 2457.6 (owned convention), which **nothing owned supplies**,
  and which combined with kill 2 (depth-2 exactness ⇒ near-nilpotency) and kill 3 (dim 30) now
  gives a sharp three-way constraint set any future realization must satisfy simultaneously:
  `{radius ≥ 12288/5} ∧ {depth-≥3 seam-annihilating} ∧ {factors through the owned dim-30 archive}`.
  These three constraints are mutually hostile (large radius vs nilpotency vs small dimension) —
  the realization problem is now posed as a concrete, probably-very-hard operator existence
  question, and a future NO-GO ("no owned operator satisfies all three") is equally a legitimate
  closing move.
- Behind it: the saturation clause (both owners dead/loaded), the uniform-vs-weighted ledger
  counting gap, the 30-vs-2048 identification.

## Process note

Seventh consecutive kill of an author forcing/candidate. The system continues to work as designed;
the campaign's durable products remain the theorem-grade layer of `CAMPAIGN_FINAL.md` plus this
round's three new exact facts and the three-way constraint set — which is the sharpest statement
of the α-realization problem the corpus has had.

## Post-verdict sharpening (verified): the realization is forced into the DIRECTED class

**Symmetric impossibility (two lines, verified):** for any symmetric archive D with C = Bᵀ,
μ₃ = Tr(BD²Bᵀ) = ‖DBᵀ‖²_F, so μ₃ = 0 ⇒ DBᵀ = 0 ⇒ μ₂ = 0. **Depth-2 exactness with nonzero μ₂ is
impossible in the self-adjoint class entirely** (strengthens skeptic kill 2 from "positive
invertible" to "any symmetric").

**The directed escape exists and is already owned:** a nilpotent directed transport N (N² = 0)
gives exact depth-2 truncation with nonzero μ₂ (verified: μ₃ ≡ 0 exactly, μ₂ ≠ 0). The corpus's
own α-holonomy row `D0-ALPHA-HOLONOMY-LINEAR-FORM-001` (Iter26, CORE-FORMALIZED,
`D0.Spectral.SeamTransportLinear`) runs on precisely this mechanism — "DIRECTED transport N
(CP-seam crossing, N² = 0)... exp(sN) = I + sN EXACTLY... nilpotency proven LOAD-BEARING."

**The α-realization problem, final form:** a directed (non-self-adjoint) seam through the owned
dim-30 archive with N-type truncation (owned mechanism) and moment pair
(Tr(BC)/rank, Tr(BNC)) = (1/3, 12288/5) under the owned asymmetric normalization. The working
route the amplitude cert itself names (lines 84–91: BLOCKED_RESIDUE_ROUTE / WORKING_ROUTE = seam
holonomy) is the same directed-seam architecture. The construction question is now fully typed;
the radius constraint transmutes (non-symmetric: the bound argument no longer applies as-is —
Tr(BNC) is not spectrally bounded by ‖N‖ against Tr(BC) in the same way), which is exactly why
the directed class can reach 12288/5 where every symmetric carrier provably cannot.
