# GAP_E_COMPLETENESS — extension-list completeness `{D₂, ABCD}`: honest underdetermination with a two-route missing-object spec (DRAFT v2, post-skeptic-#1 — R3 verdict UPHELD; repair log at end)

**Status:** DRAFT candidate; no registry row edited; no .lean added to the built tree. Pre-flight run: `EXTENSION COMPLETENESS TOWER-STOP ABCD DYAD` — no existing row owns "extension-list completeness"; cross-referenced owners: D0-TOWER-STOP-NOEXT-001 (count only), D0-ABCD-001, D0-OMEGA8-001, D0-CARRIER-FULL-FORCING-001, D0-SCENE-TRIPLE-UNIQUE-001, D0-DETECTION-QUADRATIC-001 (row 264: "STRENGTHENS obligation 5, NOT a separate machine-checked categorical theorem").
**Companion check:** `_TASKS_CENTER_ATTACK/gap_e_completeness_check.py` — **27/27** check() calls PASS, rc=0 (count printed by the script's own counter, not hand-tallied — E1 discipline). Exact integer arithmetic only; scan bound declared (`z ≤ 21`); every check compares an independently computed quantity to a fixed target.
**Parent:** `WINDOW_9_13_FORCING_MEMO.md` W-HI.2 (GAP-E). Task charter: this is a hard M1 meta-step; an honest underdetermination with a sharp missing-object spec is a valid outcome; **no fit is forced here** (and §III shows a cheap fit is computationally blocked).

## Verdict up front

**GAP-E does NOT close.** Route R1 (owned enumeration principle) is a **near-miss**: the skeleton is owned (extension COUNT = 2, inheriting the count's declared-reading status, E2.1; |ABCD| = 4 forced both directions *given* the role assignment, E2.4; once-only assembled from owned pieces, E4), but ONE predicate — type-identity of capacities — is missing, and §III computes that its two natural candidates disagree on the owned tower itself, so it cannot be silently filled. Route R2 (capacity saturation) is **dead**: the §01.22 ledger runs strictly downstream of the extension list; its one weld identity `φ(44)=20=d₁₃` holds for the rival tower `(9,11,15)` too (computed). Route R3 delivers the outcome: a **sharp two-route missing-object spec** (§R3), either route of which — once owned — pins `(9,11,13)` uniquely against every computed alternative (§IV of the script).

Three structural findings sharpen the gap beyond the parent memo's statement of it:

- **F1 (dual guard — REFRAMED per skeptic #1, repair E-GE-2).** At the *narration* level the upper end is guarded **twice, independently**: the §01.20 extension list (G-CAP) and the `+2`-minimality clause (G-STEP, §01.22 CORE-FORCING + §00.2 Darboux "strictly" + BOOK_03:1058). Either alone pins `(9,11,13)` (computed, kill matrix — both pins independently reproduced by the skeptic). Both are narrated; **neither is Lean**. **The two guards are UNEQUAL (skeptic):** G-STEP's only *printed* ⊥-proof is `det(T^{n+2})=det(T^n)` (BOOK_01:1899; BOOK_03:1042-47), which kills only orientation-PARITY-flipping steps — but `+4` **also** preserves the orientation class, so the words "minimal"/"any other step" (1899/1905, BOOK_03:1058) are conclusion-words the printed argument never reaches. G-STEP is therefore **one-sentence-killable** by a referee; only G-CAP is robust. So the honest statement is: Lean/cert layer — single point of failure (cert literally `10>9`); narration layer — one robust guard (G-CAP) + one soft guard (G-STEP). The earlier "must kill two independent narrated arguments" burden is withdrawn.
- **F2 (condensation loss — CORRECTED per skeptic #1, repair E-GE-1; this is the strongest finding).** The audit ledger (`03_THEORY_MAP/GOLDEN_COVERAGE_LEDGER.csv:811`) records that the v17 layer's Darboux forcing was **dropped in condensation** ("symplectic non-degenerate phase space extends only by conjugate coordinate pairs (+2), so alternative layer spacings violate the conjugate-pair rule"). **But "theorem-form" over-credited it (error of record):** the skeptic recovered the v17 text at git `a83cdb0` (v17 BOOK_01:217-223) — "Theorem 1.8" is a theorem-**label** + a 3-sentence proof *gesture* (E-b-shaped: "extends only by conjugate coordinate pairs (+2)"; "alternative layer spacings violate the conjugate-pair rule"), NOT a proof. **And its cited executable certificate `vp_minimal_holographic_carrier.py` NEVER EXISTED at any commit in git history (git-verified) — a THIRD phantom cert of the exact fabrication class already in `_QUARANTINE/QUARANTINE_LEDGER.md`.** So the accurate F2 is: **"asserted once with a fabricated-style verification pointer, lost in condensation"** — evidential weight ZERO for the clause's truth; re-derivation must be from scratch. The only unmined seed is the v17 kill-sketch "larger gaps introduce rational phase-locking" (a NAMED lead for E-b). **This phantom-cert fact belongs on the owner's quarantine-consideration list** and upgrades ATT-4 from prudence to necessity.
- **F3 (extension-layer fork — mirrors the 9-fork).** The corpus owns TWO structurally different readings of the same extension arithmetic, and GAP-E's *shape* depends on which one is meant:
  - **over-base**: `V₁₁ = V₉ ⊔ D₂`, `V₁₃ = V₉ ⊔ ABCD` — extensions `{+2, +4}` both over the pointed shell (§01.20:1548-1556; §01.22:1827-1833; Lean **types** `FiniteTypes.lean:15-16`, `V11 := Sum V9 Dyad`, `V13 := Sum V9 Role` — note `V11 ⊄ V13` as types);
  - **consecutive**: `9 →(+2) 11 →(+2) 13` — steps `{+2, +2}` (BOOK_03:1049-1056 ladder; Lean **numerals** `CarrierForcing.lean:81-82` `address_ladder`; the §00.2 Darboux route; the nesting narration BOOK_03 §03.23.7 "Shell(13) ⊃ Torus(11) ⊃ Defect(9)").
  They agree numerically **only because** `|ABCD| = 2·|D₂|` (4 = 2+2 = 2×2; script §I). No owned statement identifies them (trap i/d: two objects until proven one). Under over-base, the missing object is a *capacity-list completeness clause*; under consecutive, it is a *no-skip clause*. The fork is Lean-vs-Lean (`FiniteTypes` types vs `address_ladder` numerals), same as the parent memo's `8+1` vs `4+5` fork one layer down.

## Claim E (DEF-0.2.2 form — candidate, an underdetermination claim, not a forcing)

**E.** The completeness of the zone-extension list — "the admissible extensions of the pointed shell `V₉` are exactly `{D₂, ABCD}`, hence `max zone = 13` and no tower `(9,11,z₃)` with `z₃ > 13` exists" — is **not owned** by any Lean module, cert check, or defined-predicate narration in the active corpus. What is owned is a **skeleton** (§E1-E4 below) that reduces the gap to exactly ONE missing clause, statable in two equivalent-in-effect forms (§R3); absent that clause the M1-admissible tower space is the one-parameter family `(9, 11, z₃)`, `z₃ ∈ {13, 15, 17, …}` (computed; `z₂ = 11` via the maturity-threshold centre reading — dropping that too widens it to the 15-tower L0 family in range). `(9,11,15)` dies **only** on narrated clauses; `(9,11,13,15)` dies on **owned** COUNT-3 — the two rival towers named in the charter die at different layers, and only the first is GAP-E's.

*Scope honesty:* E asserts underdetermination **at the owned-formal layer**. It does NOT assert the corpus is silent — §01.20's list plus the `+2`-minimality clause are real narrated guards with CORE-FORCING status language, and the soft-joint register (BOOK_01:1617) *names their upstreams* ("the role lists and the +2 parity"). E is the precise statement of what a maximally conservative referee can refuse to grant.

## Owned pre-facts (verbatim, file:line)

### E1. The bare list and its exclusions — all downward, none upward

**E1.1** `BOOK_01…md:1548` (§01.20, heading :1521):

> "The next two shell extensions are the only primitive unresolved capacities over the pointed shell: the direct/return dyad and the full terminal role square."

The completeness claim of the corpus, in full. "Primitive" and "unresolved" are **never defined as predicates** anywhere in the active corpus (searched: the phrase occurs exactly twice — the monolith and its §-split copy).

**E1.2** `BOOK_01…md:1556`:

> "The construction rules out alternatives: `V8` has no basepoint, `V10` has an extra hidden marker, `V11` cannot be replaced by `V10` without losing direct/return capacity, and `V13` cannot be replaced by `V12` without losing one terminal role."

The four exclusions cover the **interior** gaps of the tower — V8 (below base), V10 (marker surplus between 9 and 11 — the witness-repeat kill, consistent with once-only), V12 (one role short of the square, twice). **No owned sentence excludes anything ABOVE 13** — `V₉ ⊔ X` with `|X| ∈ {3, 5, 6, 8, …}`, i.e. nothing kills `(9,11,15)` or `(9,11,17)` by name.

**E1.3** The cert leg is content-free exactly here — `05_CERTS/vp_v1141_abcd_omega8_v9_phi_capacity.py:64`: `check('V10 duplicates witness/hidden state', 10>V9, 'extra state')` — literally `10 > 9`; the "3-role alternative" check (`:61`) is `3 < D2*D2`; **there is no V₁₅/V₁₇/composite-extension check at all** (whole cert read).

### E2. What IS owned at the upper end

**E2.1 COUNT (owned-Lean, with declared reading).** `NoExtension.lean:61-65` (three algebra facts), docstring `:25-27`: "the tower stops at the three rungs `[9,11,13]`. The COUNT (3 slots, no 4th) is proved here; the role-NAMES … are the operational reading". `BOOK_05…md:371` carries it as closed: "the tower-stop M1 no-extension meta-step, closed as a no-go". **Corollary (this memo, arithmetic): the extension COUNT is owned = 2** (3 zones − 1 base; inherits the count's declared-reading status, not upgraded here). What is not owned is the *value* of either extension.

**E2.2 CASE-2 kill machinery (owned-Lean, stated for zones).** `NoExtension.lean:47` `repeat_has_nontrivial_copy_symmetry : 1 < Fintype.card (Equiv.Perm (Fin 2))`; `:52` `first_instance_canonical`. Its scope, per the cert `vp_zone_repeat_catalog.py:4-5`: "Suppose a fourth zone Z4 REPEATS an existing structural type (shell-for-shell, memory-for-memory, ...)" — the argument is owned for a **fourth zone repeating a type**, not for the *capacity content* of zones 2-3.

**E2.3 CASE-1 slot bijection (owned-written).** `vp_member_zone_isomorphism.py:20-22`: "The map {slot -> type} is a bijection of 3-element sets: p<->DISTINGUISH, p^2<->PRESERVE, 1<->CLOSE … each type from exactly one slot and each slot to exactly one type." Docstring honesty: the COUNT is proved; the names are the reading.

**E2.4 |ABCD| = 4 forced BOTH directions — conditional on the role assignment.** `BOOK_03…md:910-935` (§03.23.1): "`D_anchor < 4`: some role is unrealized … Banned. `D_anchor > 4`: empty address slots appear; to tell an empty slot from a significant one you must import an exogenous significance catalog. Banned." Plus the four-operator-role completeness table at `BOOK_01…md:672-746` (§01.7) and `roles_card_four` (`CarrierForcing.lean`). **So IF zone-3's extension is "the terminal role alphabet", its size is owned-forced = 4.** The IF — the assignment shell↦role-square — is a role list.

**E2.5 The `+2` step guards (narrated, CORE-FORCING status language).**
- `BOOK_01…md:1899-1907` (§01.22): "the minimal admissible junction step is `+2`" … "Status: CORE-FORCING". Lean owns evenness only (`det_T_pow`); the *minimality* word is narrated (parent memo D2/D3, confirmed).
- `BOOK_03…md:1058`: "Any step other than `+2` re-imports an external gluing parameter, so `{9,11,13}` is the unique M1-admissible spectrum."
- **`BOOK_00…/0004__00.2__primitive-thesis.md:52` (monolith :183) — the sentence the parent memo did not cite:** "**Discrete Darboux extensions.** The structural carrier expands **strictly** via conjugate symplectic pairs (`+2`), generating the unique `K(9,11,13)` tripartite tensor network…" — an active-corpus, extension-**type**-quantified sentence ("strictly" = the no-skip clause, in one word). Its citation trails off mid-sentence ("…). for the Darboux-route statement." — a dangling reference; the full statement is the dropped v17 Theorem 1.8, F2 above).

**E2.6 Size-distinctness of zones (owned reasoning, different question).** `BOOK_01…md:1518` (D0-CARRIER-NOT-ICOSAHEDRAL-001): "an *equal*-zone `K(n,n,n)` would admit the full `S₃` zone-swap, but `9≠11≠13` kills it." Owned as a theorem ABOUT `K(9,11,13)`; used in this memo (assembled, §E4) as the once-only kill.

**E2.7 The soft-joint register covers GAP-E's upstreams by name.** `BOOK_01…md:1617`: "The aliasing step is stated but not formalized — this is a soft joint of the corpus, alongside **the role lists and the +2 parity**." The parent memo's repair A-1/E2 stands (the *label* "extension completeness" is absent from the register) — but this memo adds the refinement: G-CAP rests on the **role lists** and G-STEP on the **+2 parity**, i.e. the register names both upstreams of GAP-E without naming GAP-E. The corpus's own bookkeeping is one indirection away from this gap, not silent on it.

### E3. What is owned and IRRELEVANT (negative findings, each checked)

- **J_scene cannot own completeness** — disclaimed in-corpus, `BOOK_01…md:880-900` (§01.8): "Each squared term has its target as its own additive constant … `J_{scene}` cannot force the scene"; co-ownership routed at `:900` to §01.20 + §01.22.
- **No owned δ₀ → |ABCD| derivation** (task-charter hint checked, unfounded): `BOOK_01…md:728` owns `δ₀ = (p₊−p₋)/2 = 1/(2φ³)` as the centered half-gap; |ABCD| = 4 is owned by §01.7 operator completeness / 03.23.1 / `D₂×D₂` — no text derives 4 from δ₀. (Matches the parent memo's D4 negative on `2δ₀ ↔ +2`.)
- **No owned dyadicity principle for alphabets** (searched `dyadic/ternary/trit` in BOOK_00/BOOK_01): §01.22:1827 says "The first **dyadic** extension" as a *name*, not a rule "all alphabets are dyadic". The parent memo's OB-2/3 sketch ("non-dyadic ⇒ exogenous alphabet catalog") remains a sketch with no owned text.
- **R2 dead (computed, script §V).** The §01.22 ledger — `q_T = lcm(4, V₁₁) = 44`, `m_T = 4+3 = 7`, `φ(44) = 20 = d₁₃`, `B = 5`, `L = 2·33+5 = 71`, `q_EW = 710`, `m_EW = 113`, `φ(710) = 280 = 35·8` — reproduced exactly from `(9,11,13)`; recomputed from `(9,11,15)`: `d_top = 9+11 = 20` and `q_T = 44` and `φ(44) = 20` are **unchanged** (the weld never sees `z₃`), and the one candidate a-priori identity `φ(q_T) = d_top` **holds for both towers**. Downstream `q_EW = 750 ≠ 710`, `m_EW = 115 ≠ 113` change — detection after the fact, not forcing; nothing owned pins `φ(q_EW)/8 = 35` a priori. Direction of dependence is strictly one-way: capacity chain → ledger.

### E4. Assembled results (owned pieces, new composition — flagged as assembly, not ownership)

- **Once-only / no-reuse (kills "second D₂" in-tower).** Over-base re-use of any capacity produces two equal-size zones; equal-size zones admit a nontrivial zone-swap (`|S₂| > 1`, Lean `NoExtension.lean:47`) — by the owned size-distinctness reasoning (E2.6) that swap is exactly a which-copy catalog ⇒ `⊥M1`. So the tower does **not** leak at re-use: `(9,11,11)` dies assembled; `(9,11,13,15)` (the charter's "second D₂ giving 13+2=15") dies earlier, on **owned COUNT-3** (any 4-zone tower does; script §IV). **The tower leaks at exactly one coordinate: the size of the top zone in a 3-zone tower.**
- **Kill matrix (script §IV, `z ≤ 21` declared).** L0 (Lean-owned today: base-9 + count-3 + step-evenness): **15 towers pass**, family unbounded analytically (no owned constraint references the top size). L0+ (add the window-free centre reading, `z₂ = ` smallest Lucas `> Ω₈ = 8`, [Iter25] `BOOK_01…md:1500` + `level_five_forced` numerics): survivors = `(9,11,z₃)`, `z₃ ∈ {13,15,17,19,21}` — **`(9,11,15)` passes**. G-CAP alone → `{(9,11,13)}`. G-STEP alone → `{(9,11,13)}`. **Either narrated guard suffices; they agree; neither is Lean.**

## The scoping dilemma — why the missing predicate cannot be silently filled (script §III)

The natural closure of R1 is: "an extension is inadmissible iff it repeats structure already in the base" (CASE-2 transferred from zones to capacities). Computed on the owned inventory (`witness 1, D₂ 2, {±} 2, ABCD 4, Ω₈ 8`; base `V₉ = (ABCD × {±}) ⊔ {ω₀}`):

- **P_broad** (X shares a constituent alphabet with the base): kills `witness, D₂, ABCD, Ω₈` — **everything, including both owned extensions** (`ABCD` is a factor of `Ω₈`; `D₂` is a factor of `ABCD`). Too strong: destroys the owned tower. Not the corpus predicate.
- **P_narrow** (X is whole-alphabet-identical to a base layer): kills exactly `{witness, Ω₈}` — the right residue `{D₂, ABCD}` survives, `(9,11,17)` (the `+8` tower) dies. **But P_narrow is this memo's coinage**; no owned text states it, and nothing owned explains why the direct/return dyad `D₂` is *not* "the same type" as the `{±}` dyad or the two dyad factors inside `ABCD` (four dyad instances coexist in the owned construction, distinguished only by role names — the role lists again).

The choice between P_broad and P_narrow is **exactly** the missing owned object; NC4 (anti-fit control) shows the broad choice leaves no admissible extension at all, so a fit cannot be waved in — it must thread between too-strong and coined. This is the computational content of "do not force a fit".

## R3 — the missing object, EXACT-MISSING grammar

Per the exogeneity hinge (BOOK_00 DEF 0.3.1, verbatim via `TRICHOTOMY_V2_MEMO.md:19-23`: "θ counts as exogenous iff it (1) is not derived inside the corpus, (2) affects a distinguishable result or a law's formulation, and (3) is not an unavoidable part of the distinguishability protocol") — the missing object is ONE owned clause, in EITHER form:

**E-a (capacity route / over-base fork).** An owned predicate `AdmExt(X)` — "X is a primitive unresolved capacity over `V₉`" (the corpus's own phrase, §01.20:1548, currently undefined) — with:
  (i) an owned **type-identity** relation on capacities settling P_broad vs P_narrow (the §III dilemma) — equivalently, an owned statement of *which role-typings individuate dyad instances* (the "role lists" soft joint, BOOK_01:1617, promoted from list to rule);
  (ii) an owned **primitivity** clause killing composites (`6 = 2 ⊔ 4` = one zone realizing two necessity-slots — the member↔zone bijection E2.3 used as an admissibility rule, not a written fact);
  (iii) the proof `{X : AdmExt(X)} = {D₂, ABCD}` — quantifier: `∀X (AdmExt(X) → X ≅ D₂ ∨ X ≅ ABCD)`.
  Given (i)-(iii) plus owned COUNT-3 and the assembled once-only, `(9,11,13)` is unique (script §IV, G-CAP row).

**E-b (step route / consecutive fork).** An owned M1-forcing of junction-step **minimality** — the no-skip clause: "the junction step is the minimal orientation-class-preserving step (+2); a non-minimal even step imports [the skip] as an exogenous parameter". The corpus owns the *pattern* twice — `D_anchor > 4` ⇒ "empty address slots … exogenous significance catalog" (BOOK_03:910-935) and `N > 9` ⇒ "empty steps … catalog of significance" (§01.22 cycle-normalization forcing) — and owns the *conclusion-word* twice ("minimal", §01.22:1899-1907; "strictly", §00.2:52 Darboux). Missing: the one sentence transferring the empty-slot pattern to junction steps. **Named begging-point (pre-registered):** the transfer presumes the skipped address (13, when stepping 11→15) already *is* a slot that can be "empty" — a pre-laid-out address space, which is close to assuming the ladder. The dropped v17 Darboux theorem (F2) is the historical form of this clause; the coverage ledger's own action field says `integrate`.
  Given E-b plus owned COUNT-3 and base-9, `(9,11,13)` is unique (script §IV, G-STEP row).

**Either clause closes GAP-E; the two are independent (capacity vs orientation, per §01.22's own "neither leans on the other"). The underdetermination is honest: the corpus contains both patterns, names both conclusions, and owns neither derivation.**

## Named risks & PRE-REGISTERED attack surface

- **ATT-1 (strongest): "role-algebra enumeration is your coinage."** Correct, and pre-registered as such: the owned-alphabet inventory principle, P_narrow, and the slot→alphabet map are this memo's constructions; the corpus owns the *phrase* "the only primitive unresolved capacities" (§01.20:1548) but defines neither predicate. If the skeptic produces an owned definition of "primitive" or "unresolved" (or an owned type-identity rule for dyad instances), §III collapses — and the verdict IMPROVES from R3 to a closable R1; that outcome is welcomed, not defended against. The sweep found none (phrase occurs twice, both bare).
- **ATT-2: the once-only assembly over-reaches.** `BOOK_01:1518`'s zone-swap argument is a theorem about the icosahedral question, not an admissibility rule; `|S₂|>1` is owned for *zones repeating a type*. Transferring to equal-**size** zones is this memo's move. If rejected: the L0/L0+ families are unchanged (re-use towers were killed by strict-size-distinctness inside `tier_L0`, itself the same assembly — noted in-script), and the headline `(9,11,z₃)` family is unaffected for `z₃ > 11`. Downgrade path: mark once-only as narrated-only; nothing else moves.
- **ATT-3: the centre tier is narrated.** `z₂ = 11` as "smallest Lucas `> Ω₈`" is the [Iter25] reading with Lean numerics (`level_five_forced` checks odd levels only; minimality is parity-free per the parent memo's E4). If the skeptic strips it: the residual family widens from 5 to 15 towers in range (both computed and printed) — GAP-E's shape is unchanged, `z₃` remains the free coordinate, `z₂` joins it. Both tiers are reported precisely to make this attack costless.
- **ATT-4: F2 cites an audit artifact about quarantined text.** `GOLDEN_COVERAGE_LEDGER.csv:778/:811` is a coverage-audit table mapping v17 → active corpus; v17 itself is quarantined (fabricated-citation incident). F2 is therefore claimed ONLY as "documented condensation loss + never-executed `integrate` action + cert absent from `05_CERTS/`", NOT as "an owned theorem exists elsewhere". The active-corpus owned residue of the Darboux route is the single sentence §00.2:52.
- **ATT-5: fork-mixing.** G-CAP and G-STEP implement the two forks of F3; their agreement on `{(9,11,13)}` is a computed convergence, NOT an owned identification of the readings (trap d/i/j: convergence ≠ forcing; two different extension-structures until an owner identifies them). Any future closure memo must pick ONE fork and cross-cite the other, exactly as the parent memo ruled for `8+1` vs `4+5`.
- **ATT-6: "the dual guard weakens the referee case, so GAP-E is overstated."** No: F1 relieves the *narrative* exposure and leaves the *formal* exposure exactly as the parent memo scored it (W-HI.2). The correct statement is now two-layer: Lean/cert layer — single point of failure, cert literally `10>9`; narration layer — two independent CORE-FORCING-labelled guards. Both halves are load-bearing and neither substitutes for the other.

## What this does NOT show

- It does NOT close GAP-E, does NOT force P_narrow, does NOT upgrade `+2`-minimality beyond narration, does NOT restore the v17 Darboux theorem, and does NOT derive the extension list from M1 in Lean.
- It does NOT identify the over-base and consecutive readings (F3 stays a fork; flagged, like the 9-fork, for its own task).
- It does NOT change any registry row, cert, or built Lean file; the parent memo's W-HI.2 row stays accurate with one refinement (the dual-guard two-layer statement, F1/ATT-6).
- The `(9,11,13)` tower remains: fully guarded at narration level (twice over), open at the owned-formal level at exactly one coordinate (`z₃`), with the missing clause specified in two ownable forms (E-a / E-b).

## Follow-up targets (ranked by cost, per this memo's findings)

1. **E-b via §00.2:52 + §01.22:1899-1907** — one clause, both conclusion-words already in active text; the empty-slot pattern owned twice; the begging-point (pre-laid address space) must be confronted head-on, not hidden.
2. **E-a(i) via the role-list soft joint** — promoting the dyad-instance typing (direct/return vs write/erase vs ABCD axes) from list to rule; simultaneously discharges the register's own "role lists" joint (BOOK_01:1617).
3. **Darboux re-integration audit** — execute or formally retire the `integrate` action of ledger rows 778/811 (owner decision needed on citing the quarantined layer). **NOTE (E-GE-1): the v17 cert it points to is a phantom — re-derive from scratch, do not "restore".**

## Skeptic #1 verdict + repair log (2026-07-05, accepted in full)

**Overall: the R3 honest-underdetermination verdict STANDS; the E-a/E-b missing-object spec is exact and independently verified.** Script 27/27 confirmed by independent count; NC4 genuinely conclusion-failable; every E-a sub-clause re-checked missing; R2 death re-confirmed with extra robustness (no downstream identity discriminates: `gcd(q_EW,m_EW)=5` kills only `z₃=15`, 17/19/21 stay coprime; `m_EW`-primality unowned; `φ(q_EW)/8` integral for all rivals). F3 fork confirmed REAL (`FiniteTypes.lean:15-16` Sum-types vs `CarrierForcing.lean:81-82` `address_ladder`, plus BOOK_03:1064 nesting `Shell(13)⊃Torus(11)⊃Defect(9)` structurally contradicting the Sum types — agree only via `4=2+2`; corpus must adjudicate).

**Wounds repaired (errors of record):**
- **E-GE-1 (F2, strongest):** "theorem-form" → "theorem-label + gesture, E-b-shaped"; the cited cert `vp_minimal_holographic_carrier.py` is a **third phantom cert** (git-verified absent at every commit), fabrication-class per `_QUARANTINE/QUARANTINE_LEDGER.md`. Evidential weight zero; on the owner's quarantine list. (F2 bullet rewritten above.)
- **E-GE-2 (F1):** guards unequal — G-STEP is one-sentence-killable (`+4` also preserves the orientation class the printed `det` argument checks); dual-guard *computation* stands (both pins reproduced), the "two independent narrated arguments" *burden* withdrawn. (F1 bullet rewritten above.)
- **E-GE-3 (ATT-2, once-only assembly):** `BOOK_01:1518` is a decidable negative about **A₅ embedding** (icosahedral question); the zone-swap sentence is a counterfactual aside; the `⊥M1` "equal-size zones" reading is owned only for **a fourth zone repeating a type** (`vp_zone_repeat_catalog.py:4-5`), not for tower admissibility in general — the transfer is this memo's move (E4 assembly, correctly flagged as assembly, now scope-tagged). Script `tier_L0` docstring over-claimed "strictly increasing" as Lean-owned — it is not; dropping it widens L0 15→28 towers and L0+ admits `(9,11,11)` (killed only by the same assembly). Both counts are printed; label corrected.

**Skeptic gifts adopted (named material, not owners):**
- **G-1 (E-a(ii) material):** an owned primitivity PATTERN the sweep missed — `BOOK_01:491` / `BOOK_02:670`: "`p+p^k=1` with `k>2` is **not primitive**: it contains the quadratic return plus additional unresolved iterations" — non-decomposability in the branch-equation domain. Using it for *capacities* needs the same unowned transfer as E-b; it is candidate material for the E-a type-identity predicate, not an owner.
- **G-2 (third-route failure, closes a would-be shortcut):** the Lean capstone `scene_triple_unique` (registry row 530) is conditional on `{+2-ladder, window [9,13]}` — the first hypothesis IS G-STEP's unproved content and the window presupposes 13 (its own cert's control shows `[9,29]` admits a second centre `L₆=18`) — so the capstone **cannot** substitute for GAP-E.
- **G-3 (preflight hazard):** `09_LEAN_FORMALIZATION/D0/Extensions/AdmissibleExtensionCategory.lean:34` defines `ExtObject.admissible` — a NAME COLLISION with E-a's `AdmExt`: it is the D0-X5 **theory-extension** layer (no-manual-data ∧ no-post-hoc over 5 contract components), NOT a carrier-capacity predicate. Future preflights must not confuse them.
