# SHELL-RULE-R — the axis/register membership selector: 13/13 on the frozen Core-13 shells, exact-null p = 1.44e-5, built from owned scene structure with four named post-hoc connectives (DRAFT — SKEPTIC #1 VERDICT ACCEPTED: S2-as-derivation KILLED (bijection joint fitted; see ledger); target-specialness upheld at 1.85e-7 (free family); grade = candidate (post-hoc, owned-vocabulary-motivated, structural signals net-adverse). ATTEMPT 4 EXECUTED 2026-07-04: root-asymmetry rebuild returns CLEAN UNDERDETERMINATION — no selector survives; δ-strata reframe reduced to a value-echo; six missing owned pieces named; see ledger + shell_rule_r_attempt4.py., pre-skeptic)

**Status:** DRAFT; consolidated memo of the SHELL-RULE-R campaign (attempts 1–4,
ledger `SHELL_RULE_R_LEDGER.md`); no registry row edited, no cert minted, no status file touched.
Vocabulary cap: **candidate selector (post-hoc, structurally motivated)** — never "derived/forced".
The confirmed target was KNOWN during construction; every claim below is graded accordingly.
**Campaign state after attempt 4: NO live selector.** S2 stays killed; the root-asymmetry
rebuild (constraints (a)-(d)) produced no survivor — every uniform root-connected selector
scores 1-2/13, at or below the structure-null floor (2; null mean 5), and the uniform affine
class over owned integer fields ceilings at 5/13 = chance. Outcome: "root-asymmetry material
underdetermines membership", with six named missing owned pieces (M1-M6, ledger). The
target-specialness fact (1/5,405,400) is UNTOUCHED and remains the campaign's hard residue.

## Claim (candidate language)

There EXISTS a total, mass-blind membership selector R: {13 core slots} → {subsets of {A,B,C}}
assembled from owned scene structure (zone-generation binding, the A₂ charge-projection axis, the
exact lepton exponent row with its owned sheet-typing, the Q₈ block signatures, and the owned
W/Z/γ/H placement texts) that reproduces the confirmed Core-13 shell membership EXACTLY
(13/13 slots, including the {μ,s} exclusion and the {b,d} double-membership as mechanical
OUTPUTS of the rule), with exact structure-matched null probability 1/69,300 = 1.44e-5
(Bonferroni ×4 selectors: 5.77e-5; union-bounded over its entire 96-variant connective space:
still ≤ 1.44e-5). The selector's raw ingredients are all cited owned text; FOUR connective
joints are not owned and were resolved knowing the target — this memo names them and
pre-registers them as the attack surface.

Target (frozen, Q2-confirmed at exact p = 8.66e-05, LOO 13/13, circle-specific; slot 1 = γ light
node, ν_e displaced by the by_id dedup): A = {γ, b, d, e, u}, B = {τ, W, c, Z}, C = {b, d, t, H},
excluded = {μ, s}, A∩C = {b, d}.

## Owned pre-facts (verbatim, file:line — the raw material; nothing here is re-derived)

1. **THE 04.6.M1.gen** (BOOK_04:912): "The generation index is the zone label 9/11/13 of the
   minimal scene. … center/point → e, torus/cycle → μ, shell/sphere → τ."
2. **THE 04.6.M1.charge** (BOOK_04:910): charge is "the linear functional projection on the
   weight lattice" and "projecting the equilateral-triangle vertices on the scene's
   center↔shell symmetry axis yields exactly +2/3, −1/3, −1/3." (The down value is written
   TWICE — the off-axis conjugate pair; the up value once — the on-axis vertex.)
3. **§04.CVFT.F3c** (BOOK_04:406): V_shell = span{|9⟩,|11⟩,|13⟩} ≅ ℂ³ — the finite shell
   carrier the three-orbit geometry is identified with.
4. **Exponent row, exact THE with owned keying** (BOOK_04:1113, 1135): "(p_e, p_μ, p_τ) =
   (0, 1/4, 1/3): the electron unramified (0), the muon through the 4-cycle (1/4), the tau
   through the 3-cycle (1/3)"; sheet types at BOOK_04:1939: "The 4-cycle and 3-cycle blocks
   encode terminal role capacity and scene-rank holonomy sheets." Reaffirmed as "THE-status
   material … owned keying" in T2_PRIME_FINAL_STATE.md.
5. **Electron = forced base register** (BOOK_04:964-996): "the base register is forced, not
   nominated"; "the electron is now formally the unique terminal calibration register …
   it cannot be done by a table rewrite."
6. **THE 04.8.L.1** (BOOK_04:1046): "The muon is an excitation localized in the memory zone
   (the torus, address 11)." (The only charged lepton with an owned bulk-localization clause.)
7. **Q₈ block signatures, machine facts** (w8_holomorph_check.py, 18/18 PASS; recorded in
   T2_PRIME_FINAL_STATE.md): E₀ = the common fixed/trivial block ("unramified"); E₄ = the
   unique block with L₋₁ = −1, i.e. the unique block on which the order-memory register
   [Q₈,Q₈] = Z(Q₈) = {±1} (BOOK_01:804-830, §01.7.1B, CORE-FORMALIZED D0-OMEGA8-CENTER-001)
   acts nontrivially.
8. **Zone↔block identification m** (T2_PRIME_FINAL_STATE.md): the forcing was killed twice;
   what stands is "9↔E₀ agreed by every party", "m′ has zero positive owned support", and
   "every coherent owned-material chain converges on m = (9↔E₀, 11↔E₄, 13↔E₃)" — an
   X5-residual identification (R1: the z⁴↔Q₁₁ weld is CONDITIONAL-EXTENSION, "lambda chosen,
   not forced"; R2: TorusShellAttachment.lean is DRAFT plumbing), NOT a theorem.
9. **Loop order** (BOOK_00:168): "a torus is abelian ⇒ loop order is not encoded ⇒ defect";
   (BOOK_01:794, §01.7.1A blade 2): "a completed readout records the order of its operations."
10. **W/Z/H placement text** (REM 04.6.M1.mH, BOOK_04:926): "Vector bosons W,Z set the edges
    of the vacuum cell; the Higgs scalar H sets its diagonal (breathing mode)." **H owner**
    (BOOK_04:1615): "The Higgs is then a macroscopic phason condensation in the V13 shell."
    **Doublet register** (BOOK_04:1377): "SU(2) doublet splitting Q = T3 + Y, T3 = ±1/2";
    **THE 04.6.M1.gauge** (BOOK_04:908): "double-braid ⇒ SU(2)".
11. **Terminal matter triad** (BOOK_04:643-665): "stable macroscopic records use a charged
    terminal register, a neutral leakage branch and a massless line carrier"; γ = "massless
    line carrier; core readout" (BOOK_04:1702). **T2-P1**: V₉ = Ω₈ ⊔ {ω₀} — zone 9 is the
    unique zone hosting the terminal role cycle.
12. **Neutrino** (BOOK_04:631-641, §04.4.9): "neutral bulk phason wave with zero active EM
    coupling" — a BULK object; ν_R ∈ ker A is zone-balanced (30-dim of 33), not zone-localized
    (BOOK_04:1392).
13. **Matter/vacuum split** (REM 04.6.M1.Λ, BOOK_04:922): matter = zones 9+11; vacuum/terminal
    = shell 13.

## The construction (selector S2; the other three attempts are logged failures in the ledger)

Zone first, seat second. Every slot gets zone(gen) by pre-fact 1 (H → 13 by pre-facts 10/13);
the zone→circle bijection 9→A, 11→B, 13→C is CARRIED FROM ATTEMPT 1 (score-selected, not owned
— named risk N-2). Then the seat rule, by owned structural type:

- **Up-type quarks (u, c, t)** — the axis-invariant A₂ vertex (pre-fact 2): the projection is
  faithful on them; single seat at the zone shell. → u→A, c→B, t→C.
- **Down-type quarks (d, s, b)** — the degenerate conjugate pair (pre-fact 2: "−1/3, −1/3"):
  their charge IS the axis projection, so the pair is STORED by that projection. Formalizing
  the owned "center↔shell symmetry axis" inside the owned carrier V_shell (pre-fact 3) as
  span{|9⟩,|13⟩}: a pair member in an axis zone survives projection but its image is
  DEGENERATE (two states, one image — distinguishing them would need a catalog, ⊥M1), so the
  pair occupies BOTH axis shells; the projection ANNIHILATES |11⟩, so the zone-11 pair member
  has no image and no seat. → d→{A,C}, b→{A,C}, s→excluded.
- **Charged leptons (e, μ, τ)** — by the exact exponent row (pre-fact 4): e is UNRAMIFIED and
  the forced base register (pre-fact 5) → seats on the unramified base block E₀ (pre-facts
  7, 8; double word-match "unramified") → A. μ rides the 4-cycle = terminal ROLE capacity
  sheet: its winding lives in role space, not shell space — the shell chart registers only
  shell-space structure → no seat → excluded. (Secondary flavor, same output: a zone-11
  resident's order-datum cannot register on zone 11's own register — self-catalog, ⊥M1-shaped;
  ingredients pre-facts 6-8.) τ rides the 3-cycle = SCENE-RANK sheet: it winds the 3-zone/shell
  triple itself; a cyclic winding's distinguishing datum is its loop ORDER (pre-fact 9), and
  the unique block that registers order is E₄ (pre-fact 7) → seat on the E₄ shell → B.
- **W, Z** — the doublet-splitting ± register's own quanta (pre-fact 10: T3 = ±1/2;
  SU(2) = double-braid, and a braid crossing is an order-of-operations datum, pre-fact 9)
  → seat on the order-register block E₄ → B. Consistent with the mH line read as: the
  "edges" of the vacuum cell = the shared matter|vacuum seam (the 11-boundary), vs H = the
  cell's own diagonal → C (pre-fact 10, second clause + pre-fact 13).
- **γ** — the massless line carrier of the terminal readout triad (pre-fact 11) seats with
  the terminal role cycle's zone (9) → A. **H** — V13 condensation (pre-fact 10) → C.

**Full table:** γ→A, b→{A,C}, d→{A,C}, τ→B, W→B, μ→∅, s→∅, c→B, Z→B, e→A, u→A, t→C, H→C.
This equals the frozen target on all 13 slots; the {μ,s} exclusion and {b,d} doubling are
mechanical OUTPUTS of the stated rule.

## Verification (all numbers exact, no sampling)

Scorer: reimplementation of `shell_membership_v2.py::all_assignments()` (asserted 69,300
members; frozen target asserted ∈ family); metric = attempt-1 metric extended to 13 slots
(set equality per slot; partial = nonempty proper subset). Results:

- S2: 13/13 exact; null P(agree ≥ 13) = **1/69,300 = 1.443e-5** (the target is the unique
  family member agreeing on ≥13 slots); Bonferroni over the 4 attempted selectors: 5.77e-5.
- Logged failures: S1 (zone+boson anchors) 8/13, p = 1.04e-3; S3 (base-anchor doubling)
  9/13, p = 2.89e-4; S4 (all-ℤ₂→B strawman) 8/13, p = 1.52e-3. None reproduces the pattern.
- **Ablation over the full connective space** (4 down-pair storages × 4 lepton placements ×
  3 W/Z options × 2 γ options = 96 variants; e, H, up-type owned-fixed): agreement histogram
  {13:1, 12:3, 11:6, 10:12, 9:17, 8:19, 7:20, 6:14, 5:4}. Exactly ONE variant hits 13/13 —
  S2 — and it is the only variant whose prediction lies inside the structure family at all.
  Union bound charging the whole space: p ≤ 1/69,300.
- Out-of-construction tests: **T1 FAIL** (no owned radius functional; the owned Lean radial
  order inner9 < core11 < outer13 transported through the bijection predicts r_A < r_B < r_C,
  contradicted by the frozen chart order r_B = φ²r₀ < r_A ≈ φ⁴r₀ < r_C ≈ φ⁶r₀; attempt-1's
  tension stands; the rung VALUES do match the owned AF Dirac² even ladder {φ⁰,φ²,φ⁴,φ⁶},
  BOOK_02:1295, but no owned zone→rung map exists). **T2 PRE-REGISTERED**: ν_μ and ν_τ →
  EXCLUDED under core extension (pre-fact 12: bulk phason, zone-balanced kernel; the T3-mirror
  alternative ν_μ→B/ν_τ→C is rejected on the owned bulk text — a target-blind discrimination).
  Falsifier: any future fit seating a neutrino on a circle kills the lepton logic. **T3**: the
  exclusion/doubling follow from the rule, but the ablation shows the connectives were
  target-informed (see attack surface).

## Named risks & PRE-REGISTERED attack surface (strongest first)

- **N-1 (strongest): the selector was assembled knowing the target — which pieces are
  genuinely forced vs decorated?** Honest decomposition: OWNED-FORCED: e→A (forced base
  register + double "unramified" word-match), H→C (V13 condensation), up-type zone seats
  (given the bijection), the μ/τ sheet-type ASYMMETRY (owned keying: μ↔4-cycle/role-capacity,
  τ↔3-cycle/scene-rank), the doubled "−1/3, −1/3" in the owned charge text. MY CONNECTIVES
  (target-informed, 96-variant space, exactly one winner): (c1) degenerate axis image ⇒
  residence on BOTH axis shells (vs nearest-single/own-zone/register — nothing owned selects);
  (c2) sheet-winding-space ⇒ shell-chart visibility (μ out) and winding-order ⇒ E₄ seat
  (τ→B) — the "seat = block that registers the branch's datum" principle is not owned text;
  (c3) W,Z seat ON the register they generate (vs the vacuum-cell-own-shell reading of the
  same mH sentence); (c4) γ seats at the terminal-triad zone (vs the abelian/E₃ reading).
  The skeptic should attack each connective's a-priori status; the defense is only the
  post-assembly textual echoes plus the ablation arithmetic, not ownership.
- **N-2: the zone→circle bijection (9→A, 11→B, 13→C) is score-selected**, inherited from
  attempt 1 (5/10 vs ≤3/10 for the other five bijections; calibration 0/69,300 at ≥7). Its
  only owned anchor (H/V13 → C) is read off the target — circular as a derivation. If the
  bijection falls, S2 falls entirely.
- **N-3: the 11↔E₄ leg is an identification, not a theorem.** T2′'s forcing died twice; m
  survives as the convergent X5-residual with a CONDITIONAL-EXTENSION weld (λ chosen, not
  forced) and DRAFT Lean plumbing. Every E₄ statement in the construction inherits R1+R2.
- **N-4: compositionality.** The two exclusions use two different mechanisms (s: axis
  annihilation; μ: role-space winding) — the classic overfitting signature. Mitigation, not
  defense: the mechanisms track the particles' OWNED structural types, which differ (conjugate
  pair vs ramification sheet); a single-mechanism account is not available in owned text.
- **N-5: T1 failed.** The selector family cannot say why the E₄ shell is INNERMOST
  (r_B = φ²r₀); the owned radial order predicts the opposite of the chart. Either the chart
  radii are not the Lean radial coordinate (supported by attempt 2a's dead torus profile) or
  the bijection is wrong (contradicting the membership score). Unresolved; do not paper over.
- **N-6: the null conditions on the target's structure** (excluded pair fixed, sizes 5/4/4,
  |A∩C| = 2). Skeptic #1's larger free-exclusion family (5,405,400) would dilute p by ~×78
  naively (→ ~1.1e-3 scale, still < 0.01), but the honest computation for S2's k*=13 under
  the free family has not been run — pre-registered as the first skeptic task.
- **N-7: the axis formalization** (axis = span{|9⟩,|13⟩} in V_shell; π|11⟩ = 0) is my
  rendering of the owned phrase "center↔shell symmetry axis"; the owned sentence is about the
  A₂ WEIGHT triangle's projection axis, and identifying weight-space geometry with V_shell
  geometry is exactly the kind of two-different-spaces rhyme the corpus has previously
  rejected (cf. the Iter25 Galois-vs-Weyl "seductive coincidence rejected out loud").

## What this does NOT show

- Does NOT derive the shells from the scene; "shells derived from the scene" stays FORBIDDEN
  language (Q1 negative of SHELL_MEMBERSHIP_V2_RESULTS.md is unchanged).
- Does NOT lift the grading cap: T1 failed, T2 is pending data, T3 exposed four target-informed
  joints. "Candidate selector (post-hoc, structurally motivated)" is the ceiling here.
- Does NOT resolve the radius/bijection tension (N-5), does NOT promote the 11↔E₄
  identification (N-3), does NOT touch any NO-GO (lepton-branch 2<3, R1 representation,
  DSIGMA-ROLE-CYCLE, hypercharge Φ) — the selector NEEDS the lepton-branch NO-GO's two-branch
  structure and is consistent with it, but does not repair it.
- Does NOT edit the registry; CLAIM_TO_LEAN_MAP.csv untouched; no cert minted for any claim
  in this memo.
- The forward content that CAN be attacked with future data is exactly two items: the ν_μ/ν_τ
  exclusion prediction (T2) and the totality of the rule on any core extension (every new slot
  gets a forced assignment from its owned type; no re-tuning without changing a named
  connective, which would be logged as a new attempt).
  [SUPERSEDED by skeptic #1 (K3: the ν falsifier is untestable under the frozen protocol) and
  by attempt 4, which replaces it with two protocol-computable falsifiers — see below.]

## Attempt 4 outcome (2026-07-04) — root-asymmetry rebuild: CLEAN UNDERDETERMINATION

Full record: ledger "Attempt 4" section; archived script `shell_rule_r_attempt4.py` (runs the
audits, both exact nulls, the ceiling class, and the reframe echo tests deterministically).

- **Reframe verdict (constraint (a)):** "circles = δ-degree strata / even-ladder rungs" does
  NOT survive as a meaning: no owned rung semantics exist for D∈{6,8,10} (ladder anchors only
  at D=1, D=4; cert range D=1..8; "rung" absent from the books); the owned even-ladder object
  (AF Dirac², BOOK_02:1295) carries multiplicities {3,8,21} ≠ occupancies {4,5,4}; the fitted
  circles are not concentric (|cB−cC|=13.35 vs r_B=2.56). What survives is a VALUE ECHO:
  size exponents log_φ(r)=(1.952, 4.091, 5.895) vs (2,4,6) with r₀≈0.977≈1=Q(4); LOO-stable
  11/13 folds; mass-scramble null 0/200 (vs (2,4,6)) and 2/200 (vs any even triple). The
  bijection joint is DISSOLVED, not resolved: under the rung reading m_rad has nothing to
  contradict, but the rung-typing joint is equally unowned.
- **Selectors (all scored vs both exact nulls, 69,300 fixed + 5,405,400 free):** R4-S1a/b
  cascade-generation (audit-DEAD: unowned offset, 3 parity readings) 1/13; R4-S2 charge-branch
  2·|3Q| (the ONLY audit-clean uniform root selector) 1/13; R4-S3a/b sheet-order (mechanizes
  the μ-wound: the owned keying uniformly seats μ→A) 1/13; R4-S4 K1-compliant zone control
  (m_rad) 2/13. All at or below the fixed-family agreement FLOOR of 2 (mean 5.0). Uniform
  affine ceiling over (g,|3Q|): 210 assignments, max 5/13 = chance (p_fixed=0.61).
- **Metric lemma:** no single-valued slot→shell rule can exceed 11/13 (the {b,d} doubling
  needs a multi-valued mechanism); the only root-native two-valued mechanism (branch pair,
  doubled) places the pair on {2,4}, {4,6} or {2,6} with nothing owned selecting — a 1-of-3
  fitted joint of exactly the killed-bijection shape (R4-L1).
- **Missing owned pieces (M1-M6, verbatim in ledger):** rung semantics for D=6/8/10; a
  mass-blind per-slot ℚ(φ)\ℚ invariant (Galois is empty on the all-rational per-slot data);
  a fixed generation↔cascade offset; one principle covering bosons+fermions; an owned
  pair-placement mechanism; the {4,5,4}-vs-{3,8,21} occupancy account.
- **Protocol-computable falsifiers (replacing the killed ν item):** (i) any refit/LOO/new-mcd
  rounding of the exponent triple away from (2,4,6) kills the value echo (margin 0.105;
  fragile folds Z, H); (ii) a fresh-seed scramble null at chance kills it.
- **No promotion.** Nothing here lifts any cap; "shells derived from the scene" stays
  FORBIDDEN; the campaign's hard residue is unchanged: the frozen membership is a
  1/5,405,400-special assignment that neither the zone reading, nor the register reading,
  nor the root asymmetry currently reaches.
