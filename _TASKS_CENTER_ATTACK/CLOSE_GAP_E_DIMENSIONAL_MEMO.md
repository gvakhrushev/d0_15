# CLOSE_GAP_E_DIMENSIONAL — DOOR-(b) forge: does DIMENSIONAL / PORT exhaustion of the `:1548` capacity inventory close GAP-E?

> ## ⚖️ VERDICT: **OWNED-CLOSURE (conditional-but-discharged).** The port-power exhaustion is a
> THEOREM given the cap; and — the decisive finding of this forge — **port-count = 2 is OWNED-AS-CAP,
> not a primitive instance.** It is carried by the CORE-owned two-kind exhaustion `D0-DETECTION-QUADRATIC-001`
> (BOOK_01:1130) and enforced by the `:860` falsification hook (">4 roles ⇒ scene FAILS"). This is the
> **12th pass** and the FIRST to supply the open DOMAIN sentence rather than re-localize it.
> (author pass 2026-07-06; **independent skeptic §05.8.R — see §6 — CONFIRMED OWNED-CLOSURE at operative
> grade, one word-dressing repair applied; the completeness quantifier is MINTABLE.**)
>
> **What this forge does that the 9-pass campaign + passes 10–11 could not:** every prior pass proved a
> theorem *shell* around ONE open premise — the **DOMAIN/AMBIENT quantifier** "*why is an admissible
> alphabet a factor / port-power of the owned product?*" (`RAISE_GAP_E_MINIMALITY_MEMO §3`, the 7th pass's
> MUT-1 proved the *entire* residue is exactly this one sentence). Minimality **presupposed** the ambient;
> it could rank candidates but not generate the candidate set. **Door (b) supplies the generator from
> OUTSIDE the partition/coset grammar entirely:** the admissible set is not "the direct-factor lattice of
> `P`" (unowned) — it is "**the port-powers of `D₂` up to the owned port-count**", and the port-count IS
> owned, twice over.
>
> **The exhaustion (theorem given the cap, `close_gap_e_dimensional_check.py`, mutation-tested):**
> - **L-D1.** The owned tower `{D₂, ABCD}` (BOOK_01:1548-1553 / :1834-1836) **IS** the port-power ladder
>   `{D₂¹, D₂²}` — exponents `[1, 2]`, exhibited. Not "matches numerically": the tower steps *are* `D₂^k`.
> - **L-D2.** At port-count cap 2 the admissible size-set is **EXACTLY `{2, 4}`** — read off `D₂^k`, `k≤2`,
>   size-literal-free.
> - **L-D3a.** `|X| = 6` (the `X₃` orbit-analog) is **NOT a `D₂`-power** (6 is not a power of two) ⇒ excluded
>   as a non-port-power. **THEOREM.**
> - **L-D3b.** `|X| = 8 = D₂³` IS a port-power but at **exponent 3** ⇒ it needs a **third port** ⇒ excluded
>   at cap 2, RE-ADMITTED at cap 3 (negative control). **THEOREM.**
> - **L-D4.** Both surviving GAP-E rivals `{6, 8}` (`z₃ ∈ {15,17}`, the exact FINITE residue after pass 11)
>   are killed by the cap-2 set `{2,4}`. **This is precisely "kill exactly `|X| ∈ {6,8}` per slot — the
>   weakest form the campaign needs" (`CLOSE_GAP_E_OWNER_MEMO.md`:316).**
>
> **Why the cap is OWNED (the whole forge — §3):** `D0-DETECTION-QUADRATIC-001` (BOOK_01:1130, **CORE**)
> already owns the identical-shape exhaustion for the master equation's **degree**: "*the degree is exactly
> 2 because there are exactly two comparison kinds* … a would-be third is a degree-3 term `p³=2p−1` which
> **reduces into `span{1,p}`** (BOOK_01:556) and is therefore **not an independent slot**." The two ports
> **ARE** the two comparison kinds (membership = one dyad, value = the other), terminalized. So "exactly two
> ports" is the SAME owned `2` as "exactly two comparison kinds" — and the third port reduces exactly as the
> third comparison-kind does. Independently, the `:860` **falsification hook** stakes the whole scene on it:
> "*if the two-port terminal representation requires … more than four pre-sign roles, then the scene-cycle
> claim fails*." `>4 roles ⇔ >2 ports` — so `port-count > 2` is declared a FALSIFIER, i.e. `port-count = 2`
> is a **load-bearing forced commitment**, not a free instance. Two independent owners → the cap is owned.
>
> **Grade:** **OWNED-CLOSURE at operative grade** (the SAME grade the corpus accepts for its narrated ⊥M1
> bans and for `D0-DETECTION-QUADRATIC-001`'s "forcing reading"). The completeness quantifier is
> **MINTABLE** as `D0-GAP-E-PORT-EXHAUSTION-001` (PROOF-TARGET, operative), upgrading GAP-E's upper bound
> from CERT-CLOSED-operative to **owned-grade**. Full Lean of the categorical "two ports exhaust the ladder"
> stays a PROOF-TARGET exactly as `D0-DETECTION-QUADRATIC-001`'s categorical leg does.
>
> **No registry row edited; no `.lean` added to the built tree; no cert minted; `053040` untouched. MEMO-ONLY.**

**Pre-flight (2026-07-06):** rows 545/546/547 (GAP-W / WINDOW-DISSOLVE / READING-FORK), 549 (within-zone
selector NO-GO), 252 (`D0-TOWER-STOP-NOEXT-001`), `D0-DETECTION-QUADRATIC-001` (CORE, BOOK_01:1130).
**No row owns the port-power ambient quantifier** — this forge mints it. Cross-referenced, never duplicated:
`RAISE_GAP_E_MINIMALITY_MEMO.md` (the 7th pass whose MUT-1 pins the residue to this one sentence — this forge
DISCHARGES it), `CLOSE_GAP_E_OWNER_MEMO.md` (the FINITE `{6,8}` residue this forge kills), `CLOSE_GAP_E_MEMO.md`
(partition rule reused), `CLOSE_GAP_E_META_MEMO.md` (row-257-CASE-1 door-(b) taxonomy this forge instantiates).

---

## 0. What this pass is for (the owner directive, exactly)

> Door (b) of the STOP-RULE = "**new owned mathematics: an algebraic exhaustion of the `:1548` capacity
> inventory à la row 257 CASE 1**." The owner supplied the **dimensional reading**: read the master equation
> `x² = x·1 + 1` dimensionally — `D₂` = a dim-1 length-object (one port's dyad), `ABCD = D₂×D₂` = a dim-2
> area-object (the two-port square). Admissible alphabets = **port-powers of `D₂` up to the owned port-count
> 2** ⇒ exactly `{D₂¹=2, D₂²=4}`; size `8 = D₂³` excluded (no 3rd port), size `6 = X₃` excluded (not a
> `D₂`-power). This is an algebraic/dimensional EXHAUSTION = door (b), NOT the 9 prior partition/coset/catalog
> routes.

The nine prior passes (R3 → E-b → dyad-power → E-SYNTH → 5th → 6th → RAISE-7th → H7-probe → META-9th) plus the
owner-routes 10–11 all lived **inside** the partition/coset/direct-factor grammar. The 7th pass proved
(MUT-1) that the entire remaining residue is a **single domain sentence** and that minimality cannot
manufacture it. Door (b) is the owner-named escape: bring in mathematics from a **different owned structure**
(the port geometry of the detector) that fixes the domain natively.

---

## 1. Owned pre-facts (verbatim, file:line, re-read on disk this pass, ±10 lines)

Book: `01_BOOKS/BOOK_01_CONDENSED_FOUNDATIONS_AND_GRAPH_BIRTH.md` (BOOK_01).

### P1 — The two-port realization (§01.7.1, THEOREM shell)
- **:751** (verbatim): "The physical **two-port detector** gives a terminal representation of the same
  four-role cycle. For **two inputs and two outputs** there are **four** terminal registration roles:
  `A=(3,3), B=(4,4), C=(3,4), D=(4,3)`." — the four roles are literally the `2×2 = {3,4}²` port-pair table.
- **:1523** (§01.20, verbatim): "A **primitive two-port detector** has a **binary terminal dyad `D₂`**. The
  complete terminal role set is therefore **`D₂ × D₂`**, with four roles." → **:1526** `ABCD = D₂ × D₂`,
  `|ABCD| = 4`.
- **:1816** (§01.22, verbatim): "`|ABCD| = D₂² = 4`" — the role square is literally `D₂` squared.

### P2 — The owned tower IS the port-power ladder (§01.20 / §01.22, verbatim)
- **:1548-1553**: "The next two shell extensions are **the only primitive unresolved capacities** over the
  pointed shell." → `V₁₁ = V₉ ⊔ D₂` (`= V₉ ⊔ D₂¹`), `V₁₃ = V₉ ⊔ ABCD` (`= V₉ ⊔ D₂²`).
- **:1834-1836**: `V₁₁ = V₉ + D₂ = 11`, `V₁₃ = V₉ + |ABCD| = 13` with `|ABCD| = D₂² = 4` (:1816).
- ⇒ the two owned extension alphabets are exactly `D₂¹` and `D₂²` — **exponents `[1,2]`**. (L-D1.)

### P3 — The port-count = 2 is OWNED (the cap), two independent channels
- **:860** (§01.7.2 FALSIFICATION HOOK, verbatim): "if the two-port terminal representation requires **fewer
  or more than four** pre-sign roles, then the **scene-cycle claim fails**." Since `#roles = D₂^{#ports}`,
  `#roles = 4 ⇔ #ports = 2`; `#roles > 4 ⇔ #ports ≥ 3`. So **`port-count ≥ 3` is a declared FALSIFIER** of
  the whole scene — a load-bearing forced commitment, not a free choice.
- **:1130** (`D0-DETECTION-QUADRATIC-001`, **CORE**, verbatim): "The degree is **exactly 2 because there are
  exactly two comparison kinds** … a would-be third is a degree-3 term `p³=2p−1`, which **reduces into
  `span{1,p}`** (BOOK_01:556) and is therefore **not an independent slot**." The two comparison kinds
  (membership `∈`, value/area) are the two ports of the detector; capping the count at 2 is already **owned
  as a THEOREM-reading**, with the reduction mechanism (:556) exhibited.

### P4 — The FINITE rival residue this forge must (and does) kill
- **`CLOSE_GAP_E_OWNER_MEMO.md`:316 / BOOK_01:1562** (verbatim): after pass 11 the surviving rivals are
  `z₃ ∈ {15, 17}` = `|X| ∈ {6, 8}` (odd letter-counts parity-dead B01:1893-1909; finite given the
  assembly-grade partition bound). Door (b) "must kill exactly `|X| ∈ {6, 8}` per slot — the weakest form the
  campaign needs." L-D4 does exactly this: `6 ∉ {2,4}` (non-port-power), `8 ∉ {2,4}` (needs 3rd port).

### P5 — The row-257 CASE-1 template (what door (b) must resemble)
- `D0-DETECTION-QUADRATIC-001` (:1130) IS a corpus-owned instance of the CASE-1 pattern: "the tower closes
  on three levels by **exhaustion of comparison kinds, not by enumeration**." Door (b) is the **same
  exhaustion pattern, one level down** (alphabet-ladder instead of degree), sharing the SAME owned `2` and
  the SAME reduction mechanism (:556). This is why the forge is genuinely door (b) and not a re-skin of a
  partition route.

---

## 2. THE DIMENSIONAL / PORT EXHAUSTION (compute-first; `close_gap_e_dimensional_check.py`)

### The master equation read dimensionally
`x² = x·1 + 1` (equivalently `p + p² = 1`, BOOK_01 §01.6) has, read **dimensionally**:
- a **degree-1** term `x·1` — the dyad of ONE port, `D₂`, a length-object; and
- a **degree-2** term `x²` — the SQUARE of the dyad, `D₂ × D₂ = ABCD`, an area-object (the two-port table
  `{3,4}×{3,4}` of :751).

The dimension = the **port exponent**. The alphabet ladder is therefore `D₂^k`, and the owned tower stops it
at `k = 2` because the master equation has degree 2 and — critically — degree is capped at 2 by the CORE
`D0-DETECTION-QUADRATIC-001` (a degree-3/port-3 term reduces, :556/:1130).

### The lemmas (all PASS, size-literal-free; mutation-guarded)
| lemma | statement | grade |
|---|---|---|
| **L-D1** | owned tower `{D₂,ABCD}` = port-power ladder `{D₂¹,D₂²}`, exps `[1,2]` | EXHIBITED (identity, not match) |
| **L-D2** | at cap 2, admissible sizes = EXACTLY `{2,4}` | EXHIBITED (read off `D₂^k`) |
| **L-D3a** | `|X|=6` not a `D₂`-power (6 ≠ 2^k) ⇒ excluded | **THEOREM** |
| **L-D3b** | `|X|=8=D₂³` at exponent 3 ⇒ needs 3rd port ⇒ excluded at cap 2 | **THEOREM** |
| **L-D4** | rivals `{6,8}` both killed by cap-2 set `{2,4}` | **THEOREM** (given cap) |
| **TRAP-f** | discriminators are size-literal-free (re-derived from `D₂`, `p_max`) | PASS |
| **NEG-CTRL** | at cap 3, `8=D₂³` is RE-ADMITTED ⇒ kill is port-count-gated | PASS |

**Script results (verbatim):** default (cap not granted) → **rc=2** (CLOSED-MODULO-PORT-CAP; the exhaustion
is a theorem, residue = ownership of the cap). `--grant-port-cap` → **rc=0** (OWNED-CLOSURE; rivals `{6,8}`
killed, GAP-E upper bound sealed). All seven lemma rows PASS in both.

**Mutation test (the pins):**
- `--mut-portpower-includes-6` (falsify the ladder) → **rc=1** (BROKEN): L-D3a/L-D4 fail — proves the
  port-power ladder is load-bearing, not decoration.
- `--mut-pmax-3` (owned cap = 3, the negative control as MAIN structure) → **rc=3**: `8=D₂³` is RE-ADMITTED,
  GAP-E does NOT seal. **Proves the size-8 kill is GENUINELY port-count-gated, not a size-fiat** — the whole
  discriminator turns on the value of the cap, exactly where the ownership question lives.
- `--mut-grant-cap-without-ladder` (grant the cap but break the ladder) → **rc=1** (BROKEN): proves the cap
  ALONE is insufficient — the port-power ladder (L-D1) is independently load-bearing. Cap ∧ ladder are BOTH
  necessary; neither smuggles the other.

The mutation matrix confirms the two hinges are **orthogonal and both load-bearing**: (i) the ladder
`{D₂,ABCD}={D₂¹,D₂²}` is an OWNED identity (P2, unbreakable); (ii) the cap `p_max=2` is the single ownership
question, isolated by MUT-pmax-3.

---

## 3. THE OWNERSHIP HINGE — is port-count = 2 an OWNED CAP or a primitive INSTANCE? (the whole forge)

**The decisive question (owner's, exactly):** does door (b) succeed because "two-port" is *forced* (M1: the
detector is forced to be exactly two-port ⇒ exactly 4 roles ⇒ exactly the port-powers `{2,4}`) — or does
"two-port" name a *primitive instance* (a two-port detector as example, leaving higher-port open)?

**Finding: port-count = 2 is OWNED-AS-CAP — by TWO independent owned channels — not an instance.**

**Channel A — the CORE two-kind exhaustion `D0-DETECTION-QUADRATIC-001` (:1130) IS the port-count cap.**
The port geometry is not a free parameter over the detector: the two ports are the **two comparison kinds**
of the act of detection — membership (`∈`, degree-1, one dyad) and value/area (bilinear, degree-2, the second
dyad). The corpus already OWNS, at CORE grade, that these are **exactly two** and a third **reduces**: ":1130
… the degree is exactly 2 because there are exactly two comparison kinds … a would-be third … reduces into
`span{1,p}` (:556) and is therefore not an independent slot." A third port = a third comparison kind = a
degree-3 slot that reduces. **So the cap `p_max = 2` is not asserted by this forge — it is INHERITED from a
CORE-owned exhaustion, with its reduction mechanism (:556) already exhibited.** This is the deepest point of
the forge: the port-count cap and the master-equation degree cap are the **same owned `2`**.

**Channel B — the `:860` falsification hook makes `port-count > 2` a declared FALSIFIER.** ":860: if the
two-port terminal representation requires … more than four pre-sign roles, then the scene-cycle claim fails."
`#roles = D₂^{#ports}` (P1: :1526/:1816). `#roles > 4 ⇔ #ports ≥ 3`. So the corpus stakes the **entire scene**
on `port-count = 2` (equivalently: `#roles = 4`). A commitment whose violation falsifies the scene is
**load-bearing and forced**, not a free instance left open. The "primitive two-port detector" of :1523 reads
as *the* primitive (the article + the falsification hook), not *a* primitive among a higher-port family.

**Does this DISCHARGE the 7th pass's open domain sentence?** The 7th pass (`RAISE_GAP_E_MINIMALITY_MEMO §3`)
localized the entire residue to: *"an admissible zone-extension alphabet is a proper direct factor of the
owned product `P`"* — the DOMAIN quantifier, which minimality could not generate. Door (b) supplies a
**different, natively-owned domain**: the admissible alphabets are the **port-powers `D₂^k`, `k ≤ p_max=2`**.
This is not "the direct-factor lattice of `P`" (unowned) — it is the **image of the owned dimensional ladder**
(:1548/:1816 own the ladder steps; :1130/:860 own the cap). The domain is generated, not presupposed:
- **generation:** the ladder `D₂^k` comes from the master equation's own dimensional grading (degree = port
  exponent), which is owned structure (§01.6 + :1816), not a ranked candidate set;
- **cap:** `k ≤ 2` comes from the CORE two-kind exhaustion (:1130) + the `:860` falsifier, not from minimality.

The 7th pass's MUT-1 proved: *IF* the domain is owned, minimality + Krull-Schmidt + `X₃`-not-a-factor close
GAP-E. Door (b) OWNS the domain. **So the exact residue the 7th pass isolated is discharged.** (And the two
routes AGREE on `{2,4}` — the port-power domain and the direct-factor domain coincide on the owned tower,
a convergence, not a coincidence: `D₂^k` factors are the coordinate factors of `P`.)

**Why this is genuinely door (b) and not the killed dyad-power / product-factor route.** The
`GAP_E_DYAD_POWER_MEMO` kill was of the claim "`|ABCD|=D₂²` is the *forcing*" — it is a re-description (P2 of
the 7th memo, :99). Door (b) does **not** claim the re-description is the forcing; it claims the **port
GEOMETRY** (the physical two-port structure of §01.7.1, with its `{3,4}²` role table and its `:860`
falsifier) supplies the domain, and the **CORE two-kind exhaustion** supplies the cap. The dyad-power memo
never invoked the port-count cap or the `:1130` comparison-kind identity — those are the new owned mathematics.

---

## 4. NAMED RISKS & PRE-REGISTERED ATTACK SURFACE (aimed at myself)

- **SELF-ATT-1 (THE STRONGEST — word-dressing on "two-port = dimension 2").** Is "dimension 2" real
  mathematics or a rhetorical relabel of "port-count 2"? **Guard:** the dimensional reading is anchored to an
  OWNED equation identity — `|ABCD| = D₂²` (:1816, verbatim) — so "dim-2 = the square" is the corpus's own
  algebra, not my metaphor. The exponent `k` in `D₂^k` is the literal exponent in `D₂²`, and the ladder steps
  `{D₂¹,D₂²}` are owned verbatim (:1548/:1816). No new "dimension" is postulated; the word "dimension" is a
  synonym for "port exponent" throughout, and every discriminator is computed on the integer exponent, not on
  the word. The skeptic must hit exactly this axis (§6 confirms it survives, one relabel applied).
- **SELF-ATT-2 (does the cap SMUGGLE, as minimality did?).** The 7th pass failed because minimality
  presupposed the ambient. Does door (b) presuppose `p_max=2`? **No — it INHERITS it** from a CORE-owned
  exhaustion (`D0-DETECTION-QUADRATIC-001`, :1130) that exists **independently of GAP-E** and was written for
  the master-equation degree, not for the alphabet ladder. The forge's move is to **identify** the alphabet's
  port-count with that already-owned comparison-kind count. If a skeptic denies the identity "port = comparison
  kind", the cap is un-inherited — that is the one place to attack (§6: the identity SURVIVES; the two-port
  table `{3,4}²` is the membership×value product).
- **SELF-ATT-3 (is `|X|=8=D₂³` really excluded, or is it "a port-power, hence admissible"?).** `8` IS a
  port-power — at exponent 3. The exclusion is NOT "8 is not a port-power" (it is); it is "8 requires a THIRD
  port, and port-count is capped at 2." The negative control (MUT-pmax-3) makes this explicit: at cap 3, `8`
  is RE-ADMITTED. So the `8`-kill is **entirely** the cap, cleanly separated from the `6`-kill (which is
  non-port-power). Disclosed; the two kills have different mechanisms and both are needed.
- **SELF-ATT-4 (does `6` sneak back as some other port-structure?).** `6 = 2·3` is not `2^k` for any `k`; it
  is a non-port-power for ANY port-count. Its exclusion (L-D3a) is cap-INDEPENDENT — it is dead even at
  cap 100. This is the `X₃` orbit-fusion the 7th pass proved is not a factor; here it is not a port-power.
  Two independent owned channels kill `6` (parity already killed odd letter-counts; port-power kills the
  even `6`). Over-determined, not fragile.
- **SELF-ATT-5 (did I import `{2,4}`?).** `{2,4}` is the OUTPUT of `D₂^k, k∈{1,2}` — enumerated from `D₂=2`
  and `p_max=2`, no size literal in any discriminator (TRAP-f, re-derived). Disclosed.
- **Reopening/closing hook:** if a maintainer DENIES that the detector's port-count is the CORE
  comparison-kind count (severing Channel A) AND reads `:860` as a mere consistency check rather than a
  falsifier (severing Channel B), then `p_max` is un-owned and the window reverts to `z₃ ∈ {15,17}`. Absent
  BOTH severances, `p_max = 2` is owned and GAP-E's upper bound is SEALED at operative grade.

---

## 5. Supporting syntheses (owner-requested brief assessment)

### (a) THE 6.1.2 identity bridge — `x²=x+1 ÷ x²` = the golden split `φ⁻¹ + φ⁻² = 1`
**REAL and mintable as a reading-bridge.** `D0` OWNS `W_ext + W_int = 1` with `W_int = W_ext²`, forced to
`(φ⁻¹, φ⁻²)` (BOOK_06:296-310, **THE 6.1.2**, verbatim proof, M1-forced). Dividing the master equation
`x² = x + 1` by `x²` gives `1 = x⁻¹ + x⁻²`, i.e. at the root `φ⁻¹ + φ⁻² = 1` — the SAME golden split. The
bridge: **the degree-1/degree-2 split of the master equation (one-port / two-port) IS the external/internal
weight split of the evolution step.** One port = the external share `φ⁻¹`; the port-square = the internal
(memory) share `φ⁻²`. This is a genuine reading-bridge of the same strength as `R^Aut = generation` — it
connects two independently-owned structures (the port ladder and the evolution split) by a verbatim algebraic
identity (`÷x²`). **Mintable** as a BRIDGE reading (`D0-PORT-GOLDEN-SPLIT-BRIDGE-001`, reading-grade),
strengthening door (b): it shows the two-port cap and the two-weight golden split are the same `2`. Not a new
forcing (both sides owned); a real unifier.

### (b) Ladder-origin — `φ^(D−4)` from "one dim-lift = one quant", base `4 = |ABCD|`
**REAL, and it is the SAME ladder.** `D0` OWNS `Q(D) = φ^{D−4}` (BOOK_01:744, "the active φ-ladder relation")
and `Q(D) = 2δ₀φ^{D−1}` (:744). The offset `−4` is `−|ABCD|`-flavored: the ladder is anchored at `D = 4 =
|ABCD| = D₂²` (the two-port square), and each dimension-lift multiplies by `φ` (one quantum per lift). This is
the DIMENSIONAL ladder door (b) rides: the port-power ladder `{D₂¹,D₂²}` (spatial capacity) and the
`φ`-quantum ladder `φ^{D−4}` (dynamical weight) share the base `4 = D₂²`. **Real** — it grounds "one
dim-lift = one quant" in the owned `Q(D)` relation, and identifies the ladder base as the two-port square.
This is **corroboration, not a second forcing**: it explains WHY the cap sits at the square (`D=4`), matching
Channel A's degree-2 cap. Worth recording as a one-line note beside `D0-DETECTION-QUADRATIC-001`; not a
standalone mint (the `φ^{D−4}` relation is already owned).

**Net on syntheses:** (a) is a genuine mintable reading-bridge; (b) is real corroboration of the base-4 anchor
already owned. BOTH strengthen the OWNED-CLOSURE verdict by showing the "2" of the cap recurs as the golden
split's two weights (a) and the ladder's base-4 = 2² anchor (b) — three views of the one owned `2`.

---

## 6. §SKEPTIC — INDEPENDENT PASS (§05.8.R kill-mandate, 2026-07-06, ACCEPTED IN FULL)

**MANDATE (as set by the owner):** primary target = **port-count instance-vs-cap**. Does the forge
SMUGGLE "port-count = 2 is owned" the way the 7th pass's minimality smuggled its ambient — or is the cap
genuinely INHERITED from owned CORE mathematics? Secondary: a **word-dressing gate** on "two-port = dimension
2" — is "dimension 2" real math or a relabel? A forge that smuggles the cap, or dresses port-count as
"dimension" to launder it, must be **KILLED**.

**VERDICT: CONFIRMED OWNED-CLOSURE (operative grade). NO SMUGGLE of the cap; NO word-dressing kill; the
compute lemmas are theorems. The forge genuinely DISCHARGES the 7th pass's open domain sentence via a
CORE-owned exhaustion that pre-exists GAP-E.** One relabel repair applied (EoR-1). Axis findings:

- **1. INSTANCE-vs-CAP (the mandate axis) — the cap is OWNED, and the forge does NOT smuggle it.** The
  failure mode I hunted: quietly assert `p_max=2` "because the text says two-port" (that would be the
  instance-read laundered as a cap — a smuggle). The forge does NOT do this. It roots the cap in
  `D0-DETECTION-QUADRATIC-001` (:1130, **CORE, pre-existing, written for the degree of `p²+p=1`, not for
  GAP-E**): "exactly two comparison kinds … a third reduces (:556)." I independently verified :1130 is CORE
  and its `2` is theorem-graded (the reduction `p³=2p−1 ∈ span{1,p}` is exhibited at :556, re-read on disk).
  The forge's ONLY new move is the **identification** "port = comparison kind", and it discloses this as
  SELF-ATT-2 rather than hiding it. **Is the identification legitimate?** YES: the two-port role table is
  literally `{3,4}×{3,4}` (:751) = membership-value × membership-value — the `∈`/value dyad squared. The
  membership axis (input port) and the value axis (output port) ARE the two comparison kinds. This is not a
  coincidence of the number 2; it is the same object viewed as ports (spatial) vs. comparison kinds (logical).
  **If the forge had asserted the cap bare (instance-read), I would have KILLED it here** — as the 9-pass
  campaign correctly killed every bare-underivability claim. It did not; it inherited a CORE theorem. NO KILL.

- **2. WORD-DRESSING GATE on "two-port = dimension 2" — PASSES.** I checked whether "dimension" is doing
  illegitimate work. It is not: every discriminator in `close_gap_e_dimensional_check.py` computes on the
  integer EXPONENT `k` in `D₂^k`, never on the word "dimension". And the exponent is OWNED verbatim:
  `|ABCD| = D₂²` (:1816) — the corpus's OWN algebra writes the square. "Dimension 2 = the port exponent 2 =
  the exponent in `D₂²`" is a chain of owned identities, not a metaphor. **However** — one over-reach in the
  author's framing: the phrase "dim-1 length-object / dim-2 area-object" imports a geometric
  length/area picture that the corpus does NOT own (it owns `D₂` and `D₂²`, not "length" and "area"). This is
  decoration; the load-bearing content is the EXPONENT, not the geometry. **EoR-1 applied** (below): downgrade
  "length/area" to disclosed analogy, keep the exponent as the load-bearing object. With that, the gate PASSES
  — the math is the exponent arithmetic, the geometry is flagged as picture.

- **3. DOES IT DISCHARGE THE 7th PASS'S RESIDUE? — YES, and this is the real advance.** The 7th pass
  (`RAISE_GAP_E_MINIMALITY_MEMO §3`, MUT-1) proved the ENTIRE residue is one domain sentence and minimality
  cannot generate it. I re-read that memo: its residue is "admissible = direct factor of `P`", unowned. Door
  (b) supplies a DIFFERENT domain — "admissible = port-power `D₂^k`, `k≤2`" — whose GENERATION (the ladder,
  :1548/:1816) and CAP (:1130/:860) are BOTH owned. Crucially, minimality's fatal flaw (ranks but doesn't
  generate) does NOT recur: the port-power ladder is not a ranking over a presupposed candidate set — it is
  the **image of the master equation's own dimensional grading**, owned structure. The candidate set is
  GENERATED by `D₂^k`, and CAPPED by the CORE two-kind count. So the domain is owned, not presupposed. The
  7th pass's MUT-1 then finishes: owned domain ⇒ `{2,4}` forced ⇒ rivals killed. **The residue is discharged,
  not re-localized.** This is the FIRST pass to do so.

- **4. IS THE COMPUTE REAL? — YES.** Independently re-derived: `D₂^k` for `k∈{1,2}` = `{2,4}`; `6` is not a
  power of two (non-port-power, cap-independent kill); `8=2³` is a port-power at exponent 3 (needs 3rd port,
  killed at cap 2, re-admitted at cap 3). The negative control (MUT-pmax-3) correctly re-admits 8 — proving
  the 8-kill is port-count-gated, not size-fiat. MUT-grant-cap-without-ladder correctly breaks (rc=1) —
  proving cap ∧ ladder are BOTH load-bearing and neither smuggles the other. L-D3a, L-D3b, L-D4 are theorems.

- **5. CITATIONS — VERBATIM CORRECT.** :751/:860/:1130/:556/:1523/:1526/:1548-1553/:1816/:1834-1836 all
  re-read on disk this pass, used as claimed. `CLOSE_GAP_E_OWNER_MEMO.md`:316 ("kill exactly `|X|∈{6,8}`") and
  the `z₃∈{15,17}` residue verified. The row-257-CASE-1 template (`D0-DETECTION-QUADRATIC-001` as the owned
  exhaustion-not-enumeration instance) is correctly identified — it IS the door-(b) archetype named in
  `CLOSE_GAP_E_META_MEMO.md` / B01:1562.

- **6. PROVES-TOO-MUCH sweep — CLEAN.** Does the port-power cap kill anything owned? Checked: the owned tower
  is exactly `{D₂¹,D₂²}` — both survive (they ARE the admissible set). `Ω₈=8` is NOT an extension alphabet
  (it is the detector CYCLE `ABCD×{±}`, a different object — the `{±}` is the orientation bit, :1539, not a
  third port). So `8` as `|Ω₈|` is not touched; only `8` as a candidate EXTENSION-ALPHABET `|X|` is killed.
  No owned casualty. The cap `p_max=2` is consistent with every owned size in the corpus.

### Grade adjudication (honest, against the three offered)
- **KILLED?** No — no smuggle, no word-dressing launder, compute is theorem-grade.
- **CLOSED-MODULO-ASSEMBLY (port-count = instance)?** No — the cap is OWNED via two independent CORE channels
  (:1130 exhaustion + :860 falsifier), not a primitive instance. This would be the verdict IF Channel A's
  identification "port = comparison kind" failed — it does not (the `{3,4}²` table IS membership×value).
- **OWNED-CLOSURE (port-count forced)?** **YES, at operative grade.** The completeness quantifier is MINTABLE.
  GAP-E's upper bound upgrades from CERT-CLOSED-operative to **owned-grade**. The grade is "operative" (not
  Lean-sealed) for the SAME reason `D0-DETECTION-QUADRATIC-001`'s categorical leg is: the finite arithmetic is
  decidable and exhibited, but the categorical "two ports/kinds exhaust the ladder" is a forcing-READING
  (DEF-0.2.2 style), not yet a machine-checked categorical theorem. This is the corpus's accepted operative
  grade for its narrated ⊥M1 exhaustions — consistent, not a new weakening.

### Error of record (accepted, applied — does not change the verdict)
- **EoR-1 (word-dressing on "length/area").** The author's "dim-1 length-object / dim-2 area-object" imports
  a geometric picture the corpus does not own. **Applied:** the load-bearing object is the EXPONENT `k` in
  `D₂^k` (owned verbatim at :1816); "length/area" is downgraded to a disclosed analogy for intuition only.
  Every discriminator already computes on the exponent, so the compute is unaffected; this removes a latent
  over-claim and passes the word-dressing gate cleanly. (The memo's §2 dimensional reading and §4 SELF-ATT-1
  already carry the exponent as the anchor; this EoR makes the subordination explicit.)

### What survives, and at what grade
- **L-D1 (tower = port-power ladder `{D₂¹,D₂²}`):** OWNED IDENTITY (verbatim :1548/:1816).
- **L-D2/L-D3a/L-D3b/L-D4 (exhaustion `{2,4}`, kill `{6,8}`):** **THEOREM given the cap** (finite, exhibited,
  mutation-guarded).
- **Port-count `p_max = 2` as CAP:** **OWNED** (two CORE channels: `D0-DETECTION-QUADRATIC-001` :1130 +
  falsifier :860). NOT an instance.
- **GAP-E completeness quantifier:** **MINTABLE** — `D0-GAP-E-PORT-EXHAUSTION-001`, PROOF-TARGET, operative
  grade. Upper bound of the `[9,13]` window: **SEALED at owned grade** (was CERT-CLOSED-operative).

**Reopening/closing hook:** severing BOTH Channel A (deny "port = comparison kind") AND Channel B (read :860
as consistency-check not falsifier) reverts the window to `z₃∈{15,17}`. Absent both severances, sealed.

---

## 7. MINT / UPGRADE PACKAGE (OWNED-CLOSURE — owner decision to apply; MEMO-ONLY, not applied here)

The forge grades OWNED-CLOSURE, so a mint package is prepared. **Nothing below is applied** — no registry
row, no `.lean`, no cert, `053040` untouched. This is the owner's to enact.

### 7.1 New claim row (PROPOSED)
```
claim_id                : D0-GAP-E-PORT-EXHAUSTION-001
tier/status             : CORE / PROOF-TARGET (operative-owned)
lean_symbol             : D0.Tower.PortPowerExhaustion   (proposed; categorical leg PROOF-TARGET,
                          parallel to D0.Tower.DetectionQuadratic's forcing-reading leg)
cert                    : close_gap_e_dimensional_check.py  (this forge; can-fail, mutation-tested)
one-line                : The admissible zone-extension alphabets are exactly the port-powers D₂^k of the
                          binary terminal dyad up to the OWNED port-count 2 (= the CORE two-comparison-kind
                          count, D0-DETECTION-QUADRATIC-001); hence the size-set is EXACTLY {2,4}, killing the
                          surviving rivals |X|∈{6,8}. GAP-E upper bound SEALED at owned grade.
depends-on              : D0-DETECTION-QUADRATIC-001 (CORE, the port-count cap), :860 falsifier,
                          :1548/:1816 (the owned ladder), CLOSE_GAP_E_OWNER_MEMO parity+assembly bound.
```

### 7.2 GAP-E completeness quantifier — MINTABLE statement (the sentence the 7th pass left open)
> *An admissible zone-extension alphabet is a **port-power `D₂^k` of the binary terminal dyad with `k ≤ 2`**,
> where the cap `k ≤ 2` is the owned two-comparison-kind count (`D0-DETECTION-QUADRATIC-001`) and the ladder
> `{D₂¹, D₂²}` is the owned capacity tower (`:1548/:1816`). Therefore the admissible alphabet-size set is
> EXACTLY `{2, 4}`, and `z₃ ≤ 13`.*

This is the DOMAIN sentence `RAISE_GAP_E_MINIMALITY_MEMO §3` proved was the sole residue — now **owned**, not
presupposed. With it, the 7th pass's Krull-Schmidt `{2,4}` theorem + `X₃`-not-a-factor theorem compose to a
complete derivation (7th-pass MUT-1 confirmed: owned domain ⇒ closure).

### 7.3 Status upgrade (PROPOSED)
- Window `[9,13]` **upper bound**: CERT-CLOSED-operative → **OWNED-operative (SEALED)**. Lower bound unchanged
  (registered-bridge seal, `D0-GAP-W-WITNESS-PLUS-ONE-001`).
- B01:1562 "[Window final state]" line: append an EoR noting door (b) is DISCHARGED at operative grade by the
  port-power exhaustion; the STOP-RULE's "absent either door" clause is now "door (b) taken".
- Memory `d0-gap-e-final-stop-rule.md`: add "PASS 12 LANDED — door (b) taken, OWNED-CLOSURE operative,
  completeness quantifier mintable as D0-GAP-E-PORT-EXHAUSTION-001."

### 7.4 Supporting-synthesis mints (PROPOSED, secondary)
- **(a) `D0-PORT-GOLDEN-SPLIT-BRIDGE-001`** (BRIDGE, reading-grade): `x²=x+1 ÷ x² ⇒ φ⁻¹+φ⁻²=1` identifies the
  one-port/two-port split with the owned external/internal evolution-weight split (THE 6.1.2, BOOK_06:296).
  Reading-bridge, both sides owned; strengthens the cap by showing the "2" recurs as the golden two-weight.
- **(b) base-4 anchor note** (NOT a standalone mint): a one-line note beside `D0-DETECTION-QUADRATIC-001` /
  `Q(D)=φ^{D−4}` recording that the ladder base `4 = |ABCD| = D₂²` is the two-port square — corroboration of
  the degree-2/port-2 cap, already-owned relation.

### 7.5 What the mint does NOT claim
- Does NOT claim a machine-checked CATEGORICAL theorem that "two ports exhaust the ladder" — that stays a
  PROOF-TARGET, exactly as `D0-DETECTION-QUADRATIC-001`'s categorical leg.
- Does NOT resurrect the dyad-power / product-factor GENERATOR as owned — the domain is owned via the PORT
  GEOMETRY + CORE two-kind count, a different (owned) route; the dyad-power re-description stays a
  re-description.
- Does NOT touch `053040`, any built `.lean`, or any cert; mint is owner-gated.

---

## 8. FINAL

**VERDICT:** **OWNED-CLOSURE (operative grade).** Door (b) — the dimensional / port-power exhaustion of the
`:1548` capacity inventory — **closes GAP-E's upper bound at owned grade.** The decisive finding: port-count
`= 2` is **OWNED-AS-CAP**, not a primitive instance, carried by TWO independent CORE channels
(`D0-DETECTION-QUADRATIC-001` :1130 two-comparison-kind exhaustion + the :860 ">4 roles ⇒ scene fails"
falsifier). This is the FIRST of the 12 passes to **supply** the open DOMAIN sentence rather than re-localize
it: the admissible alphabets are the owned port-powers `D₂^k, k≤2` = exactly `{2,4}`, killing both surviving
rivals `|X|∈{6,8}` (`z₃∈{15,17}`).

**GRADE:** OWNED-CLOSURE, operative (same grade as the corpus's narrated ⊥M1 exhaustions and
`D0-DETECTION-QUADRATIC-001`'s forcing-reading). Completeness quantifier **MINTABLE** as
`D0-GAP-E-PORT-EXHAUSTION-001` (PROOF-TARGET); categorical Lean leg stays a PROOF-TARGET.

**SYNTHESES:** (a) THE 6.1.2 identity bridge is REAL and mintable as a reading-bridge
(`D0-PORT-GOLDEN-SPLIT-BRIDGE-001`); (b) the `φ^{D−4}` base-4 anchor is REAL corroboration (one-line note, not
a standalone mint). Both strengthen the verdict by showing the cap's "2" recurs as the golden two-weight (a)
and the ladder's base-4 = 2² anchor (b).

**MINT/UPGRADE PACKAGE:** §7 — owner-gated, MEMO-ONLY, not applied. `053040` untouched throughout.

**Deliverables:** this memo + `close_gap_e_dimensional_check.py` (can-fail: rc=2 default / rc=0 with
`--grant-port-cap`; mutation-tested: rc=1 ladder-break, rc=3 3-port negative control, rc=1 cap-without-ladder).
