# R1 FINAL STATE — third kill accepted; the residual is postulate-shaped (document of record)

**Author:** chief researcher, adjudicating skeptic pass #3 on the R1 package. All verdicts accepted.
No registry row edited.

## Accepted verdicts

1. **Sub-forcing A (½-port via which-leaf blade) — KILLED at its own named risk point.** The two
   U_br copies are NOT ℤ₂-exchanged leaves: the certs' own construction types them as
   retained-sector vs mirror/bulk (`verify_unified_feedback.py:111`), an owned asymmetric
   distinction (retained/traced split, BOOK_06 §06.20; `external_mirror_model_forbidden`,
   BOOK_00 §00.2). The §01.7.1B ℤ₂ lives INSIDE one copy (Umu² realizes the central −1 within
   a single U_br). θ ≠ π/4 carries no forbidden register. What survives: the **θ-family cert gap**
   (real; unitary unbalanced ports are invisible to the existing negative control —
   `r1_theta_port.py` is the fix template) and the unwritten §02.5.2-factor-2 candidate for the ½.
2. **Sub-forcing B — survives as a conditional norm, two wounds accepted.** If the chain has a
   physical instance, λ cannot be a free U(1) label (M1+). Corrections of record:
   (a) **my error** — K†K does NOT vary with λ (K is built λ-free); λ and the contact bit live in
   **disjoint sectors** of the present construction, so a forced λ would not discriminate m vs m′
   (scout route 1 is thereby closed); (b) "finite candidate set" overstated — without a frozen
   derivation grammar, derived points are countable-dense.
3. **Λ1 (λ = 1 forced) — DEAD.** Leg 1: §03.16.B verbatim is the single *dimensional* anchor plus
   "zero dimensionless free parameters" — it bans even one free phase (that is just B again) and
   permits any number of *derived* phases; single-vs-two does not follow. Leg 2: the §01.22 blades
   govern the addressor cycle's step count, not holonomy values; the dichotomy misses the third
   branch (off-lattice phases registered by spectral outcomes); and **the only owned phase-circle
   discretization is the base μ₉ lattice** (§01.22:103–122, N = D = 9) — on owned rails the route
   terminates at λ ∈ μ₉ (nine candidates), not λ = 1. G3: the joint-register alternative is
   actively favored by owned material (edges as addresses, 359 pairs, §04.5.2). G4 sweep: **no
   owned row anywhere requires λ ≠ 1** — the bare slice is unfalsified; "golden phase" has no owned
   phase reading (W_int = φ⁻² is owned as a weight, not a U(1) phase).
4. **Zone-typing correction:** the labels defect/torus-memory/shell at 9/11/13 are verbatim in a
   CORE-FORCING section (§01.22, +2-step) but as underbrace annotations on the address ladder; the
   forced object of that section is the +2 step. "§01.22 owns the typing" is retracted to
   "§01.22 carries the typing inside a forced display" — annotation-grade, not owner-grade.

## The strategic conclusion after three kills

v1 (necessity-typing), v2 (representation-home), Λ1 (lattice-coprimality) died for structurally
similar reasons: the corpus's own architecture — extensions seeing only the abelianization,
λ-uniform cert identities, disjoint λ/contact sectors, the μ₉-owned phase circle — leaves the
remaining items **without present-core forcing routes**. Three independent failures with three
independent skeptics is the honest signal that the residual is genuinely postulate-shaped. Per the
corpus's own method ("take the only-possible, never the best"; and when only-possible is not
available, name the postulate), the campaign's product is the exact minimal contract:

**Minimal honest X5 content (final form):**
- **S1 — the contact bit:** m = (9↔E₀, 11↔E₄, 13↔E₃), selected by three independent owned-material
  chains (return-order key, downstream consumption, contact construction), zero owned support for
  m′, provably unforceable through zone extensions (E₄ extension-invisible) and cert identities
  (swap-uniform). One bit, selection.
- **S2 — the λ point:** λ = 1, the distinguished bare slice, unfalsified by anything owned (G4
  sweep), unique completion requiring no new forcing text. Selection; decoupled from S1.
- **S3 — the port ½:** owner-candidate = the §02.5.2 factor-2 forcings, unwritten; until written,
  a selection with a computed uniqueness base (F = ½ ⇔ θ = π/4).
- **R2 — the structural scene attachment** (`TorusShell → Shell3/radius` Lean map): mechanical,
  not a postulate — an engineering debt.

## Assets banked by the campaign (all machine-checked, none killed)

Ihara–Bass cert (W1); Bartholdi typed-zeta cert with golden-point exactness (W7); the holomorph
module with 18 checks incl. block-typing signatures (W8); exact contact spectra with equal-det
structure (R1); the θ-port control (R1); impossibility results: E₄ extension-invisibility,
swap-uniformity of cert identities, λ/contact sector disjointness; and the T1 presentation-
covariance frame with the Bartholdi identity family as the single Ξ candidate.

## Recommended next phase: consolidation

Forcing-hunting at this residual is exhausted; the open fronts that remain live are different in
kind: (1) fix the θ-control gap in the corpus cert (protocol-clean patch); (2) draft the re-scoped
X5 contract statement {S1, S2, S3} (plan-only, then owner review); (3) discharge R2 (Lean
attachment); (4) T1's remaining Ξ_N-level upgrade via the Bass block-proof maps — the one
still-live forcing-grade front of the whole programme; (5) mint the banked certs into `05_CERTS`
via `VERIFIED_CLOSURE_PROTOCOL.md`.
