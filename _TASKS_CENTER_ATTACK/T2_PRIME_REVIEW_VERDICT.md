# T2′ REVIEW VERDICT — the skeptic's kill is accepted; the residual is one identification

**Author:** chief researcher, adjudicating the adversarial review of the T2′ package.
Status: verdict of record for this task pack; no registry row edited.

## Accepted kills and wounds (with the reviewer's named objects)

1. **Type forcing v1 (T2_PRIME_TYPE_FORCING.md) — KILLED, correctly.** Step (i)'s premise ("zone
   11's memory necessity = the obligation to encode order") is contradicted by its own cited owner:
   BOOK_00:168 locates the torus memory in *abelian* two-loop data and expels order-encoding to the
   defect. The named survivor **m′ = (9↔E₀, 11↔E₃, 13↔E₄)** passes v1's blades. Step (ii) used
   blade 1 outside its owned scope (subgroup normality ≠ basepoint registration in isotypes).
   Both criticisms are per §05.8.R and stand.
2. **Scene half — wounded to cardinality.** `GenerationPhasonMode := TorusShell` transports only
   `card = 3`: there is **no Lean map** `TorusShell → Shell3` or `TorusShell → radius`; the
   zone-degree labels are constructor spellings with no attaching theorem. Named gap of record.
3. **Citation defects (mine):** the three-necessities text is in BOOK_00's Method subsection
   (lines 161–169), not §00.8 as I wrote twice; and it only COUNTS the necessities — the per-zone
   assignment was assembled from scattered prose. The phrase "forced role dictionary" over-reads
   §01.7.1A (the group is forced; the axis assignment is Out(Q₈)-gauge — the memos consume only the
   Aut-invariant set-level fact, so no load-bearing damage, but the phrase is retracted).
   Check-count bookkeeping (11 vs 14) noted.

## What survives review (reviewer-confirmed)

- **E₀-existence** of the third branch (internal, unramified) — survives.
- **The holomorph module** (L_i, α) with its three block signatures — survives attack D outright:
  signatures are conjugation/axis-invariant; no named second pair produces different signatures.
  (λ=1-slice conditionality of the TerminalReturn contact is inherited and must be stated.)
- **The base leg 9↔E₀** — survives in *both* named matchings (m and m′); typed, not forced.

## The sharpened residual: ONE identification

With the base leg agreed, the entire open content of the matching is the single bit
`{11,13} ↔ {E₄,E₃}`. The §01.20 owned extension structures (verified verbatim this session, with
cert `vp_v1141_abcd_omega8_v9_phi_capacity.py`):

```
V₁₁ = V₉ ⊔ D₂     (the direct/return dyad, 2 elements)
V₁₃ = V₉ ⊔ ABCD   (the full terminal role square = D₂×D₂, 4 elements)
```

mirror exactly the canonical exact sequence of the role group, `1 → Z → Q₈ → V₄ → 1`:
Z = {±1} (2 elements; = [Q₈,Q₈] = Φ(Q₈), the "one ℤ₂" of §01.7.1B, CORE-FORMALIZED
`D0-OMEGA8-CENTER-001`) and Q₈/Z ≅ V₄ = the ABCD square (4 elements). On the Fourier side
(verified exact): E₄ = the unique sector where Z acts nontrivially; E₀⊕E₃ = the V₄-character sector.

So the one-bit question reduces to one cross-identification:

> **ID₂:** the V₁₁-extension dyad D₂ ("direct/return", §01.20) is an incarnation of the structural
> ℤ₂ (= Z(Q₈), the order-memory register).

If ID₂ is owned ⇒ 11 ↔ E₄ (Z-nontrivial sector) is forced, 13 ↔ E₃ by elimination, and the matching
closes. If ID₂ is refused ⇒ the honest minimal X5 contract contains exactly: ID₂'s bit + the
structure-level scene half (the missing `TorusShell → Shell3/radius` attachment). Either way the
contract is far below its current form ("postulate the full branch→row assignment").

**Why ID₂ is plausible but NOT claimed:** the corpus already unifies orientation bit, order-memory
remainder and Frattini remainder into one ℤ₂ (§01.7.1B triple collapse), and BOOK_06 adds det T = −1
as another incarnation. Blade 2 (§01.7.1A) attaches order-memory to the *forward/return split
C₊/D₋* — which reads as a role sub-dyad inside ABCD, not obviously the sign/central bit. Whether the
§01.20 "direct/return dyad" is the C/D role dyad, the ± sign bit, or the p/p² channel dyad (§01.6
Route A) is precisely the ambiguity ID₂ must resolve — the reviewer correctly called this "a new
nail of exactly the P_iso shape." It is now the SINGLE such nail, precisely posed, with three named
candidate readings and one candidate owner-home (the §01.7.1B "one ℤ₂" unification list).

## Honest endpoint of T2′ as of this verdict

- Third-generation existence: internal (E₀). Assignment: reduced to ID₂ (one bit).
- Scene half: cardinality paid; structural attachment (shells ↔ radii/zones in Lean) is a named gap.
- "Postulated content = ∅" is NOT reached and is retracted as a target claim until ID₂ and the
  structural attachment have owners; the reviewer's endpoint ("minimal contract = matching rule +
  structural scene half") is adopted, now sharpened to (ID₂ + attachment).
