# R-A1 result — FAIL under the frozen criterion (scalar fidelity vindicated)

**Freeze:** `A1_A2_ACQUISITION_2026_08_14.md`, commit `02413ce`, BEFORE data contact
(scout saw header+rowcount only). **Data:** Potapov–Ong 2017 `results.csv`, hash-pinned
(manifest `08_PASSPORTS/PolymeraseFidelity/polymerase_fidelity_manifest.json`, status
READY, first fetch = the pin). **Runner:** `r_a1_run.py` (deterministic, seed 20260814;
verifies the SHA256 pin before reading).

## Outcome under the frozen adjudication

- **R-A1 (primary): FAIL, control FIRED.** 46 samples with nonzero substitution counts
  (3 zero-count samples excluded, recorded), 10 enzyme labels. Mean within-enzyme JSD
  0.2518 (129 pairs) vs mean between-enzyme JSD 0.2385 (906 pairs): **R = 0.947**, far
  below the 2.0 threshold. Null control: 0/10 000 enzyme-label permutations reach R ≥ 2.0
  — the criterion was informative; the FAIL is real. **Frozen consequence:** the "which
  distinction is lost is channel-specific" refinement is DEAD on this dataset; the scalar
  parameterization of record fidelity (a single rate) is vindicated — substitution-spectrum
  shape is dominated by sequence context (amplicon), not by channel identity.
- **R-A1b (secondary, declared underpowered):** Deep Vent exo⁺/exo⁻ mean JSD 0.0414 vs
  within-enzyme 99th percentile 0.6931 — NOT selective: removing proofreading rescales
  the error rate without reshaping the spectrum, consistent with a uniform attenuator.

## Post-hoc notes (recorded as post-hoc, NOT promoted)

- The `Enzyme` column contains the label `pWB407` (a template/plasmid control set, not a
  polymerase); the frozen criterion grouped by the column as declared. Sensitivity to
  excluding it was NOT run (would be post-hoc); anyone re-running may freeze a new
  criterion in a new file.
- Consistency observation only: a scalar fidelity parameter and a uniform proofreading
  attenuator are exactly the shape of the (independently built, this same day) proofreading
  floor's carrier (single δ, multiplicative stages). This is CONSISTENCY, not
  confirmation — R-A1 tested and killed the vector hypothesis, it did not test the floor.

## Program consequence

The genetics column of the coverage matrix acquires its first PASSPORT cell (move 1,
record): `potapov_ong_pcr_fidelity_2017`, with an executed frozen-criterion result. Both
A-target runs so far (R1 quasicrystal, R-A1 genetics) returned honest negatives — the
sieve discipline is holding under the new-data road as well.
