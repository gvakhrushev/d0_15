# SEAM_RATE_ADOPTION — closing the (ii) residual: the owner sentence for §02.13.h:88 and the Lean carrier `D0.Spectral.SeamRateImport` (POST-SKEPTIC v2; W1–W6 repaired; MINTED)

> ## ⚖️ VERDICT (skeptic #22, independent agent, 2026-07-18): **BULLET WOUNDED (4) · LEAN WOUNDED lightly (docstring) · MINT PACKAGE WOUNDED (2) — NO KILL; mint authorized after repairs.**
> Skeptic reproduced: module compiles exit 0; the induction genuinely consumes `ih`; the
> hyperbolic control genuinely can-fail (an orthogonal imposter makes `decide` refuse); no
> duplicate theorem in D0/; no-tautology guard PASS; both book guards simulated SAFE on the
> proposed text (backtick-stripping + lowercase-member rule; ledger-sync window keeps its
> OPEN_W suppressors; the edit even removes a closure word from :88); the retired phrase breaks
> no citation; the grading is faithful, not a quiet upgrade. **Wounds, accepted in full:**
> (W1) isometry clause cited `D0-Q8-SIN-CHANNEL-001` — the one-turn isometry is owned by
> `D0-SEAM-HOLONOMY-001` (row 285; 288 owns the sin-channel) — REPAIRED in bullet + Lean
> docstring. (W2) "the value machine-checked at D0-IF-KS-FORMULA-FIX-001" blurred row 253's
> own boundary (Lean owns the RADIUS; the entropy reading and the numeric are the wrapper) —
> REPAIRED ("the radius |λ_max|=φ machine-checked…; the entropy reading the named external
> wrapper"). (W3) "the golden time dynamics" was an undefined coinage colliding with row 248's
> named-gap vocabulary — REPAIRED ("the toral time generator (BOOK_06 §06.30a)"); Lean
> docstring's "golden symbolic system" likewise replaced. (W4) ":88" source-coordinate leak in
> the :95 companion — REPAIRED ("the stretch bullet above"). (W5, strongest finding) the mint's
> own verification gate failed undeclared: the new :88 line lands in C4's `incidental` class
> (machine-demonstrated) while ATT-5 declared the flip confined to C5/C6 — REPAIRED: cert v4
> adds the ADOPTED_SIG class to C4 with re-verified mutants. (W6) §4.3 was append-vs-replace
> ambiguous and silently re-worded the residual — REPAIRED ("append (additive)" + the
> assembly-cap clause: assembly = the ceiling available; the entropy reading is permanently
> the external wrapper, boundary as row 253). Memo §1's justification also mis-attributed a
> paraphrase as a located quote ("one closure crosses it once and cannot return" is §02.13.h:91,
> with the single-crossing input graded a NAMED PHYSICAL HYPOTHESIS, not a theorem) — REPAIRED.

**Date:** 2026-07-18 · **Status:** POST-SKEPTIC v2; Lean module compiled standalone (exit 0);
mint applied after the verdict (see §4 APPLIED markers). Owner continuation ("продолжай") authorizes mint after the
skeptic pass, per this session's established flow.
**Lineage:** executes the single residual of `SEAM_RATE_IMPORT_MEMO.md` (POST-SKEPTIC v2,
skeptic #20) — "owner adoption at owned grade" for obligation (ii) of
`D0-ALPHA-SEAM-FORM-FORCED-001` (row 564). Pre-flight: unchanged from that memo (rows
243/244/248/253/285/529/564 re-read; no new owner appeared; `ALPHA_SEAM_NOGO_V2` hooks
untouched).
**Companion artifacts:** `09_LEAN_FORMALIZATION/D0/Spectral/SeamRateImport.lean` (new; three
theorems, one of which is the conclusion-failing control); cert `seam_rate_import_check.py`
v4 (post-adoption state: C4 gains the ADOPTED_SIG class — W5 — and C5/C6 re-pointed; mutants
re-verified).

---

## §0. What adoption means here (graded exactly)

Obligation (ii) = "the cross-scope identification of the registered multiplier rate (row 285)
with the owned rate value (row 243)". After the (ii) pass the identification is
ASSEMBLY-CANDIDATE-MINTED (in-print §06.30a sentence + reciprocity + record cert); the residual
is that the α-line's own stretch bullet (§02.13.h:88) still names NO system, and no Lean
carries the structure. Adoption =

1. **The owner sentence** — §02.13.h:88 rewritten to bind the system in-print on the α side,
   with every load-bearing clause carried by an existing owner (below);
2. **The Lean carrier** — the machine-checkable STRUCTURAL half (isometry at every power ⇒
   zero intrinsic rate ⇒ import), new module `D0.Spectral.SeamRateImport`;
3. **The registry note** — obligation (ii) moves from "owner adoption residual" to "ADOPTED
   (assembly grade); the Lyapunov/entropy reading stays the named external wrapper" — the SAME
   permanent wrapper boundary row 253 already carries for the source side. Release status of
   row 564: UNCHANGED (PROOF-TARGET — held by (i) and (iii)).

What adoption does NOT claim: no dynamical-systems formalization of KS entropy; no new THE;
no change to the FORM row's open obligations (i)/(iii); no touch of the 9-digit CHK/HYP split.

## §1. The proposed owner sentence (replaces the :88 bullet; every clause owned)

> - stretch `h_{KS}=\ln\varphi` — the Kolmogorov–Sinai rate of the toral time generator
>   (BOOK_06 §06.30a), imported once per monodromy turn: the seam monodromy itself is an
>   isometry at every power (`D0-SEAM-HOLONOMY-001`; iterated form
>   `D0.Spectral.SeamRateImport`) and so carries no intrinsic rate; the imported rate is the
>   toral `h_{KS}=\log|\lambda_{\max}|=\log\varphi` (BOOK_01 §01.21.4; the radius
>   `|\lambda_{\max}|=\varphi` machine-checked at `D0-IF-KS-FORMULA-FIX-001`, the entropy
>   reading the named external wrapper), and the single-turn count is the one directed
>   crossing of `D0-ALPHA-HOLONOMY-LINEAR-FORM-001`;

Clause-by-clause ownership: "isometry at every power" — `seamU_pow_orthogonal` (new Lean, plus
the existing one-turn `seamU_orthogonal`, row 285 `D0-SEAM-HOLONOMY-001` — W1); "no intrinsic
rate" — Pesin wrapper, SAME grade as row 253's registered wrapper (cited, not hidden); "the
imported rate is the toral…" — the §06.30a in-print identification + §01.21.4 [THE] value
chain + row 253 Lean (`toral_spectral_radius_eq_phi`, the RADIUS only — W2); "once per
monodromy turn" — the named single-crossing physical input plus the parabolic transport
theorems of row 529 (`D0.Spectral.SeamTransportLinear`; the book's own wording at §02.13.h:91:
"one closure crosses it **once** and cannot return" — quote located, hypothesis grading
preserved). The phrase "forced by the `\varphi` eigenvalue" of the old bullet is RETIRED — it
was referent-free prose; the new text names the referent instead of gesturing at it.

Companion edit in the SAME source file (`0018__02.13__…md:95`, the (ii) clause; W4-repaired):
from "…ASSEMBLY-CANDIDATE, cert-carried; owner adoption is the residual; the seam monodromy
is an isometry, so the rate is necessarily an import…" to "…ADOPTED at assembly grade — the
stretch bullet above binds the system (structure `D0.Spectral.SeamRateImport`; §06.30a
reciprocal; the entropy reading remains the named external wrapper, boundary as
`D0-IF-KS-FORMULA-FIX-001`)…".

## §2. The Lean carrier (compiled, exit 0, standalone)

`D0.Spectral.SeamRateImport`: `seamU_pow_orthogonal (θ n) : ((seamU θ)^n)ᵀ·(seamU θ)^n = 1`
(induction on the owned one-turn orthogonality — the iterated turn dynamics never expands);
`seamU_det_one` (proper rotation, not a reflection); `hypCtrl_not_orthogonal` — the
conclusion-failing control: the hyperbolic `[[2,1],[1,1]]` (radius `φ²`) fails orthogonality
at one step (`decide`, exact ℤ). Docstring carries the honest scope (wrapper external; the
identification is the in-print sentence + record cert, not a Lean theorem). Reuses, does not
restate, `toral_spectral_radius_eq_phi` (§05.8.U).

## §3. Pre-registered attack surface

- **ATT-1 (strongest: "adoption by whom?").** The reviewer drafts and mints under the owner's
  session continuation, exactly as rows 563/564 were minted in the owner-authorized session.
  If a skeptic rules that adoption of a THEORY sentence needs more than session authorization,
  the fallback is explicit: the package stays DRAFT and the residual stands — nothing else in
  the memo changes. The mint is sequenced AFTER the skeptic pass for exactly this reason.
- **ATT-2 (grade inflation in the new bullet).** The bullet must not smuggle "THE" onto the
  identification. Audit invited: the proposed text grades each clause by its named owner and
  calls the entropy reading a wrapper. If any clause reads as owning more than its cited
  owner, that clause must be cut — invited verbatim comparison against rows 253/285/529.
- **ATT-3 (Lean triviality).** `seamU_pow_orthogonal` is a five-line induction; its value is
  the STRUCTURE attribution (the iterated dynamics, not one turn) + the control. Priced: same
  class as the M1-universality Leg-A grading ("the abstract theorems' sole content is the
  schema attribution") — the campaign has accepted this class when the attribution is the
  claim. `hypCtrl_not_orthogonal` makes the class non-vacuous.
- **ATT-4 (guard interactions).** The :88 edit inserts claim-IDs into a line scanned by
  `check_book_ledger_sync` (IDs of CLOSED-tier rows 529/253 near no closure words — safe by
  design, verify by running); `check_book_publication` (no IDs in headings — :88 is a bullet,
  not a heading); the audit budget (line starts with `-` → OK_STRUCTURAL). All guards re-run
  at mint.
- **ATT-5 (does :95's edit break the (ii) cert?).** Yes, by design: C5's `owner_adopted`
  live-flag flips when ":95 names the residual" disappears. The cert update to the
  post-adoption state ships IN the same mint (C5 asserts ADOPTED; C6 asserts the :88 bullet
  NOW names the toral generator; mutants updated) — the mint is atomic: book + Lean wiring +
  registry + cert together, then the full suite.

## §4. Mint package — **APPLIED 2026-07-18, post-skeptic #22, all W1–W6 repairs in** (verification: `lake build D0.All` GREEN with `SeamRateImport` wired; books reassembled idempotent; cert v4 7/7 + 7/7 mutants incl. M7; both α-line certs rc=0; full guard suite + `validate_csv` + `check_no_dangling_lean_module` PASS; row 564 release UNCHANGED — PROOF-TARGET, held by (i)/(iii))

1. Wire `D0.Spectral.SeamRateImport` into `D0/All.lean`; full `lake build D0.All`.
2. §02.13.h:88 bullet → §1 text; :95 (ii) clause → §1 companion text; reassemble; guards.
3. Row 564 note APPEND (additive — W6; the skeptic-#20/#21 history stays intact): "(ii)
   ADOPTED [2026-07-18, post-skeptic #22] at assembly grade — the ceiling available: the
   entropy reading is permanently the external wrapper, boundary as
   D0-IF-KS-FORMULA-FIX-001. The 02.13.h stretch bullet now binds the rate to the toral time
   generator (structure D0.Spectral.SeamRateImport — isometry at every power + hyperbolic
   control; radius machine-checked at 253; single-turn count = the named single-crossing
   input + parabolic theorems of 529; record cert seam_rate_import_check.py v4). Remaining
   open: (i) five-candidate fork, (iii) joint exhaustion." Row 285 note: one-line pointer
   append. Sync registries.
4. Cert v4: C5/C6 re-pointed to the adopted state (+mutant M7: strip the :88 system-binding →
   C6 fails).
5. Re-run both α-line certs, full guard suite, validate_csv, lake build.
