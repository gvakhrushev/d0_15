# ARROW INTERLOCK — equilibrium depth collapses; the exit is forced (MINTED)

**Status:** MINTED as `D0-CASCADE-INTERLOCK-ARROW-001` (2026-08-14) after independent
skeptic verdict **MINT-AFTER-REPAIRS** (0 kills; skeptic reproduced both builds, axioms,
identifiers, and PROVED the class inhabitation himself with a compiled witness — vacuity
kill dead). **Repairs applied (errors of record):** (1) "same grade as link 4" was FALSE —
link 4 has the independently owned §03.23.5; this identification is docstring-anchored
NEW prose with no book owner and no arrow-side formal object, strictly below link-4
grade; book-prose target named as continuation; (2) "some step must break detailed
balance" sat inside "Lean-forced" — moved out: necessity is CLASS-RELATIVE (factor-list
representability scope; un-modeled carrier shapes outside the statement, no exhaustion
claimed); (3) elementarity honesty carried: the depth-universal insufficiency composes
the floor's own one_test_insufficient through a one-rewrite reach-set inclusion;
repair_breaks_equilibrium is the elementary annotation δ²≠δ; the class-exit at target is
carried by equilibrium_depth_collapse; (4) advisory accepted: equilibrium_class_inhabited
lemma ADDED (inhabited at depth 1 and 2 — now 5 theorems); (5) preflight enumeration
corrected: 'detailed balance'/'irreversib' = 4 registry hits including the floor row
itself (the memo's own anchor). Pre-flight (corrected): 'equilibrium' in registry = 1 hit
(the floor row); other hits D0-SCENE-NORMALIZED-QUOTIENT-DESCENT-001 (graph adjacency),
D0-RANK3-CAUSAL-CONE-FORCING-001 (metric cone), D0-PISOT-CONTRACTION-TIME-ARROW-001
(anchor, T5), D0-CASCADE-FLOOR-PROOFREADING-001 (anchor row) — no rival owner;
D0-TIME-ARROW-ORDERED-SELF-READOUT-001 non-binding (order-readout carrier). INTERLOCK
namespace: only D0-CASCADE-INTERLOCK-SCALE-001 existed. ID
`D0-CASCADE-INTERLOCK-ARROW-001`; the row closes the named target of the floor row's
note at the stated grade (floor row untouched — lighter handling per skeptic vector 7).

## Claim X (DEF-0.2.2 form)

X (Lean `D0.Foundation.CascadeInterlockArrow`; standalone 3031 GREEN; all four theorems
at axioms propext/Classical.choice/Quot.sound): for every discrimination limit
`0 < δ < 1`, with the equilibrium-pathway carrier `EquilibriumPathReaches` (nonempty list
of positive stage factors whose product TELESCOPES to the endpoint ratio δ — detailed
balance as the defining hypothesis, cited reading):

1. **Collapse** (`equilibrium_collapses_to_one_test`): every equilibrium pathway is
   extensionally a single test — whatever it reaches, one test at the endpoint ratio
   reaches. Depth adds nothing at equilibrium (any length, one theorem);
2. **Depth-universal insufficiency** (`equilibrium_depth_collapse`): no equilibrium
   pathway of ANY depth reaches `errOf (δ²)` — closes the "just add more equilibrium
   tests" escape against the proofreading floor;
3. **Forced exit** (`repair_breaks_equilibrium`): the repair's stage product `δ²` cannot
   telescope to the endpoint ratio `δ` — the two-stage carrier lies OUTSIDE the
   equilibrium class; some step must break detailed balance;
4. assembly `interlock_proofreading_arrow`.

**The upgrade over the floor row:** in `D0-CASCADE-FLOOR-PROOFREADING-001` the
irreversible reset is an assumption inside the repair's definition; after this module the
EXIT from equilibrium is NECESSARY (equilibrium alternatives of every depth are provably
insufficient), not stipulated. The identification of the broken-balance step with the
time arrow's owned carrier (Pisot contraction, T5) is PROSE-ANCHORED — same grade as
interlock link 4 (shell-closure), stated as such in-module.

## Named risks & PRE-REGISTERED attack surface (strongest first)

- **ATT-A (strongest: the collapse is definitional telescoping).** `fs.prod = δ` makes
  the collapse one `rw`. Defense, pre-registered: TRUE and stated — the class IS the
  cited detailed-balance structure (path-independence of equilibrium constants); the
  formal content is (i) depth-universality (∀ length in one statement — the floor had
  only depth 1), (ii) the collapse stated at CARRIER level (`EquilibriumPathReaches →
  OneTestReaches` — an inclusion of reach-sets, not just an error bound), (iii) the
  forced-exit wiring: the SAME product the repair needs is the one the class forbids.
  Precedent grade: registered floors' below-carriers are also definitional classes
  (one ℤ register; abelian torus). A kill must name a floor/link-grade defect, not the
  elementarity.
- **ATT-B (δ² ≠ δ is trivial).** Yes — elementary arithmetic; its content is the wiring
  (honest scope #2 in-module). Same defense pattern as ATT-A(iii).
- **ATT-C (arrow identification).** NOT formalized; prose-anchored, graded in-module and
  in the row; the Lean statement never mentions the arrow. A kill must show the row
  claiming more.
- **ATT-D (carrier mismatch with the arrow rows).** T5's carrier is Pisot contraction
  (real quadratic field); TIME-ARROW-ORDERED-SELF-READOUT's is an order readout; this
  module's is lists of rationals. No binding claimed either way — pre-registered.
- **ATT-E (duplication).** Pre-flight above; 'equilibrium' owner is the floor row itself;
  this row is its named-target completion (the floor note says "the arrow-floor interlock
  is a named target" — this row closes exactly that target at the stated grade).

## What this does NOT show

The arrow floor's own content (T5 untouched); any n-stage floor family (ladder caveat
stands); any thermodynamic cost accounting (energy per reset — future); any biochemical
claim; any binding between the rational-list carrier and the Pisot carrier (prose only).
