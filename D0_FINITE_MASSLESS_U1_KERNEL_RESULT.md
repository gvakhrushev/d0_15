# D0 Finite Massless-U(1) Kernel — Track A Result

$$
\ker B_{\rm graph}\;\xrightarrow{\;\Phi\ (\text{PROOF-TARGET})\;}\;\mathcal H_{\rm abelian}\;\xrightarrow{\;C_{U(1)}\;}\;M_{U(1)}=C_{U(1)}^\dagger C_{U(1)}\;\to\;\ker M_{U(1)}=\operatorname{span}\{Y,\,B\!-\!L\}
$$

> **Outcome: Case A2 — already decided by existing owners; CITE-ONLY.** The massless-`U(1)` kernel is the
> **2-dimensional** anomaly-free family `span{Y, B−L}` (NOT a unique direction). Collapse to `span{Y}` requires
> the bridge `Ξ_Y` (`ν^c` uncharged = `ν_R∈ker`, a MECH-LIMIT, not a forced identity). The source-native
> coupling map `Φ` (graph-flow → Weyl) is **not** forced — it is the open `PROOF-TARGET` + `PASSPORT`. No new
> Track-A theorem is warranted; this result consolidates and cites.

## 1. Frozen source objects / 2. Operators
`K(9,11,13)`, anomaly matrix `A=[[6,3,3,2,1,1],[3,0,0,1,0,0],[2,1,1,0,0,0]]` (`HyperchargeAnomalyVariety`),
the divergence-free current lattice `ker B_graph` (dim 327, `HyperchargeFlowLattice`), the charge ledgers
`L₅=(q_L,u^c,d^c,ℓ_L,e^c)` and `L₆=L₅∪{ν^c}`.

## 3. Classification of admissible choices
The joint (linear+cubic) anomaly-free locus **contains the 2-dim `span{Y, B−L}`** over ℚ
(`anomaly_free_contains_two_independent`; `rank A = 3` via `anomaly_rank_eq_three`, so linear solution dim
`6−3=3`; `Y` and `B−L` both `grav/su2/su3/cubic`-free and ℚ-independent).

## 4. Exact theorem / no-go (per case)
- **Case A2 (the kernel):** `dim ker M_{U(1)} ≥ 2`, residual `B−L`. **NO-GO** `D0-HYPERCHARGE-ANOMALY-VARIETY-2DIM-001`.
- **Direction collapse:** `span{Y,B−L} → span{Y}` iff `Ξ_Y` (`charge(ν^c)=0`); necessary+sufficient+minimal,
  **BRIDGE** `D0-HYPERCHARGE-BL-DIRECTION-BRIDGE-001` (MECH-LIMIT, not THE).
- **5-field truncation:** `L₅` cubic factors `−18a(t−3a)(t+3a)`; SM row unique on the `t=−3a` branch up to
  normalization/`u↔d`/degenerate-branch — but this *bakes in* `Ξ_Y` (`ν^c` excluded), so it is near-A1 only
  relative to the bridge (`D0-SM-HYPERCHARGE-ROW-OWNER-001`).
- **A4 graph-flow origin (separate):** **no** forced `Φ_{graph→chiral}` — `D0-GRAPH-SPACE-NO-ISOMETRY-001`
  (distinct sizes ⇒ trivial induced zone-symmetry ≠ cyclic `Aut(Q₈)`; anisotropic norms).

## 5. Removed/downgraded; 6. Minimal missing operator
No unique-massless-direction `THE` is claimed (would contradict the 2-dim NO-GO). The one open item is the
coupling/flow map `Ξ_{U(1)} = Φ : ker B_graph (327) → Weyl charge ledger`, **already owned** as
`D0-HYPERCHARGE-GRAPH-FLOW-OWNER-001` (PROOF-TARGET) + `D0-HYPERCHARGE-FLOW-ORIGIN-PASSPORT-001` /
`D0-FINITE-SPECTRAL-TRIPLE-REPRESENTATION-PASSPORT-001` (PASSPORT), needing `PRIM-FINITE-SPECTRAL-TRIPLE-REP`;
blocked by `D0-REPRESENTATION-RECONSTRUCTION-MAXIMALITY-NOGO-001` (bare `Aut` gives only rank-3 `M₃`/`GL(3)`
freedom). **Do not re-mint; do not fabricate from bare `Aut(K(9,11,13))`.**

## Where the derivation stops (acceptance)
`ker B_graph → 𝓗_abelian` is the **open arrow** (`Φ`, PROOF-TARGET). Everything downstream
(`C_{U(1)} → M_{U(1)} → ker = span{Y,B−L}`, collapse via `Ξ_Y`) is owned. No string/brane/flux/Wilson/GSO/α′
input was used.
