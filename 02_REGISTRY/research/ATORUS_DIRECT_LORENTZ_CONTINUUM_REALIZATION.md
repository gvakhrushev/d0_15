# E-TORUS — Direct Typed Torus / Lorentz Continuum

CONTROL disposition: **ACCEPT AS RESEARCH / DIRECT-T4-METRIC-CONVERGENCE-BRIDGE-OPEN**  
Source memo: `MEMO_24_ATORUS_DIRECT_LORENTZ_CONTINUUM_REALIZATION.md`

## Main result

The explicit archive role-phase tower admits a much lower-debt continuum target than the current proposition-only reconstruction shells.

The finite group shadow is
[
X_N\simeq(\mathbb Z/L\mathbb Z)^{Role},
\qquad L=N+2,
]
and pinned mathlib provides the ingredients for a direct coordinatewise map
[
\iota_N:X_N\hookrightarrow(\mathbb R/\mathbb Z)^4\cong(S^1)^4
]
through the roots-of-unity map
[
j\mapsto e^{2\pi i j/L}.
]

Thus Rieffel/Connes need not remain on the critical path merely to answer:

- what the continuum point set is;
- what smooth four-manifold is being used as a testbed;
- where each finite grid point maps.

## Metric status

The one-coordinate cyclic distance is compatible with the unit additive-circle quotient metric and should admit an exact grid identity.

The unresolved load-bearing step is four-dimensional Riemannian metric ownership:

[
d_{2,N}(x,y)
=
\sqrt{\sum_r d_N(x_r,y_r)^2}
]

is not currently an owned D0 metric.

This distinction is essential because the graph/path metric retains the accepted (ell^1)-versus-(ell^2) mismatch. Defining an explicit (ell^2) product metric would be a new object and would not invalidate that no-go.

## Laplacian status

The owned role-product Laplacian has exactly the finite-difference shape expected for flat (T^4), and the repository already contains the formal eigenvalue expressions
[
4L^2\sum_r\sin^2(\pi k_r/L)
]
and the continuum target
[
4\pi^2\sum_r k_r^2.
]

But the actual matrix Fourier-eigenpair theorem and fixed-mode convergence are not yet owned.

## Lorentz realization

On the same smooth torus one can explicitly define a flat Lorentz metric
[
g_L=\vartheta_A^2-\vartheta_B^2-\vartheta_C^2-\vartheta_D^2
]
and a global timelike direction.

This is a real Lorentz metric construction, not merely `RoleSign`.

However, making a periodic direction timelike gives an explicit closed timelike curve. Therefore:

[
\boxed{
\text{typed local equation-class testbed: YES}
}
]
but
[
\boxed{
\text{realistic globally causal physical spacetime: NO}.
}
]

## Lovelock firewall

One fixed flat (T^4) metric is not the Lovelock antecedent.

The remaining issue is whether a local response law over the full fixed-signature metric bundle on (T^4), natural under local diffeomorphisms and depending only on metric 2-jets, is already sufficient for the four-dimensional Lovelock/Navarro classification or whether D0 needs a natural family over arbitrary manifolds.

That exact question is assigned to
`EXP-ATORUS-LOVELOVK-LOCAL-UNIVERSALITY`.

Terminal result:
[
\boxed{\texttt{DIRECT-T4-METRIC-CONVERGENCE-BRIDGE-OPEN}}.
]
