# E-PROV — Normal Sampling Provenance and Finite Gauge

CONTROL disposition: **ACCEPT AS RESEARCH / NORMAL-SAMPLING-PROVENANCE-BRIDGE-OPEN**  
Source memo: `MEMO_31_ATORUS_NORMAL_SAMPLING_PROVENANCE_GAUGE.md`

## Result

Strict exponential normal coordinates are not the remaining conceptual problem.

Given a smooth Lorentz metric (g), point (x), and (g_x)-orthonormal frame (F),
[
\psi_F(u)=\exp_x^g(Fu)
]
is fixed wherever defined. There is no residual same-frame higher-order chart ambiguity inside this strict prescription.

Generic Lorentz frame changes do alter the finite axis stencil, so exact finite frame invariance is not expected. But the isotropic reconstructed response has the same continuum limit for every fixed admissible frame.

Thus normal sampling is a legitimate metric-dependent BRIDGE/PASSPORT.

## Missing theorem

The real gap is finite metric provenance.

D0 owns the test carrier
[
LocalSymRoleField(N)
=
X_N\to Sym^2(Role),
]
and the finite symmetric gradient
[
K_N\xi=D_a\xi_b+D_b\xi_a.
]

But no owned theorem identifies current physical finite metric/conductance/Laplacian variables with that ten-component local tensor field.

The strongest current conductance theorem has target a site-site symmetric nearest-neighbour Laplacian variation matrix, not a local tangent Role-pair field.

The seam-derived metric route is diagonal/separable and remains restricted by the accepted cross-role curvature no-go.

## Gauge boundary

`symmetricRoleGradient` is an actual finite map, but D0 does not own a finite local reparametrization action whose tangent is that map.

Global Role translations/permutations and A1 Stueckelberg symmetry do not supply this metric gauge.

Therefore the finite gauge law is still a modelling/bridge principle unless a new provenance theorem is proved.

## Minimal positive interface

The missing owner should provide an actual finite metric state carrier
[
\mathcal M_N^{D0}
]
and an actual map
[
\Theta_N:\mathcal M_N^{D0}\to LocalSymRoleField(N),
]
together with response factorization
[
\mathcal E_N^{D0}(m)
=
E_{\eta,N}(\Theta_Nm)
]
or the exact selected equivalent.

For true finite gauge provenance it should additionally provide an action
[
\rho_N(\xi,m)
]
with
[
\Theta_N(\rho_N(\xi,m))
=
\Theta_N(m)+K_N\xi
]
or the corresponding linearized theorem.

Only after such an owner exists can E-NJET be promoted from an Einstein-consistent stencil passport to a D0-derived Einstein response claim.

Terminal result:
[
\boxed{\texttt{NORMAL-SAMPLING-PROVENANCE-BRIDGE-OPEN}}.
]
