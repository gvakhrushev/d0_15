# SYNTHESIS_A4D_ELEMENTARY_MATTER_LINK_THREE_CHANNEL

## Status

DURABLE SYNTHESIS / NEXT-FRONT SPECIFICATION.

This packet does not own a new theorem. It reorganizes the already landed fixed-level results after merged PR #103, PR #107 and PR #108 and sharpens the next research target.

## 0. Executive statement

The fixed-N constitutive frontier is no longer best described as choosing a nonlinear \(Q(e)\), a comparison jet \(\mathcal S\), a universal \(K\), or an abstract crossed package \(C_N\).

The surviving primitive is one **positive labelled matter letter**

\[
\ell_N^+(A,e,n;x,r):
V_{x+r}\xrightarrow{\sim}V_x,
\]

or an explicitly typed site-corner/path-expression equivalent if a single local fibre map is too small.

The negative letter is not independent. It is the shifted inverse of the positive letter.

A labelled word transport is not a second primitive:

\[
\Pi_{A,e,n}(x;p)
\]

is the ordered product of the letters along the literal \`List ChainStep\` word.

A constitutive/energy object is likewise not a separate selector. The research target is an explicit assembly/section \(W\) transported by the same letters/word action, with

\[
W_0=I,\qquad D_eW_0[e]=H(e),
\]

where \(H(e)\) is already owned.

The unresolved work is therefore an **integration problem for one elementary arrow**.

## 1. Three typed channels of one letter

The letter must simultaneously account for three logically distinct inputs. Calling them channels does **not** assume that the final letter factors as three commuting matrices. Their coupling law must be derived.

### Channel L — linear Cartan / exterior transport

Owned by PR #103.

The current exterior path transport lifts the linear part of affine Cartan transport to the existing 16-state exterior/Fock carrier. It owns:

- exact functorial composition;
- inverse/reversal;
- creator/contraction covariance;
- all-degree exterior action;
- moving observer form \(h_n\);
- frame covariance;
- located-anchor boundary.

This channel sees linear holonomy.

It does not see a pure affine translation \((I,b)\) once only the linear part is fed into the exterior lift.

The worker \`WRK-A4D-AFFINE-SHIFT-EXTERIOR-BLINDNESS\` is the formal truth-firewall for exactly this statement.

### Channel E — raw coframe amplitude / constitutive tangent

Owned at first order, not yet integrated to a finite letter.

PR #75 owns the complete uncentered first jet \(H(e)\), including scalar edge polarization, both half-average terms, mixed CAR blocks, both corner paths, the L=2 Nyquist control and the L=3 corner control.

PR #101 owns the exact pure-gauge benchmark.

PR #108 owns the scalar **infinitesimal descent constraints** for the landed first-jet law at \(L\ge3\):

\[
\sum_{k\in\mathbb Z/L} e_t{}^r(x+k\,t)=0,
\]

and

\[
(U_s-I)e_t{}^r=(U_t-I)e_s{}^r.
\]

These are not yet exact nonlinear relations of the unknown finite letter. They are the tangent equations that any exact finite endpoint-descent relations of the letter must differentiate to.

This distinction is essential.

### Channel B — affine translational/site response

Missing.

The current 16-state exterior lift is blind to the affine shift when the linear part is identity.

The earlier 16-state nilpotent affine candidate and the homogeneous 32-state affine exterior lift do not by themselves supply the spatial/site support required by the landed scalar commutator and full \(H(e)\).

The surviving candidate target is therefore site-aware: a site corner, path-expression corner, or another explicit enlargement that retains archive support while still having invertible labelled letters.

This is the genuinely new channel.

## 2. Observer and located dual are not extra channels

The observer \(n\) enters through the already owned \(h_n\) transport/covariance law. It dresses or constrains the same letter. It is not an additional constitutive primitive and is not physical time.

The fixed located \(J\) is applied only after the primal letter/word action exists. It forces an endpoint-aware contragredient dual package with shifted anchors. It does not construct the primal letter.

## 3. Exact finite relations to integrate

The research target must no longer treat PR #108 as a detached diagnostic.

Instead it must seek exact finite relations of the elementary letter whose first derivative is exactly the PR #108 tangent system.

### Cycle relation

In a sector where the labelled word descends to the compressed periodic endpoint algebra, the finite cycle word must satisfy, in the repository's literal pull order,

\[
\ell_t(x)\,
\ell_t(x+t)\cdots
\ell_t(x+(L-1)t)=I.
\]

Differentiating the correct finite relation at the flat identity must reproduce the owned zero-cycle-sum condition.

### Plaquette relation

In the endpoint-descended sector, the two labelled paths around a plaquette must agree:

\[
\Pi(x;[s,t])=\Pi(x;[t,s]),
\]

again in the literal repository order.

Differentiating this finite equality must reproduce the owned plaquette equation from PR #108.

For generic curl backgrounds the equality is not imposed; its failure is labelled holonomy and therefore blocks endpoint descent.

### L=2 labelled period

At \(L=2\), \`.fwd r\` and \`.bwd r\` have the same endpoints but are different labels.

The exact labelled descent skeleton therefore forces the first finite period relation of the letter:

\[
\ell^+(x,r)\,\ell^+(x+r,r)=I
\]

in the actual pull-order convention.

This is exact word-level structure, not a centered/Nyquist approximation.

The worker \`WRK-A4D-LABELLED-PATH-HOLONOMY-DESCENT\` owns the generic skeleton.

## 4. Assembly, not logarithm

\(H(e)\) is additive while \(\ell^+\) is multiplicative.

Therefore the missing bridge is not the slogan \(H=\log\ell\).

The research must exhibit a literal assembly from site corners / labelled letters to a global constitutive operator or form \(W\) such that

\[
W_0=I,\qquad D_eW_0[e]=H(e).
\]

The coefficients and path supports of \(H(e)\) must emerge from that assembly.

A construction that merely writes \(W(e)=I+H(e)\) has not integrated the elementary link unless it proves that this is the strongest possible site-letter assembly and terminally identifies the missing coupling.

## 5. Pure gauge is the exact chart

For \(e=d_f\varphi\), the already-owned benchmark is

\[
\ell_t=F_\varphi U_tF_\varphi^{-1}.
\]

Any new finite letter must specialize to this law, or be related to it by an explicitly typed proved comparison.

Pure gauge is not permission to identify arbitrary raw \(e\) with a gauge potential.

## 6. The correct role of labelled descent

The free labelled path parent is primary.

Endpoint compression is a quotient derived only after labelled holonomy is trivial in the relevant sector.

Thus:

- harmonic cycle data remain visible as period holonomy;
- plaquette curl remains visible as relative path holonomy;
- L=2 parallel labels remain distinct until the exact period relation is proved;
- algebra membership in a finite matrix/crossed algebra is not the same thing as path-labelled endpoint descent.

## 7. Operational tick boundary

A length-one positive letter is the smallest owned candidate for an operational distinguishability step: source and target are distinguishable when the arrow is nonidentity.

A longer word records internal composition; a closed word with trivial labelled holonomy evaluates to identity after descent.

This is an algebraic distinguishability interpretation only.

Do not promote it to physical time, the Pisot arrow, or the golden tower.

## 8. Golden firewall

The Tower-C golden/AF problem is now a path-selection problem on a separate correspondence grid.

No golden \(\varphi\), AF index, or \(k=n\) rule belongs in the fixed-N elementary matter letter.

Do not use golden data to choose a coefficient, Role label, half-average, observer, or affine translation response.

## 9. Research decision tree

A successful next research pass should answer in this order:

1. What is the exact source/target carrier of one positive letter?
2. How is Channel L embedded?
3. What new site-aware datum makes Channel B visible?
4. How does Channel E enter the same letter/assembly without being inserted as \(I+H\) by definition?
5. What is the algebraic coupling law between L/E/B — direct product, semidirect/crossed product, triangular site-corner action, or something else?
6. What exact cycle and plaquette relations hold in the descended sector?
7. Does their derivative reproduce PR #108 exactly?
8. Does the pure-gauge chart reproduce PR #101 exactly?
9. Does the global assembly reproduce the full \(H(e)\)?
10. Only then: observer covariance and fixed-\(J\) dualization.

## 10. Terminal target

The next EXP should not end with "a crossed constitutive package is needed" unless it proves that the elementary-link integration problem itself requires another primitive.

The desired strongest terminal is an explicit positive letter plus assembly whose:

- products give the labelled path action;
- linear limit is PR #103;
- affine translation response is nontrivial;
- pure-gauge chart is PR #101;
- finite descended cycle/plaquette relations linearize to PR #108;
- assembly derivative is the complete owned \(H(e)\);
- L=2 labelled period is exact;
- generic harmonic/curl information remains in holonomy before descent.

If this cannot be built, the terminal must identify the **earliest failed channel coupling**, not retreat to a generic statement that a representation is missing.

## 11. Exactly one recommended next step

Construct the Channel-B site-corner response and its coupling to the already-owned linear exterior channel, while simultaneously demanding an explicit global assembly whose derivative is \(H(e)\); use exact finite cycle/plaquette relations as the nonlinear equations to be integrated, with PR #108 as their required tangent and labelled L=2 descent as the exact period control.
