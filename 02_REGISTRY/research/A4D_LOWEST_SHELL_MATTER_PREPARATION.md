# E-PREP — Lowest-Shell Matter Preparation

CONTROL disposition: **ACCEPT AS RESEARCH / LOWEST-SHELL-REFINEMENT-BRIDGE-OPEN**

Source memo: MEMO_42_A4D_LOWEST_SHELL_MATTER_PREPARATION.

Add explicitly the new bridge/model primitive:

> physical free matter occupies the normalized lowest-positive spectral shell of the positive CAR/Hodge Hamiltonian \(H_N=D_N^2\).

For
\[
L=N+2,
\]
the flat lowest positive eigenvalue is
\[
\lambda_{1,N}
=
4L^2\sin^2(\pi/L),
\]
with first-harmonic labels \(\{\pm e_r\}\) for \(L\ge3\). Define
\[
\rho_{1,N}
=
P_{1,N}/\operatorname{rank}P_{1,N}.
\]

Conditionally on the advertised operator stack, this is a canonical mixed state with:

- support orthogonal to the zero-mode sector;
- nonzero kinetic energy;
- exact translation symmetry;
- momentum-sign averaging;
- Role permutation symmetry;
- Fock parity invariance;
- no chosen site, phase, momentum orientation or pure ray.

The shell rank is \(64\) at \(L=2\) and \(128\) for \(L\ge3\).

For the positive \(H=D^2\) matter action,
\[
\operatorname{Tr}(\rho_{1,N}H_N)=\lambda_{1,N}>0.
\]
Uniform metric scaling proves the full-metric response is nonzero. At the flat Euclidean Role background, symmetry forces
\[
T_{ab}(x)=\tau_N\delta_{ab},
\qquad
\tau_N\ne0.
\]
Degeneracy averaging removes shear/directional anisotropy but not the isotropic kinetic response.

The naive first-order trace action vanishes by parity:
\[
\operatorname{Tr}(\rho_{1,N}D_N)=0.
\]
Thus the positive \(D^2\) route, not the naive first-order trace, is the viable mixed-state source.

Tensoring a normalized anomaly-free internal species density matrix does not cancel the metric stress because anomaly cancellation and kinetic metric response are different contractions.

Refinement scaling is positive:
\[
\lambda_{1,N}\to4\pi^2.
\]
What is not owned is an exact finite refinement intertwiner transporting the shell/projector through the actual D0 tower. Only the common first-harmonic label family and modewise continuum limit are currently safe.

The uniform flat source also exposes a gravity zero-mode solvability issue because the pure derivative linearized gravity response annihilates constants.

Terminal result:
\[
\boxed{\texttt{LOWEST-SHELL-REFINEMENT-BRIDGE-OPEN}}.
\]
