# E-STATESEL — Matter State Selection Principle

CONTROL disposition: **ACCEPT AS RESEARCH / CAR-STATE-PREPARATION-PRIMITIVE-REQUIRED**

Source memo: MEMO_40_A4D_MATTER_STATE_SELECTION_PRINCIPLE.

Current D0 contains no owned principle selecting a provenance-bearing nonconstant physical CAR/Hodge matter state or density matrix on the local 4D role-phase carrier.

Important distinctions:

- an eigenvalue is not a state;
- an eigenspace/projector selects a subspace, not a pure ray;
- a normalized projector is a mixed state only after the occupied shell has been physically selected;
- evolution requires initial data;
- anomaly-free internal species data are distinct from the geometric 16-state exterior/Fock fibre.

The audit classifies the existing candidates:

- zero modes: degenerate constant subspace; positive quadratic action minimization also selects this sector and gives no kinetic stress;
- lowest positive spectrum: canonical shell, but no owned occupation rule;
- parity: subspace only;
- chirality: no local CAR/Hodge state owner;
- first-order Dirac minimization: no owned positive-energy/filling prescription;
- ActionProtocol: transition-cost lower bound, not state energy;
- trace/max-entropy/thermal states: wrong carrier or require a new temperature/occupation rule;
- archive localization: supplied/nonunique, with the canonical anomaly-free uniform completion equal to zero;
- evolution: initial state still required;
- anomaly cancellation and generations: internal representation constraints only;
- periodic boundary data: operator domain, not occupied state.

No current mechanism reaches a unique pure ray or a physically selected nonconstant local excitation.

The narrowest autonomous mixed-state bridge is a new lowest-positive-shell occupation law. If
\[
H_N=D_N^2,\qquad
\lambda_{1,N}=\min(\operatorname{Spec}H_N\setminus\{0\}),
\]
then one may define
\[
\rho_{1,N}
=
P_{\lambda_{1,N}}/
\operatorname{rank}P_{\lambda_{1,N}}.
\]
This avoids choosing a phase, site, momentum orientation, or basis vector inside the degenerate shell and can carry nonzero kinetic stress. But the assertion that physical matter occupies that shell is itself new physics.

Pure/localized states require an explicit physical preparation/source/boundary event.

Terminal result:
\[
\boxed{\texttt{CAR-STATE-PREPARATION-PRIMITIVE-REQUIRED}}.
\]
