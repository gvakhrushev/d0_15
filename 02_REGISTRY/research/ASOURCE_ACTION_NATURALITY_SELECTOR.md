# A-SOURCE — Action/Naturality Selector

CONTROL disposition: **ACCEPT AS RESEARCH**  
Primary verdict: **SELECTOR-NOGO-TERMINAL**  
Audited commit: `7e4c8718abbf993c003742118781233ec4f41486`

This packet is a durable research summary, not a Lean proof owner. The source memo is
`MEMO_14_ASOURCE_ACTION_NATURALITY_SELECTOR.md`. Its attached exact checker was independently rerun by CONTROL: **42/42 grouped checks PASS**.

## Frozen starting point

Condition on an abstract scene matter amplitude

[
J:SceneC0.
]

A-STRESS already established the exact equivariant quadratic source class

[
operatorname{Hom}_H(operatorname{Sym}^2 C^0,Z)cong mathbb R^3,
]

with

[
sigma_{mathbf c}(J)
=
(c_{AB}j_Aj_B^T,;c_{AC}j_Aj_C^T,;c_{BC}j_Bj_C^T).
]

Modulo one overall nonzero scale, two essential selector ratios remain.

A-STRESS's separate primary boundary `SOURCE-CARRIER-MISSING` is not reopened here.

## Typed correction: triangle factorisation uses the codifferential

For the literal scene owners,

[
d_0=partial_1^T:C^0	o C^1,qquad
D=d_1=partial_2^T:C^1	o C^2.
]

Therefore a map `C0×C0→C2` returns to `C1` through

[
delta=D^*=partial_2:C^2	o C^1,
]

not through `d1` itself.

## Triangle factorisation does not select a coupling

For any coefficient triple (mathbf c),

[
Phi_{mathbf c}(J)_{ijk}
=
rac{c_{AB}}c,j_{A,i}j_{B,j}
-rac{c_{AC}}b,j_{A,i}j_{C,k}
+rac{c_{BC}}a,j_{B,j}j_{C,k}
]

satisfies

[
deltaPhi_{mathbf c}(J)=sigma_{mathbf c}(J).
]

Thus factorisation through the owned triangle carrier is surjective onto the full 3-dimensional quadratic selector class.

Moreover,

[
kerdelta=A_aoxtimes A_boxtimes A_c,
]

and

[
operatorname{Hom}_H(C^0otimes C^0,kerdelta)=0.
]

Hence the equivariant symmetric triangle lift of a **fixed chosen source** is unique, but the source itself still has three free block coefficients.

This distinction is load-bearing: unique lift (
eq) unique source.

## Hodge/exactness constraints do not reduce the class

For every (sigma_{mathbf c}(J)in Z):

- signed divergence vanishes;
- unsigned endpoint-sum divergence vanishes at the research level;
- all block marginals vanish;
- total edge sum vanishes;
- exact/coexact orthogonality is automatic;
- the owned Euclidean adjointness identities hold.

No equation among (c_{AB},c_{AC},c_{BC}) is generated.

On (Z),

[
Q_H|_Z
=
c,I_{Z_{AB}}oplus b,I_{Z_{AC}}oplus a,I_{Z_{BC}}.
]

The Hodge right inverse chooses the minimal-norm triangle lift for a **given** source; it does not choose the source coefficients.

## Unit SceneSpectralAction does not fix matter block weights

`SceneSpectralAction` owns the unit background conformal weight and scalar graph identities.

It does not own a matter variable, a matter-edge interaction functional, or a theorem identifying its unit background coefficient with the block coefficients of (sigma_{mathbf c}).

For the same unit scene metric one may define

[
S_{mathrm{int},mathbf c}(h,J)
=
langle h,sigma_{mathbf c}(J)angle_1
]

for any (mathbf c). The background spectral-action identities remain unchanged.

Therefore

[
ho_1equiv1
]

is not a selector theorem for

[
c_{AB}:c_{AC}:c_{BC}.
]

## Conditional positive result: universal endpoint-only edge action

If one adds the new modelling principle that every edge uses the same endpoint-only quadratic law

[
F(x,y,h)=h(Ax^2+Bxy+Cy^2)
]

with one common edge variable/background, then after projection to (Z),

[
P_Zrac{partial S_{m int}}{partial h}
=
B,sigma_{(1,1,1)}(J).
]

Similarly, the finite Dirichlet energy

[
E(J;w)=rac12sum_e w_e(J_x-J_y)^2
]

gives

[
P_Z
abla_wE=-sigma_{(1,1,1)}(J).
]

Thus the line (1:1:1) is mathematically selected **inside those added action ansätze**.

D0 does not currently own either physical identification. Hence this is `ACTION-SELECTOR-CONDITIONAL`, not CORE.

A single geometrically local law may also use an additional local invariant such as the number of triangles through an edge, producing a different line such as ((c,b,a)). Locality alone therefore does not imply (1:1:1).

## Cochain-product route remains a bridge

No owned graded cup/wedge product on the literal scene cochain complex was found.

If a standard ordered Alexander–Whitney product is added as a new primitive, different degree-compatible composites already produce competing lines:

[
P_Z(Jsmile dJ)=sigma_{(1,1,1)},
]

while

[
P_Zdelta(dJsmile dJ)=sigma_{(c,b,a)}.
]

Therefore even adding a cochain product does not select a source without additionally specifying which composite is the physical coupling.

## Naturality does not reduce frozen-scene dimension

Zone relabelling across the family (K(a,b,c)) allows coefficient functions of the form

[
(c_{AB},c_{AC},c_{BC})
=
(f(a,b;c),f(a,c;b),f(b,c;a)).
]

This family is infinite-dimensional. On the literal scene, the examples

[
f=1,qquad f=z,qquad f=z^2
]

already give three linearly independent selector triples because the evaluation determinant is (-16).

The same conclusion survives strict signed/oriented transport conventions.

Hence naturality across relabelled presentations leaves scene selector dimension 3, projective dimension 2.

## A1 compatibility does not select ratios

Conditionally on the A-CPL unit-weight completion (W=I), every (sigma_{mathbf c}in Zsubsetker B_+) gives an A1-shift-compatible interaction

[
langle h,sigma_{mathbf c}(J)angle.
]

For fixed positive block-scalar (W), changing between coordinate and Riesz gradients only rescales the three blocks invertibly; it does not reduce selector dimension.

Therefore A1 compatibility transfers weight conventions but does not choose the source coefficients.

## Q_H compatibility and reciprocity do not select ratios

Literal quadratic intertwining with a vertex operator merely enables or disables entire blocks; it does not relate the magnitudes of nonzero (c_{XY}).

For the natural vertex graph Laplacian, simultaneous-input quadratic intertwining actually kills all three blocks rather than selecting a nonzero line.

The correct infinitesimal/Leibniz intertwining is compatible with the full class after the appropriate shift.

Likewise, for a fixed positive Hodge quadratic energy, reciprocal actions

[
S_{mathbf c}(h,J)
=
rac12langle h,Qhangle
-
langle h,sigma_{mathbf c}(J)angle
]

are well-defined for every coefficient triple.

## EdgeStiffnessOrigin hides rather than removes the freedom

After a literal scene specialization,

[
P_Z,edgeStiffness(P,H_J)
=
sigma_{left(
rac{p_A+p_B}{2},
rac{p_A+p_C}{2},
rac{p_B+p_C}{2}
ight)}.
]

The map

[
(p_A,p_B,p_C)mapsto
(c_{AB},c_{AC},c_{BC})
]

has rank 3.

Even adding positive/PSD capacity hypotheses leaves a cone with projective dimension 2. Different coefficient-free constructions, such as (P=I) and (P=L_0^2), produce different source lines.

Thus the generic stiffness origin theorem does not contain a physical selector.

## Strong terminal negative control

Two nonproportional positive completions on the same frozen scene are

[
mathbf c^{(0)}=(1,1,1),qquad
mathbf c^{(1)}=(13,11,9).
]

Both satisfy the audited internal constraint class:

- full tensor-block bilinear span;
- explicit triangle lift;
- zero block marginals;
- signed and unsigned divergence constraints;
- Euclidean Hodge adjointness/exactness consequences;
- same unit scene metric/spectral background;
- conditional A1 shift compatibility at (W=I);
- reciprocal coupling to the same (Q_H);
- natural family extensions.

They are not proportional, and

[
sigma_{mathbf c^{(1)}}=Q_Hsigma_{mathbf c^{(0)}}.
]

Hence the audited owned structures do not determine a unique projective source line.

## Terminal boundary

Primary classification:

[
oxed{	ext{SELECTOR-NOGO-TERMINAL}}
]

within the audited owned constraint class.

This means:

- **SOURCE-CARRIER-MISSING** remains the separate primary A-STRESS matter-carrier boundary;
- once a scene amplitude is supplied, the current D0 structures still do not choose the two essential block ratios;
- a positive selector requires a genuinely new matter-action/cochain-product/interaction principle;
- choosing (1:1:1) is legitimate only as an explicit new modelling bridge, not as a derived CORE result.

This does not close TT, physical radiation, Einstein dynamics, A1 weight normalisation, or the continuum bridge.

## Theorem-ready residue

If later formalized, the smallest negative-side package is:

1. generic (deltaPhi_{mathbf c}=sigma_{mathbf c});
2. uniqueness of the equivariant symmetric triangle lift for a fixed source;
3. two explicit nonproportional literal-scene completions satisfying the same owned constraint package.

A separate optional positive theorem may show that an explicitly assumed universal endpoint-only quadratic edge action projects to (1:1:1), with the assumption clearly typed as new modelling input.
