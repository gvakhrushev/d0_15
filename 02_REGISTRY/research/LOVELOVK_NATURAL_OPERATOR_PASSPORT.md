# E-LOV — Lovelock Natural-Operator Passport

CONTROL disposition: **ACCEPT AS RESEARCH / FINITE-SEED-SEPARATION-REQUIRED**  
Source memo: `MEMO_22_LOVELOVK_NATURAL_OPERATOR_PASSPORT.md`

## External target

The desired continuum antecedent is one operator
[
\mathcal E:J^2\mathrm{Met}_{1,3}(M)\to S^2T^*M
]
which is metric-only, local through the metric 2-jet, diffeomorphism-natural, symmetric, covariantly divergence-free for every metric, and genuinely second-order.

In four dimensions Lovelock/Navarro then gives
[
\mathcal E=aG+b,g.
]
Genuine second order excludes (a=0).

## Finite seed audit

No current finite object simultaneously carries all finite structure needed to be the source of that one continuum operator.

### `einsteinResponse(L)=2L`

A clean single finite matrix with same-object symmetry and row-sum conservation. It is a graph kernel, not a metric variational tangent-tensor response.

### Raw weighted (a_2) response

Action-derived and symmetric, but the current finite archive divergence is nonzero. The accepted A2 divergence no-go remains active. Explicit (ho) also prevents current metric-only classification.

### A1 `physicalResponse`

Action-derived with exact (B_+) Ward conservation, but it is an edge cochain; (B_+) is not Bianchi divergence; compensator elimination is global; accepted A-X classifies the Hessian as contact/constraint stiffness rather than spatial second-order propagation.

### Strongest same-object response precursor

[
T_N=
\operatorname{conservedStressProjection}
(\operatorname{archiveCurvatureGradient}(N)).
]

For this same (T_N), Lean owns symmetry, archive row-sum conservation and variational pairing equivalence.

But it lives on the 1D phase carrier, its projection is global, and the accepted seam-curvature scope no-go forbids direct identification of the 1D seam curvature invariant with 4D spacetime curvature.

### Strongest 4D geometry precursor

`archiveMetricProductLaplacian n` is a real 4D role-product scalar operator, symmetric and constant-annihilating.

It is not a local tangent symmetric-tensor gravity response. Its curved perturbation is not derived, and product-spectrum convergence remains open.

## New bottleneck

The finite information is split:
[
\text{4D parent geometry}
+
\text{variational/conserved response}
\not\Rightarrow
\text{one common finite tensor response }E_N.
]

Therefore the first missing theorem is finite, not continuum:

`D0-ARCHIVE-4D-LOCAL-GRAVITY-RESPONSE-SEED-001`.

It should construct on the 4D role-product carrier one response (E_N[m_N]) with:

1. finite action provenance;
2. metric-only finite dependence;
3. uniformly local stencil;
4. nontrivial second-metric-difference dependence;
5. local symmetric tensor fibre;
6. one discrete Noether/divergence identity;
7. explicit compatibility with the 4D role-product parent geometry.

Only after such an (E_N) exists should D0 launch the operator-convergence/naturality/divergence passport.

## Registry truth repair

The claim `D0-ARCHIVE-LOCAL-LAPLACIAN-VARIATION-ISOMORPHISM-001` was previously demoted because the old owner proved only edge-count/dimension arithmetic. The current Lean owner now constructs the typed undirected edge carrier, the symmetric row-sum-zero supported matrix subspace, and an explicit two-sided linear equivalence between them. This remains finite algebra only; it is not a continuum naturality or curvature result.

Terminal verdict:
[
\boxed{\texttt{FINITE-SEED-SEPARATION-REQUIRED}}.
]
