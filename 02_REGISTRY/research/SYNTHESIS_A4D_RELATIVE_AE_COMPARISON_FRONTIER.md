# SYNTHESIS_A4D_RELATIVE_AE_COMPARISON_FRONTIER

## Status

DURABLE SYNTHESIS / POST-PR-120/#121/#122 FRONTIER.

## 1. What is now owned

The reference/overlap stack is now split cleanly into owned algebra and one
missing cross-channel primitive.

Lean owns:

- conditional transported-reference mismatch \(\kappa_q\);
- full-affine origin covariance boundary;
- exact two-link junction compression iff criterion;
- scoped local-candidate no-go;
- Role-labelled overlap decomposition and twisted cocycle;
- canonical observer-positive local quadratic no-go.

Research PR #120 constructs the finite diagonal/source mechanism conditional on
a relative A/e comparison primitive.

## 2. Source-fibre increments

For \(x=y-r\),

\[
\bar b_r(y)=L_{x,r}^{-1}b_{x,r},
\qquad
\bar v_r(y)=L_{x,r}^{-1}v_r(e,x),
\]

\[
\Delta^b_r(y)=b_{y,r}-\bar b_r(y),
\]

\[
\Delta^v_r(y)=v_r(e,y)-\bar v_r(y).
\]

Both increments transform as source-fibre vectors under pure-linear frames.

The remaining geometric question is exactly how much of \(\Delta^v\) is the
solder image of \(\Delta^b\).

## 3. Missing primitive

The convenient theorem-ready interface is

\[
\mathfrak J_y:V_y\to V_y
\]

with

\[
\mathfrak J'_y=g_y\mathfrak J_y g_y^{-1}
\]

and exact translation-gauge calibration

\[
\mathfrak J_y\Delta^b_r=\Delta^v_r.
\]

The minimum object may be smaller than a full endomorphism: a map on
\(\operatorname{span}\{\Delta^b_r\}\), a quotient map, or a linear relation may
be sufficient.

## 4. Conditional package already constructed

Given \(\mathfrak J\),

\[
R_r^{A/e}
=
\Delta^v_r-\mathfrak J_y\Delta^b_r
\]

is the relative defect, and

\[
a_r
=
-\bar b_r-\mathfrak J_y\Delta^b_r
\]

is the finite diagonal seed.

The tautological predecessor defect satisfies

\[
\rho_r=a_r-R_r^{A/e}.
\]

For a labelled path \(p:y\to y'\),

\[
S_p=P_pa(y')-a(y),
\]

with exact laws

\[
S_{p++q}=S_p+P_pS_q,
\]

\[
S_{\bar p}=-P_p^{-1}S_p.
\]

The sourced diagonal equation

\[
P_p\delta(y')-\delta(y)=S_p
\]

has exactly the solutions

\[
\delta=a+h
\]

with \(h\) a global A-parallel section.

At a basepoint the remaining kernel is the common fixed space of labelled loop
holonomies.

Observer positivity is used only after this source law to select the residual
parallel kernel.

## 5. Exact controls of the conditional package

With a calibrated comparison primitive:

- flat gives \(\delta=0\);
- exact L=3 translation gauge reproduces
  \[
  (15,-3,-12)e_B
  \]
  and \(\kappa=0\);
- constant pure affine shift gives \(\delta=0,\ \kappa=b\);
- L=2 Nyquist remains visible;
- L=3 corner remains visible;
- \(z_{\rm curl}\) fails the sourced equation;
- \(z_{\rm harm}\) lies in the parallel kernel and is removed by the post-source
  positive selector.

Thus sourced transport, curl rejection and harmonic rejection are no longer
the missing principles.

## 6. Current candidate obstruction

The obvious current candidates for \(\mathfrak J\) fail:

- fixed numerical \(\eta\): not a covariant vector endomorphism under the
  rational boost;
- observer-derived \(h_n^{-1}\eta\): makes the longitudinal source observer
  dependent;
- solder-conjugate \(B_e\eta B_e^{-1}\): fails exact translation-gauge
  calibration on the L=3 shear and requires invertible solder;
- basepoint-transported \(\eta\): propagates a supplied primitive but does not
  derive one.

Curvature/torsion/curl alone also cannot distinguish exact translation gauge
from pure affine shift because the missing information is longitudinal.

## 7. Positive regular branch

Define

\[
B_e(x)e_s=v_s(e,x),
\qquad
C_A(x)e_s=b_{x,s}.
\]

When \(B_e\) is invertible,

\[
K=B_e^{-1}C_A
\]

is exactly frame invariant.

On a translation chart,

\[
B=I+\eta D,\qquad C=D,
\]

and

\[
D-\eta DK=K,
\]

equivalently

\[
B-\eta BK=I.
\]

When

\[
\mathcal L_K:X\mapsto X-\eta XK
\]

is invertible, the translation-chart representative is uniquely reconstructed.

This is a genuine regular A/e passport.

It does not extend automatically across singular \(B_e\) or singular
\(\mathcal L_K\).

## 8. Current heavy research

The active task is

\`EXP-A4D-RELATIVE-AE-COMPARISON-PRIMITIVE\`.

Highest-priority route:

\[
U_y=\operatorname{span}\{\Delta^b_r(y)\}_r
\]

and ask whether

\[
J_y^{\rm span}(\Delta^b_r)=\Delta^v_r
\]

is canonically well defined.

The exact condition is that every linear relation among the \(\Delta^b_r\)
must also be satisfied by the corresponding \(\Delta^v_r\).

If not, the natural next object is the linear relation generated by

\[
(\Delta^b_r,\Delta^v_r)\subset V_y\oplus V_y.
\]

The research must classify covariance, rank jumps, stabilizer ambiguity and the
singular/degenerate locus.

## 9. Current workers

Two theorem-ready workers may run in parallel:

- \`WRK-A4D-CONDITIONAL-SOURCED-DIAGONAL-TRANSPORT\`;
- \`WRK-A4D-REGULAR-AE-PASSPORT\`.

They must not construct the physical global comparison primitive.

## 10. Finite E remains blocked

\`EXP-A4D-FINITE-GRADED-COFRAME-DRESSING\` remains BLOCKED.

The gate opens only after a usable global relative A/e comparison primitive or
an explicitly adopted canonical span/relation/rank-stratified datum lands.

## 11. One current question

Does the finite labelled set of pairs

\[
\{(\Delta^b_r,\Delta^v_r)\}_r
\]

canonically determine a frame-covariant comparison object across all rank
strata, including degenerate solder backgrounds, or is an additional
rank-stratified geometric datum required?
