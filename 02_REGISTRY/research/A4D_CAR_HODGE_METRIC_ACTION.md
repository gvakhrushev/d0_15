# E-CARHODGE — Full-Metric CAR/Hodge Matter Action

CONTROL disposition: **ACCEPT AS RESEARCH / CAR-HODGE-COBOUNDARY-OWNER-MISSING**

Source memo: MEMO_38_A4D_CAR_HODGE_METRIC_ACTION.

The exterior/Hodge route is constructively viable but current main lacks the first literal operator needed by the construction.

Owned ingredients include:

- \(X_N=ArchiveRolePhaseGroup(N)\);
- centered Role translations/differences;
- the 16-state \`ArchiveFockState\` fibre;
- definitionally identical cubical subset labels;
- creation/annihilation matrix-element definitions;
- \`LocalSymRoleField N\`;
- metric-side centered integration by parts.

The current \`ArchiveCubicalCoboundary\` is not a literal cochain differential owner: it does not define a cochain amplitude space, a matrix/linear map \(d_N\), degree raising, or \(d_N^2=0\). Likewise, the currently exported CAR relation owner does not yet prove the full creation/annihilation anticommutators required by a clean nilpotency proof.

Conditional on a literal cochain differential, define on the 16-state exterior fibre:
\[
W_m^{(k)}(x)
=
\sqrt{|\det m(x)|}\,\Lambda^k(m(x)^{-1}),
\]
with site-diagonal global weight. Every one of the six off-diagonal metric shears enters nontrivially already through
\[
W_m^{(1)}=\sqrt{|\det m|}\,m^{-1}.
\]

Then
\[
\delta_m=W_m^{-1}d_N^TW_m,
\qquad
D_m=d_N+\delta_m
\]
is finite-local. Lorentz signature makes the full exterior pairing indefinite with total signature \((8,8)\), but keeps \(W_m\) invertible. Thus a finite Krein-self-adjoint action and literal metric variation remain algebraically available; Lorentz/Krein indefiniteness is not the first blocker.

First- or second-order Hodge actions have explicit finite metric derivatives and the resulting linear functional on metric variations is represented by a unique
\[
T_N\in LocalSymRoleField(N).
\]

The Ward route has an additional compatibility problem: the natural cubical \(d\) uses forward differences, while the already-owned metric gauge uses centered differences. A genuine finite Cartan/reparametrization law therefore needs a centered differential, averaging/staggering prescription, or exact intertwiner.

State selection remains the first new physics principle after the operator stack exists.

Terminal result:
\[
\boxed{\texttt{CAR-HODGE-COBOUNDARY-OWNER-MISSING}}.
\]
