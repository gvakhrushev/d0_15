# EXP-ATORUS-LOWEST-SHELL-CONTINUUM-STRESS-PASSPORT

## Class
EXPENSIVE

## Parent
CTRL-GRAVITY-DYNAMICS-CLOSURE

## Objective

Determine whether exact interlevel shell/projector transport is actually necessary for the continuum physics target.

Construct or terminally classify a direct finite-to-fixed-\(T^4\) passport for the lowest-positive mixed shell and its stress, using the common Fourier labels rather than an unowned finite refinement isometry.

Repository edits: **NONE**.

## Frozen finite preparation

Use:
\[
\lambda_{1,N}
=
4(N+2)^2
\sin^2\!\left(\frac{\pi}{N+2}\right),
\]
\[
\rho_{1,N}
=
P_{1,N}/\operatorname{rank}P_{1,N},
\]
with first-harmonic labels \(\{\pm e_r\}\) for \(N\ge1\).

At fixed \(N\), the positive Hodge route gives a site-uniform Role-isotropic nonzero stress.

## Direct \(T^4\) passport

Use the already accepted fixed-\(T^4\) realization.

Construct explicitly:

1. finite normalized Fourier characters on \(X_N\);
2. their reconstruction/interpolation as the same continuum characters
   \[
   e^{\pm2\pi i x_r}
   \]
   on \(T^4\);
3. finite first-shell projectors/kernels;
4. their direct convergence to the continuum projector onto the eight first harmonics;
5. the mixed density matrix limit;
6. the correct cell-volume normalization of the finite stress.

Determine whether this direct target map makes an exact finite \(J_N\) unnecessary for the continuum passport.

## Stress limit

Track the raw site coefficient versus cell-volume normalized density.

Use
\[
|X_N|=(N+2)^4
\]
and verify whether the reconstructed integrated stress has a finite nonzero limit.

State the exact continuum tensor shape reached at the flat Euclidean Role background.

## Lorentz/time issue

The current shell averages all four Roles symmetrically.

Determine exactly what is needed to interpret it as physical Lorentzian matter:

- a distinguished time Role;
- a spatial Cauchy slice;
- a 3D lowest spatial shell;
- Hamiltonian rather than 4D Euclidean Laplacian preparation.

Do not call the current Euclidean pure-trace tensor radiation unless the \(1+3\) split is actually supplied.

## Backreaction / zero mode

The finite derivative-only gravity response annihilates constant metric modes while the prepared flat stress is constant.

Classify the first honest resolution:

- nonflat background expansion around a sourced solution;
- a zeroth-order/cosmological metric term;
- projection/removal of the homogeneous source;
- or another explicit background equation.

Do not silently solve the equation on flat background.

## Terminal verdict

Return exactly one:

- \`LOWEST-SHELL-T4-STRESS-PASSPORT-REACHED\`
- \`LOWEST-SHELL-EXACT-REFINEMENT-REQUIRED\`
- \`LOWEST-SHELL-LORENTZ-HAMILTONIAN-BRIDGE-OPEN\`
- \`LOWEST-SHELL-ZEROMODE-BACKREACTION-BLOCKER\`
- \`LOWEST-SHELL-STRESS-NORMALIZATION-BRIDGE-OPEN\`

## Deliverable

\`MEMO_44_ATORUS_LOWEST_SHELL_CONTINUUM_STRESS_PASSPORT.md\`
