# EXP-A4D-CROSSED-CONSTITUTIVE-REPRESENTATION

## Class

EXPENSIVE / DEEP RESEARCH

## Parent

`CTRL-GRAVITY-DYNAMICS-CLOSURE`

## State

PLANNED

## Start gate

SATISFIED.

Required landed inputs:

- PR #70 affine Cartan path geometry and path-word algebra;
- PR #75 complete staggered first jet `H(e)`;
- PR #76 fixed located topological `J`;
- PR #80 second-order/action-groupoid/scalar-advective package;
- PR #95 `MEMO_A4D_PATH_RESOLVED_MATTER_WORD_ACTION.md`;
- PR #98 endpoint/overlap comparison boundary;
- PR #99 golden/RG defect interface as a truth firewall only.

Primary durable packet:

`02_REGISTRY/research/MEMO_A4D_PATH_RESOLVED_MATTER_WORD_ACTION.md`.

Also read completely:

- `MEMO_A4D_COMMON_CENTER_MATTER_GROUPOID_ACTION.md`;
- `MEMO_A4D_ENDPOINT_COMPARISON_JET_OVERLAP_LAW.md`;
- `MEMO_A4D_SOLDERED_CREATOR_OBSERVER_FRAME_LIFT.md`;
- `SYNTHESIS_A4D_PATH_RESOLVED_MATTER_WORD_ACTION.md`.

The observer/frame Lean worker may proceed in parallel. Do not wait for it; distinguish merged theorem owners from theorem-ready research input.

## Frozen terminal from PR #95

A bare fixed-background path-word action is too small.

PR #95 constructs the positive horizontal linear/exterior path action and proves the surviving primitive must contain both:

1. horizontal path-expression composition at fixed background;
2. vertical background/action-groupoid composition.

The exact scalar diagnostic is

```text
[G_xi, U] = -(1/2) M_(h_xi) (U^2 - I)
```

so vertical background action mixes word lengths.

The full owned first jet `H(e)` is an additive length-≤2 path-expression/CAR kernel, not one multiplicative word value.

The terminal is:

`PATH-WORD-HORIZONTAL-ACTION-CONSTRUCTED-CROSSED-CONSTITUTIVE-LAW-REQUIRED`.

## Objective

Construct or terminally classify one **fixed-N crossed constitutive representation package**.

Use explicit background data

```text
b = (A, e, n)
```

until a theorem derives a solder/Cartan map `e -> A(e)`.

The target package must contain, with literal source/target types:

```text
A_N(b)                         path-expression / CAR algebra
pi_b : A_N(b) -> End(Gamma_N)  horizontal representation
gamma : b -> b'                vertical background arrow
R_gamma(b)                     vertical matter equivalence
alpha_gamma                    transport of path-expression algebra
W_b                            constitutive quadratic form / section
J_N                            fixed located primal-dual pairing
```

with crossed covariance

```text
R_gamma pi_b(a) R_gamma^-1
  = pi_b'(alpha_gamma(a))
```

and, where the symmetry applies,

```text
W_b' = R_gamma^-T W_b R_gamma^-1.
```

The research must either construct this package from owned finite geometry or identify the earliest additional primitive with exact types.

Do not satisfy the task by merely defining a structure containing these fields.

## Central questions

1. What is the literal path-expression algebra needed on `ArchiveCochain N`? Can it be built from the existing crossed/path-word algebra, Role shifts, site multipliers and degree-preserving CAR endomorphisms without becoming an untyped full matrix placeholder?
2. What are the actual background objects and arrows? Keep affine connection `A`, raw uncentered coframe `e`, and observer `n` distinct.
3. Can the positive PR #95 horizontal action be promoted from linear/Lorentz path transport to the whole required path-expression representation?
4. How, if at all, does the affine translation part of PR #70 holonomy act on the existing 16-state matter carrier?
5. What is the vertical matter action on:
   - pure-gauge coframes;
   - constant harmonic strain;
   - plaquette/curl backgrounds;
   - frame/observer changes?
6. Does the vertical action induce a closed action `alpha_gamma` on the path-expression/CAR algebra?
7. Can the exact scalar word-length-mixing law be extended to all Role/CAR generators?
8. Is there one geometrically supplied `W_b` whose flat derivative is the complete owned `H(e)` for arbitrary uncentered coframes?
9. Does crossed covariance select the previously free comparison jet `S`, or does quadratic endpoint dressing remain a genuine modulus?
10. Does the moving observer constrain the constitutive section beyond transport covariance?
11. Can fixed located `J` force the dual horizontal and vertical actions with all shifted anchors?
12. Is a local inverse-free parent available even if the compressed finite transformation is nonlocal?
13. If `e -> A(e)` is required, can it be derived inside the same construction? If not, name the exact missing solder/Cartan compatibility datum.

## Mandatory attack order

### A. Literal types first

Audit the existing types for:

- `ArchiveRolePhaseGroup N`;
- `ArchiveCochain N`;
- affine Cartan links/paths;
- raw coframe;
- observer field;
- path words / path-expression algebra;
- CAR endomorphisms;
- fixed located `J`.

Do not write `C_N(w;e,n)` if the path evaluation actually depends on an independent affine connection `A`.

### B. Horizontal algebra

Construct the smallest path-expression/CAR algebra that contains the complete first jet.

It must distinguish:

- additive path expressions;
- multiplicative path composition;
- word length / locality;
- Fock degree/parity.

Recover the PR #95 horizontal linear/exterior transport as a positive subrepresentation.

### C. Affine shift response

Audit the translational part of affine holonomy.

The existing exterior lift is blind to a pure affine loop `(I,b)`.

Try actual finite representations compatible with the current matter carrier and CAR structure.

If an enlarged carrier is necessary, state the minimal enlargement and why the existing 16-state carrier is insufficient.

Do not call absence of an existing implementation a no-go.

### D. Vertical action and infinitesimal crossed law

Start from the exact pure-gauge/action-groupoid owners.

Derive the infinitesimal vertical action on algebra generators.

Mandatory scalar check:

```text
[G_xi,U] = -(1/2) M_(h_xi)(U^2-I).
```

Then compute, where possible:

- Role shifts `U_s`;
- site multipliers;
- CAR generators / `E_sr`;
- the length-2 corner words already appearing in `H(e)`.

Decide whether a finite generator set closes.

### E. Beyond pure gauge

Test:

- constant harmonic strain outside `im d_f`;
- plaquette curl / torsion;
- arbitrary uncentered coframes;
- rational A/B frame boost.

A pure-gauge cocycle alone does not complete this task.

### F. Crossed covariance

Construct or obstruct `alpha_gamma` and the exact covariance square.

Keep separate:

- path concatenation at fixed background;
- vertical background composition.

No silent identification of their parameters.

### G. Constitutive section

Search for one geometrically supplied symmetric/observer-positive `W_b`.

Required flat data:

```text
W_0 = I
D_e W_0[e] = H(e)
```

for the **full** uncentered `H(e)`.

Do not define `W` by inserting `H` as a Taylor coefficient and calling that a construction.

### H. Second jet / dressing freedom

Re-test the exact endpoint dressing freedom inside the crossed package.

Determine whether crossed covariance, affine shift response, observer covariance or located-`J` compatibility removes the `S_patch + lambda T_edge` family.

If not, identify what additional datum selects the constitutive section.

### I. Observer and frame

Use the moving positive form `h_n`.

Keep:

- positive observer form;
- Lorentzian exterior form;
- physical causal time

strictly separate.

### J. Located dual

Derive the dual horizontal/vertical actions from fixed located `J` with endpoint/Fock-dependent anchors.

A common-fiber inverse-transpose slogan is insufficient if site anchors split by Fock component.

### K. Locality

Audit separately:

- factorized word locality;
- maximum word length;
- compressed matrix support;
- background dependence;
- inverse locality;
- auxiliary-parent locality.

Do not turn the existing uniform-radius no-go into a no-go for factorized local parents.

## Exact hostile controls

At minimum:

- flat background;
- constant matter;
- pure gauge;
- constant harmonic strain;
- plaquette curl;
- L=2 raw Nyquist;
- L=3 corner;
- L=5 delta and non-delta;
- scalar word-length mixing;
- all Fock degrees and parity;
- signed Role permutations;
- rational A/B Lorentz boost with moving observer;
- linear holonomy;
- pure affine translation holonomy;
- fixed located-`J` shifted anchors;
- quadratic endpoint dressing / second-jet freedom.

Use exact rational/symbolic checks.

## Allowed terminal forms

Use the strongest justified terminal, for example:

- `CROSSED-CONSTITUTIVE-REPRESENTATION-CONSTRUCTED`;
- `CROSSED-ACTION-CONSTRUCTED-CONSTITUTIVE-SECTION-MISSING`;
- `CROSSED-CONSTITUTIVE-REPRESENTATION-SCOPED-NOGO`;
- `CROSSED-CONSTITUTIVE-NEW-PRIMITIVE-REQUIRED`.

Do not claim a universal no-go unless the exhausted class matches the wording exactly.

## Deliverable

One theorem-ready durable memo in `02_REGISTRY/research/` containing:

- literal types;
- positive constructions;
- exact composition/covariance laws;
- first/second-jet calculations;
- affine-shift result;
- observer/frame and fixed-`J` analysis;
- locality classification;
- hostile controls;
- terminal verdict;
- theorem-ready handoff;
- exactly one recommended next step.

No Lean source.

Do not change claim statuses or release metadata.

## GitHub-first flow

1. fresh branch from current `main`;
2. `python tools/task_lifecycle.py start EXP-A4D-CROSSED-CONSTITUTIVE-REPRESENTATION`;
3. immediately open Draft PR before research edits;
4. research and durable memo only inside that PR;
5. keep PR metadata synchronized;
6. before Ready: `python tools/task_lifecycle.py retire EXP-A4D-CROSSED-CONSTITUTIVE-REPRESENTATION`;
7. Ready with `Lifecycle: REVIEW`;
8. do not self-merge.

## Truth firewall

Do not infer:

- Spin structure;
- physical causal time;
- metric interpretation of located `J`;
- unique `S`, `K` or scalar coefficient without derivation;
- golden carrier weld;
- provenance-bearing stress;
- Einstein dynamics.
