# EXP-A4D-GOLDEN-ROLE-PHASE-REFINEMENT-WELD

## Class

EXPENSIVE

## Parent

`CTRL-GRAVITY-DYNAMICS-CLOSURE`

## State

PLANNED

## Objective

Determine the strongest **typed inter-level relation** that current D0 can derive between:

1. the canonical golden cylinder / Bratteli / AF refinement structure, and
2. the finite Role-phase / CAR geometry on which `D_H`, located `J` and the staggered first jet `H(e)` live.

This is the nearest inter-level research seam. It is not a request to derive \(\phi\) again and not a request to identify all archive towers.

The desired output is one terminal mathematical verdict:

- a constructed weld/naturality law;
- a sharply scoped no-go for a specified weld class;
- or the minimal typed comparison primitive still missing.

Produce one theorem-ready research memo. Do not edit Lean or lifecycle files.

## Mandatory reading

Read completely before starting:

- `02_REGISTRY/research/ROADMAP_A4D_RELATIONAL_REFINEMENT_SYNTHESIS.md`
- `02_REGISTRY/research/MEMO_A4D_COMMON_CENTER_MATTER_GROUPOID_ACTION.md`
- `02_REGISTRY/research/MEMO_A4D_SECOND_ORDER_CARTAN_CELL_ENERGY_INTEGRABILITY.md`
- `02_REGISTRY/research/MEMO_A4D_SOLDERED_CREATOR_OBSERVER_FRAME_LIFT.md`

Audit literal owners, especially:

- `D0.Geometry.ArchiveRefinementTower`
- `D0.Geometry.ArchiveLightProfinite`
- `D0.Foundation.ContinuumAsInverseLimit`
- `D0.Geometry.ArchiveRolePhaseProductCarrier`
- `D0.Geometry.ArchiveRolePhaseGroup`
- `D0.Geometry.ArchiveRolePhaseCarrier`
- `D0.Geometry.ArchiveLaplacianRG`
- `D0.Geometry.ArchiveFlatProductBondingNoGo`
- `D0.Geometry.ArchiveTwoLimitSeparation`
- `D0.CondensedAnchor.DetectorSupportGoldenWeight`
- `D0.Geometry.FibonacciBratteliRefinement`
- `D0.Spectral.CanonicalRefinementScaleFlow`
- corrected Hodge/CAR, located-star and flux/first-jet owners from PRs #75/#76.

Also inspect:

- `02_REGISTRY/forcing_routes.json`
- `D0.NumberTheory.HurwitzClassCanonization`
- `D0.NumberTheory.JonesSlotSelector`
- `D0.Geometry.ArchiveSpatialHistorySplit`.

## Truth-repaired starting facts

### A. Do not re-derive phi by duplicated routes

The old detector relation

\[
p+p^2=1
\]

and the old period-one fixed point

\[
x=1+\frac1x
\]

are algebraically the same relation under \(x=1/p\). Do not count them as independent forcings.

The current repaired two-route inventory is:

1. Hurwitz noble-class + `M1+` canonization, with \(x^2-x-1=0\) as output;
2. Jones slot selection, BRIDGE-scoped to its explicit mathematical inputs.

This EXP consumes the existing golden refinement owners. It does not strengthen their forcing status.

### B. Delta sign

Keep

\[
\delta_0=\frac{\sqrt5-2}{2}=\frac1{2\phi^3}>0.
\]

Do not use \((2-\sqrt5)/2\) as a positive quantity.

### C. The compact dimensional ladder is owned but not a spacetime theorem

The exact identity

\[
Q(D)=2\delta_0\phi^{D-1}=\phi^{D-4}
\]

and its unit value at \(D=4\) are already formalized.

Do not identify this normalization index with physical Lorentz spacetime dimension without a typed bridge.

## Three-tower dictionary — absolute firewall

### Tower A: record / profinite

\[
A_n=\operatorname{ArchivePoints}(n)=\operatorname{Fin}((n+2)^4)
\]

with `archiveProjection` given by flattened integer modulo.

This defines the record `LightProfinite` inverse limit.

### Tower B: Role-phase / CAR geometry

\[
B_N=\operatorname{ArchiveRolePhaseGroup}(N)
=
\mathrm{Role}\to\mathbb Z/(N+2)\mathbb Z.
\]

This carries:

- cyclic Role translations;
- an already-owned coordinatewise consecutive-level finite-set projection `archiveRolePhaseProjection`, which is surjective;
- Archive cochains / Fock carrier;
- corrected `D_H`;
- located primal/dual `J_N`;
- `H_N(e)`;
- coframe / affine Cartan / path geometry.

The natural coordinate-wise period reduction of this product carrier is **not** the flat record bonding map.

The repository already proves two separate negative facts that must be consumed rather than rediscovered:

1. flat record bonding and coordinatewise Role-product bonding are not stagewise isomorphic; the first-step fiber mismatch is

\[
6\ne16.
\]

Therefore Tower A and Tower B may not be identified by levelwise bijections compatible with those bondings.

2. `ArchiveLaplacianRG.exact_projective_compatibility_fails` proves that for `n>1` the accepted one-dimensional coordinatewise phase projection does not give exact nearest-neighbor Laplacian pullback naturality. Thus the owned Role-phase projection is a finite-set coarse-graining map, not already an operator-natural physical refinement.

### Tower C: golden cylinder / Bratteli / AF

The golden-mean cylinder language recovers

\[
M_\phi=
\begin{pmatrix}1&1\\1&0\end{pmatrix},
\qquad
M_\phi^2=M_\phi+I,
\]

with Fibonacci growth, golden trace ratio and canonical scale flow

\[
\Lambda_{k+1}/\Lambda_k=\phi.
\]

At the measure layer

\[
\mu(wA)+\mu(wB)=\mu(w).
\]

Tower C is already a genuine internal refinement owner at algebra/trace/scale level.

## Primary question

What is the strongest mathematically legitimate relation

\[
\boxed{\text{Tower C}\quad\rightsquigarrow\quad\text{Tower B}}
\]

that is derivable from current D0?

Do not assume in advance that the answer is a function of levels.

The correct type may be:

- a map of finite carriers;
- a natural transformation between functors;
- a correspondence / span / bimodule;
- a level-selection subsequence;
- an operator intertwiner;
- a trace/measure-preserving factor;
- only a common scale law;
- or no canonical relation in the audited class.

Classify the type before trying to prove formulas.

## Phase 1 — index and carrier audit

Write an explicit table for every `n/N/k` appearing in the relevant owners:

- its type;
- its stage cardinality;
- its bonding/inclusion map;
- whether the system is inverse or inductive;
- whether the index denotes resolution, cycle period, word length, AF depth, spectral scale or history depth.

Do not reuse one symbol after this audit unless a typed map between the indices has been constructed.

Determine whether any existing theorem already maps:

- golden word length -> Role-phase period;
- Bratteli level -> archive fiber count;
- AF depth -> cochain period;
- Perron scale -> archive edge metric scale.

A numerical resemblance is not a map.

## Phase 2 — rule out naive identifications first

Before constructing a positive weld, test and, where possible, prove failure of:

1. `k=N`;
2. Fibonacci stage cardinality \(F_k=(N+2)^4\);
3. record `archiveProjection` as the Role-phase bonding;
4. simple levelwise bijection between Tower C states and Tower B sites;
5. identifying the Perron eigenvalue \(\phi\) with the cyclic period increment \(L\mapsto L+1\);
6. identifying a golden tick with Role-A translation;
7. identifying the AF inclusion map with a physical cochain pullback/pushforward without a Hilbert comparison map.

These negative controls are valuable: the task is a weld, not an equality.

## Phase 3 — classify the already-owned Role-phase projection and stronger alternatives

Do **not** start by inventing a projection. Tower B already owns the coordinatewise consecutive-level finite-set map

\[
\operatorname{archiveRolePhaseProjection}_N:
B_{N+1}\to B_N,
\]

built from the one-dimensional \`archiveRGPhaseProjection\`, and it is surjective.

First classify exactly what it is and is not:

- a map of finite sets;
- Role-coordinatewise;
- Role-permutation equivariant if provable from the definition;
- generally **not** a homomorphism of the additive cyclic groups when consecutive moduli are incompatible;
- already known not to give exact nearest-neighbor Laplacian projective compatibility for \`n>1\`.

Reproduce the logic of the existing \`exact_projective_compatibility_fails\` owner only as an input boundary; do not spend the EXP re-proving it unless a generalized statement is required.

Then classify stronger alternatives.

For periods \(L,L'\), determine when there exists a canonical group homomorphism

\[
(\mathbb Z/L'\mathbb Z)^4\to(\mathbb Z/L\mathbb Z)^4
\]

compatible with Role permutations and coordinate translations.

Reduction mod \(L\) descends as an additive group map only under the appropriate divisibility relation. Consecutive periods \(L+1\to L\) therefore behave differently from divisibility-compatible subsequences.

Classify:

1. consecutive finite-set coarse graining;
2. divisibility-compatible group maps;
3. possible spans/correspondences when neither direction is a homomorphism;
4. whether a cofinal subsequence can support exact or renormalized operator naturality.

The actual research question is now sharper:

> can Tower C select or canonize a comparison structure that upgrades the already-owned finite-set projection to a physically meaningful **renormalized/operator-compatible** refinement, or must the physical tower use a different subsequence/correspondence?

This classification is a prerequisite to any cross-level statement for \`J_N\`, \`D_H(N)\` or \`H_N(e)\`.

## Phase 4 — ask what the golden tower can canonically select

Given the intrinsic admissible Role-phase inter-level relations from Phase 3, test whether Tower C canonically selects:

- a cofinal sequence of periods \(L_k\);
- a sequence of divisibility morphisms;
- metric scales on a pre-existing sequence;
- only a real-valued scale \(\Lambda_k\) without a site-level map.

Candidate period sequences may be Fibonacci/Lucas/golden-language quantities only if derived from existing Tower-C data.

Do not choose \(L_k=F_k\), \(F_k\pm1\), \(\lfloor\phi^k\rfloor\), etc. merely because they are attractive. Each candidate needs an internal derivation and hostile alternatives.

## Phase 5 — operator naturality ladder

For every positive carrier/level comparison map \(C_{k}:B_{L_{k+1}}\to B_{L_k}\) or its correct contravariant cochain map, test in increasing strength:

### 5.1 Role symmetry

Does it commute with all Role permutations?

### 5.2 Incidence / translation

Does it intertwine the cyclic forward/backward shifts required by the cubical differential?

### 5.3 Fock grading

Can it be lifted trivially or canonically over the 16-state Fock fibre while preserving degree/parity?

### 5.4 Located J

State the exact naturality square for primal/dual cells/cochains.

Do not assert

\[
C J_{k+1}=J_k C
\]

until primal and dual maps, shifted anchors and variance are typed correctly.

Compute whether the occupied-complement anchor translation survives the level map.

### 5.5 Corrected Hodge differential / Dirac

Test `dForward`, its counting adjoint and `D_H`.

Because finite differences contain the period scale \(L\), exact commutation may be wrong; derive the correct scale-covariance equation if one exists.

Distinguish:

- exact operator intertwining;
- similarity;
- isometric compression;
- rescaled intertwining;
- only spectral convergence.

### 5.6 First jet H(e)

Only after the coframe itself has a typed inter-level map, ask for a relation of the form

\[
C\,H_{L'}(e') \stackrel{?}{=} \alpha\,H_L(e)\,C
\]

or the correct pullback/pushforward analogue.

Audit separately scalar neighbor, endpoint half-average and corner paths.

Do not use a pure-gauge test as evidence for arbitrary coframes.

## Phase 6 — golden trace / measure compatibility

If a positive Role-phase comparison survives, test whether it can also carry the Tower-C relational invariant.

Possible questions:

- Does pushforward of a finite weight agree with the golden cylinder refinement identity?
- Can a two-symbol golden cylinder be represented as a typed refinement choice on the Role-phase comparison maps?
- Does the Perron trace induce a canonical normalization on cochains without adding a catalogued measure?
- Is the only common invariant the dimensionless scale ratio \(\phi\)?

A trace/weight correspondence without a carrier naturality map is a legitimate partial result. Do not promote it to a full weld.

## Phase 7 — compatibility with the intra-level comparison seam

The separate active EXP studies

\[
\mathcal S:
\operatorname{Sym}^2(\operatorname{im}d_f)\to\operatorname{End}(C^0)
\]

at fixed Role-phase period.

Do not assume \(\mathcal S\) is golden.

Instead derive the **naturality question** that would have to hold if both positive structures existed.

For example, after all variances and scale factors are typed, ask whether an inter-level comparison \(C\) can satisfy a relation schematically like

\[
C\,\mathcal S_{L'}(h_1',h_2')
=
\alpha\,\mathcal S_L(C_eh_1',C_eh_2')\,C.
\]

This equation is a future synthesis target, not an assumption of either EXP.

Determine exactly which pieces of such a square can already be tested using the forced scalar \(\mathcal S+\mathcal S^T\) entries from the common-center memo.

## Phase 8 — time firewall

`ArchiveSpatialHistorySplit` is frozen:

- history tick is not cyclic A-phase;
- phi-ladder is separate from Role-A phase;
- observer representation is separate;
- the causal-axis bridge remains a distinct primitive.

Therefore no positive refinement weld may be described as physical time evolution.

At most it is a resolution/renormalization relation unless a future typed time bridge proves more.

## Phase 9 — record/profinite relation

Tower A is not discarded. Classify its role correctly.

Given the existing A/B bonding no-go, test only weaker structures such as:

- common stage cardinalities;
- forgetful enumerations;
- non-natural bijections;
- spans through a third carrier;
- common limiting passports.

If a factor map or correspondence A <- X -> B exists, state its non-canonicity/canonicity precisely.

Do not let the record inverse limit silently stand in for the physical Role-phase metric limit.

## Exact finite controls

Use exact arithmetic and finite enumeration aggressively.

At minimum include controls at:

- \(L=2,3,5\) for Role-phase geometry;
- first 6--10 Bratteli/Fibonacci levels;
- at least two divisibility-compatible period pairs and two incompatible consecutive pairs;
- nontrivial Role permutation;
- located one-form complement anchor;
- one `D_H` eigenmode / low shell where comparison is typed;
- Nyquist at \(L=2\);
- one first-jet scalar neighbor and one corner path;
- both golden cylinder letters and a two-step cylinder refinement.

If proposing a cofinal period sequence, test several steps and prove the structural recurrence/divisibility property rather than extrapolating from numerics.

## Hostile controls

Every proposed weld must survive:

- alternative stage reindexings;
- changing the enumeration of a finite carrier;
- the A/B fiber-cardinality no-go;
- a Role permutation;
- a nonexact coframe;
- period parity / Nyquist;
- replacing \(\phi\) by another Perron number in the same formal construction when the claim is supposed to be golden-specific;
- a non-golden branch pair at the cylinder-weight layer;
- exact dimension/cardinality mismatch.

If the same construction works unchanged for arbitrary positive scale ratio, it has not derived a golden weld.

## Required direct answers

The final memo must answer:

1. What are the exact three tower types?
2. Which pairs are already proved non-identical?
3. What is the correct index type for each?
4. What inter-level maps exist intrinsically on Tower B?
5. What exactly does the owned coordinatewise `archiveRolePhaseProjection` preserve, and why is that weaker than a group/operator naturality map?
6. Are consecutive Role-phase periods naturally comparable as groups?
7. Does Tower C canonically select a Role-phase period sequence?
8. Is there a carrier map, a correspondence, or only a scale law?
9. Does the best weld preserve Role permutations?
10. Does it preserve located `J`?
11. What is the strongest correct relation for `D_H`, given the already-owned exact-projective-compatibility failure of the nearest-neighbor Laplacian?
12. What is the strongest correct relation for `H(e)`?
13. Can golden cylinder weights be transported to the Role-phase/cochain carrier without new measure data?
14. Does the result constrain the future endpoint-comparison jet \(\mathcal S\)?
15. Does any part of the result have physical-time content? The expected answer is “no” unless a new theorem is actually proved.
16. What is the single earliest missing typed datum if full weld fails?

## Terminal verdicts

Return exactly one primary terminal. Suitable semantic forms:

### Positive full weld

`GOLDEN-ROLE-PHASE-REFINEMENT-WELD-CONSTRUCTED`

Use only if there is a canonical typed inter-level relation carrying nontrivial Role-phase operator structure, not merely the real scale \(\phi\).

### Partial positive

`GOLDEN-SCALE-WELD-OWNED-CARRIER-WELD-MISSING`

Use if the only canonical common structure is scale/trace and no canonical finite carrier map is selected.

Or, if an operator/carrier comparison exists but does not yet integrate `J/H` fully:

`ROLE-PHASE-REFINEMENT-COMPARISON-CONSTRUCTED-FULL-NATURALITY-MISSING`.

### Scoped no-go

`GOLDEN-ROLE-PHASE-WELD-CLASS-NOGO`

Use only for a clearly defined exhausted class.

### New primitive

`GOLDEN-ROLE-PHASE-COMPARISON-NEW-PRIMITIVE-REQUIRED`

Use if current owners do not canonically select the comparison object. Name its minimal typed signature.

## Deliverable

Produce one durable theorem-ready memo suitable for `02_REGISTRY/research/`.

It must include:

- repository baseline;
- owner audit;
- the three-tower type table;
- truth repairs;
- exact positive construction or scoped no-go;
- finite controls;
- operator-naturality results;
- relation to the fixed-`N` common-center seam;
- time firewall;
- one terminal verdict;
- theorem-ready handoff;
- exactly one recommended next step.

Do NOT edit Lean, task lifecycle, manifest, claims, generated work views or release metadata.

Do NOT open further speculative tasks.

## Forbidden promotions

Do not infer:

- Tower A = Tower B = Tower C;
- `archiveProjection` is physical Role-phase refinement;
- \(\phi\) scale = physical time;
- Role A = causal time;
- \(\mathcal S\) is golden;
- `J` is metric-dependent;
- a unique nonlinear `Q(e)`;
- physical stress or Einstein equations;
- BOOK `F_N`;
- direct scene-to-shell coupling;
- SM gauge structure.
