# CTRL-GRAVITY-DYNAMICS-CLOSURE

## Class
CONTROL

## Objective
Supervise and terminally classify the post-C1 gravity dynamics seam across the kinetic, relative-normalization, and spatial Hodge selector lanes.

## Scope
1. Coordinate formalization and review of the two-tick symplectic kinetic structure.
2. Treat `D0.Geometry.SceneSourceStratification` and `D0.Geometry.SignlessSignedCommonCarrier` as the accepted finite source/carrier owners. The matter→tensor arrow is terminally split into A-STRESS `SOURCE-CARRIER-MISSING` plus A-SOURCE `SELECTOR-NOGO-TERMINAL`. A-WEIGHT is terminally `WEIGHT-SELECTOR-NOGO-TERMINAL`. A-WEIGHT-DYN now exactly classifies the general positive H-invariant weighted A1 Hessian and proves four observable finite moduli `(mu, gamma, u, v)` in the nonzero interior; A-NORM is exactly the uniform subfamily `u=v=1`, where `mu=m^2`. A-CAP is now terminally `CAPACITY-WEIGHT-SELECTOR-NOGO-TERMINAL`: the exact bare-metric mismatch has rank strata 0/22/20/18/30 and vanishes exactly at `u=v=1`, but the reduced physical A1 Hessian preserves `K+` and no owned theorem turns the bare mismatch into additional non-reusable saturated archive memory. A-PARENT is now terminally `COMMON-PARENT-ACTION-SELECTOR-NOGO-TERMINAL`: an exact common type-I generator exists for every member of the observable `(mu,gamma,u,v)` family, so common variational/generating structure selects none of those four moduli. A selector would require a stronger uniqueness theorem deriving one normalized parent law from one primitive finite history cost. The A1 compensator/Noether mechanism is now Lean-owned by `D0.Gravity.A2CompensatorNoether` after PR #48: arbitrary positive `W`, finite Stueckelberg invariance, off-shell Ward identity, normal-equation elimination, unique physical residual and `BPlus G_phys=0` are CORE-FORMALIZED within their finite Ward scope. One ordinary WORKER is active: `WRK-ACAP-RIESZ-MISMATCH-LEAN`, formalizing only the exact algebraic ACAP mismatch theorem on the literal common carrier. `D0.Gravity.A1RieszMismatch` is accepted as Lean support for the exact A-CAP algebraic residue: tensor-sector annihilation, standard-sector coefficients 143(a-b), 117(a-c), 99(b-c), individual 8/10/12 image ranks when nonzero, and zero mismatch iff uniform weight are machine-owned, without any capacity/physical-leakage promotion. `D0.Gravity.FiniteGravityResponseSingleWitness` is accepted as Lean support: one stored `E_L=2L` now owns its identity, symmetry, row-sum conservation and nontriviality predicates without cross-witness aggregation. `D0-ARCHIVE-LOCAL-LAPLACIAN-VARIATION-ISOMORPHISM-001` is honestly CORE-FORMALIZED after PR #51. `D0.Geometry.A4DSymRoleCentralDifference` is accepted Lean support after PR #52. `D0.Gravity.A4DLinearizedMetricResponse` is accepted Lean support after PR #53: one explicit finite Euclidean response now owns sitewise symmetry, exact gauge nullity, centered Role-divergence zero, self-adjointness, quadratic action/first variation, radius-two locality and the L=2 degeneration, without uniqueness/Lorentz/Einstein promotion. The typed T4 reconstruction owners from PR #54 are also accepted: actual smooth fixed T4, tangent bilinear Lorentz metric data, local Role frames/stencils, metric sampling, pointwise symmetric-tensor reconstruction and the reconstruction pullback square are machine-owned without convergence/naturality claims. The actual local execution environment shares one checkout/cache, so local workers are intentionally serialized. `WRK-A4D-PRIMAL-DUAL-PARENT-ALGEBRA` is accepted and removed: the odd one-role transport no-go, the radius-one centered closure no-go, the conditional mixed parent Ward, and the conditional centered stress descent are the single owners, and no constitutive star was selected. `EXP-SCENE-RIESZ-M1-OPERATOR-WELD` is accepted and removed: the opposite-cut identification, the M1 representation premise, and the adjacent-only Role/Fock conjugation boundary are owned. `WRK-A4D-EXTRA-RAY-FRAME-NOGO` remains PLANNED. Certificate freshness has moved to the semantic-hardening CONTROL lane.
3. E-CONN is accepted as research with verdict `CARTAN-CONNECTION-NEW-PRIMITIVE`: the cubical path groupoid plus landed 16-state CAR fibre gives a canonical local factorization envelope; the number-preserving associative CAR coefficient algebra is exactly the 70-dimensional degree-preserving endomorphism algebra. Current D0 does not derive the local centered-Cartan action `delta_xi Omega` on elementary link holonomies or the curvature-sensitive constitutive selection `star=S(e,n,Omega)`; Wilson covariance and pure Lorentz spin transport do not supply this. E-SPHAM is accepted as research with verdict `LORENTZ-STRESS-VARIATION-STILL-MISSING`, with upstream truth repair `LANDED-CAR-DISPERSION-MISMATCH`: fixed-time spatial compression is exact and `|D_Sigma|` is canonical, but the literal directional matrix has cosine hopping dispersion, so the rank-96 six-label harmonic projector is not the lowest positive spectral shell of the landed Hamiltonian and its energy does not approach 2π. E-CARDISP now audits/reconstructs the operator layer before any Lorentz-stress promotion. The parent algebra/no-go/descent package is accepted; `HODGE-CONSTITUTIVE-PRIMITIVE-REQUIRED` remains the constitutive boundary.
4. Maintain `02_REGISTRY/RESEARCH_LEDGER.md` and `02_REGISTRY/research/` so accepted research changes the active closure map before formal integration.
5. **Lean throughput policy:** ordinary gravity workers preserve the local Lake/Mathlib cache. Iterate with narrow target builds, run one incremental `lake build D0.All` before PR/REVIEW, and rely on remote `lean-build` as the independent integration gate. `lake clean`, `.lake` deletion, or global Mathlib cache deletion are not worker gates and require an explicit CONTROL/release reason (toolchain/dependency change, confirmed cache corruption, or requested cold-build evidence).
6. Provide terminal classification: assign an honest CORE, NO-GO, BRIDGE/PASSPORT, or EMPIRICAL boundary to each lane, with no unclassified physical TT/propagation choice remaining.

## Affected Claims
- `D0-HODGE-LINKS-001`

## Exit Condition
The gravity dynamics seam is terminally classified when the common-carrier/source seam, source-to-tensor reach, spatial selector/relative-normalization lane, temporal law, physical readout, and continuum tensor/Einstein interface are each assigned an honest CORE, NO-GO, BRIDGE/PASSPORT, or EMPIRICAL boundary, with no unclassified internal propagation choice remaining.
