# EXP-M1-RIESZ-READOUT-REPRESENTATION

## Class
EXPENSIVE

## Parent
CTRL-GRAVITY-DYNAMICS-CLOSURE

## State
PLANNED

## Objective

Construct or refute an independently motivated representation from the two M1 verification lines to two scene/Riesz readouts.

Do not define those readouts as the two sides of the bare mismatch in order to close the theorem. Do not call `u=v=1` M1-selected unless that representation theorem exists and forces the uniform weight.

## Terminal results

Return exactly one:

- representation exists and is canonical;
- representation exists but has moduli;
- an additional M1 premise is required;
- no compatible representation exists.
