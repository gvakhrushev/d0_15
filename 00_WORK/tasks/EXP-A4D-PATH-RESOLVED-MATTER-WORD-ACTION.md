# EXP-A4D-PATH-RESOLVED-MATTER-WORD-ACTION

## Class

EXPENSIVE / DEEP RESEARCH

## Parent

`CTRL-GRAVITY-DYNAMICS-CLOSURE`

## State

PLANNED

## Start gate

SATISFIED.

Required landed inputs:

- PR #70 affine Cartan path geometry;
- PR #75 full staggered first jet `H(e)`;
- PR #76 located two-color `J`;
- PR #80 second-order / scalar-advective package;
- PR #84 endpoint-overlap terminal;
- PR #86 golden/Role-phase terminal.

Required durable packets:

- `02_REGISTRY/research/MEMO_A4D_SOLDERED_CREATOR_OBSERVER_FRAME_LIFT.md`;
- `02_REGISTRY/research/MEMO_A4D_COMMON_CENTER_MATTER_GROUPOID_ACTION.md`;
- `02_REGISTRY/research/MEMO_A4D_ENDPOINT_COMPARISON_JET_OVERLAP_LAW.md`;
- `02_REGISTRY/research/MEMO_A4D_GOLDEN_ROLE_PHASE_REFINEMENT_WELD.md`;
- `02_REGISTRY/research/SYNTHESIS_A4D_PATH_RESOLVED_MATTER_WORD_ACTION.md`.

The frame/CAR Lean worker may proceed in parallel. This EXP does not wait for its merge, but must distinguish already-owned Lean facts from theorem-ready research input.

## Objective

Construct or terminally classify one typed fixed-level matter word action/comparison

```text
C_N(w ; e, n)
```

on the existing Role-phase/CAR carrier, where `w` is an already-owned archive path/incidence word.

The goal is to determine whether one law can unify the objects that are currently separate shadows:

- the complete first jet `H(e)`;
- the allowed comparison second jet `S`;
- the forced output-site-local `K` when restricted to that class;
- relative/open holonomy;
- observer/frame covariance;
- fixed-`J` dualization.

Do not assume the unification. Prove it or terminally obstruct it.

## Central research questions

1. What is the exact source/target type of `C_N(w;e,n)`?
2. Is it a representation of PR #70 path concatenation at fixed background?
3. How does that path composition interact with the separate background-dependent Cartan action-groupoid cocycle?
4. Does the link/exterior transport from the frame packet extend to the required constitutive comparison, or is an additional local cell datum unavoidable?
5. Is there a well-typed congruence law transporting one energy `W` by the same `C_N`?
6. On what class of words/background moves is that congruence meaningful?
7. Does its first derivative reproduce the entire owned `H(e)`, including scalar bond, half-average and corner terms?
8. Does its second derivative select a unique `S), leave a modulus, or contradict the mandatory L=5 constraints?
9. Does the observer argument `n` select the residual nearest-neighbor family or merely transform covariantly with it?
10. Can the dual word action be derived from fixed located `J` with correct endpoint anchors?
11. Does loop response equal the owned relative/open holonomy?
12. What is the strongest finite locality class of the resulting law?
13. If the construction fails, what is the earliest additional primitive, stated with exact source/target types?

## Mandatory attack order

### A. Type the word category first

Use the literal PR #70 path/chain objects. Do not invent a midpoint site or a second path algebra.

Record explicitly:

- object type;
- oriented letter type;
- source/target;
- word concatenation;
- reversal/inverse when defined;
- open/relative holonomy owner.

### B. Build the matter action from owned geometry

Start from the already-derived exterior lift of Lorentz-restricted link transport.

Derive the path action by composition where possible.

Then identify exactly what is still missing to reproduce the constitutive comparison.

Do not define the target `H(e)` coefficients as the action.

### C. Separate two compositions

Keep distinct:

1. path concatenation at fixed `e`;
2. background/frame/Cartan action-groupoid composition.

If one `C_N` participates in both, prove the compatibility square/cocycle.

### D. Test the energy-congruence hypothesis

Investigate a typed version of

```text
W(e') = C_N(w;e,n)^(-T) W(e) C_N(w;e,n)^(-1).
```

First derive `e'` and the domain where this equation is meaningful.

If inverse locality is problematic, search for an equivalent inverse-free parent formulation.

Do not use the formula as a definition merely to force covariance.

### E. Word-length one

Recover or fail to recover every block of the owned first jet:

- scalar neighbor bond;
- endpoint polarization;
- `A_r` half-average;
- Fock bilinear `E_sr`;
- mixed corner paths;
- degree/parity.

A candidate that only recovers `dForward` or a centered scalar shadow fails.

### F. Word-length two

Same-axis:

- compare with the exact `S_patch` constraints;
- compute the induced symmetric second derivative;
- decide whether the continuous nearest-neighbor modulus is selected.

Mixed-axis:

- reproduce the L=3 corner exactly;
- keep the two geometric paths distinct until relative holonomy is applied.

### G. Loops / holonomy

Two words with the same endpoints but nontrivial relative holonomy must not collapse to one unlabelled center.

Recover the PR #70 open/relative transport in the matter action.

A universal telescoping loop is a terminal failure mode.

### H. Observer/frame test

Use the exact moving observer `h_n` and rational A/B boost.

Determine whether observer covariance:

- selects the residual word-length/counting modulus;
- leaves it free;
- or is incompatible with the full staggered jet.

Do not interpret `n=e_A` as physical time.

### I. Located-J dual word

Derive endpoint-aware contragredient dualization.

Do not use a single-site formula if the two Fock components land at different located dual anchors.

Test:

- complement degree/sign;
- parity;
- Role permutation pseudoequivariance;
- shifted anchors;
- loop dual holonomy.

### J. Locality and transverse coframes

Audit locality by:

- maximum word length;
- cell/patch radius;
- path memory;
- dependence on inverse transport;
- dependence on observer transport.

Test harmonic strain and plaquette curl outside the pure-gauge image.

### K. Golden boundary

Do not use `phi`, Bratteli words, Perron measure or record projection to construct `C_N`.

Only state what a future inter-level task could test after a positive fixed-level law exists.

## Exact hostile controls

Mandatory exact controls:

- L=2 Nyquist;
- L=3 corner;
- L=5 delta;
- L=5 non-delta with `G^2 != 0`;
- constant matter;
- constant harmonic strain;
- plaquette curl;
- pure gauge;
- flat background;
- all Fock degrees 0..4;
- Fock parity;
- signed Role permutations;
- rational A/B Lorentz boost with moving observer;
- fixed located-`J` shifted anchors.

Prefer exact rational/symbolic algebra. Floating-point evidence is not a proof.

## Required terminal forms

Use the strongest justified terminal, for example:

- `PATH-RESOLVED-MATTER-WORD-ACTION-CONSTRUCTED`;
- `PATH-WORD-ACTION-OWNED-ENERGY-CONGRUENCE-MISSING`;
- `PATH-RESOLVED-MATTER-WORD-ACTION-SCOPED-NOGO`;
- `PATH-WORD-MATTER-REPRESENTATION-NEW-PRIMITIVE-REQUIRED`.

Do not use a broad no-go unless the class actually exhausted matches the terminal wording.

## Deliverable

One durable theorem-ready memo suitable for `02_REGISTRY/research/` containing:

- exact typed definitions;
- owned inputs versus new derivations;
- composition/cocycle formulas;
- first- and second-jet calculations;
- observer and located-`J` laws;
- exact controls;
- positive construction and scoped no-gos;
- locality statement;
- terminal verdict;
- theorem/module handoff if positive;
- exactly one recommended next step.

No Lean source in this task.

Do not change claims, release metadata or generated formalization views.

Under the GitHub-first flow:

1. run `python tools/task_lifecycle.py start EXP-A4D-PATH-RESOLVED-MATTER-WORD-ACTION`;
2. open a Draft PR before research edits;
3. keep the PR body lifecycle synchronized;
4. commit the durable memo and any allowed roadmap updates only inside that PR;
5. before Ready, run `python tools/task_lifecycle.py retire EXP-A4D-PATH-RESOLVED-MATTER-WORD-ACTION`;
6. mark Ready with `Lifecycle: REVIEW`;
7. do not self-merge.

## Truth firewall

Do not infer:

- physical causal time;
- Spin structure;
- metric `J`;
- unique `S` or universal `K` without derivation;
- a Tower-C/Tower-B carrier weld;
- provenance-bearing stress;
- continuum Einstein dynamics.
