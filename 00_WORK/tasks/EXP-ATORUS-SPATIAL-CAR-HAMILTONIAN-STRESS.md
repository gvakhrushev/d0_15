# EXP-ATORUS-SPATIAL-CAR-HAMILTONIAN-STRESS

## Class
EXPENSIVE

## Parent
CTRL-GRAVITY-DYNAMICS-CLOSURE

## Objective

Proceed conditionally on the explicit BRIDGE primitive identified by E-TIMEAXIS:

> archive Role A / slot 0 is the same physical axis as causal/Pisot e0, with a chosen orientation equivalence, and physical time is unwrapped to \(\mathbb R\).

Do not try to derive this bridge again.

Construct or terminally classify the spatial CAR/Hamiltonian/Lorentz-stress architecture on \(\mathbb R\times T^3\) using the now-landed finite CAR operator.

Repository edits: **NONE**.

## Spatial carrier

Use the derived time Role \(t=A\).

Define the spatial Role subtype and
\[
\Sigma_N\simeq(\mathbb Z/L\mathbb Z)^3.
\]

Give the exact finite product decomposition of the archive carrier and the fixed-time embedding/restriction maps.

## Reuse the landed CAR owner

The current main now literally owns:

- directional \`spatialDiracComponent N r\`;
- \`fockGamma r\`;
- total \`carDirac N\`;
- self-adjointness;
- parity anticommutation.

Do not rewrite these.

Define the spatial CAR basis on the fixed-time slice and the three-Role operator by compression/restriction:
\[
D_{\Sigma,N}
=
\sum_{s\in Role_{sp}}
D_{\Sigma,N}^{(s)}\otimes\Gamma_s.
\]

Prove/derive the strongest exact relation to compression of \`carDirac\`.

## Positive Hamiltonian

Audit the finite operator
\[
H_{\Sigma,N}=|D_{\Sigma,N}|
\]
or the minimum exact positive alternative.

Determine what theorem/library is needed for the absolute value of a finite self-adjoint matrix and whether this introduces any new physical choice.

## Lowest spatial mixed shell

Construct the six-direction first shell and normalized mixed projector.

Derive:

- rank, including the retained 16-state Fock fibre;
- positive energy;
- translation/spatial-Role/parity symmetries;
- \(E_{1,N}\to2\pi\);
- exact common-target reconstruction to the first spatial harmonics on \(T^3\).

## Lorentz stress

Use an actual Hamiltonian/coframe or lapse/shift/spatial-metric variation.

Derive, rather than assume:
\[
T_{00},\qquad T_{0i},\qquad T_{ij}.
\]

Check whether the isotropic shell yields
\[
T_{00}=\rho,\quad T_{0i}=0,\quad T_{ij}=p\delta_{ij},
\]
and whether \(p=\rho/3\) and vanishing Lorentz trace follow.

## Evolution and background

State the exact finite/continuum interface for
\[
i\partial_\tau\psi=H_{\Sigma,N}\psi.
\]

Then formulate the sourced background equation
\[
\mathcal E_N[\bar m_N]
=
\kappa_N T_N[\bar m_N,\rho_N]
\]
before perturbative linearization.

Leave \(\kappa_N\) unselected unless an owned principle fixes it.

## Terminal verdict

Return exactly one:

- \`SPATIAL-CAR-HAMILTONIAN-STRESS-REACHED\`
- \`SPATIAL-CAR-COMPRESSION-OWNER-MISSING\`
- \`POSITIVE-HAMILTONIAN-FUNCTIONAL-CALCULUS-MISSING\`
- \`LORENTZ-STRESS-VARIATION-STILL-MISSING\`
- \`SOURCED-BACKGROUND-OWNER-FIRST\`

## Deliverable

\`MEMO_50_ATORUS_SPATIAL_CAR_HAMILTONIAN_STRESS.md\`
