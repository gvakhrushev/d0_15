# EXP-A4D-NONLINEAR-EINSTEIN-J2-BRIDGE

Class: `EXPENSIVE`  
State on registration: `PLANNED`  
Parent: `CTRL-A4D-RESOLVED-AFFINE-PROGRAM-WAVE`

Repository: `gvakhrushev/d0_15`
Base: `main`
Branch: `exp/a4d-nonlinear-einstein-j2-bridge`
Primary artifact: `02_REGISTRY/research/MEMO_A4D_NONLINEAR_EINSTEIN_J2_BRIDGE.md`
Execution: `GitHub-first`

## Why delegated

PR #201 closed the finite flat action-Hessian gate:
[
K_{\star,\mathrm{metric}}=\frac14 K_{E_\eta},
\qquad \beta_{\rm sp}=0.
]
The remaining upper-wall problem is nonlinear and geometric: determine whether this
selected finite action response extends to one metric-only smooth second-jet response
on the explicit T4 testbed satisfying the hypotheses needed by Navarro/Lovelock, or
identify the first exact obstruction. This is research, not routine formalization.

## GitHub execution contract

Start only from current `main`; run
`python tools/task_dispatch.py EXP-A4D-NONLINEAR-EINSTEIN-J2-BRIDGE`;
open a Draft PR before substantive research; keep checkpoint-first durable results;
never promote continuum assumptions to finite theorems; no claims/release/BOOK or
Lean promotion in this task; never self-merge.

## Objective

Build the shortest exact bridge

[
S_\star
\longrightarrow
E_\eta
\longrightarrow
E[g]\in \Gamma(S^2T^*T^4)
\longrightarrow
aG[g]+bg
]

without inserting the Einstein field equation by hand.

The task starts from already-owned facts:

- exact finite F5: star metric Hessian is pure (E_\eta);
- normal-jet bridge: (E_{\eta,N}\to -2G+O(\varepsilon_N^2));
- extra (E_{\rm sp}) ray fails metric-only frame erasure;
- fixed-manifold T4 local universality is sufficient for the cited natural-tensor theorem.

## Required gates

1. Define the candidate nonlinear metric-only local (J^2) response generated by
   the selected star action, or prove that the current finite variables cannot define it.
2. Separate finite reconstruction assumptions from consequences.
3. Prove or falsify local-frame/grid erasure on arbitrary Lorentz metric 2-jets.
4. Prove or isolate the missing covariant-divergence/Noether bridge.
5. Test zeroth-order (bg) separately; do not tune a cosmological term from phi.
6. State precisely whether Navarro/Lovelock hypotheses are all met on T4.
7. If met, identify the resulting class (aG+bg) and normalization relative to the
   finite F5 coefficient. If not met, produce the smallest named obstruction.

## Two-sided attack

Work from both ends:

- bottom-up from the exact star Hessian and its finite metric quotient;
- top-down from the minimal hypotheses of Navarro/Lovelock/Palatini.

Use known continuum geometry as a target/classifier, not as imported D0 dynamics.

## Falsification controls

- a frame-dependent (G+\alpha Rq_{grid}) response must be rejected;
- an (E_{\rm sp}) contamination must be detected;
- a result valid only in one normal frame is insufficient;
- contracted Bianchi imported after recognizing (G) must be labeled external and
  kept separate from a finite Noether derivation.

## Desired terminal

Either

[
\boxed{\texttt{STAR-ACTION-NONLINEAR-J2-RESPONSE-ENTERS-EINSTEIN-LOVELOCK-CLASS}}
]

with explicit hypotheses and coefficient map, or a precisely scoped terminal obstruction.

## Chat handoff

Return the PR, final SHA, strongest exact/structural theorem, all imported hypotheses,
and the single smallest remaining blocker.
