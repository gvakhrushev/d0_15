# D0 v15 Internal Sync and Passport Markers (moved from books)

All active-book working markers, sync headers, exact sync tokens, PASS_/FAIL_ logs, weighted_mean=, events_used=, RMSE= etc. have been removed from Books 00–08.

Books now contain only:
- theorem statement
- operator protocol
- passport reference (e.g. "see 08_PASSPORTS/_RESULTS/ and the corresponding cert")

Preserved here for internal continuity only.

## Examples of moved content (illustrative, not exhaustive)
- v15 sync header lines
- Exact sync token:
- PASS_LIGO_..., events_used = N, rmse_d0 = ...
- FAIL_DESI_..., PASS_SPARC_..., weighted_mean = ...
- Similar for other cert outputs and release notes

The published corpus is cleaned for release-candidate form.
