## 02.15 Archive pressure and spectral transfer

The current canonical theorem schema for transfer is:

```math
C_{static}^{D0}\rightarrow L_D\rightarrow \operatorname{Spec}(L_D)\rightarrow S_{DE}(u)\rightarrow\mathcal T_{\mathcal O}^{D0}\rightarrow\mathcal O_{obs}.
```

The BAO/archive-pressure comparison protocol is a strong empirical example, but this book records the general typed rule: the static coefficient, finite spectrum and transfer window must be fixed before external comparison.

**[Finite Feshbach -> profinite tower].** The finite Feshbach-Schur effective operator `W_eff(z)=A-B(D-zI)^-1 C` (depth-2 pole sum = `alpha_alg^-1`, `D0-ALPHA-FESHBACH-RESIDUE-FINITE-SUM-001`) extends to the canonical phi-ladder refinement tower with weights `r^N` (`r=phi^-3`) and `2^11` Boolean-ledger increments (`D0-ALPHA-PROFINITE-SPECTRAL-TOWER-OWNER-001`, Lean `D0.Spectral.AlphaProfiniteSpectralTower`). This is the ADMISSIBLE infinite-pro-object route; the finite heat trace itself is analytic at `0` and carries no `1/s` pole (`D0-DIXMIER-FESHBACH-FINITE-HEATTRACE-001`).

**[Canonical Bratteli refinement tower].** The golden cylinder language (golden-mean SFT, forbid `11`) canonically determines its refinement Bratteli incidence `M_phi=[[1,1],[1,0]]` -- RECOVERED from the allowed-word rule, not inserted (`D0-BRATTELI-FIBONACCI-REFINEMENT-OWNER-001`, Lean `D0.Geometry.FibonacciBratteliRefinement`): Perron `phi`, Fibonacci dims, unique AF trace ratio `phi`, forced scale ratio `phi` (`D0-PERRON-SCALE-FLOW-OWNER-001`). So the present core DOES carry a canonical renormalization tower at the algebra+trace+scale level (`D0-CANONICAL-RENORMALIZATION-SPECTRAL-TOWER-001`). It does NOT carry a canonical inductive SPECTRAL-TRIPLE (Dirac-compatible isometric `J_N`): the corpus is an inverse limit of downward projections; the isometric inductive realization is external (`D0-INDUCTIVE-SPECTRAL-TRIPLE-OWNER-001`, ASSUMP-CONNES-RECONSTRUCTION / propinquity). Master A Outcome B.

**[Iter23 — the isometric leg is constructible after all (`vp_inductive_spectral_triple_isometric_leg.py`).** The "Outcome B" framing above located the obstruction in the *isometric Dirac-compatible* `J_N` itself; that is too strong, and the finite leg is now supplied internally. For the standard graded (Christensen–Ivan) Dirac on this Fibonacci tower (dims `2,3,5,8,13`), the successive spectra **nest as multisets** `spec(D_N) ⊆ spec(D_{N+1})` (the graded Dirac only adds higher eigenvalues upward), so the eigenspace-inclusion selection matrix `J_N` is an explicit **isometry** (`J_N^† J_N = I`) that is **Dirac-compatible** (`J_N^† D_{N+1} J_N = D_N`) at every level `N = 0..3` — verified in exact integer arithmetic. The condition is nontrivial (a non-nesting Dirac admits no such `J`, by Cauchy interlacing). So the isometric `J_N` is **not** the obstruction: it *exists* and is present-core-constructible. What genuinely stays external is only the **canonicity/forcing** of the upward inductive structure over the corpus's *downward* inverse limit (`D0-ARCHIVE-LIGHTPROFINITE-001`) together with the smooth metric via propinquity. The `EXACT-MISSING` therefore sharpens from "construct an isometric Dirac-compatible `J_N`" (done) to "force it as canonical over the inverse limit"; the claim stays PROOF-TARGET with its existence leg now internally owned.

**[Iter23/24 — seven scene-matched VNEXT/VNEXT2 owners: TWO closed positively, four sharpened NO-GO, one aspirational (`vp_vnext_scene_matched_owners_derived_nogo.py` and companions).** Seven scene-matched "OWNER" targets were PROOF-TARGET. The constructive attack resolves them as follows.

*Two are now CERT-CLOSED positively.* (i) `D0-VNEXT-AF-D0-FESHBACH-COMPRESSION-OWNER-001`: the scene's own `Aut`-invariant zone/archive Schur complement `W_eff = A_bb − (1/20)·B·Bᵀ` exists exactly (boundary block `D_tt = 20·I₁₃`), eigenvalues `{0,22,24,33}` on the 20-dim bulk — a Feshbach compression needs only a projection split, not a unitary (`vp_vnext_scene_feshbach_compression_owner.py`). (ii) `D0-VNEXT2-SCENE-DIRAC-COVARIANCE-OWNER-001`: its `PRIM-DIRAC-SCALE-SELECTION` blocker was resolved to `λ_N = λ_0·φ^N`, so the covariance cocycle `ω(N) = λ_{N+1}/λ_N = φ` is a determined, base-independent constant (Lean `D0.VNext2.CanonicalDiracCovariance`, cert `vp_vnext2_canonical_dirac_covariance_owner.py`).

*Four stay NO-GO, on sharpened grounds.* The Feshbach tower-compatibility owner, scene-Feshbach-lift, and scene-spectral-lift require transporting operators/spectra between the scene and the AF carrier. The **root** obstruction is not merely "no *unitary* `Ξ`" (distinct-count 4 vs 5) but a stronger interlacing fact: no **UCP corner / correspondence** realizes the full AF Dirac² in the scene at any scale — the AF-reduced corner embeds for dim `k ≤ 4` but is infeasible for every `k ≥ 5` (exact binding inequality `20/φ² > 33/φ⁴ ⇔ 20φ > 13`), because the scene's nonzero spectrum is too *bunched* — its dynamic range `d₁/d_top = 20/33 = 0.606` exceeds the threshold `1/φ² = 0.382`, leaving too little range to host the AF *geometric* ladder `φ²:φ⁴:φ⁶`. This depends only on the eigenvalue range, not on how multiplicity mass is distributed: a spread control (same multiplicities, `d₁/d_top = 2/33`) admits the `k = 5` corner, while permuting the scene's multiplicities (same eigenvalue set, same `d₁/d_top`) does not (`vp_vnext2_scene_spectral_lift_interlacing_nogo.py`). The endpoint conditional-expectation owner's **measure** blocker is removable (the Perron–Frobenius eigenvector is the canonical `Aut`-invariant measure) but its **carrier** blocker is genuine: the endpoint operator lives on the history carrier, and the admissible families give different carriers (all-walks vertex dim 33 vs non-backtracking/directed-edge dim `2|E| = 718`); Ihara–Bass links only their *spectra*, not the operator.

The scene Laplacian `L(K(9,11,13))` carries 5 distinct eigenvalues (`{0,20,22,24,33}` with multiplicities `{1,12,10,8,2}`), the AF-reduced Dirac² carries 4 (`{1,3,8,21}`), both partitioning 33. The aspirational pair `D0-VNEXT-ISOMETRIC-DIRAC-TOWER-OWNER-001` keeps its own NO-GO twin, unchanged.

**[Iter25 — the dimension itself DOES lift, and it is *forced* (`vp_scene_dim_even_fibonacci_forcing.py`, Lean `D0.VNext2.SceneDimEvenFibonacci`, `D0-SCENE-DIM-EVEN-FIBONACCI-FORCING-001`).** The obstruction above is to *spectral* transport; the *dimensional* transport is not obstructed — it is a forced convergence the corpus had not recorded. The scene dimension is `|V| = 9+11+13 = 33`, and the AF Dirac² eigenspace multiplicities are the even-indexed Fibonacci numbers `{F₂,F₄,F₆,F₈} = {1,3,8,21}` on the even powers `{φ⁰,φ²,φ⁴,φ⁶}`. These sum to the *same* 33, because `∑_{k=1}^{4} F_{2k} = F₉ − 1 = 33` (the classical even-index partial-sum identity at `n=4`). This is not a coincidence of integers: the zone ladder steps by `+2` (a `+1` step would import an external `ℤ₂` orientation bit, `⊥`M1, §01), and the Dirac² ladder steps by `+2` in the exponent (even powers) — one shared `+2` root, so both are `+2`-graded partitions of 33. The whole scene Laplacian spectrum is itself forced by that same `+2`: it is the classical complete-multipartite closed form `0¹, (N−nᵢ)^{nᵢ−1}, N^{k−1}` = `0¹,24⁸,22¹⁰,20¹²,33²`, proved by explicit eigenvectors (`D0-SCENE-LAPLACIAN-SPECTRUM-FORCED-001`, Lean `D0.VNext2.SceneLaplacianSpectrumForced`, cert `vp_scene_laplacian_spectrum_forced.py`) — so the "5 distinct eigenvalues", the spectral bunching, and the whole shape of the transport obstruction are *downstream* consequences of the single zone forcing, not independent inputs. The grammar of each side is fixed by M1 *independently and before* the identity is read off, exactly the multi-channel forcing the corpus already uses for `rank = 3` (Frobenius `ℍ`, tripartite rank, icosahedral slice, tower-stop). **Honest scope:** this lifts *dimension and grading*, not spectrum — the mass profiles differ (`{1,12,10,8,2}` over 5 bands vs `{1,3,8,21}` over 4), and refining the coarse scene band into the fine AF ladder is the separate refinement-rule NO-GO. It is orthogonal to the refuted *algebra* anchor (`D0-VNEXT-33-SCENE-ANCHOR-NOGO-001`): the AF matrix-algebra dimension is `5²+3² = 34 = F₉ = 33+1`, one more than the eigenspace dimension — the `+1` is the kernel mode.

The **sixth** owner, `D0-VNEXT-AF-D0-FESHBACH-COMPRESSION-OWNER-001`, was initially swept into the same NO-GO — and that was an **over-scoped error, now retracted**. A Feshbach compression needs only a keep/trace *projection split*, not a unitary `Ξ`. Against the scene's own `Aut`-invariant zone/archive split (bulk zones 1,2 = `9+11 = 20` vertices; traced boundary zone 3 = `13` vertices), the boundary block is `D_tt = 20·I₁₃` **exactly** (zone 3 is edge-free, each boundary vertex has degree `33−13 = 20`), so the Schur complement `W_eff = A_bb − (1/20)·B·Bᵀ` is exact and `z`-independent. Its eigenvalues are `{0,22,24,33}` with multiplicities `{1,10,8,1}` on the 20-dimensional bulk — **4** distinct eigenvalues. So the canonical compression *performs exactly the `5→4` distinct-eigenvalue reduction a unitary cannot* (the boundary trace removes the eigenvalue `20`, which is the boundary block's own value). This closes the compression owner **PROOF-TARGET → CERT-CLOSED**. Honest bound: the compressed spectrum `{0,22,24,33}` is *not* the geometric `φ`-ladder, so the compression does **not** prove scene↔AF spectral *congruence* — the lift/covariance owners stay NO-GO, correctly scoped to that stronger claim. A further **positive** counterpoint: the AF-side *trace-preserving conditional expectation itself* exists and is unique (finite-dimensional Takesaki). The aspirational pair `D0-VNEXT-ISOMETRIC-DIRAC-TOWER-OWNER-001` keeps its existing NO-GO twin and is unchanged.

**[vNext: AF/GNS isometric tower].** Distinct from the INVERSE D0 support tower (downward projections), the vNext INDUCTIVE Fibonacci AF/GNS tower has trace-preserving Bratteli inclusions whose Perron-trace compatibility (`1+phi^-1=phi`) induces genuine isometries `J_N` (`D0-VNEXT-AF-GNS-ISOMETRY-OWNER-001`, CERT, Lean `D0.VNext.PerronGNSTower`). The Dirac/Laplacian defect test then FAILS canonically: the Dirac scale is underdetermined (`D0-VNEXT-MARTINGALE-DIRAC-OWNER-001`, NO-GO) and there is no comparison map `Xi_N` to the fixed 33-dim D0 Laplacian (`D0-VNEXT-DIRAC-LAPLACIAN-COMPATIBILITY-OWNER-001`, NO-GO; AF dims skip 33). The candidate primitive `PRIM-ISOMETRIC-DIRAC-J_N` splits into +2 (scale + comparison map).

**[vNext+1: 34-to-33 is a tested quotient, not an identity].** `dim A_3 = 5^2+3^2 = 34` is the dimension of the matrix algebra `M_5(+)M_3` (block sizes = Fibonacci path counts 5,3), NOT `33+1` vertices; `34-1=33` is forbidden as a quotient proof. The spectral/Feshbach matching hierarchy fails: the unique trace=cyclic line gives a 33-dim reduction `su(5)(+)su(3)(+)u(1)=(24,8,1)` that does not match the tripartite scene `(9,11,13)` (`D0-VNEXT-AF-ONE-DIMENSIONAL-REDUCTION-CLASSIFICATION-001`, Outcome D), and the AF martingale Dirac^2 multiplicities `{1,3,8,21}` differ from the scene Laplacian's `{1,2,8,10,12}` for ANY scale (`D0-VNEXT-AF-D0-SPECTRAL-COMPRESSION-OWNER-001`, scale-independent). So `PRIM-CANONICAL-33-SCENE-ANCHOR` does not exist (`D0-VNEXT-33-SCENE-ANCHOR-NOGO-001`); the scale and Xi primitives stay independent.

**[vNext+2: scene-native conditional-expectation hierarchy].** Refining directly from K(9,11,13) (history/path tower, endpoint conditional expectation) the comparison-map question is tested without the AF route. The intertwining hierarchy is typed exactly: scene compression (`C_n L J_n = L_scene`) < spectral equality < heat-semigroup equality (`C_n e^{-uL} J_n = e^{-uL_scene}`) < Feshbach equality -- a weaker level never implies a stronger one. Result: no UNIQUE scene-native refinement rule is forced -- all-walks, non-backtracking, and directed-edge families satisfy M1 but are inequivalent (depth-2 carriers 15708 != 14990; transfer dims 33 != 718), so `Xi` is not canonical (`D0-VNEXT2-SCENE-NATIVE-REFINEMENT-NOGO-001`, Outcome D). New upstream primitive `PRIM-SCENE-HISTORY-REFINEMENT-RULE`; `PRIM-COMPARISON-MAP-XI-N` and `PRIM-DIRAC-SCALE-SELECTION` remain independent. The scene fingerprint (mults {1,2,8,10,12}, trace 718, structural R(3)+K(8,10,12)) is owned (`D0-VNEXT2-SCENE-FINGERPRINT-OWNER-001`, CERT).