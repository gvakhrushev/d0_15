## 07.CVFT.Horizon emission as conjugate archive leakage

At boundary-capacity saturation the emission channel is the conjugate feedback operator
\[
F_Q^{emit}(R)=Q_RU_R^\dagger P_RU_RQ_R.
\]
For a boundary mode \(\psi_{\ell,\omega}\), the greybody candidate is the finite expectation value
\[
\Gamma_\ell(\omega)=
\langle\psi_{\ell,\omega},F_Q^{emit}(R)\psi_{\ell,\omega}\rangle.
\]
Horizon emission is archive-to-retained boundary leakage under capacity saturation. Thermal continuum descriptions are downstream passports of this finite operator law.

**[Iter26 — the gauge-boundary commutator obstruction is now an internally-owned finite theorem (`D0-CVFT-F6`, CORE_BRIDGE_SPLIT, Lean `D0.VNext2.GaugeBoundaryCommutator`, certs `vp_gauge_boundary_commutator_obstruction.py;vp_gauge_boundary_commutator_theorem.py`).** The leakage/mass-gap *language* above rests on an exact operator identity: for any Hermitian generator \(g\) and orthogonal projection \(P\) (with \(Q = I-P\)),
\[
\lVert [P,g] \rVert_F^2 \;=\; 2\,\lVert Q g P \rVert_F^2 .
\]
Its algebraic core — Lean-verified for *any* \(P\) — is that the commutator carries no diagonal blocks, \([P,g] = PgQ - QgP\), so a **zone-preserving** generator (block-diagonal w.r.t. the retained/archive split, \(PgQ = QgP = 0\)) has \([P,g]=0\) and *zero* leakage, while a generator with even a single unit boundary-crossing (“colored”) coupling has \(\lVert QgP\rVert_F^2 \ge 1\) and hence a **hard quantized floor** \(\lVert [P,g]\rVert_F^2 \ge 2\). On the scene split (retained bulk = zones 1,2 = 20 vertices; archive/boundary = zone 3 = 13; \(V=33\)) this makes the finite Yang–Mills mass-gap-*language* obstruction an exact identity plus a quantized lower bound, not a hand-picked toy. **Status split:** the finite-scene commutator obstruction is now CORE (cert + Lean); the *continuum* Millennium Yang–Mills mass gap stays an external bridge — no continuum QFT is imported.

