## 07.41 Trace-Heat-Capacity Gravity

Trace-Heat-Capacity Gravity refines the finite gravity entry point:

```text
Toral automorphism
-> Lucas trace levels
-> heat moments of T^2
-> Lefschetz scene counts
-> boundary capacity
-> black-hole capacity saturation
-> finite gravity witness
-> Einstein macro-interface.
```

The local black-hole reading is local information-capacity saturation, not a
core singularity.  For a finite graph region `S`, the admissible finite rule is:

```text
I(S,s) = Tr_S(exp(-s Delta_G))
C(boundary S) = BoundaryCutWeight(S)/4
horizon condition: I(S,s)=C(boundary S)
saturation condition: I(S,s)>=C(boundary S)
```

When saturation holds, extra local information must be boundary-encoded.  The
factor `A/4` is read as ABCD boundary capacity: four terminal detector roles
count boundary distinguishability in quarter-cut units.  The macro Einstein
interface remains the downstream bridge after this finite heat/capacity witness.

Higher-curvature terms are not forbidden metaphysically. They are below finite
D0 readout threshold in the macro interface and therefore cannot act as leading
gravity law.

The compact statement is:

```text
D0 gravity is the finite archive macro-interface.
At the finite level: graph Laplacian controls information diffusion; boundary
cut capacity is holographic area; entropic surface tension is nonnegative;
archive flux is conserved; stress readout is symmetric; TT projector and W_TT
give the finite spin-2 carrier.
At the macro level: any compatible gravity interface must be symmetric,
divergence-free, second-order under finite readout, and compatible with TT
stress coupling.
Therefore the Einstein-Hilbert / Einstein tensor class is the macro-interface (finite A2 variation + Bianchi selects the class; this is closed finite-response status, not bridge calibration).
```

**[Iter26 — the finite Einstein-tensor response now exists as a proved object (`D0-SPECTRAL-EINSTEIN-001`, CERT-CLOSED, Lean `D0.VNext2.SpectralEinsteinResponse`, cert `vp_spectral_einstein_response.py`).** The macro-interface conditions above ("symmetric, divergence-free") were asserted for the *class*; the rank-2 response object that realizes them is now constructed. For the quadratic spectral action `S(L) = Tr(L²)` (the `a₂` / EH proxy at flat measure), the variational response is `G_ij = dS/dh_ij = 2·L_ij` — the scene graph Laplacian (up to the factor 2). It is proved *simultaneously* **symmetric** (`L` symmetric) and **divergence-free** in the corpus's own discrete sense `archiveDivergence(G)_i = Σ_j G_ij = 0` (a graph Laplacian annihilates constants, so its row sums vanish — the discrete contracted-Bianchi identity). The variational identity `d/dt Tr((L+tE)²)|₀ = Tr(G·E)` for symmetric `E` confirms `G = 2L` is genuinely the gradient, not a fitted stand-in; the scene response spectrum is `2·{0,20,22,24,33} = {0,40,44,48,66}`. This *bypasses* the former carrier mismatch: `G` lives natively on the `ℝ`-matrix EH-proxy carrier, with no `Fin 4` spin-2 carrier needed — the two-polarization TT-graviton linkage is the separate `D0-HODGE-LINKS-001`. It also correctly *scopes* the earlier conservation NO-GO: `NO_GO_CANONICAL_STRESS_CONSERVATION_PROJECTION` only exhibits *some* symmetric matrix with nonzero divergence (an arbitrary projection), which never blocked the canonical variational response — conservation is *forced by the Laplacian structure*, not automatic. **Honest scope:** the smooth-limit identification `G → Ric − ½R g` stays the external Connes/Rieffel bridge (`D0-SMOOTH-MANIFOLD-PASSPORT`), and for the weighted `a₂` at the Perron measure the conservation holds in the `√ρ`-weighted sense (the conformal Laplacian `W_ρ L W_ρ` annihilates `√ρ`), a genuine but distinct divergence operator.

**[Iter26 — the matter↔TT-gravity shared-carrier linkage is a certified NO-GO, with a positive partial (`D0-HODGE-LINKS-001`, cert `vp_hodge_links_carrier_nogo.py`).** The companion question — whether matter (edge-stress) *and* the TT-graviton polarizations sit on ONE shared finite carrier — resolves as an obstruction, once the positive leg is separated out. **Positive partial** (the closed leg, `D0-SPECTRAL-EINSTEIN-001`): matter and the Einstein response *do* share the vertex carrier `ℝ`-`Matrix N N`, since every symmetric edge-stress `E` has a unique divergence-free completion `M = E − diag(rowsum E)` (its Laplacianization), so matter conservation is exactly discrete transversality. **The obstruction** (exact): the two physical transverse-traceless graviton polarizations are *not* carried by the vertex response. On the `N = 33` vertices the transverse-traceless symmetric space is `527`-dimensional (nonempty), yet `G = 2L` is *100% trace-mode* — it is transverse (row sums `0`) but `Tr(G) = 4|E| = 1436 ≠ 0`, and its TT-projection `G_TT = G − (Tr G/Tr L)·L = 0` *exactly* (because `G = 2L`). So the natural bridge object carries no TT content on the vertex side; the two tangent-indexed polarizations live on the `Fin 4` carrier, and the vertex→tangent map is the external smooth-limit (Connes/Rieffel) bridge, not an internal cochain Hodge map. The `ℝ/ℚ` carrier mismatch the architecture audit flagged is therefore *structural* (a 33-vertex trace response vs a 4-tangent TT sector), not a Lean type nuisance. A control confirms the vanishing is specific to the trace-mode `G`: a generic divergence-free symmetric matrix (the matter completion `M`) *does* have a nonzero TT part. Honest scope: the smooth-limit tangent bridge is not claimed impossible — it is exactly the external `D0-SMOOTH-MANIFOLD-PASSPORT`.

