## 04.11 SM-facing finite gauge decomposition

The matter/gauge row is not merely the statement that D0 has an anomaly-compatible representation. The core object is a finite gauge-decomposition ledger:

```text
D0.Gauge.FrozenSMGaugeDecomposition
D0.Gauge.frozenSMGaugeDecomposition
D0.Gauge.frozen_sm_gauge_factors_closed
D0.Gauge.frozen_sm_generation_ledger_closed
D0.Gauge.frozen_sm_generation_anomaly_free
```

The theorem chain is now:

```text
finite gauge carrier labels
-> frozen factor ledger [SU(3), SU(2), U(1)]
-> one-generation Weyl ledger
-> exact rational anomaly cancellation
-> no alternative factor ledger at the same frozen decomposition
-> SM/EFT bridge only after the finite ledger is frozen.
```

The anomaly equations are exact finite rational equalities:

```math
\sum Y=0,
\qquad
\sum Y^3=0,
\qquad
SU(2)^2U(1)=0,
\qquad
SU(3)^2U(1)=0.
```

The no-free-decomposition owners are:

```text
D0.Gauge.no_alternative_sm_factors_at_frozen_ledger
D0.Gauge.no_alternative_sm_generation_ledger_at_frozen_decomposition
D0.Gauge.sm_eft_bridge_requires_frozen_decomposition
D0.Gauge.frozen_sm_eft_bridge_boundary_closed
```

Therefore the SM-facing gauge sentence is not a smooth-field assertion and not a fitted Standard-Model import.  The finite core owns only this ledger:

```text
factor list: SU(3), SU(2), U(1);
one-generation Weyl ledger: Q_L, u_R^c, d_R^c, L_L, e_R^c, optional nu_R^c;
exact anomaly sums: zero;
bridge boundary: QFT/EFT running and collider observables attach only after freeze.
```

A different gauge factor list or a different one-generation ledger is not a second core solution under the same owner.  It is a new carrier attempt and must be routed to a separate theorem/no-go/bridge target.  `SU(3)×SU(2)×U(1)` is not a name attached after the fact: the finite ledger fixes the factor list and forbids retuning it.

This still does not prove the full Standard-Model Lagrangian from first principles.  What is closed is the finite carrier/representation/anomaly skeleton and the rule that the SM/EFT bridge cannot alter it.  The remaining work is stronger: derive the representation ledger itself from the lower D0 matter-operator origin and then evaluate running couplings/masses as external transfer, not as core knobs.

**Hypercharge as a graph current and the anomaly-cancellation owner (`D0-SM-ANOMALY-CANCELLATION-OWNER-001`).** On the complete tripartite carrier `K(9,11,13)` (33 vertices, 359 cross-zone edges) the one-generation hypercharge row (left-handed Weyl convention `Y = (1/6, −2/3, 1/3, −1/2, 1, 0)`) is **anomaly-free**: the gravitational·U(1), U(1)³, SU(2)²·U(1) and SU(3)²·U(1) sums all vanish exactly over `ℚ` (Lean `D0.Matter.HyperchargeGraphFlowOwner.sm_anomaly_cancellation_owner`, consolidating the CORE `D0.Gauge.AnomalySums` / `D0-GAUGE-MATTER-002` with the `K(9,11,13)` framing; cert `vp_sm_anomaly_cancellation_owner.py`, controls: omit `eRc` ⇒ U(1)³ ≠ 0, a stray denominator-5 charge breaks a sum, SM-table-as-origin rejected). **The minimal hypercharge denominator `6` is now CLOSED (`D0-SM-HYPERCHARGE-MINIMAL-DENOMINATOR-001`, CERT-CLOSED).** It is *derived*, not read off the table, by the correct route — integer-charge quantization of colour-singlet bound states, using only structure D0 carries (SU(2) doublet splitting `Q = T3 + Y`, `T3 = ±1/2`; colour triality, a baryon = **3** quarks = the rank-3 / three-zone structure): a 3-quark baryon `uud` has charge `3Y_q + 1/2`, which is an integer iff `Y_q = (2m+1)/6`, so the **minimal positive quark hypercharge is `1/6`** (`one_sixth_minimal`); the lepton-doublet quantum `1/2` and the charged-singlet quantum `1` are integer multiples, so the hypercharge lattice is `(1/6)ℤ` and the minimal denominator is exactly `6` (Lean `D0.Matter.HyperchargeMinimalDenominator`, cert `vp_sm_hypercharge_minimal_denominator.py`; controls: `Y_q = 1/3 ⇒` non-integer baryon, the row is not in `(1/5)ℤ`, SM-table-as-origin rejected since `1/6` is the *derived* minimal value). **Honest demotion of the wrong route:** the divergence-free edge-current space of `K(9,11,13)` has dimension `E − V + 1 = 327`, so the graph flow **alone** underdetermines a five-entry row — deriving the *full* row as such a current stays PROOF-TARGET (`D0-HYPERCHARGE-GRAPH-FLOW-OWNER-001`), but the denominator is closed by integrality, not by topology. No SM charge table is imported as a proof of origin.

**Deep-stitch closure (Iteration 22): the divergence-free hypercharge flow lattice has dimension 327 (CERT-CLOSED), but underdetermines the row.** On `K(9,11,13)` (V=33, E=359) the space of divergence-free integer edge-currents is the cycle lattice `dim ker(B)=E−V+1=327` with `rank(B)=V−1=32`. Honestly, this 327-dimensional flow alone underdetermines the hypercharge row, and anomaly-freedom is met by a 2-dimensional space (B−L is itself anomaly-free with denominator 3), so neither the flow nor anomaly-freedom forces the row or the denominator. The flow→Weyl-ledger map `Φ` is now a certified NO-GO (`D0-HYPERCHARGE-GRAPH-FLOW-OWNER-001`; see the Iter25 four-wall closure below); the denominator 6 is owned separately by the integrality route `D0-SM-HYPERCHARGE-MINIMAL-DENOMINATOR-001`. Owner: `D0-HYPERCHARGE-FLOW-LATTICE-001` (CERT-CLOSED), Lean `D0.Matter.HyperchargeFlowLattice`, cert `vp_hypercharge_graph_flow_owner.py`.

**[Iter22 eight-front]** Front F7 (NO-GO), closed honestly. The Standard-Model hypercharge row Y=(1/6,-2/3,1/3,-1/2,1,0) on the one-generation charge 6-vector (q_L,u^c,d^c,ℓ_L,e^c,ν^c) is NOT forced by anomaly-freedom plus the proposed constraints. Over Q we prove: (a) the three linear anomaly/gauge constraints — gravitational·U(1), SU(2)²·U(1) and SU(3)²·U(1) — are the rows of the 3×6 multiplicity matrix A=[[6,3,3,2,1,1],[3,0,0,1,0,0],[2,1,1,0,0,0]], which has rank exactly 3 (an explicit 3×3 minor on columns {0,1,4} has determinant 3≠0, and A has only three rows), so by rank–nullity the solution space has dimension 6−3=3; (b) both Y and B−L=(1/3,-1/3,-1/3,-1,1,1) satisfy every constraint — gravitational, SU(2), SU(3) and the cubic U(1)³ — and are Q-linearly independent; (c) therefore the joint anomaly-free set contains the 2-dimensional span{Y,B−L}: it is not a single ray, so the SM row is underdetermined. The only thing that removes B−L is imposing Y_{ν^c}=0 (that the right-handed neutrino is hypercharge-neutral), which is an SM convention — an input, not a derived consequence — so singling out Y this way is question-begging. (The minimal denominator 6 of Y is a separate fact, owned by the integrality route D0-SM-HYPERCHARGE-MINIMAL-DENOMINATOR-001, and likewise does not single out the row: B−L is anomaly-free with denominator 3.) Lean: D0.Matter.HyperchargeAnomalyVariety (theorem anomaly_free_contains_two_independent); certificate `vp_hypercharge_anomaly_variety.py`. Claim D0-HYPERCHARGE-ANOMALY-VARIETY-2DIM-001 (CERT-CLOSED, NO-GO). Uniqueness owner D0-SM-HYPERCHARGE-ROW-OWNER-001 is NOT minted (it is false); the flow→Weyl-ledger map Φ is the named missing artifact under D0-HYPERCHARGE-GRAPH-FLOW-OWNER-001.

**[Iter25 — the flow→Weyl map `Φ` is now a certified NO-GO (`D0-HYPERCHARGE-GRAPH-FLOW-OWNER-001`, cert `vp_hypercharge_flow_weyl_nogo.py`).** No canonical flow on `K(9,11,13)` carries the `SU(3)×SU(2)×U(1)` root/Weyl structure, so `Φ` cannot exist internally and any hypercharge labeling is an external passport. Four independent walls: (1) **rank** — active adjacency rank `3` < SM Cartan rank `4`; (2) **Weyl** — the induced graph-automorphism action on the three eigendirections is *trivial* (order 1) versus `|W_SM| = 12`; (3) **roots** — the three active eigendirections are orthogonal (`90°`) with *unequal* lengths (eigenvalues `−12.08, −9.76, 21.84`), not the `120°`-equal `A₂` geometry; (4) **pigeonhole** — hypercharge takes 6 distinct values but the scene canonically distinguishes only 3 `Aut`-invariant (zone-constant) functionals. The seductive coincidence is *rejected out loud*: the active cubic `λ³ − 359λ − 2574` has non-square discriminant `6185264`, so its **Galois** group is `S₃ = |Weyl(SU(3))|` — an order-6 group-name match, but a Galois group permuting conjugate algebraic numbers is **not** an orthogonal graph symmetry (it cannot permute eigenvectors of distinct eigenvalues and commute with `A`), so asserting it as a physical Weyl carrier is exactly the back-fit the entry contract forbids. **Root cause:** for equal zones `K(n,n,n)` the zone-block permutations `S₃` *are* graph automorphisms (a geometric Weyl `S₃` exists, and the quotient degenerates to a repeated eigenvalue — a genuine symmetric point), so the M1-forced `+2` progression `{L₅−2, L₅, L₅+2}` that makes the zones pairwise-distinct is precisely what destroys the `SU(3)` Weyl carrier — one cause, two effects, the same carrier-count wall as the owned LEPTON-BRANCH (`2 < 3`) and role-cycle NO-GOs, one dimension up. This *sharpens* the root-carrier reason of `D0-DSIGMA-ROLE-CYCLE-CARRIER-CANONICAL-NOGO-001` into four explicit spectral walls. Honest scope: this does not touch the anomaly row (owned, 2-dim variety, `B−L` in RH singlets), and does not claim the SM group is wrong — only that the canonical scene data cannot geometrically derive its root/Weyl structure.

**[Iter23 — where the `B−L` freedom lives (localization of the F7 NO-GO).** One might hope to escape the `ν^c`-neutrality convention by instead reading off the *observed electric charges* through the Gell-Mann–Nishijima completion `Q = T3 + gen`. This does not help, and the reason is exact and instructive. Imposing the two left-handed **doublet** charges — up-quark `Q_u = +1/2 + gen[q_L] = 2/3`, electron `Q_e = −1/2 + gen[l_L] = −1` — forces only `gen[q_L] = 1/6` and `gen[l_L] = −1/2`, i.e. the line `a = 1 − 2b` in the `span{Y, B−L}` plane; along this line the two doublet entries are **`b`-independent**, while all four right-handed singlet entries `gen[u^c], gen[d^c], gen[e^c], gen[ν^c]` carry `b` linearly (coefficients `+1, −1, −1, +1`). Hence the entire `B−L` residual is carried by the **right-handed singlet sector**, and the left-handed doublet electric charges are `B−L`-**blind**: no selector using charged-fermion (doublet) charges alone can remove `B−L`. Any `Y`-selector must therefore assign a right-handed-singlet charge, and `ν^c` uncharged is the minimal such input — which is exactly the convention flagged above. This does not overturn the NO-GO; it *localizes* it, showing the obstruction is not removable by electric-charge observation on charged fermions, only by a right-handed-singlet input or a new scene primitive (cert `vp_hypercharge_bl_freedom_localized.py`, failable; status unchanged, NO-GO).

**Literature grounding of the singlet selector (independent support, not a closure).** The standard-physics literature reaches exactly this structure and names the *physical* selector. Lohitsiri–Tong (*Hypercharge Quantisation and Fermat's Last Theorem*, arXiv:1907.00514) show that, once hypercharge is quantised, the cubic `U(1)³` gauge-anomaly condition can be recast as `x³ + y³ = z³`, whose only integer solutions (`x=0` or `y=0`, by Fermat) reproduce Nature's hypercharge assignments (the mixed gauge-gravitational anomaly is then automatic). The D0 cubic factorization `−18a(t−3a)(t+3a)=0` is the same statement in D0's `(a,t)` coordinates: its three linear branches `{a=0}, {t=3a}, {t=−3a}` are exactly those trivial solutions, and the SM row sits on the `t=−3a` branch (verified by `sympy` factorization). Their non-question-begging selector is *not* "declare `ν^c` uncharged" but the physical requirement that `ν^c` **admit a gauge-invariant Majorana mass**: a term `∼ (ν^c)²` is gauge invariant iff `2·Y_{ν^c}=0`, i.e. `b=0`, which singles out `Y`. This is the seesaw origin of neutrino mass, and it gives the D0 bridge (`D0-HYPERCHARGE-BL-DIRECTION-BRIDGE-001`, currently grounded in `ν_R ∈ ker A`, the R2 localization MECH-LIMIT) a *second, independent* support with the same endpoint `Y_{ν^c}=0`. It is recorded here as a cross-reference that strengthens the bridge's credibility; it is **not** a proof that D0's `ker A` localization is equivalent to Majorana-admissibility (that would need a scene-level argument), and it does **not** change the status — hypercharge-row selection stays a bridge/NO-GO.

**[Iter23 — the Majorana follow-up, resolved: the scene does NOT force it (NO-GO).** The natural next question — does the φ-quasicrystalline scene *carry* a Majorana structure for `ν^c`, which would let D0 adopt the seesaw selector internally and turn the bridge into a forced identity? — has been investigated and answered **no**, on three independent, separately-verified routes (cert `vp_majorana_not_forced.py`, failable; the routes were tested in parallel and each reproduced against the corpus's own Lean owners):

1. **Real-structure route.** A Majorana mass requires `ν^c` in a *real* representation with a graded antilinear charge-conjugation `J` (`J²=−1`, `JΓ=−ΓJ`, KO-dimension 6). The scene's one-generation spinor carrier is the `Q₈` two-dimensional irrep, whose Frobenius–Schur indicator is `−1` (**quaternionic / pseudoreal**, not real), and the *frozen* role real structure is KO-dimension 0 (`J²=+1`, Dirac-compatible), not KO-6. The precondition fails on the carrier itself (owner `D0.Representation.RoleRealStructureNoGo`).
2. **`ker A` ⟺ Majorana route.** The membership `ν_R ∈ ker(A)` is codimension 3 (zone-balanced, 30-dim of 33) while Majorana-admissibility `2·Y_{ν^c}=0` is codimension 1 in `span{Y, B−L}` — strictly stronger, *not* equivalent. Worse, no scene-*forced* functional can bridge them: the only `Aut(K(9,11,13)) = S₉×S₁₁×S₁₃`-invariant covectors are the three zone-indicators (distinct zone sizes ⇒ no zone swaps), and every one of them vanishes on `ker(A)`; `Y_{ν^c}` reads a single vertex, is not zone-constant, hence is not `Aut`-invariant and needs an exogenous label (owners `D0.Foundation.GraphSpaceNoIsometry`, `D0.Matter.HyperchargeU1MassKernelA2`).
3. **`359`-rigidity route.** The `E=359`-primality rigidity removes continuous internal gauge *orbits* on the edge carrier; `B−L` is a discrete rational charge direction (never gauged in D0), so rigidity does not touch it — wrong category. And "`B−L` removed" and "Majorana admissible" are the *same* condition `b=0` (circular, not cause→effect); removing a protecting symmetry *permits* the `(ν^c)²` term but never *forces* a nonzero mass.

So `ν^c` is at most **Dirac** from the frozen scene, and the seesaw selector remains an *external* cross-reference, not an internalizable one. The single missing statement all three routes converge on — a scene-derived, `Aut`-equivariant charge-localization operator (equivalently a frozen KO-6 antilinear real structure `J`) — is exactly the object the corpus flags as absent (the flow→ledger map `Φ` of `D0-HYPERCHARGE-GRAPH-FLOW-OWNER-001`, now a certified NO-GO; the missing primitive of `D0-HYPERCHARGE-U1-MASS-KERNEL-A2`, NO-GO⁺). This **confirms** the bridge stays `BRIDGE-ASSUMPTIONS-EXPLICIT` rather than upgrading; it is registered as a companion NO-GO on `D0-HYPERCHARGE-BL-DIRECTION-BRIDGE-001`, not a status change.

**[Gauge-matter: hypercharge row owner].** The one-generation **5-field** hypercharge row is the unique anomaly-free assignment, up to overall normalization (electron readout `Y_e=1`), the `u↔d` labeling, and exclusion of the degenerate `Y_Q=0` branch: the three linear anomaly conditions give the family `Y_Q=a, Y_u=−a+t, Y_d=−a−t, Y_L=−3a, Y_e=6a`, and the cubic `U(1)_Y³` anomaly factors **exactly** as `−18a(t−3a)(t+3a)`, so the anomaly-free rays are `{a=0}∪{t=3a}∪{t=−3a}` and the non-degenerate `t=−3a` branch at `a=1/6` is `(1/6,−2/3,1/3,−1/2,1)`. The 2-dimensional freedom seen when a right-neutral `ν_R` is added is the `B−L` direction, not the row (`D0-HYPERCHARGE-ANOMALY-VARIETY-2DIM-001`). The row is derived from the anomaly solve, never imported (`D0-SM-HYPERCHARGE-ROW-OWNER-001`, Lean `D0.Matter.SMHyperchargeRowOwner`, cert `vp_sm_hypercharge_row_owner.py`). The graph flow→Weyl map `Φ` (`D0-HYPERCHARGE-GRAPH-FLOW-OWNER-001`) is a certified NO-GO (four spectral walls, Iter25).

**[Role-to-operator: canonical carrier no-go].** The five DΣ roles `Code→Canon→Test→History→Access→Code` form a single canonical directed 5-cycle (`D0-DSIGMA-CANONICAL-ROLE-ADDRESS-OWNER-001`, Lean `D0.Matter.DSigmaRoleTransitionGraph`). But there is **no canonical rank-5 role-cycle carrier**: the oriented automorphism group `Aut(K(9,11,13)) = S₉×S₁₁×S₁₃` is transitive on the 1287 triangles (a single orbit), and the operational roles carry no intrinsic geometric attachment, so the five roles cannot be canonically injected into distinct cycle orbit-classes — any rank-5 carrier is an arbitrary symmetry-breaking choice (`D0-DSIGMA-ROLE-CYCLE-CARRIER-CANONICAL-NOGO-001`, NO-GO, Lean `D0.Matter.DSigmaRoleCycleCarrierNoGo`). This is the root cause of the role-bound PROOF-TARGETs (flow→Weyl `Φ`, CKM address-action, role-bound lepton branches, the unified functor): their physical content is owned by non-role routes, but the canonical role carrier they would be built from does not exist. See `04_VERIFICATION/ROLE_TO_OPERATOR_BLOCKERS.csv`.

**[Colour vs generation on the typed carriers — `M₃` is generation, not colour (`D0-COLOUR-GENERATION-TYPED-CARRIER-NOGO-001`, cert `vp_colour_generation_typed_carrier_nogo.py`).]** The tempting reading "baryon = 3 quarks = rank-3 = three zones = colour" fails on both frozen carriers, by two exact finite obstructions. **(A) Typed collapse.** On the scene-zone carrier `ℂ³³` the raw commutant on the three zone-lines is the full matrix algebra `M₃(ℂ)` (dim 9), but the typed degree operator `D_zone = diag(24,22,20)` has a *simple* (distinct-eigenvalue) spectrum, so its commutant inside `M₃(ℂ)` is the **diagonal** algebra `ℂ³` (dim 3, abelian): the three zone-lines are the abelian **generation** lines, not an unbroken non-abelian colour. The `+2` zone increment is load-bearing — a degenerate frame `diag(24,24,20)` would leave a non-abelian block (dim 5). **(B) Commutant gap.** On the terminal spinor carrier `ℂ⁸ = ℂ[Q₈]` (`Ω₈ ≅ Q₈`) a source-native colour `M₃` would have to lie in `Commutant(weak)`, but `dim Commutant(weak) = dim R(Q₈) = 8 < 9 = dim M₃(ℂ)` — no 9-dimensional `M₃` fits. The joint quark gauge module `ℂ³⊗ℂ²` therefore requires an **external** `⊗ℂ³` (the inserted `𝒜_F` completion `ℋ_q = W₃⊗V₂`), not a source-derived factor. Honest scope: this is NO-GO on the scene-zone and terminal carriers only — it removes the most tempting false candidate, but does not classify colour on `H_edge` / the `P·Q` feedback globally; global colour stays a **terminal-passport** (octonion / E₈) route, not scene-native, consistent with the root/Weyl no-go above.