## 04.16 Higgs scalar projector + Yukawa section (closed)

Higgs scalar projector is SCALAR-PROJECTOR-CERT-CLOSED (rank-2 FrozenSU2-compatible).
Higgs-Yukawa section is YUKAWA-SECTION-CERT-CLOSED (finite block map compatible with the
projector, no second mass anchor). The remaining hierarchy problem is not an unspecified
“numerical passport”: the coefficient route needs a coefficient-sensitive functional, while
the shell route needs one gap-sensitive scalar equation plus the Green/EFT–IR transfer to
physical masses.

**[2026-08-22 selector boundary.]** The equivariant commutant route now has a theorem-level
selection no-go (`D0-YUKAWA-QUALITATIVE-SELECTOR-NOGO-001`). Every non-scalar
`a+bQ+cQ²` gives three pairwise-distinct irrational values, but all such coefficient triples
have the same full currently-owned equality/rationality profile; the injective family
`(a,b,c)=(0,1,t)`, `t∈ℚ`, lies entirely in one profile class. Hence these universal properties
cannot select a unique operator. A completion requires either (i) a coefficient-sensitive
quantitative functional not constant on that family, or (ii) the separately registered
shell-overlap/Green-function/Puiseux/EFT–IR route. Thus “numerical passport only” is too weak:
the missing mathematical object is now exactly identified.

**[2026-08-22 exact shell-metric boundary.]** The separate shell/Puiseux route now closes all
combinatorial ambiguity. The Puiseux order uniquely sends
`electron/muon/tau → innerD9/coreD11/outerD13`, and the shell order uniquely sends those modes
to the low/middle/high transport roots (`D0-CHARGED-LEPTON-SHELL-BRIDGE-001`,
`D0-GENERATION-ROOT-ORDER-BRIDGE-001`). The numerical shell geometry is then classified, not
left arbitrary: for one `g>0` its radii are exactly `(1,1+g,1+2g)`, and every positive rational
`g` gives a unique admissible torus parameter `a=1+2g`
(`D0-MASS-SECTOR-METRIC-UNDERDETERMINATION-001`). Radial order is constant while `g` varies,
so Lean proves that no order-only selector can fix the metric. The exact remaining shell
primitive is one gap-sensitive scalar condition; physical mass decimals still require the
Green/EFT–IR bridge and an empirical comparison.

**[2026-08-22 joint completion boundary.]** Combining the two present routes does not cancel
their fibers (`D0-MASS-SECTOR-COMPLETION-NOGO-001`). On
`YukawaCoeff × TorusParameter`, the owned qualitative coefficient profile and owned radial order
are jointly constant over all non-scalar candidates. A metric-sensitive but coefficient-profile
blind selector cannot be unique; a coefficient-sensitive but radial-order blind selector cannot
be unique. Therefore the missing Green/EFT–IR owner must carry load-bearing information on both
axes, possibly through one cross-coupled functional. Neither current route can use the other's
coarse invariant as a substitute for its own missing quantitative selector.

**[2026-08-22 transfer must be nonlinear.]** The missing functional's shape is also pinned
(`D0-AFFINE-SHELL-READOUT-NOGO-001`). A linear readout `m(s)=α+β·radius(s)` of the affine shell
`(1,1+g,1+2g)` is always equally spaced: its second difference is identically zero and its span
is exactly twice its first gap for every `g, α, β`. The owned Puiseux exponent row `(0,1/4,1/3)`
has gaps `1/4` and `1/12`, so it is provably not a linear shell readout. Hence the shell-overlap
Green transfer must be genuinely nonlinear in the shell radius; a linear rescaling of the shells
is excluded without any PDG input.

**[2026-08-22 unique minimal transfer.]** The nonlinear lower bound is attained uniquely
(`D0-CANONICAL-PUISEUX-SHELL-TRANSFER-001`). The intrinsic coordinate
`u=(radius−1)/g` sends inner/core/outer to `0/1/2` for every admissible metric. The unique
quadratic matching `(p_e,p_mu,p_tau)=(0,1/4,1/3)` is
`P(u)=u/3−u²/12`; its curvature is negative and the coefficient `−1/12` is forced. Therefore
the minimal polynomial transfer degree is exactly two. This closes the three-shell interpolation
shape, but not its dynamical origin: the finite Green resolvent must still derive this polynomial,
and the EFT/IR functor must still map its exponents to physical masses.

**[2026-08-22 saturation law.]** The unique quadratic obeys
`P(2)−P(u)=(u−2)²/12` (`D0-PUISEUX-TRANSFER-SATURATION-001`), so the outer/tau shell is its
unique global vertex. Its successive exponent increments are exactly `1/4` and `1/12`, giving a
parameter-free `3:1` compression. This is a structural law of the Puiseux exponent transfer, not
a statement that physical mass differences have ratio three.

**Rank-3 shell-overlap Yukawa tensor and the Higgs-as-condensation reading (`D0-YUKAWA-SHELL-OVERLAP-MATRIX-001`).** Beyond the rank-2 scalar projector, the Yukawa coupling is read as a rank-3 shell-overlap tensor on the three-shell space `V9 ⊕ V11 ⊕ V13`: a symmetric `3×3` matrix over `ℚ` with nonzero off-diagonal overlap (the shells genuinely mix; it is not diagonal), gauge-compatible with the frozen rank-2 projector (Lean `D0.Matter.YukawaShellOverlapMatrix.yukawa_shell_overlap_scaffold`, cert `vp_yukawa_shell_overlap_matrix.py`, controls: rank-1/diagonal overlap rejected, a 246 GeV or PDG-mass entry rejected). The Higgs is then a macroscopic phason condensation in the V13 shell. **Honest residuals (PROOF-TARGET):** the macroscopic order-parameter EOM deriving the Higgs VEV from the V13 condensation (`D0-HIGGS-PHASON-CONDENSATION-OWNER-001`; the shell geometry `D0-TORUS-CORE13-GEOMETRY-001` and the rank-2 projector are CORE, the EOM is missing), and the generation mass HIERARCHY from the overlap eigenvalues + ramification exponents (`D0-LEPTON-YUKAWA-HIERARCHY-OWNER-001`; the exponent row `(0,1/4,1/3)` is exact THE, the Green function / branch-index uniqueness / EFT–IR functor are missing, decimals stay HYP). No `246 GeV` VEV and no PDG mass enters as a core input.

**[Iter22 eight-front]** **Scalar-sector stationary condition (F6, CERT-CLOSED).** On the scalar sector the Jacobi/log-det free-energy functional `S_fb(θ) = −2·log(1 − z·f(θ))` has first variation `dS_fb/dθ = 2·z·f'(θ)/(1 − z·f(θ))` (the chain rule, with the matrix provenance `d/dt(−log det(I−zF)) = Tr[(I−zF)⁻¹·z·dF/dt]` verified symbolically for a concrete 2×2 `F`). On the resolvent domain `1 − z·f ≠ 0` with nonzero coupling `z ≠ 0` the prefactor is a nonzero scalar, so the stationary condition collapses **exactly** to the scalar first-order condition `dS_fb/dθ = 0 ⇔ f'(θ) = 0`. This is machine-checked over ℚ (`D0.Matter.HiggsLogdetStationary.higgs_logdet_scalar_sector_stationary`) — the coefficient relation `D·S' = 2·z·f'`, the equivalence, and a concrete Lorentzian profile `f(θ)=θ/(1+θ²)` with `f'(1)=0` (stationary) and `f'(0)=1≠0` (non-stationary) — and the cert `vp_higgs_logdet_stationary.py`. We deliberately do **not** claim the quartic Mexican-hat `V_eff=λ(θ²−v²)²` (it is not an identity: a genuine log-det expansion carries a nonzero `θ⁶` term, coefficient `z⁶/3`), nor the SSB sign (the quadratic coefficient is `z²≥0`, never forced negative), nor any 246 GeV value (an external SI datum). The full order-parameter EOM with a proven double-well negative-quadratic sign remains owned, open, under `D0-HIGGS-PHASON-CONDENSATION-OWNER-001` (PROOF-TARGET).

**[Higgs phason orbit: Outcome B].** The finite scalar-condensation route (return action → archive-projector orbit → log-det EOM → Hessian → condensate) is closed-negative from present inputs. The return modulus `q_T=44` is **not** a toral period: `T=[[0,1],[1,-1]]` has infinite order in `GL(2,ℤ)` and order **30** on `ZMod 44`, so `T⁴⁴ ≠ I` (`D0-HIGGS-RETURN-QUOTIENT-ACTION-OWNER-001`). A nontrivial conjugation orbit `Q_n = Tⁿ Q₀ T⁻ⁿ` requires a `Q₀` that does not commute with `T` — a commuting `Q₀` gives a constant orbit — and the corpus supplies no canonically-frozen non-commuting `(U, Q₀, Π_H)`; choosing one is the forbidden arbitrary step (`D0-HIGGS-PHASON-ORBIT-TRIVIAL-NOGO-001`, NO-GO). The finite-condensation owner stays PROOF-TARGET with the exact blocker: a new independently-forced non-commuting scalar/archive action, derived not chosen. No quartic potential, 246 GeV, or Higgs-mass input enters. See `04_VERIFICATION/HIGGS_PHASON_ORBIT_BLOCKERS.csv`.

**[Higgs condensation maximality].** Every present-core projector is a polynomial `a*1+b*T` in the return operator and commutes with `T`, giving a constant orbit -- no double-well, no condensation. A non-commuting witness `Qnc=!![1,0;0,0]` (`[T,Qnc]!=0`) exists but is not a polynomial in `T` (not present-core) (`D0-HIGGS-CONDENSATION-PRESENT-CORE-MAXIMALITY-NOGO-001`, Lean `D0.Matter.HiggsCondensationPresentCoreMaximalityNoGo`). The condensation route does not start from present-core; a new independently-forced non-commuting `(U,Q0,Pi_H)` is required.
