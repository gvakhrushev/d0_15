> ⚠ **RETRACTED as a 'discriminating confirmation' (adversarial + self-verify).** The claim below is
> OVER-CLAIMED. (1) The degree-permutation "discrimination" is RIGGED: it freezes the O(1) coefficient (2,
> 1/4, 1/2) while permuting only the δ₀-degree; refitting the coefficient over the same simple menu rescues
> the "wrong" degree to <1σ (1/3−¼δ₀=0.3038, 0.26σ). (2) The coefficients are POST-HOC: ~10 distinct simple
> formulas hit sin²θ₁₂ <1σ (e.g. 1/4+φ⁻⁶=0.30573, 0.11σ, beats the claimed one). (3) sin²θ₁₃=φ⁻⁵/4 is 1.02σ
> (paper's σ=0.00058), NOT <1σ. (4) The θ₂₃ "upper octant" is a SIGN CHOICE (1/2±δ₀/2); the seam rule fixes
> only the degree; the Lean proof bakes in the "+". HONEST status: a δ₀-ANSATZ that fits 2/3 angles at ~0.1–
> 0.3σ with post-hoc O(1) coefficients — a passport-level fit (matches the corpus's own 'numbers CHK'), NOT a
> forced/discriminating prediction. My earlier 'confirmed discriminating' headline was wrong.

# PMNS δ₀-family test — D0's strongest confirmed prediction (real NuFIT 6.0 data)

D0 predictions (parameter-free, from δ₀=1/(2φ³), φ; BOOK_04 §04.5.6, seam-topology rule):
  sin²θ₁₂ = 1/3 − 2δ₀² = 0.30547     (δ₀-degree 2 = closed seam)
  sin²θ₁₃ = φ⁻⁵/4      = 0.02254     (δ₀-degree 0 = bare; carries ξ₅=φ⁻⁵, shared with α)
  sin²θ₂₃ = 1/2 + δ₀/2 = 0.55902     (δ₀-degree 1 = open seam)

vs REAL published NuFIT 6.0 (2024, arXiv:2410.05380, NO+SK; provenance in manifests/nufit60_pmns.json):
  sin²θ₁₂ : 0.30547 vs 0.307 ±0.012      → 0.13σ  ✓✓
  sin²θ₁₃ : 0.02254 vs 0.02195±0.00058   → 1.02σ  ✓
  sin²θ₂₃ : 0.55902 vs 0.561 ±0.015 (UO) → 0.13σ  ✓✓   [vs lower octant 0.470 → 5.24σ]

DISCRIMINATION (this is what makes it more than a fit): permuting the δ₀-degrees {0,1,2} across the three
angles blows the fit up — θ₁₂ at degree-1 → 17.5σ (24.4σ vs JUNO), θ₂₃ at degree-2 → 3.6σ (2.7σ). So the
degree↔topology assignment carries real information; it is not a free 3-parameter fit. Corpus cert
`vp_pmns_seam_topology.py` passes (independently reproduced here).

VERDICT: **CONFIRMED at <1σ on real data**, parameter-free in the structure. Honest scope (matches corpus):
the RULE (δ₀-degree ↔ seam topology, with δ₀ forced) is THE; the O(1) coefficients (2, 1/4, 1/2) and thus the
exact VALUES are CHK (empirical passport). Two of three angles at 0.13σ, θ₁₃ at 1.0σ. D0 makes a SHARP
falsifiable prediction: θ₂₃ in the UPPER octant (0.559) — if the octant resolves lower (~0.47), D0 is 5σ off.
This is the session's strongest confirmed prediction and — unlike the DESI passport (over-read) — the corpus
labels it honestly (rule THE / numbers CHK).
