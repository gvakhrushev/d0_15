# D0 Horizonless Echo Falsifier Passport — Protocol

**Pre-registered rejection protocol. Not a detection claim. Not a fit. Not CORE.**

## Frozen internal target
- Causal compactness ceiling `C_max = 3/8` (Book 07 owner) -> reflecting surface `R = 8M/3`.
- Photon sphere `r_ph = 3M`; would-be horizon `2M`. Cavity `[8M/3, 3M]` nonempty; `2M < 8M/3 < 3M`.
- Echo delay (GR tortoise, EXTERNAL formalism): `tau_echo/M = 2/3 + 4 log(3/2) ~= 2.2885` (exact, mass-invariant).

## Pre-registration rules (no post-hoc tuning)
- `C_max = 3/8` is frozen; NO event may be used to select it.
- The search window in `tau/M` is centered on `2.2885` with a pre-declared tolerance; NO delay is tuned after a residual.
- Event selection, mass/redshift handling, calibration, and template subtraction are fixed before unblinding.
- Look-elsewhere / trials correction applied; noise-line vetoes applied.

## Controls (see ECHO_NEGATIVE_CONTROL_MATRIX.csv)
- ordinary black-hole ringdown control (must NOT yield an echo at 2.2885),
- near-horizon `R->2M` echo control (long, diverging delay -- distinct regime),
- short-delay false-positive control, injection-recovery, null-waveform, noise-line.

## PASS / FAIL
- FAIL (theory disfavored): no excess at `tau/M ~ 2.2885` at the pre-declared significance after all controls.
- PASS (not excluded): a calibrated, trials-corrected excess consistent with `2.2885` survives every control.
- Neither outcome promotes any quantity to CORE; the GR tortoise stays external formalism.
