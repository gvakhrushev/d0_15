# Self-Reading Dependence Witnesses

`D0.Extensions.PrimitiveMinimalityAfterSelfReading`: among the four combinatorial frontiers E1-E4 the proof-edge graph has ZERO internal edges; the only two edges (E2->E5, E3->E5) target the SEPARATE analytic layer E5. So no implication merges any two of the four => conjunction-of-independent.

## The 5 key forcing tests (all answered honestly)
- role resolver (E1) => branch selector (E4)? NO (disjoint carriers; no edge).
- finite representation (E1) => physical phason coordinate (E3)? NO (firewall; no edge).
- common archive sector (E3) => CMB smoothing (E2)? NO (no common sector exists; no edge).
- history functor (E2) => archive coordinate (E3)? NO (no edge).
- phi^3 growth (E2/E3) => analytic residue (E5)? YES (the only two edges; the a=3 exponent leg, asymmetric).
