# ROOT Operator Independence Witnesses (semantic, not Nodup)

Lean owner `D0.Verification.RootOperatorSemanticDependence` proves: exactly one directed proof-edge `R3 -> R5`, `R4` isolated, asymmetric. Witnesses:

- **R1 vs R2 (independent, shared substrate K_scene)**: D0-MATTER-REP-001 (R1's commutant/count) is closed with zero reference to any dynamics operator U/Feshbach/w(z); R2's QUQ spectral-absence needs no isotypic multiplicity. Shared object, disjoint proofs.
- **R2 vs R1 (independent)**: D0-PHASON-WZ-KERNEL-ONLY-NOGO-001 is proved from Q + the S_DE Riesz eigenpair, never the commutant dim.
- **R1/R2/R3 vs each other (independent)**: same K_scene, but the load-bearing facts are an Aut-multiplicity (R1), a U-spectral radius 24 (R2), a Perron/refinement-count (R3) -- none imports another.
- **R3 -> R5 (genuine one-way implication)**: R5's a=3 critical-line fact IS rate_three_eq_one (phi^3 phi^-3 = 1) which R3 re-derives / the maximality no-go owns; R5 adds only the transcendence of the limit value. R3 needs nothing from R5.
- **R4 vs all (fully isolated)**: R4's input is a dim-7 shell torus with no vertex/edge/kernel/Perron/weight of the 33-vertex scene; deleting the scene leaves R4 intact.

**Merged primitives: NONE.** R1/R2/R3 share substrate K_scene but assert logically independent invariants (no subsumption). R5 is a corollary-edge of the alpha maximality no-go (shares the single rate_three_eq_one lemma; the primitives are not fused). The assignment-freedom schema (R1 Weyl-role, R4 branch->generation, R2 pressure-energy role) is a reusable PROOF PATTERN over disjoint objects, not a fusible primitive.
