# Post-core Extension Merger Theorems

**No primitives merge.** The five component types are distinct objects over (largely) disjoint carriers; the only relations are the two `a=3` exponent proof-edges `E2→E5`, `E3→E5` (a shared LEMMA reused, not a fusion). Verified: `D0.Extensions.ExtensionMinimality.components_distinct` (`Nodup`, used ONLY for type distinctness, never as the dependence proof) + `two_exponent_edges` (the real dependence).

## The 5 mandated forcing tests
| test | answer |
|---|---|
| Does history refinement (E2) force branch selection (E4)? | NO (E2 swap-invariant; E4 rejects swap-invariant) |
| Does finite representation (E1) force a physical phason coordinate (E3)? | NO (different operators; firewall) |
| Does common archive sector (E3) force CMB smoothing (E2)? | NO (no common sector exists; even granting, window free) |
| Does φ³ growth (E2/E3) force analytic residue realization (E5)? | YES (the only two edges; the `a=3` exponent leg) |
| Does representation reconstruction (E1) force the neutral-current count? | NO (count `p²+q²+3` free in grading) |
