# D0 vNext Extension Interface

The candidate primitive `PRIM-ISOMETRIC-DIRAC-J_N` does NOT collapse to one extension datum. To realize a
Dirac-compatible inductive tower, vNext+1 must supply BOTH:

1. **PRIM-DIRAC-SCALE-SELECTION** — a forcing rule that singles out one admissible scale law `lambda_N`
   from the family (phi^N, 2^N, Fibonacci-dim, ...). Without it the AF spectral triple metric is not unique.
2. **PRIM-COMPARISON-MAP-XI-N** — a canonical map intertwining the AF Dirac/Laplacian with the frozen D0
   K(9,11,13) Laplacian. Obstructed at present: the AF tower is inductive (dims 2,5,13,34,...) and skips the
   fixed 33-dim D0 Laplacian carrier (13<33<34); the towers run opposite directions.

Realized for free: the GNS isometry `J_N` (trace-preserving Bratteli inclusion). FORMALISM-only until both
primitives above are supplied; no physical (alpha/n_s/Higgs/metric) claim is authorized.
