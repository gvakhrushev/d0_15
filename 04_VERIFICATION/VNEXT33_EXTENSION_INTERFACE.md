# D0 vNext+1 Extension Interface

The minimal unifying extension `PRIM-CANONICAL-33-SCENE-ANCHOR` was tested and **refuted**: there is no
canonical one-dimensional AF reduction whose 33-dim quotient anchors the actual `(9,11,13)` D0 scene.

Two obstructions, each sufficient:

1. **Structural (Outcome D)** — the AF level-3 carrier is `M_5⊕M_3` (2 blocks, sizes = Fibonacci path
   counts 5,3); its canonical trace-reduction is `su(5)⊕su(3)⊕u(1) = (24,8,1)`, which does not match the
   scene's tripartite `(9,11,13)`. No canonical scene-encoding `χ`, hence no canonical `Ξ`.
2. **Spectral (scale-independent)** — the AF martingale Dirac² multiplicities are the reduced Fibonacci
   increments `{1,3,8,21}` (4 distinct eigenvalues), versus the scene Laplacian's `{1,2,8,10,12}` (5
   distinct). A unitary `Ξ` must preserve multiplicities; the scale sets only eigenvalues. No scale, no `Ξ`.

Consequence: the two vNext+1 primitives stay INDEPENDENT.
- `PRIM-COMPARISON-MAP-XI-N` needs a 3-partite finite carrier whose canonical operator has the scene
  spectrum `{0:1,20:12,22:10,24:8,33:2}` — not supplied by the AF tower.
- `PRIM-DIRAC-SCALE-SELECTION` needs an intrinsic covariance/normalization; the anchor does not supply it.

No physical (alpha/n_s/Higgs/metric) claim is authorized; all downstream gates remain closed.
